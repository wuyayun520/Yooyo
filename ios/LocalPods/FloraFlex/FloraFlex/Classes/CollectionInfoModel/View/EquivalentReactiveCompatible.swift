
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let kStr_picTitle:[UInt8] = [0x96,0x9b,0x96,0xa1,0x55,0x90,0x9c,0x91,0x92,0x9f,0x67,0x56,0x4d,0x95,0x8e,0xa0,0x4d,0x9b,0x9c,0xa1,0x4d,0x8f,0x92,0x92,0x9b,0x4d,0x96,0x9a,0x9d,0x99,0x92,0x9a,0x92,0x9b,0xa1,0x92,0x91]

fileprivate func equalImage(need num: UInt8) -> UInt8 {
    let value = Int(num) - 45
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Cancel" :*/
fileprivate let kStr_shareData:String = "hide follow start push ifCance"
fileprivate let kStr_backName:[Character] = ["l"]

/*: "Submit" :*/
fileprivate let kStr_viewName:[Character] = ["S","u","b","m","i","t"]

/*: "Please tell us your problems                   " :*/
fileprivate let kStr_intimateValue:[UInt8] = [0xe,0x2a,0x23,0x1f,0x31,0x23,0xde,0x32,0x23,0x2a,0x2a,0xde,0x33,0x31,0xde,0x37,0x2d,0x33,0x30,0xde,0x2e,0x30,0x2d,0x20,0x2a,0x23,0x2b,0x31,0xde,0xde,0xde,0xde,0xde,0xde,0xde,0xde,0xde,0xde,0xde,0xde,0xde,0xde,0xde,0xde,0xde,0xde,0xde]

fileprivate func leadingData(gift num: UInt8) -> UInt8 {
    let value = Int(num) + 66
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "You need to choose at least one." :*/
fileprivate let kStr_domainData:[UInt8] = [0x2e,0x65,0x6e,0x6f,0x20,0x74,0x73,0x61,0x65,0x6c,0x20,0x74,0x61,0x20,0x65,0x73,0x6f,0x6f,0x68,0x63,0x20,0x6f,0x74,0x20,0x64,0x65,0x65,0x6e,0x20,0x75,0x6f,0x59]

/*: "Please tell us your problems." :*/
fileprivate let kStr_modeData:[Character] = ["P","l","e","a","s","e"," ","t","e","l","l"," ","u","s"," ","y","o","u","r"," ","p","r","o","b","l"]
fileprivate let kStr_dataName:String = "enter type share layerems."

/*: "app/userReport" :*/
fileprivate let kStr_guideTitle:[Character] = ["a","p","p","/","u","s","e","r","R","e","p","o","r","t"]

/*: "id" :*/
fileprivate let kStr_modelUserTitle:[UInt8] = [0x31,0x3c]

private func appItem(gift num: UInt8) -> UInt8 {
    return num ^ 88
}

/*: "reason" :*/
fileprivate let kStr_cornerDataValue:String = "REASON"

/*: "desc" :*/
fileprivate let kStr_statusValue:[UInt8] = [0x63,0x73,0x65,0x64]

/*: "mf/moment/report" :*/
fileprivate let kStr_pageValue:String = "remove flag button transactionmf/mo"
fileprivate let kStr_textData:String = "playerport"

/*: "momentId" :*/
fileprivate let kStr_showValue:[UInt8] = [0x64,0x49,0x74,0x6e,0x65,0x6d,0x6f,0x6d]

/*: "mf/momentReply/report" :*/
fileprivate let kStr_viewTitle:String = "all show normalmf/mo"
fileprivate let kStr_sizeData:String = "Repllet image self view in"
fileprivate let kStr_addValue:String = "pototalt"

/*: "app/videoReport" :*/
fileprivate let kStr_fromTitle:[Character] = ["a","p","p","/","v","i","d","e","o","R"]
fileprivate let kStr_skipModelElseValue:String = "withport"

/*: "logId" :*/
fileprivate let kStr_myTitle:[UInt8] = [0xd,0x10,0x8,0xea,0x5]

fileprivate func backgroundStart(view num: UInt8) -> UInt8 {
    let value = Int(num) + 95
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Submitted successfully" :*/
fileprivate let kStr_clickData:String = "Submiback voice self index"
fileprivate let kStr_partyName:String = "with user say suc"
fileprivate let kStr_formatData:String = "utemptempy"

/*: "Nudity" :*/
fileprivate let kStr_beatValue:String = "array locationNudity"

/*: "Violence" :*/
fileprivate let kStr_byData:[Character] = ["V","i","o"]
fileprivate let kStr_activityText:[Character] = ["l","e","n","c","e"]

/*: "Harassment" :*/
fileprivate let kStr_lineData:String = "Harasfirst party"

/*: "Suicide or Self-injury" :*/
fileprivate let kStr_pathUpValue:String = "Suicidremove contact status frame data"
fileprivate let kStr_inputName:String = "elf-ireply list let style phone"
fileprivate let kStr_viewTotalimateContent:[Character] = ["n","j","u","r","y"]

/*: "False Information" :*/
fileprivate let kStr_imageName:[Character] = ["F"]
fileprivate let kStr_requestOfName:[Character] = ["a","l","s","e"," ","I","n","f","o","r","m","a","t","i","o","n"]

/*: "Spam" :*/
fileprivate let kStr_objectModelTitle:String = "self let color length guardSpam"

/*: "Hate Speech" :*/
fileprivate let kStr_colorData:[Character] = ["H","a","t","e"," ","S"]
fileprivate let kStr_cancelName:[Character] = ["p","e","e","c","h"]

/*: "Terrorism" :*/
fileprivate let kStr_giftHeadValue:String = "button var inTerroris"
fileprivate let kStr_toName:[Character] = ["m"]

/*: "Sale of Illegal or Regulated Goods" :*/
fileprivate let kStr_applicationText:[UInt8] = [0xa1,0x93,0x9e,0x97,0xd2,0x9d,0x94,0xd2,0xbb,0x9e,0x9e,0x97,0x95,0x93,0x9e,0xd2,0x9d,0x80,0xd2,0xa0,0x97,0x95,0x87,0x9e,0x93,0x86,0x97,0x96,0xd2,0xb5,0x9d,0x9d,0x96,0x81]

private func theCarrier(background num: UInt8) -> UInt8 {
    return num ^ 242
}

/*: "Sending disgusting messages" :*/
fileprivate let kStr_dataTitle:[Character] = ["S","e","n","d","i","n","g"," ","d","i","s","g","u"]
fileprivate let kStr_giftHalfValue:[Character] = ["s","t","i","n","g"," ","m","e","s","s","a","g","e","s"]

/*: "Using photos of nudity or drugs" :*/
fileprivate let kStr_checkMakeName:[UInt8] = [0x1f,0x39,0x23,0x24,0x2d,0x6a,0x3a,0x22,0x25,0x3e,0x25,0x39,0x6a,0x25,0x2c,0x6a,0x24,0x3f,0x2e,0x23,0x3e,0x33,0x6a,0x25,0x38,0x6a,0x2e,0x38,0x3f,0x2d,0x39]

/*: "Harassing or sending ads" :*/
fileprivate let kStr_withTitle:String = "Harassuper model detail super view"
fileprivate let kStr_modelSizeTitle:String = " or sendiwhite self view count manager"
fileprivate let kStr_changeData:[Character] = ["n","g"," ","a","d","s"]

/*: "Defraud or using fake info" :*/
fileprivate let kStr_lineValue:[Character] = ["D","e","f","r","a","u","d"," ","o","r"," ","u"]
fileprivate let kStr_momentData:[Character] = ["s","i","n","g"," ","f","a","k","e"," ","i","n","f","o"]

/*: "Others" :*/
fileprivate let kStr_captureName:[Character] = ["O","t","h","e","r","s"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EquivalentReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Bolo on 2022/9/14.
//

//: import Then
import Then
//: import UIKit
import UIKit

//: enum TalkingReportAlertType: CaseIterable {
enum CheckedCaseIterable: CaseIterable {
    //: case reportCommentType
    case reportCommentType // 举报动态
    //: case reportDynamicType
    case reportDynamicType
    //: case reportUserType
    case reportUserType // 举报用户
    //: case reportVideo
    case reportVideo // 举报视频通话
}

// 是否举报成功block
//: typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void
typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void

//: class TalkingReportAlertView: UIView {
class EquivalentReactiveCompatible: UIView {
    //: var reportType: TalkingReportAlertType?
    var reportType: CheckedCaseIterable?
    //: var dataArray: Array<TalkingReportModel> = []
    var dataArray: [FillViewReportModel] = []
    //: var selectedModel = TalkingReportModel()
    var selectedModel = FillViewReportModel()
    //: var tempId: String = ""
    var tempId: String = ""
    //: var reportHideBlock: ReportViewHideBlock?
    var reportHideBlock: ReportViewHideBlock?
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: public init(frame: CGRect, type: TalkingReportAlertType?, rId: String) {
    public init(frame: CGRect, type: CheckedCaseIterable?, rId: String) {
        //: tempId = rId
        tempId = rId
        //: reportType = type
        reportType = type
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        biologyLab()
        //: setupSubViewsConstraint()
        restriction()
        //: setupReasons()
        reasons()
        //: bindInteraction()
        dismissIndex()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kStr_picTitle.map{equalImage(need: $0)}, encoding: .utf8)!)
    }

    // MARK: - Lazy load

    //: lazy var cancelButton = UIButton().then {
    lazy var cancelButton = UIButton().then {
        //: $0.setTitle("Cancel".localized, for: .normal)
        $0.setTitle((String(kStr_shareData.suffix(5)) + String(kStr_backName)).localized, for: .normal)
        //: $0.setTitleColor(UIColor.fromBuild(), for: .normal)
        $0.setTitleColor(UIColor.fromBuild(), for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.groupSize(type: .Medium, fontSize: 17)
        //: $0.backgroundColor = UIColor.white
        $0.backgroundColor = UIColor.white
        //: $0.layer.borderWidth = 1
        $0.layer.borderWidth = 1
        //: $0.layer.borderColor = UIColor.fromBuild().cgColor
        $0.layer.borderColor = UIColor.fromBuild().cgColor
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerCancelAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(control), for: .touchUpInside)
    }

    //: lazy var submitButton = UIButton().then {
    lazy var submitButton = UIButton().then {
        //: $0.setTitle("Submit".localized, for: .normal)
        $0.setTitle((String(kStr_viewName)).localized, for: .normal)
        //: $0.setTitleColor(UIColor.white, for: .normal)
        $0.setTitleColor(UIColor.white, for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.groupSize(type: .Medium, fontSize: 17)
        //: $0.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        $0.setBackgroundImage(UIImage.upTo(colors: UIColor.thirdColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerSubmitAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(cityCenterAction), for: .touchUpInside)
    }

    //: lazy var tableFooterView = UIView().then {
    lazy var tableFooterView = UIView().then {
        //: $0.frame = CGRect(x: 0, y: 0, width: ScreenWidth-actualWidth(w: 108), height: actualHeight(h: 74))
        $0.frame = CGRect(x: 0, y: 0, width: kLet_halfData - originalScene(w: 108), height: mediaBlock(h: 74))
        //: $0.backgroundColor = UIColor.RGBA(245, 245, 248, 1)
        $0.backgroundColor = UIColor.largesse(245, 245, 248, 1)
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 5
        $0.layer.cornerRadius = 5
    }

    //: lazy var otherTextView = PlaceholderTextView(placeholder: "Please tell us your problems                   ".localized, placeholderColor: UIColor.statusMin(), frame: CGRect(x: 10, y: 10, width: ScreenWidth-actualWidth(w: 128), height: actualHeight(h: 54))).then {
    lazy var otherTextView = PathViewDelegate(placeholder: String(bytes: kStr_intimateValue.map{leadingData(gift: $0)}, encoding: .utf8)!.localized, placeholderColor: UIColor.statusMin(), frame: CGRect(x: 10, y: 10, width: kLet_halfData - originalScene(w: 128), height: mediaBlock(h: 54))).then {
        //: $0.backgroundColor = .clear
        $0.backgroundColor = .clear
        //: $0.placeholdertextView.backgroundColor = .clear
        $0.placeholdertextView.backgroundColor = .clear
        //: $0.limitWords = 200
        $0.limitWords = 200
        //: $0.isShowCountLabel = false
        $0.isShowCountLabel = false
        //: $0.isReturnHidden = true
        $0.isReturnHidden = true
        //: $0.plaleLabel.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.plaleLabel.font = .groupSize(type: .Regular, fontSize: 16)
        //: $0.placeholdertextView.textColor = UIColor.appTitleColor()
        $0.placeholdertextView.textColor = UIColor.dataFormat()
        //: $0.placeholdertextView.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.placeholdertextView.font = .groupSize(type: .Regular, fontSize: 16)
    }

    //: lazy var bgView = UIView().then {
    lazy var bgView = UIView().then {
        //: $0.backgroundColor = UIColor.RGBA(0, 0, 0, 0.6)
        $0.backgroundColor = UIColor.largesse(0, 0, 0, 0.6)
    }

    //: lazy var containerView = UIView().then {
    lazy var containerView = UIView().then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 15
        $0.layer.cornerRadius = 15
    }

    //: lazy var tableView = UITableView.init(frame: .zero, style: .plain).then {
    lazy var tableView = UITableView(frame: .zero, style: .plain).then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.separatorStyle = .none
        $0.separatorStyle = .none
        //: $0.showsVerticalScrollIndicator = false
        $0.showsVerticalScrollIndicator = false
        //: $0.delegate = self
        $0.delegate = self
        //: $0.dataSource = self
        $0.dataSource = self
        //: if reportType != .reportCommentType, reportType != .reportVideo {
        if reportType != .reportCommentType, reportType != .reportVideo {
            //: $0.tableFooterView = self.tableFooterView
            $0.tableFooterView = self.tableFooterView
        }
        //: $0.register(TalkingReportViewCell.self, forCellReuseIdentifier: TalkingReportViewCell.className())
        $0.register(MakeViewCell.self, forCellReuseIdentifier: MakeViewCell.className())
    }
}

// MARK: - Bind && Event

//: extension TalkingReportAlertView {
extension EquivalentReactiveCompatible {
    //: @objc func func__TapGestureRecognizer() {
    @objc func liveImage() {
        //: self.endEditing(true)
        self.endEditing(true)
    }

    //: public func showReportViewIn(view: UIView?) {
    public func showAppView(view: UIView?) {
        //: if view == nil {
        if view == nil {
            //: let window = TableMacroDefine.getWindow()
            let window = TableMacroDefine.oeilDeBoeufView()
            //: window.addSubview(self)
            window.addSubview(self)
            //: return
            return
        }
        //: view?.addSubview(self)
        view?.addSubview(self)
    }

    /// 移除视图
    /// - Parameter succeed: 是否举报成功
    //: private func dismissReportView(succeed: Bool) {
    private func chuckleForthDoingAttempt(succeed: Bool) {
        //: reportHideBlock?(succeed)
        reportHideBlock?(succeed)
        //: self.removeFromSuperview()
        self.removeFromSuperview()
    }

    /// 取消
    //: @objc func registerCancelAction() {
    @objc func control() {
        //: self.dismissReportView(succeed: false)
        self.chuckleForthDoingAttempt(succeed: false)
    }

    /// 提交举报内容
    //: @objc func registerSubmitAction() {
    @objc func cityCenterAction() {
        //: if selectedModel.selected == false {
        if selectedModel.selected == false {
            //: self.func__showStatusBarErrorMsg(showMsg: "You need to choose at least one.".localized)
            self.detailDownArray(showMsg: String(bytes: kStr_domainData.reversed(), encoding: .utf8)!.localized)
            //: return
            return
        }
        //: if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
        if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please tell us your problems.".localized)
            self.detailDownArray(showMsg: (String(kStr_modeData) + String(kStr_dataName.suffix(4))).localized)
            //: return
            return
        }

        //: var param: [String: String] = [:]
        var param: [String: String] = [:]
        //: var path: String = ""
        var path = ""

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportUserType:
        case .reportUserType:
            //: path = "app/userReport"
            path = (String(kStr_guideTitle))
            //: param = ["id": tempId, "reason": selectedModel.type, "desc": otherTextView.placeholdertextView.text]
            param = ["id": tempId, (kStr_cornerDataValue.lowercased()): selectedModel.type, String(bytes: kStr_statusValue.reversed(), encoding: .utf8)!: otherTextView.placeholdertextView.text]

        //: case .reportDynamicType:
        case .reportDynamicType:
            //: path = "mf/moment/report"
            path = (String(kStr_pageValue.suffix(5)) + "ment/r" + kStr_textData.replacingOccurrences(of: "player", with: "e"))
            //: param = ["momentId": tempId, "reason": selectedModel.type]
            param = [String(bytes: kStr_showValue.reversed(), encoding: .utf8)!: tempId, (kStr_cornerDataValue.lowercased()): selectedModel.type]

        //: case .reportCommentType:
        case .reportCommentType:
            //: path = "mf/momentReply/report"
            path = (String(kStr_viewTitle.suffix(5)) + "ment" + String(kStr_sizeData.prefix(4)) + "y/re" + kStr_addValue.replacingOccurrences(of: "total", with: "r"))
            //: param = ["id": tempId, "reason": selectedModel.type]
            param = ["id": tempId, (kStr_cornerDataValue.lowercased()): selectedModel.type]

        //: case .reportVideo:
        case .reportVideo:
            //: path = "app/videoReport"
            path = (String(kStr_fromTitle) + kStr_skipModelElseValue.replacingOccurrences(of: "with", with: "e"))
            //: param = ["logId": tempId, "reason": selectedModel.type]
            param = [String(bytes: kStr_myTitle.map{backgroundStart(view: $0)}, encoding: .utf8)!: tempId, (kStr_cornerDataValue.lowercased()): selectedModel.type]

        //: default:
        default:
            //: break
            break
        }

        //: self.submitButton.isEnabled = false
        self.submitButton.isEnabled = false
        //: TalkingUserRequestManager.func__reportRequest(reqPath: path, reqParams: param) { succeed, result, errorModel in
        SomeoneThen.tap(reqPath: path, reqParams: param) { succeed, _, _ in
            //: self.submitButton.isEnabled = true
            self.submitButton.isEnabled = true
            //: if succeed == true {
            if succeed == true {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Submitted successfully".localized)
                self.property(showMsg: (String(kStr_clickData.prefix(5)) + "tted" + String(kStr_partyName.suffix(4)) + "cessf" + kStr_formatData.replacingOccurrences(of: "temp", with: "l")).localized)
                //: self.dismissReportView(succeed: true)
                self.chuckleForthDoingAttempt(succeed: true)
            }
        }
    }
}

// MARK: - Delegate

//: extension TalkingReportAlertView: UITableViewDelegate, UITableViewDataSource {
extension EquivalentReactiveCompatible: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return dataArray.count
        return dataArray.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return actualHeight(h: 40)
        return mediaBlock(h: 40)
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingReportViewCell.className()) as! TalkingReportViewCell
        let cell = tableView.dequeueReusableCell(withIdentifier: MakeViewCell.className()) as! MakeViewCell
        //: let model = dataArray[indexPath.item]
        let model = dataArray[indexPath.item]
        //: cell.updateReportCell(model: model)
        cell.magnitude(model: model)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: for model in dataArray {
        for model in dataArray {
            //: model.selected = false
            model.selected = false
        }

        //: let itemModel = dataArray[indexPath.item]
        let itemModel = dataArray[indexPath.item]
        //: itemModel.selected = true
        itemModel.selected = true
        //: self.selectedModel = itemModel
        self.selectedModel = itemModel

        //: tableView.reloadData()
        tableView.reloadData()
    }
}

// MARK: - Layout

//: extension TalkingReportAlertView {
extension EquivalentReactiveCompatible {
    /// 添加视图
    //: private func setupSubviews() {
    private func biologyLab() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(containerView)
        addSubview(containerView)
        //: containerView.addSubview(tableView)
        containerView.addSubview(tableView)
        //: containerView.addSubview(cancelButton)
        containerView.addSubview(cancelButton)
        //: containerView.addSubview(submitButton)
        containerView.addSubview(submitButton)
        //: tableFooterView.addSubview(otherTextView)
        tableFooterView.addSubview(otherTextView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func restriction() {
        //: var height = actualHeight(h: 325)
        var height = mediaBlock(h: 325)
        //: if reportType == .reportUserType {
        if reportType == .reportUserType {
            //: height = actualHeight(h: 375)
            height = mediaBlock(h: 375)
            //: } else if reportType == .reportVideo {
        } else if reportType == .reportVideo {
            //: height = actualHeight(h: 338)
            height = mediaBlock(h: 338)
        }
        //: let topY = (self.frame.height - height) / 2.0
        let topY = (self.frame.height - height) / 2.0

        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.leading.top.trailing.bottom.equalTo(0)
            make.leading.top.trailing.bottom.equalTo(0)
        }
        //: containerView.snp.makeConstraints { make in
        containerView.snp.makeConstraints { make in
            //: make.top.equalTo(topY)
            make.top.equalTo(topY)
            //: make.leading.equalTo(actualWidth(w: 40))
            make.leading.equalTo(originalScene(w: 40))
            //: make.trailing.equalTo(actualWidth(w: -40))
            make.trailing.equalTo(originalScene(w: -40))
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: tableView.snp.makeConstraints { make in
        tableView.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(originalScene(w: 14))
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(originalScene(w: -14))
            //: make.top.equalTo(actualHeight(h: 36))
            make.top.equalTo(mediaBlock(h: 36))
            //: if reportType == .reportVideo {
            if reportType == .reportVideo {
                //: make.bottom.equalTo(actualHeight(h: -62))
                make.bottom.equalTo(mediaBlock(h: -62))
                //: } else {
            } else {
                //: make.bottom.equalTo(actualHeight(h: -82))
                make.bottom.equalTo(mediaBlock(h: -82))
            }
        }
        //: cancelButton.snp.makeConstraints { make in
        cancelButton.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(originalScene(w: 14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(mediaBlock(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
        //: submitButton.snp.makeConstraints { make in
        submitButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(originalScene(w: -14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(mediaBlock(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
    }

    /// 添加数据
    //: private func setupReasons() {
    private func reasons() {
        //: var reasonArray: [String] = []
        var reasonArray: [String] = []
        //: var type = 0
        var type = 0

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportDynamicType, .reportCommentType:
        case .reportDynamicType, .reportCommentType:
            //: type = 6
            type = 6
            //: reasonArray = ["Nudity".localized,
            reasonArray = [(String(kStr_beatValue.suffix(6))).localized,
                           //: "Violence".localized,
                           (String(kStr_byData) + String(kStr_activityText)).localized,
                           //: "Harassment".localized,
                           (String(kStr_lineData.prefix(5)) + "sment").localized,
                           //: "Suicide or Self-injury".localized,
                           (String(kStr_pathUpValue.prefix(6)) + "e or S" + String(kStr_inputName.prefix(5)) + String(kStr_viewTotalimateContent)).localized,
                           //: "False Information".localized,
                           (String(kStr_imageName) + String(kStr_requestOfName)).localized,
                           //: "Spam".localized,
                           (String(kStr_objectModelTitle.suffix(4))).localized,
                           //: "Hate Speech".localized,
                           (String(kStr_colorData) + String(kStr_cancelName)).localized,
                           //: "Terrorism".localized,
                           (String(kStr_giftHeadValue.suffix(8)) + String(kStr_toName)).localized,
                           //: "Sale of Illegal or Regulated Goods".localized]
                           String(bytes: kStr_applicationText.map{theCarrier(background: $0)}, encoding: .utf8)!.localized]

        //: case .reportUserType:
        case .reportUserType:
            //: type = 1
            type = 1
            //: reasonArray = ["Sending disgusting messages".localized,
            reasonArray = [(String(kStr_dataTitle) + String(kStr_giftHalfValue)).localized,
                           //: "Using photos of nudity or drugs".localized,
                           String(bytes: kStr_checkMakeName.map{$0^74}, encoding: .utf8)!.localized,
                           //: "Harassing or sending ads".localized,
                           (String(kStr_withTitle.prefix(5)) + "sing" + String(kStr_modelSizeTitle.prefix(9)) + String(kStr_changeData)).localized,
                           //: "Defraud or using fake info".localized,
                           (String(kStr_lineValue) + String(kStr_momentData)).localized,
                           //: "Others".localized]
                           (String(kStr_captureName)).localized]

        //: case .reportVideo:
        case .reportVideo:
            //: if let reason = LocationThen.share.appConfigMode.videoReport["reason"] {
            if let reason = LocationThen.share.appConfigMode.videoReport[(kStr_cornerDataValue.lowercased())] {
                //: reasonArray = reason as! [String]
                reasonArray = reason as! [String]
            }

        //: default:
        default:
            //: type = 0
            type = 0
            //: reasonArray = []
            reasonArray = []
        }

        //: for i in 0..<reasonArray.count {
        for i in 0 ..< reasonArray.count {
            //: let model = TalkingReportModel()
            let model = FillViewReportModel()
            //: model.reason = reasonArray[i]
            model.reason = reasonArray[i]
            //: if self.reportType == .reportVideo {
            if self.reportType == .reportVideo {
                //: model.type = "\(i)"
                model.type = "\(i)"
                //: } else {
            } else {
                //: model.type = "\(i+type)"
                model.type = "\(i + type)"
            }
            //: dataArray.append(model)
            dataArray.append(model)
        }

        //: tableView.reloadData()
        tableView.reloadData()
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func dismissIndex() {
        //: let tapGesture = UITapGestureRecognizer()
        let tapGesture = UITapGestureRecognizer()
        //: bgView.addGestureRecognizer(tapGesture)
        bgView.addGestureRecognizer(tapGesture)
        //: tapGesture.rx.event.bind { [weak self] recognizer in
        tapGesture.rx.event.bind { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.endEditing(true)
            self.endEditing(true)
        }
        //: .disposed(by: disposeBag)
        .disposed(by: disposeBag)
    }
}
