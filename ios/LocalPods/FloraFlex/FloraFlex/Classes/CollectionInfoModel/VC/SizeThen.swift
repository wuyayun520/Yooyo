
//: Declare String Begin

/*: "nil" :*/
fileprivate let kStr_hiddenContent:[Character] = ["n","i","l"]

/*: "Edit profiles" :*/
fileprivate let kStr_momentTopData:[Character] = ["E","d","i","t"," ","p","r","o"]
fileprivate let kStr_equalContent:String = "FILES"

/*: "Done" :*/
fileprivate let kStr_gestureAddText:String = "Doneraw style view after"

/*: "#999999" :*/
fileprivate let kStr_originalValue:[Character] = ["#","9","9","9","9","9","9"]

/*: "uid" :*/
fileprivate let kStr_editText:String = "UID"

/*: "CamshaftViewCell" :*/
fileprivate let kStr_jumpValue:[Character] = ["T","a","l","k","i","n","g","E","d","i","t","N","a","m","e","C","e"]
fileprivate let kStr_dealName:String = "center"

/*: "ShareViewDelegate" :*/
fileprivate let kStr_homeContent:String = "Talkbackground return detail view extension"
fileprivate let kStr_appText:[Character] = ["i","g","n","C","e","l","l"]

/*: "Save the change?" :*/
fileprivate let kStr_scopeIndexFromName:String = "import index iconSave "
fileprivate let kStr_voiceName:[Character] = ["t","h","e"," "]
fileprivate let kStr_addName:String = "extra for count onchange?"

/*: "Cancel" :*/
fileprivate let kStr_groupName:String = "self progress behaviorCance"
fileprivate let kStr_pushData:[Character] = ["l"]

/*: "Save" :*/
fileprivate let kStr_blockPadName:[Character] = ["S","a","v","e"]

/*: "nickname" :*/
fileprivate let kStr_sectionData:String = "niindexna"
fileprivate let kStr_normalValue:[Character] = ["m","e"]

/*: "birthday" :*/
fileprivate let kStr_toolSucceedValue:String = "brequestt"

/*: "desc" :*/
fileprivate let kStr_viewValue:String = "dephysicsc"

/*: "Modify the success" :*/
fileprivate let kStr_textName:[Character] = ["M","o","d","i","f","y"," ","t","h","e"," ","s","u","c","c","e","s","s"]

/*: "pic" :*/
fileprivate let kStr_toolText:[Character] = ["p","i","c"]

/*: "[ :*/
fileprivate let kStr_addValue:String = "app"

/*: ]" :*/
fileprivate let kStr_styleModelInfoData:[Character] = ["]"]

/*: "pids" :*/
fileprivate let kStr_textValue:String = "piguides"

/*: "aboutMe" :*/
fileprivate let kStr_kitData:String = "aboutMequal block game"
fileprivate let kStr_imageTitle:String = "year"

/*: "interest" :*/
fileprivate let kStr_groupValue:[Character] = ["i","n","t","e","r","e","s","t"]

/*: "tagIds" :*/
fileprivate let kStr_frameValue:String = "T"
fileprivate let kStr_centerValue:[Character] = ["a","g","I","d","s"]

/*: "category" :*/
fileprivate let kStr_capacityData:[Character] = ["c","a","t","e","g","o","r","y"]

/*: "An error occurred, please try again" :*/
fileprivate let kStr_makeReloadName:[UInt8] = [0x15,0x3a,0x74,0x31,0x26,0x26,0x3b,0x26,0x74,0x3b,0x37,0x37,0x21,0x26,0x26,0x31,0x30,0x78,0x74,0x24,0x38,0x31,0x35,0x27,0x31,0x74,0x20,0x26,0x2d,0x74,0x35,0x33,0x35,0x3d,0x3a]

/*: "imgUrl" :*/
fileprivate let kStr_statusAddInfoValue:String = "imgUrlnormal block gift manager use"

/*: "videoUrl" :*/
fileprivate let kStr_modeSubName:[Character] = ["v","i","d","e","o","U","r"]
fileprivate let kStr_withName:String = "image"

/*: "Video cover submitted successfully" :*/
fileprivate let kStr_lengthData:[UInt8] = [0x6d,0x52,0x5f,0x5e,0x54,0x1b,0x58,0x54,0x4d,0x5e,0x49,0x1b,0x48,0x4e,0x59,0x56,0x52,0x4f,0x4f,0x5e,0x5f,0x1b,0x48,0x4e,0x58,0x58,0x5e,0x48,0x48,0x5d,0x4e,0x57,0x57,0x42]

private func managerView(empty num: UInt8) -> UInt8 {
    return num ^ 59
}

/*: "PointCompartmentVideoCell" :*/
fileprivate let kStr_boxValue:String = "super equal area type viewTalkin"
fileprivate let kStr_sharedValue:String = "idemodel"

/*: "video_url" :*/
fileprivate let kStr_labelTitle:String = "capturede"
fileprivate let kStr_picData:[Character] = ["l"]

/*: "img_url" :*/
fileprivate let kStr_contentGiftDataValue:String = "title"
fileprivate let kStr_keyDraftText:[Character] = ["m","g","_","u","r","l"]

/*: "status" :*/
fileprivate let kStr_transactionData:[Character] = ["s","t","a","t","u","s"]

/*: "PointPhotoCell" :*/
fileprivate let kStr_modelText:[Character] = ["T","a","l","k","i","n","g","E","d","i","t","P","h","o","t","o","C","e","l","l"]

/*: "DataThen" :*/
fileprivate let kStr_equalTitle:String = "size momentTalkingE"
fileprivate let kStr_halfData:[Character] = ["d","i","t","B","i","r","t","h"]
fileprivate let kStr_labTitle:[Character] = ["d","a","y","C","e","l","l"]

/*: "MakeItemView" :*/
fileprivate let kStr_makeValue:String = "right succeed beTalk"
fileprivate let kStr_liveGiftYouContent:String = "tAboudetail let"

/*: "UITableViewCell" :*/
fileprivate let kStr_bagTitle:[Character] = ["U","I","T","a","b","l","e","V"]
fileprivate let kStr_sizeName:[Character] = ["i"]
fileprivate let kStr_labNeedLetData:[Character] = ["e","w","C","e","l","l"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SizeThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/8/30.
//

//: import UIKit
import UIKit

//: class TalkingEditProfilesVC: TalkingBaseViewController {
class SizeThen: LayerRecognizerDelegate {
    //: var seleteAboutme: [UserSeleteTagModel] = []
    var seleteAboutme: [NameTagTransformable] = []
    //: var seleteInters: [UserSeleteTagModel] = []
    var seleteInters: [NameTagTransformable] = []

    //: var isdeleteAboutme = false
    var isdeleteAboutme = false
    //: var isdeleteInters = false
    var isdeleteInters = false

    //: var isTips = false
    var isTips = false

    //: var PhotoArray: [TalkingUserInfoGalleryModel] = []
    var PhotoArray: [CountModelType] = []
    //: var seletePhotoArray: [UIImage] = []
    var seletePhotoArray: [UIImage] = []
    //: var DeletePhotoArray: [String] = []
    var DeletePhotoArray: [String] = []

    //: var nameStr: String = ""
    var nameStr: String = ""
    //: var birthday: String = ""
    var birthday: String = ""
    //: var signStr: String = "nil"
    var signStr: String = (String(kStr_hiddenContent))

    //: private var VideoIconPath = ""
    private var VideoIconPath = ""
    //: private var videoPath = ""
    private var videoPath = ""
    //: private var videoStatus = -3
    private var videoStatus = -3 // 视频审核状态：0待审核 1审核通过 -1审核拒绝 -3未上传
    //: private var isChangeVideo = false
    private var isChangeVideo = false

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = false
        self.hideNavi = false
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.bottomValue()
        //: self.title = "Edit profiles".localized
        self.title = (String(kStr_momentTopData) + kStr_equalContent.lowercased()).localized

        //: let btn = UIButton(frame: CGRect(x: 0, y: 0, width: 40, height: 18))
        let btn = UIButton(frame: CGRect(x: 0, y: 0, width: 40, height: 18))
        //: btn.setTitle("Done".localized, for: .normal)
        btn.setTitle((String(kStr_gestureAddText.prefix(4))).localized, for: .normal)
        //: btn.setTitleColor(UIColor.init(hex: "#999999"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(kStr_originalValue))), for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.groupSize(type: .Medium, fontSize: 15)
        //: btn.addTarget(self, action: #selector(doneBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(isError), for: .touchUpInside)
        //: let item = UIBarButtonItem(customView: btn)
        let item = UIBarButtonItem(customView: btn)
        //: self.navigationItem.rightBarButtonItem=item
        self.navigationItem.rightBarButtonItem = item

        //: seleteAboutme = LocationThen.share.loginUserMode.aboutMe!
        seleteAboutme = LocationThen.share.loginUserMode.aboutMe!
        //: seleteInters = LocationThen.share.loginUserMode.interest!
        seleteInters = LocationThen.share.loginUserMode.interest!
        //: designView()
        project()
        //: GetGallery()
        sharedTo()
        //: getVideoInfo()
        progressObtain()

        //: NotificationCenter.default.addObserver(self, selector: #selector(keyboardShowBeHidden(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(atTotal(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: deinit {
    deinit {
        //: ProgressHUD.dismiss()
        TintProgressHUD.inkingPadDismiss()
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy load

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: kLet_halfData, height: kLet_noSeatName), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()
}

// MARK: - Laod data

//: extension TalkingEditProfilesVC {
extension SizeThen {
    //: func GetGallery() {
    func sharedTo() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["uid"] = LocationThen.share.loginUserMode.userID
        dict[(kStr_editText.lowercased())] = LocationThen.share.loginUserMode.userID
        //: TalkingMeRequestTool.req_GalleryByUid(params: dict) { succeed, result, errorModel in
        LoadEqualRequestTool.byEqualCompletion(params: dict) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: let array: Array =  result as! Array<Any>
                let array: Array = result as! [Any]
                //: if array.count>0 {
                if array.count > 0 {
                    //: self.PhotoArray.removeAll()
                    self.PhotoArray.removeAll()
                }
                //: if let datas = Array<TalkingUserInfoGalleryModel>.deserialize(from: array as? Array) {
                if let datas = Array<CountModelType>.deserialize(from: array as? Array) {
                    //: self.PhotoArray.append(contentsOf: (datas as? [TalkingUserInfoGalleryModel])!)
                    self.PhotoArray.append(contentsOf: (datas as? [CountModelType])!)
                }
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: override func naviPopback() {
    override func viewDownPopback() {
        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(kStr_jumpValue) + kStr_dealName.replacingOccurrences(of: "center", with: "ll")) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: CamshaftViewCell = i as! CamshaftViewCell
                //: namecell.nameTF.resignFirstResponder()
                namecell.nameTF.resignFirstResponder()
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(kStr_homeContent.prefix(4)) + "ingEditS" + String(kStr_appText)) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: ShareViewDelegate = i as! ShareViewDelegate
                //: namecell.textView.resignFirstResponder()
                namecell.textView.resignFirstResponder()
            }
        }
        //: if (nameStr.count > 0&&LocationThen.share.loginUserMode.nickname != nameStr)||( birthday.count > 0&&LocationThen.share.loginUserMode.birthday != self.birthday)||(signStr != "nil" && LocationThen.share.loginUserMode.signature != signStr)||self.seletePhotoArray.count>0||self.DeletePhotoArray.count>0||isdeleteAboutme||isdeleteInters || isChangeVideo {
        if (nameStr.count > 0 && LocationThen.share.loginUserMode.nickname != nameStr) || (birthday.count > 0 && LocationThen.share.loginUserMode.birthday != self.birthday) || (signStr != (String(kStr_hiddenContent)) && LocationThen.share.loginUserMode.signature != signStr) || self.seletePhotoArray.count > 0 || self.DeletePhotoArray.count > 0 || isdeleteAboutme || isdeleteInters || isChangeVideo {
            //: let config = ShowAlertConfig()
            let config = StreetwiseAlertConfig()
            //: config.alignment = .center
            config.alignment = .center
            //: TalkingAlertShow.customAlert(message: "Save the change?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Save".localized, leftBlock: { [weak self] in
            ClickReactiveCompatible.anyAppearConfig(message: (String(kStr_scopeIndexFromName.suffix(5)) + String(kStr_voiceName) + String(kStr_addName.suffix(7))).localized, leftBtnTitle: (String(kStr_groupName.suffix(5)) + String(kStr_pushData)).localized, rightBtnTitle: (String(kStr_blockPadName)).localized, leftBlock: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: TalkingAlertShow.hideAlert()
                ClickReactiveCompatible.closeHideRelease()
                //: self.navigationController?.popViewController(animated: true)
                self.navigationController?.popViewController(animated: true)

                //: }, rightBlock: { [weak self] in
            }, rightBlock: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: TalkingAlertShow.hideAlert()
                ClickReactiveCompatible.closeHideRelease()
                //: self.uploadTool(isBack: true)
                self.memoryImageBack(isBack: true)
                //: }, config: config)
            }, config: config)
            //: } else {
        } else {
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
    }

    //: @objc func doneBtnClick() {
    @objc func isError() {
        //: uploadTool(isBack: false)
        memoryImageBack(isBack: false)
    }

    //: func uploadTool(isBack: Bool) {
    func memoryImageBack(isBack: Bool) {
        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(kStr_jumpValue) + kStr_dealName.replacingOccurrences(of: "center", with: "ll")) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: CamshaftViewCell = i as! CamshaftViewCell
                //: namecell.nameTF.resignFirstResponder()
                namecell.nameTF.resignFirstResponder()
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(kStr_homeContent.prefix(4)) + "ingEditS" + String(kStr_appText)) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: ShareViewDelegate = i as! ShareViewDelegate
                //: namecell.textView.resignFirstResponder()
                namecell.textView.resignFirstResponder()
            }
        }

        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()

        //: if nameStr.count > 0 && LocationThen.share.loginUserMode.nickname != nameStr {
        if nameStr.count > 0, LocationThen.share.loginUserMode.nickname != nameStr {
            //: dict["nickname"] = nameStr
            dict[(kStr_sectionData.replacingOccurrences(of: "index", with: "ck") + String(kStr_normalValue))] = nameStr
        }
        //: if birthday.count > 0 && LocationThen.share.loginUserMode.birthday != birthday {
        if birthday.count > 0, LocationThen.share.loginUserMode.birthday != birthday {
            //: dict["birthday"] = birthday
            dict[(kStr_toolSucceedValue.replacingOccurrences(of: "request", with: "ir") + "hday")] = birthday
        }
        //: if signStr != "nil" && LocationThen.share.loginUserMode.signature != signStr {
        if signStr != (String(kStr_hiddenContent)), LocationThen.share.loginUserMode.signature != signStr {
            //: dict["desc"] = signStr
            dict[(kStr_viewValue.replacingOccurrences(of: "physics", with: "s"))] = signStr
        }

        //: photoAndTagTool(BackT: isBack, isTips: dict.count == 0)
        border(BackT: isBack, isTips: dict.count == 0)

        //: if VideoIconPath.count>1 && videoPath.count>1 && isChangeVideo == true && LocationThen.share.loginUserMode.sex == Gender.female.rawValue {
        if VideoIconPath.count > 1, videoPath.count > 1, isChangeVideo == true, LocationThen.share.loginUserMode.sex == SucceedComparable.female.rawValue {
            //: uploadVideo()
            transform()
        }

        //: if dict.count == 0 {
        if dict.count == 0 {
            //: return
            return
        }

        //: ProgressHUD.show()
        TintProgressHUD.weight()

        //: TalkingMeRequestTool.req_UpdateUserInfo(params: dict) { [weak self] succeed, result, errorModel in
        LoadEqualRequestTool.voiceCompletion(params: dict) { [weak self] succeed, _, errorModel in
            //: ProgressHUD.dismiss()
            TintProgressHUD.inkingPadDismiss()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                self.property(showMsg: (String(kStr_textName)).localized)

                //: if self.nameStr.count > 0 {
                if self.nameStr.count > 0 {
                    //: LocationThen.share.loginUserMode.nickname = self.nameStr
                    LocationThen.share.loginUserMode.nickname = self.nameStr
                }
                //: if self.birthday.count > 0 {
                if self.birthday.count > 0 {
                    //: LocationThen.share.loginUserMode.birthday = self.birthday
                    LocationThen.share.loginUserMode.birthday = self.birthday
                }
                //: LocationThen.share.loginUserMode.signature = self.signStr
                LocationThen.share.loginUserMode.signature = self.signStr

                //: if isBack {
                if isBack {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }

                //: } else {
            } else {
                //: if errorModel?.errorCode != 100 {
                if errorModel?.errorCode != 100 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg )
                    self.detailDownArray(showMsg: errorModel!.errorMsg)
                }
            }
        }
    }

    //: func photoAndTagTool(BackT: Bool, isTips: Bool) {
    func border(BackT: Bool, isTips: Bool) {
        //: self.isTips = isTips
        self.isTips = isTips
        //: if self.DeletePhotoArray.count>0 {
        if self.DeletePhotoArray.count > 0 {
            // 先删再加
            //: deletePhoto()
            statusTo()
            //: } else {
        } else {
            //: if self.seletePhotoArray.count>0 {
            if self.seletePhotoArray.count > 0 {
                //: uploadPhoto()
                effect()
            }
        }

        //: if isdeleteAboutme {
        if isdeleteAboutme {
            //: deleteTag(type: 1)
            indexTitle(type: 1)
        }
        //: if isdeleteInters {
        if isdeleteInters {
            //: deleteTag(type: 2)
            indexTitle(type: 2)
        }
        //: if BackT {
        if BackT {
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
    }

    /// 上传相册
    //: func uploadPhoto() {
    func effect() {
        //: for i in 0..<seletePhotoArray.count {
        for i in 0 ..< seletePhotoArray.count {
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: let resultData: NSData = seletePhotoArray[i].compressedImageData()! as NSData
            let resultData: NSData = seletePhotoArray[i].goaBean()! as NSData
            //: dict["pic"] = resultData
            dict[(String(kStr_toolText))] = resultData
            //: ProgressHUD.show()
            TintProgressHUD.weight()
            //: TalkingMeRequestTool.req_UploadPic(params: dict) { [weak self] succeed, result, errorModel in
            LoadEqualRequestTool.addModel(params: dict) { [weak self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                TintProgressHUD.inkingPadDismiss()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if succeed {
                if succeed {
                    //: if  self.isTips {
                    if self.isTips {
                        //: self.isTips = false
                        self.isTips = false
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                        self.property(showMsg: (String(kStr_textName)).localized)
                    }
                    //: self.seletePhotoArray.removeAll()
                    self.seletePhotoArray.removeAll()
                    //: self.GetGallery()
                    self.sharedTo()
                }
            }
        }
    }

    /// 删除相册
    //: func deletePhoto() {
    func statusTo() {
        //: var str = DeletePhotoArray.joined(separator: ",")
        var str = DeletePhotoArray.joined(separator: ",")
        //: str = "[\(str)]"
        str = "[\(str)]"
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["pids"] = str
        dict[(kStr_textValue.replacingOccurrences(of: "guide", with: "d"))] = str
        //: ProgressHUD.show()
        TintProgressHUD.weight()
        //: TalkingMeRequestTool.req_DeletePic(params: dict) { [weak self] succeed, result, errorModel in
        LoadEqualRequestTool.dismissPath(params: dict) { [weak self] succeed, _, _ in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if self.seletePhotoArray.count>0 {
            if self.seletePhotoArray.count > 0 {
                //: self.uploadPhoto()
                self.effect()
                //: } else {
            } else {
                //: ProgressHUD.dismiss()
                TintProgressHUD.inkingPadDismiss()
            }
            //: if succeed {
            if succeed {
                //: if  self.isTips {
                if self.isTips {
                    //: self.isTips = false
                    self.isTips = false
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                    self.property(showMsg: (String(kStr_textName)).localized)
                }

                //: self.DeletePhotoArray.removeAll()
                self.DeletePhotoArray.removeAll()
                //: self.GetGallery()
                self.sharedTo()
            }
        }
    }

    /// 修改tag
    //: func deleteTag(type: Int) {
    func indexTitle(type: Int) {
        //: var array: [UserSeleteTagModel] = []
        var array: [NameTagTransformable] = []
        //: var category = ""
        var category = ""
        //: if type==1 {
        if type == 1 {
            //: array=seleteAboutme
            array = seleteAboutme
            //: category="aboutMe"
            category = (String(kStr_kitData.prefix(6)) + kStr_imageTitle.replacingOccurrences(of: "year", with: "e"))
            //: } else {
        } else {
            //: array=seleteInters
            array = seleteInters
            //: category="interest"
            category = (String(kStr_groupValue))
        }
        //: var str = ""
        var str = ""
        //: for i in 0..<array.count {
        for i in 0 ..< array.count {
            //: let model = array[i]
            let model = array[i]
            //: str += model.tag_id!
            str += model.tag_id!
            //: if i<array.count-1 {
            if i < array.count - 1 {
                //: str += ","
                str += ","
            }
        }
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["tagIds"] = str
        dict[(kStr_frameValue.lowercased() + String(kStr_centerValue))] = str
        //: dict["category"] = category
        dict[(String(kStr_capacityData))] = category
        //: ProgressHUD.show()
        TintProgressHUD.weight()
        //: TalkingMeRequestTool.req_EditTag(params: dict) { [weak self] succeed, result, errorModel in
        LoadEqualRequestTool.source(params: dict) { [weak self] succeed, _, _ in
            //: ProgressHUD.dismiss()
            TintProgressHUD.inkingPadDismiss()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: if  self.isTips {
                if self.isTips {
                    //: self.isTips = false
                    self.isTips = false
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                    self.property(showMsg: (String(kStr_textName)).localized)
                }
                //: if type==1 {
                if type == 1 {
                    //: self.isdeleteAboutme = false
                    self.isdeleteAboutme = false
                    //: LocationThen.share.loginUserMode.aboutMe = self.seleteAboutme
                    LocationThen.share.loginUserMode.aboutMe = self.seleteAboutme
                    //: } else {
                } else {
                    //: self.isdeleteInters = false
                    self.isdeleteInters = false
                    //: LocationThen.share.loginUserMode.interest = self.seleteInters
                    LocationThen.share.loginUserMode.interest = self.seleteInters
                }
            }
        }
    }

    /// 上传视频
    //: func uploadVideo() {
    func transform() {
        //: ProgressHUD.show()
        TintProgressHUD.weight()
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 2, coverPath: self.VideoIconPath, videoPath: self.videoPath) { coverUrl, videoUrl, succeed in
        ViewSignatureProvider.shared.hidePath(type: 2, coverPath: self.VideoIconPath, videoPath: self.videoPath) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: ProgressHUD.dismiss()
                TintProgressHUD.inkingPadDismiss()
                //: self.func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
                self.detailDownArray(showMsg: String(bytes: kStr_makeReloadName.map{$0^84}, encoding: .utf8)!.localized)
                //: return
                return
            }

            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["imgUrl"] = coverUrl
            dict[(String(kStr_statusAddInfoValue.prefix(6)))] = coverUrl
            //: dict["videoUrl"] = videoUrl
            dict[(String(kStr_modeSubName) + kStr_withName.replacingOccurrences(of: "image", with: "l"))] = videoUrl

            //: TalkingMeRequestTool.uploadVideoUserEdit(params: dict) { [weak self] succeed, result, errorModel in
            LoadEqualRequestTool.con(params: dict) { [weak self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                TintProgressHUD.inkingPadDismiss()
                //: guard let self = self else { return }
                guard let self = self else { return }

                //: guard succeed else {
                guard succeed else {
                    //: self.func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
                    self.detailDownArray(showMsg: String(bytes: kStr_makeReloadName.map{$0^84}, encoding: .utf8)!.localized)
                    //: return
                    return
                }
                //: self.VideoIconPath = ""
                self.VideoIconPath = ""
                //: self.videoPath = ""
                self.videoPath = ""
                //: self.isChangeVideo = false
                self.isChangeVideo = false
                //: self.videoStatus = 0
                self.videoStatus = 0
                //: self.func__showStatusBarSuccessMsg(showMsg: "Video cover submitted successfully".localized)
                self.property(showMsg: String(bytes: kStr_lengthData.map{managerView(empty: $0)}, encoding: .utf8)!.localized)
                //: for i in self.MainTable.visibleCells {
                for i in self.MainTable.visibleCells {
                    //: if i.reuseIdentifier == "TalkingEditVideoCell" {
                    if i.reuseIdentifier == (String(kStr_boxValue.suffix(6)) + "gEditV" + kStr_sharedValue.replacingOccurrences(of: "model", with: "o") + "Cell") {
                        //: let videocell: TalkingEditVideoCell = i as! TalkingEditVideoCell
                        let videocell: PointCompartmentVideoCell = i as! PointCompartmentVideoCell
                        //: videocell.upDateCellView(videoPlayPath: dict["videoUrl"] as! String)
                        videocell.net(videoPlayPath: dict[(String(kStr_modeSubName) + kStr_withName.replacingOccurrences(of: "image", with: "l"))] as! String)
                        //: videocell.setVideoStatusLB(status: self.videoStatus)
                        videocell.nameExecute(status: self.videoStatus)
                    }
                }
            }
        }
    }

    //: func getVideoInfo() {
    func progressObtain() {
        //: TalkingMeRequestTool.getUploadVideoInfo { [weak self] succeed, result, errorModel in
        LoadEqualRequestTool.release { [weak self] succeed, result, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {return}
                guard let dict = result as? [String: Any] else { return }
                //: self.videoPath = dict["video_url"] as! String
                self.videoPath = dict[(kStr_labelTitle.replacingOccurrences(of: "capture", with: "vi") + "o_ur" + String(kStr_picData))] as! String
                //: self.VideoIconPath = dict["img_url"] as! String
                self.VideoIconPath = dict[(kStr_contentGiftDataValue.replacingOccurrences(of: "title", with: "i") + String(kStr_keyDraftText))] as! String
                //: self.videoStatus = dict["status"] as? Int ?? -3
                self.videoStatus = dict[(String(kStr_transactionData))] as? Int ?? -3
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }
}

// MARK: - Event

//: extension TalkingEditProfilesVC {
extension SizeThen {
    //: @objc func keyboardShowBeHidden(notification: NSNotification) {
    @objc func atTotal(notification: NSNotification) {
        //: let info = notification.userInfo
        let info = notification.userInfo
        //: let keyBoardHeight = (info![UIResponder.keyboardFrameEndUserInfoKey] as! NSValue).cgRectValue.size.height
        let keyBoardHeight = (info![UIResponder.keyboardFrameEndUserInfoKey] as! NSValue).cgRectValue.size.height

        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(kStr_jumpValue) + kStr_dealName.replacingOccurrences(of: "center", with: "ll")) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: CamshaftViewCell = i as! CamshaftViewCell
                //: if namecell.nameTF.isFirstResponder {
                if namecell.nameTF.isFirstResponder {
                    //: MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY-keyBoardHeight), animated: true)
                    MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY - keyBoardHeight), animated: true)
                }
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(kStr_homeContent.prefix(4)) + "ingEditS" + String(kStr_appText)) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: ShareViewDelegate = i as! ShareViewDelegate
                //: if namecell.textView.isFirstResponder {
                if namecell.textView.isFirstResponder {
                    //: MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY-keyBoardHeight), animated: true)
                    MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY - keyBoardHeight), animated: true)
                }
            }
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingEditProfilesVC: UITableViewDelegate, UITableViewDataSource {
extension SizeThen: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 7
        return 7
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: if LocationThen.share.loginUserMode.sex == Gender.female.rawValue {
            if LocationThen.share.loginUserMode.sex == SucceedComparable.female.rawValue {
                //: return 226
                return 226
            }
            //: return 0
            return 0

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let sizewh = Int((ScreenWidth-30-12*2-9*2)/3)
            let sizewh = Int((kLet_halfData - 30 - 12 * 2 - 9 * 2) / 3)
            //: let Y = 46+sizewh*3+18
            let Y = 46 + sizewh * 3 + 18
            //: let cellheiht = Y+12+26+15
            let cellheiht = Y + 12 + 26 + 15
            //: return CGFloat(cellheiht)
            return CGFloat(cellheiht)
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: return 107+15
            return 107 + 15
            //: } else if indexPath.section == 3 {
        } else if indexPath.section == 3 {
            //: return 64
            return 64
            //: } else if indexPath.section == 4 {
        } else if indexPath.section == 4 {
            //: return 176
            return 176
            //: } else if indexPath.section == 5 || indexPath.section == 6 {
        } else if indexPath.section == 5 || indexPath.section == 6 {
            //: if indexPath.section == 5 && seleteAboutme.count>0 {
            if indexPath.section == 5 && seleteAboutme.count > 0 {
                //: return self.computeCellheight(type: 1)+64
                return self.computeType(type: 1) + 64
                //: } else if indexPath.section == 6 && seleteInters.count>0 {
            } else if indexPath.section == 6 && seleteInters.count > 0 {
                //: return self.computeCellheight(type: 2)+64
                return self.computeType(type: 2) + 64
            }
            //: return 64
            return 64
        }
        //: return 56
        return 56
    }

    //: func tableView(_ tableView: UITableView, estimatedHeightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, estimatedHeightForRowAt _: IndexPath) -> CGFloat {
        //: return 56
        return 56
    }

    //: func tableView(_ tableView: UITableView, heightForHeaderInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForHeaderInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: let identifier = "TalkingEditVideoCell"
            let identifier = (String(kStr_boxValue.suffix(6)) + "gEditV" + kStr_sharedValue.replacingOccurrences(of: "model", with: "o") + "Cell")
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditVideoCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? PointCompartmentVideoCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditVideoCell(style: .default, reuseIdentifier: identifier)
                cell = PointCompartmentVideoCell(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.setVideoStatusLB(status: self.videoStatus)
            cell?.nameExecute(status: self.videoStatus)
            //: if VideoIconPath.count>1 && videoPath.count>1 {
            if VideoIconPath.count > 1 && videoPath.count > 1 {
                //: cell?.setCellView(iconPath: VideoIconPath)
                cell?.gestureHome(iconPath: VideoIconPath)
                //: cell?.upDateCellView(videoPlayPath: videoPath)
                cell?.net(videoPlayPath: videoPath)
            }
            //: cell?.seleteBlock = { [weak self] imagPath, VideoPath in
            cell?.seleteBlock = { [weak self] imagPath, VideoPath in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.VideoIconPath = imagPath
                self.VideoIconPath = imagPath
                //: self.videoPath = VideoPath
                self.videoPath = VideoPath
                //: self.isChangeVideo = true
                self.isChangeVideo = true
            }
            //: cell?.deleteBlock = { [weak self] in
            cell?.deleteBlock = { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.VideoIconPath = ""
                self.VideoIconPath = ""
                //: self.videoPath = ""
                self.videoPath = ""
                //: self.isChangeVideo = false
                self.isChangeVideo = false
            }
            //: return cell!
            return cell!

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let identifier = "TalkingEditPhotoCell"
            let identifier = (String(kStr_modelText))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditPhotoCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? PointPhotoCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditPhotoCell(style: .default, reuseIdentifier: identifier)
                cell = PointPhotoCell(style: .default, reuseIdentifier: identifier)
            }
            //: if PhotoArray.count>0 {
            if PhotoArray.count > 0 {
                //: cell?.setPhoto(selete: PhotoArray)
                cell?.remind(selete: PhotoArray)
                //: } else {
            } else {
                //: cell?.resetPhoto()
                cell?.app()
            }
            //: cell?.delegate = self
            cell?.delegate = self
            //: return cell!
            return cell!
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: let identifier = "TalkingEditNameCell"
            let identifier = (String(kStr_jumpValue) + kStr_dealName.replacingOccurrences(of: "center", with: "ll"))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditNameCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? CamshaftViewCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditNameCell(style: .default, reuseIdentifier: identifier)
                cell = CamshaftViewCell(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.nameBlock = { [weak self] name in
            cell?.nameBlock = { [weak self] name in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.nameStr = name
                self.nameStr = name
            }
            //: return cell!
            return cell!
            //: } else if indexPath.section == 3 {
        } else if indexPath.section == 3 {
            //: let identifier = "TalkingEditBirthdayCell"
            let identifier = (String(kStr_equalTitle.suffix(8)) + String(kStr_halfData) + String(kStr_labTitle))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditBirthdayCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? DataThen
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditBirthdayCell(style: .default, reuseIdentifier: identifier)
                cell = DataThen(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.birthdayBlock = { [weak self] day in
            cell?.birthdayBlock = { [weak self] day in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.birthday = day
                self.birthday = day
            }
            //: return cell!
            return cell!
            //: } else if indexPath.section == 4 {
        } else if indexPath.section == 4 {
            //: let identifier = "TalkingEditSignCell"
            let identifier = (String(kStr_homeContent.prefix(4)) + "ingEditS" + String(kStr_appText))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditSignCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? ShareViewDelegate
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditSignCell(style: .default, reuseIdentifier: identifier)
                cell = ShareViewDelegate(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.signBlock = { [weak self] sign in
            cell?.signBlock = { [weak self] sign in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.signStr = sign
                self.signStr = sign
            }

            //: return cell!
            return cell!
            //: } else if indexPath.section == 5 || indexPath.section == 6 {
        } else if indexPath.section == 5 || indexPath.section == 6 {
            //: let identifier = "TalkingEditAboutMeCell"
            let identifier = (String(kStr_makeValue.suffix(4)) + "ingEdi" + String(kStr_liveGiftYouContent.prefix(5)) + "tMeCell")
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditAboutMeCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? MakeItemView
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditAboutMeCell(style: .default, reuseIdentifier: identifier)
                cell = MakeItemView(style: .default, reuseIdentifier: identifier)
            }
            //: if indexPath.section == 5 {
            if indexPath.section == 5 {
                //: cell!.tagtype = .AboutMe
                cell!.tagtype = .AboutMe
                //: } else {
            } else {
                //: cell!.tagtype = .Interests
                cell!.tagtype = .Interests
            }
            //: cell?.setTitle()
            cell?.lockage()
            //: if seleteAboutme.count>0 && indexPath.section == 5 {
            if seleteAboutme.count > 0 && indexPath.section == 5 {
                //: cell?.setMessage(seleteAboutme)
                cell?.voiceUp(seleteAboutme)
            }
            //: if seleteInters.count>0 && indexPath.section == 6 {
            if seleteInters.count > 0 && indexPath.section == 6 {
                //: cell?.setMessage(seleteInters)
                cell?.voiceUp(seleteInters)
            }
            //: cell?.editBtnBlock = { [weak self] in
            cell?.editBtnBlock = { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: let vc = TalkingEditTagVC.init()
                let vc = FillUpViewController()
                //: vc.delegate = self
                vc.delegate = self
                //: if indexPath.section == 5 {
                if indexPath.section == 5 {
                    //: vc.tagtype = .AboutMe
                    vc.tagtype = .AboutMe
                    //: } else {
                } else {
                    //: vc.tagtype = .Interests
                    vc.tagtype = .Interests
                }
                //: self.navigationController!.pushViewController(vc, animated: true)
                self.navigationController!.pushViewController(vc, animated: true)
            }
            //: cell?.deleteBlock = { [weak self] tag in
            cell?.deleteBlock = { [weak self] tag in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if indexPath.section == 5 {
                if indexPath.section == 5 {
                    //: if  self.seleteAboutme.contains(where: { $0.equals(compareTo: tag)}) {
                    if self.seleteAboutme.contains(where: { $0.mistake(compareTo: tag) }) {
                        //: self.seleteAboutme.removeAll(where: {$0.equals(compareTo: tag)})
                        self.seleteAboutme.removeAll(where: { $0.mistake(compareTo: tag) })
                        //: self.isdeleteAboutme = true
                        self.isdeleteAboutme = true
                    }
                    //: } else {
                } else {
                    //: if  self.seleteInters.contains(where: { $0.equals(compareTo: tag)}) {
                    if self.seleteInters.contains(where: { $0.mistake(compareTo: tag) }) {
                        //: self.seleteInters.removeAll(where: {$0.equals(compareTo: tag)})
                        self.seleteInters.removeAll(where: { $0.mistake(compareTo: tag) })
                        //: self.isdeleteInters = true
                        self.isdeleteInters = true
                    }
                }
            }
            //: return cell!
            return cell!
            //: } else {
        } else {
            //: let identifier = "UITableViewCell"
            let identifier = (String(kStr_bagTitle) + String(kStr_sizeName) + String(kStr_labNeedLetData))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier)
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier)
            //: if cell == nil {
            if cell == nil {
                //: cell = UITableViewCell(style: .default, reuseIdentifier: identifier)
                cell = UITableViewCell(style: .default, reuseIdentifier: identifier)
            }
            //: return cell!
            return cell!
        }
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    //: func computeCellheight(type: Int) -> CGFloat {
    func computeType(type: Int) -> CGFloat {
        //: let itemSpacing = CGFloat(8)
        let itemSpacing = CGFloat(8)
        //: var contentWidthInRow = CGFloat(0)
        var contentWidthInRow = CGFloat(0)
        //: var array = Array<UserSeleteTagModel>()
        var array = [NameTagTransformable]()
        //: if type == 1 {
        if type == 1 {
            //: array = self.seleteAboutme
            array = self.seleteAboutme
            //: } else {
        } else {
            //: array = self.seleteInters
            array = self.seleteInters
        }
        //: var contentHeightInRow = CGFloat(30)
        var contentHeightInRow = CGFloat(30)

        //: for item in array {
        for item in array {
            //: var tag_name = ""
            var tag_name = ""
            //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue {
            if ViewManagerAddrTool.share.interfaceLang == CountClusterLiteral.ar.rawValue {
                //: tag_name = item.tag_name_ar
                tag_name = item.tag_name_ar
                //: } else if TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue {
            } else if ViewManagerAddrTool.share.interfaceLang == CountClusterLiteral.es.rawValue {
                //: tag_name = item.tag_name_es
                tag_name = item.tag_name_es
                //: } else if TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
            } else if ViewManagerAddrTool.share.interfaceLang == CountClusterLiteral.pt.rawValue {
                //: tag_name = item.tag_name_pt
                tag_name = item.tag_name_pt
            }
            //: if tag_name.count <= 0 {
            if tag_name.count <= 0 {
                //: tag_name = item.tag_name
                tag_name = item.tag_name
            }
            //: let rect = (tag_name as NSString).boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.pingfangFont(type: .Regular, fontSize: 15)], context: nil)
            let rect = (tag_name as NSString).boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.groupSize(type: .Regular, fontSize: 15)], context: nil)
            //: let tagWidth = rect.width + 30
            let tagWidth = rect.width + 30
            //: if contentWidthInRow + tagWidth + itemSpacing > ScreenWidth-54 {
            if contentWidthInRow + tagWidth + itemSpacing > kLet_halfData - 54 {
                // 需要换行
                //: contentHeightInRow += (10 + 30)
                contentHeightInRow += (10 + 30)
                //: contentWidthInRow = itemSpacing
                contentWidthInRow = itemSpacing
                //: } else {
            } else {
                //: contentWidthInRow += (tagWidth+itemSpacing)
                contentWidthInRow += (tagWidth + itemSpacing)
            }
        }
        //: if array.count == 10 {
        if array.count == 10 {
            //: contentHeightInRow += 40
            contentHeightInRow += 40
        }
        //: return contentHeightInRow + 12
        return contentHeightInRow + 12
    }
}

// MARK: - ValueThen

//: extension TalkingEditProfilesVC: EditTagDelegate {
extension SizeThen: ValueThen {
    //: func freshSeleteTag() {
    func outsideTag() {
        //: seleteAboutme =  LocationThen.share.loginUserMode.aboutMe!
        seleteAboutme = LocationThen.share.loginUserMode.aboutMe!
        //: seleteInters =  LocationThen.share.loginUserMode.interest!
        seleteInters = LocationThen.share.loginUserMode.interest!
        //: self.MainTable .reloadData()
        self.MainTable.reloadData()
    }
}

// MARK: - SeekReactiveCompatible

//: extension TalkingEditProfilesVC: EditPhotoDelegate {
extension SizeThen: SeekReactiveCompatible {
    /// 添加本地图片
    //: func addPhoto(_ icon: [UIImage]) {
    func send(_ icon: [UIImage]) {
        //: for i in icon {
        for i in icon {
            //: self.seletePhotoArray.append(i)
            self.seletePhotoArray.append(i)
            //: var model = TalkingUserInfoGalleryModel.init()
            var model = CountModelType()
            //: model.imagePic = i
            model.imagePic = i
            //: model.isLocal = true
            model.isLocal = true
            //: PhotoArray.append(model)
            PhotoArray.append(model)
        }
    }

    /// 删除本地相册选择的图片
    //: func deletePhoto(_ icon: UIImage) {
    func dayOfRemembrance(_ icon: UIImage) {
        //: if  seletePhotoArray.contains(where: {$0 == icon}) {
        if seletePhotoArray.contains(where: { $0 == icon }) {
            //: seletePhotoArray.removeAll(where: {$0 == icon})
            seletePhotoArray.removeAll(where: { $0 == icon })
        }
        //: var seleteIndex = -1
        var seleteIndex = -1
        //: for i in 0..<PhotoArray.count {
        for i in 0 ..< PhotoArray.count {
            //: if icon == PhotoArray[i].imagePic {
            if icon == PhotoArray[i].imagePic {
                //: seleteIndex =  i
                seleteIndex = i
                //: break
                break
            }
        }
        //: if seleteIndex>=0 && seleteIndex<PhotoArray.count {
        if seleteIndex >= 0 && seleteIndex < PhotoArray.count {
            //: PhotoArray.remove(at: seleteIndex)
            PhotoArray.remove(at: seleteIndex)
            //: self.MainTable.reloadData()
            self.MainTable.reloadData()
        }
    }

    /// 删除后台相册的图片
    //: func deletePhotoWithUid(_ iconUid: String) {
    func postName(_ iconUid: String) {
        //: var seleteIndex = -1
        var seleteIndex = -1
        //: for i in 0..<PhotoArray.count {
        for i in 0 ..< PhotoArray.count {
            //: if iconUid == PhotoArray[i].uid {
            if iconUid == PhotoArray[i].uid {
                //: seleteIndex =  i
                seleteIndex = i
                //: self.DeletePhotoArray.append(iconUid)
                self.DeletePhotoArray.append(iconUid)
            }
        }
        //: if seleteIndex>=0 && seleteIndex<PhotoArray.count {
        if seleteIndex >= 0 && seleteIndex < PhotoArray.count {
            //: PhotoArray.remove(at: seleteIndex)
            PhotoArray.remove(at: seleteIndex)
            //: self.MainTable.reloadData()
            self.MainTable.reloadData()
        }
    }
}

// MARK: - UI

//: extension TalkingEditProfilesVC {
extension SizeThen {
    //: private func designView() {
    private func project() {
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingEditVideoCell.self, forCellReuseIdentifier: "TalkingEditVideoCell")
        MainTable.register(PointCompartmentVideoCell.self, forCellReuseIdentifier: (String(kStr_boxValue.suffix(6)) + "gEditV" + kStr_sharedValue.replacingOccurrences(of: "model", with: "o") + "Cell"))
        //: MainTable.register(TalkingEditPhotoCell.self, forCellReuseIdentifier: "TalkingEditPhotoCell")
        MainTable.register(PointPhotoCell.self, forCellReuseIdentifier: (String(kStr_modelText)))
        //: MainTable.register(TalkingEditNameCell.self, forCellReuseIdentifier: "TalkingEditNameCell")
        MainTable.register(CamshaftViewCell.self, forCellReuseIdentifier: (String(kStr_jumpValue) + kStr_dealName.replacingOccurrences(of: "center", with: "ll")))
        //: MainTable.register(TalkingEditBirthdayCell.self, forCellReuseIdentifier: "TalkingEditBirthdayCell")
        MainTable.register(DataThen.self, forCellReuseIdentifier: (String(kStr_equalTitle.suffix(8)) + String(kStr_halfData) + String(kStr_labTitle)))
        //: MainTable.register(TalkingEditSignCell.self, forCellReuseIdentifier: "TalkingEditSignCell")
        MainTable.register(ShareViewDelegate.self, forCellReuseIdentifier: (String(kStr_homeContent.prefix(4)) + "ingEditS" + String(kStr_appText)))
        //: MainTable.register(TalkingEditAboutMeCell.self, forCellReuseIdentifier: "TalkingEditAboutMeCell")
        MainTable.register(MakeItemView.self, forCellReuseIdentifier: (String(kStr_makeValue.suffix(4)) + "ingEdi" + String(kStr_liveGiftYouContent.prefix(5)) + "tMeCell"))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(self.view)
            make.leading.trailing.bottom.equalTo(self.view)
            //: make.top.equalTo(self.view).offset(20)
            make.top.equalTo(self.view).offset(20)
        }
    }
}
