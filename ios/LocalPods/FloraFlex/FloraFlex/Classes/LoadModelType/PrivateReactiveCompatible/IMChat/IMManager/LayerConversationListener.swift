
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let kStr_titleAreaData:String = "IM\u{9000}出"
fileprivate let kStr_democraticValue:[Character] = ["c","o","d","e",":"]

/*: , desc: :*/
fileprivate let kStr_finishValue:[Character] = [","," ","d","e","s"]
fileprivate let kStr_thenTitle:[Character] = ["c",":"]

/*: ." :*/
fileprivate let kStr_nameValue:String = "till"

/*: "IM登录失败，code: :*/
fileprivate let kStr_equalValue:[Character] = ["I","M","\u{767b}","录","\u{5931}","\u{8d25}","，","c","o","d","e",":"]

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let kStr_validName:String = "item balance wrap label fileIM获取未读"
fileprivate let kStr_modelText:String = "消息总"
fileprivate let kStr_managerAllName:[Character] = ["数","失","败","，","c","o","d","e",":"]

/*: "crushBadge" :*/
fileprivate let kStr_succeedValue:String = "crushBmodel return"

/*: "badNumber" :*/
fileprivate let kStr_languageRangeName:[UInt8] = [0xde,0xdd,0xd8,0xf2,0xc9,0xd1,0xde,0xd9,0xce]

/*: "plaza" :*/
fileprivate let kStr_developmentName:String = "plaview"

/*: "roomNum" :*/
fileprivate let kStr_closeOriginalData:String = "roomNumbottom point show"

/*: "num" :*/
fileprivate let kStr_contentName:String = "nuhidden"

/*: "onlyOneRoomId" :*/
fileprivate let kStr_formatText:[Character] = ["o","n","l","y","O","n","e","R","o","o","m","I","d"]

/*: "LIVE-CHAT" :*/
fileprivate let kStr_clickTitle:String = "LIVE-Creturn source self"
fileprivate let kStr_objectName:[Character] = ["H","A","T"]

/*: "PARTY-CHAT" :*/
fileprivate let kStr_imageName:String = "playRTY"

/*: "extra" :*/
fileprivate let kStr_indexManagerData:[Character] = ["e","x","t","r","a"]

/*: "msgType" :*/
fileprivate let kStr_userData:String = "else you shirtmsgTyp"
fileprivate let kStr_cloudValue:String = "top"

/*: "newPkgGift" :*/
fileprivate let kStr_tagValue:String = "let partynewP"

/*: "MF:ChatMentionMsg" :*/
fileprivate let kStr_backgroundTitle:String = "MF:Chshare camera"
fileprivate let kStr_toYearValue:String = "atMentin target"

/*: "uid" :*/
fileprivate let kStr_fadeName:[UInt8] = [0x3,0xf7,0xf2]

fileprivate func wrapPath(data num: UInt8) -> UInt8 {
    let value = Int(num) - 142
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "onlineStatus" :*/
fileprivate let kStr_tableName:[UInt8] = [0x73,0x75,0x74,0x61,0x74,0x53,0x65,0x6e,0x69,0x6c,0x6e,0x6f]

/*: "isNewUser" :*/
fileprivate let kStr_detailName:[UInt8] = [0xfd,0xe7,0xda,0xf1,0xe3,0xc1,0xe7,0xf1,0xe6]

private func greetFemaleWindow(cover num: UInt8) -> UInt8 {
    return num ^ 148
}

/*: "c2c_ :*/
fileprivate let kStr_enableTitle:[Character] = ["c","2","c","_"]

/*: "IM获取会话失败，convId: :*/
fileprivate let kStr_nextShowContent:String = "IM获取会话reply color"
fileprivate let kStr_appColorText:[Character] = ["I","d",":"]

/*: , code: :*/
fileprivate let kStr_titleData:String = "player value, code:"

/*: "IM插入消息失败，targetId: :*/
fileprivate let kStr_subData:String = "IM插入消息top sub"
fileprivate let kStr_emptyText:String = "argetIself deadline size var data"
fileprivate let kStr_menuData:String = "d:written replace"

/*: "changeDirection" :*/
fileprivate let kStr_appearLeadingTitle:String = "CHANGE"
fileprivate let kStr_pathNameValue:String = "TION"

/*: "rYMsgType" :*/
fileprivate let kStr_cellName:String = "rYMsindex gift array var"
fileprivate let kStr_viewText:[Character] = ["g","T","y","p","e"]

/*: "GJ:CallCustom" :*/
fileprivate let kStr_rowData:String = "var normal makeGJ:Ca"
fileprivate let kStr_errorName:[Character] = ["m"]

/*: "IM获取会话列表失败，code: :*/
fileprivate let kStr_inviteHandleContent:String = "IM获\u{53d6}会"
fileprivate let kStr_itemOfToTitle:String = "话列\u{8868}失败，"

/*: "msgInfo" :*/
fileprivate let kStr_sizeViewData:String = "content fill jumpmsgInfo"

/*: "needFold" :*/
fileprivate let kStr_requestData:[Character] = ["n","e","e","d","F","o","l"]
fileprivate let kStr_appearName:String = "text"

/*: "group_ :*/
fileprivate let kStr_insideValue:String = "play"
fileprivate let kStr_subContent:String = "roup_medium block make"

/*: "IM删除会话失败，convID: :*/
fileprivate let kStr_selectedData:[Character] = ["I","M","删","除","\u{4f1a}","话","\u{5931}","败","，","c","o","n","v","I"]
fileprivate let kStr_succeedPlayerCellValue:String = "model group dataD:"

/*: "IM置顶会话失败，convId: :*/
fileprivate let kStr_countTimeData:[Character] = ["I","M","置","顶","会","\u{8bdd}","失","败","，","c"]
fileprivate let kStr_iconContent:[Character] = ["o","n","v","I","d",":"]

/*: "SDK 正在连接到服务器" :*/
fileprivate let kStr_toName:[Character] = ["S","D","K"," ","\u{6b63}","在","\u{8fde}","\u{63a5}","到","服","务","器"]

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let kStr_contentText:[Character] = ["S","D","K"," ","已","经","成","\u{529f}"]
fileprivate let kStr_readingData:String = "\u{8fde}接到\u{670d}\u{52a1}器"

/*: "IM连接服务器失败，code: :*/
fileprivate let kStr_targetTitle:[Character] = ["I","M","连","\u{63a5}","\u{670d}"]
fileprivate let kStr_displayEqualViewName:[Character] = ["务","器","失","败","，","c","o","d","e",":"]

/*: , error: :*/
fileprivate let kStr_dismissContent:String = "add model manager point view, er"

/*: "IM账号被挤下线。" :*/
fileprivate let kStr_listData:String = "IM账号被case point"

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let kStr_actionName:[UInt8] = []

private func typeReturn(icon num: UInt8) -> UInt8 {
    return num ^ 154
}

/*: "Offline Notification" :*/
fileprivate let kStr_backTitle:[Character] = ["O","f","f","l","i","n","e"," ","N","o","t","i","f","i"]
fileprivate let kStr_acceptData:String = "cashareion"

/*: "Cancel" :*/
fileprivate let kStr_neverData:String = "record to make up leftCancel"

/*: "Re Login" :*/
fileprivate let kStr_mainData:String = "Re Lcenter data"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LayerConversationListener.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol VideoManagerDelegate: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func bearOnData(data: [ViewConversationModel])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func remark(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func telegram(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum ClientTableDataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class LayerConversationListener: NSObject {
@objcMembers public class LayerConversationListener: NSObject {
    //: public static let shared = LayerConversationListener()
    public static let shared = LayerConversationListener()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [ViewConversationModel] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [ViewConversationModel] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [ViewConversationModel] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [ViewConversationModel] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [ViewConversationModel] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: ViewConversationModel]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.viewText()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func byFrom() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(kLet_keyName, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func viewText() {
        //: func__removeAllDelegate()
        tagDelegate()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.dismissUser()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func dismissUser() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = ViewConversationModel.packet(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = ViewConversationModel.packet(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func complete() -> [ViewConversationModel] {
        // 审核模式，在消息列表移除官方客服
        //: if LocationThen.share.appStatus == AppSkinStatus.special.rawValue {
        if LocationThen.share.appStatus == SharedInsetTarget.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == TableMacroDefine.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == TableMacroDefine.isothermal() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension LayerConversationListener {
extension LayerConversationListener {
    //: func func__removeAllDelegate() {
    func tagDelegate() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func afterStatus(_ delegate: VideoManagerDelegate) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func numbereraction(_ delegate: VideoManagerDelegate) {
        //: self.func__invokeDelegate {
        self.poetise {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func poetise(_ invocation: (VideoManagerDelegate) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! VideoManagerDelegate)
        }
    }
}

// MARK: - IM登录态管理

//: extension LayerConversationListener {
extension LayerConversationListener {
    /// 退出登录IM
    //: func func__LogingOut() {
    func failure() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: MiniLoadThen.appLine(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            MiniLoadThen.appLine(msg: (kStr_titleAreaData + "登\u{5f55}失败\u{ff0c}" + String(kStr_democraticValue)) + "\(code)" + (String(kStr_finishValue) + String(kStr_thenTitle)) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.viewText()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func deleteOff(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = LocationThen.share.appUserConfigMode.uSig
        let uSigStr = LocationThen.share.appUserConfigMode.uSig
        //: let uidStr = LocationThen.share.loginUserMode.userID
        let uidStr = LocationThen.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.unread()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: MiniLoadThen.appLine(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            MiniLoadThen.appLine(msg: (String(kStr_equalValue)) + "\(code)" + (String(kStr_finishValue) + String(kStr_thenTitle)) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func stack() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || LocationThen.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || LocationThen.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.detailDownArray(showMsg: kLet_accuracyContent)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension LayerConversationListener {
extension LayerConversationListener {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func unread() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            MiniLoadThen.appLine(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func cellMain() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: UpsetCountRequestTool.req_sysMessageList { _, result in
        UpsetCountRequestTool.cellApp { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(String(kStr_succeedValue.prefix(6)) + "adge")] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: kLet_statusName, object: self, userInfo: [String(bytes: kStr_languageRangeName.map{$0^188}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func chapter(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: UpsetCountRequestTool.req_getChatRoomInfo { succeed, result, _ in
        UpsetCountRequestTool.tin { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(kStr_developmentName.replacingOccurrences(of: "view", with: "za"))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(kStr_closeOriginalData.prefix(7)))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = ViewConversationModel.packet(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(kStr_contentName.replacingOccurrences(of: "hidden", with: "m"))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(String(kStr_formatText))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension LayerConversationListener: V2TIMAdvancedMsgListener {
extension LayerConversationListener: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT") || msg.groupID.contains("PARTY-CHAT")) { // 开播/语聊房 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(kStr_clickTitle.prefix(6)) + String(kStr_objectName))) || msg.groupID.contains((kStr_imageName.replacingOccurrences(of: "play", with: "PA") + "-CHAT"))) { // 开播/语聊房 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            FlipDanmuManager.underObserver().nameMsg(msg: msg)
            //: return
            return
        }
        //: if msg.sender == LocationThen.share.loginUserMode.userID { return }
        if msg.sender == LocationThen.share.loginUserMode.userID { return }

        //: let dic = TUIMsgChatManager.parseTXMessageData(data: msg.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(String(kStr_indexManagerData))] as! [AnyHashable: Any]
        //: let msgModel = KeyModel(dic: extraDic)
        let msgModel = KeyModel(dic: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: kLet_endFinishName, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(String(kStr_userData.suffix(6)) + kStr_cloudValue.replacingOccurrences(of: "top", with: "e"))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(kStr_tagValue.suffix(4)) + "kgGift") { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: kLet_limitTitle, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if TUIMsgChatManager.checkVoiceMessage(message: msg) {
            if TUIMsgChatManager.checkVoiceMessage(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                //: GoFarReactiveCompatible.db_inserVoiceMsgToDB(dic)
                GoFarReactiveCompatible.mini(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.rangeSectionRefuse(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            PrivateReactiveCompatible.shared.anPlus(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel.msgInfo.messageType == .MessageTypeGift {
        if msgModel.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            PrivateReactiveCompatible.shared.course(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(kStr_backgroundTitle.prefix(5)) + String(kStr_toYearValue.prefix(6)) + "ionMsg") { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            ViewReactiveCompatible.share.mention(msg: msg, extraInfo: extraDic)
        }

        //: if !msgModel.gift.unlockMsgId.isEmptyString {
        if !msgModel.gift.unlockMsgId.isEmptyString { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: FillPathChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel.gift.unlockMsgId)
            array.append(msgModel.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: FillPathChatManager.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != TableMacroDefine.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != TableMacroDefine.toVoice() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.poetise { $0.telegram?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            ClientShadowNotificationManager.shared.scaleSub(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: kStr_fadeName.map{wrapPath(data: $0)}, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: kStr_tableName.reversed(), encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel.msgInfo.isNewUser] as [String: Any]
                                String(bytes: kStr_detailName.map{greetFemaleWindow(cover: $0)}, encoding: .utf8)!: msgModel.msgInfo.isNewUser] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: kLet_screenStatusData,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: ExamineedReactiveCompatible.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            ExamineedReactiveCompatible.endName(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            FillPathChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: kLet_ageValue, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: TUIMessageCellData, isPrivete: Bool) {
    @objc public func want(targetId: String, msgCellData: TUIMessageCellData, isPrivete: Bool) {
        //: let imMsg = msgCellData.innerMessage
        let imMsg = msgCellData.innerMessage

        //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        self.buttonColor(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(kStr_enableTitle)) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.infoIconFile(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.rowText(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: MiniLoadThen.appLine(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
                MiniLoadThen.appLine(msg: (String(kStr_nextShowContent.prefix(6)) + "\u{5931}败，conv" + String(kStr_appColorText)) + "\(convId)" + (String(kStr_titleData.suffix(7))) + "\(code)" + (String(kStr_finishValue) + String(kStr_thenTitle)) + "\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func buttonColor(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = LocationThen.share.loginUserMode.userID
        let senderStr = LocationThen.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                MiniLoadThen.appLine(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func rangeSectionRefuse(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(kStr_appearLeadingTitle.lowercased() + "Direc" + kStr_pathNameValue.lowercased())].boolValue
        if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && EffectPushManager.share.quantityimateTarget()!.isKind(of: SucceedTitleControllerDelegate.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: kLet_errorText) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: kLet_errorText)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension LayerConversationListener: V2TIMConversationListener {
extension LayerConversationListener: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.infoIconFile(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.rowText(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.infoIconFile(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.rowText(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: LocationThen.share.unreadMessageNum = Int(totalUnreadCount)
        LocationThen.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.poetise {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.remark?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func rowText(conVersationList: [ViewConversationModel]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if ExamineedReactiveCompatible.shared().needReqUidsSet != nil {
            if ExamineedReactiveCompatible.force().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: ExamineedReactiveCompatible.shared().needReqUidsSet!.formUnion(set)
                ExamineedReactiveCompatible.force().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.poetise {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.bearOnData?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func vocalism(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [ViewConversationModel] = []

        // V2TIMConversation->ViewConversationModel
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = ViewConversationModel(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.playListConv(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [ViewConversationModel] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = BottomInfoTable.someoneCheck(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.playShowTop(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension LayerConversationListener {
extension LayerConversationListener {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func keyEvent(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.vocalism(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.rowText(conVersationList: arr as? [ViewConversationModel] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: MiniLoadThen.appLine(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
            MiniLoadThen.appLine(msg: (kStr_inviteHandleContent + kStr_itemOfToTitle + "code:") + "\(code)" + (String(kStr_finishValue) + String(kStr_thenTitle)) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func awakeFail(targetId: String) -> ViewConversationModel? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func infoIconFile(convList: [V2TIMConversation]) -> [ViewConversationModel] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [ViewConversationModel] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = ViewConversationModel(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.playListConv(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.playShowTop(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func playShowTop(convModel: ViewConversationModel, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.dismissUser()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: ClientTableDataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - LocationThen.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - LocationThen.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = ExamineedReactiveCompatible.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = ExamineedReactiveCompatible.belowStart(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = TUIMsgChatManager.parseTXMessageData(data: elemData)
            let dict = TUIMsgChatManager.parseTXMessageData(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(String(kStr_indexManagerData))][(String(kStr_sizeViewData.suffix(7)))][(String(kStr_requestData) + kStr_appearName.replacingOccurrences(of: "text", with: "d"))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.conTitle(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.conTitle(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func conTitle(convList: [ViewConversationModel], dataType: ClientTableDataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func prop(targetId: String, dataType: ClientTableDataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(kStr_enableTitle)) + "\(targetId)" : (kStr_insideValue.replacingOccurrences(of: "play", with: "g") + String(kStr_subContent.prefix(5))) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: ExamineedReactiveCompatible.db_removeRecord(with: targetId)
            ExamineedReactiveCompatible.quantityTable(with: targetId)
            //: GoFarReactiveCompatible.db_deleteVoiceMsg(LocationThen.share.loginUserMode.userID, toUid: targetId)
            GoFarReactiveCompatible.videoVocalisation(LocationThen.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.unread()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: MiniLoadThen.appLine(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
            MiniLoadThen.appLine(msg: (String(kStr_selectedData) + String(kStr_succeedPlayerCellValue.suffix(2))) + "\(convID)" + (String(kStr_titleData.suffix(7))) + "\(code)" + (String(kStr_finishValue) + String(kStr_thenTitle)) + "\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func inheritanceUser(convModel: ViewConversationModel, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: MiniLoadThen.appLine(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
            MiniLoadThen.appLine(msg: (String(kStr_countTimeData) + String(kStr_iconContent)) + "\(convModel.conversationID)" + (String(kStr_titleData.suffix(7))) + "\(code)" + (String(kStr_finishValue) + String(kStr_thenTitle)) + "\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.conTitle(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.conTitle(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension LayerConversationListener: V2TIMSDKListener {
extension LayerConversationListener: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        pathLine(message: (String(kStr_toName)))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        pathLine(message: (String(kStr_contentText) + kStr_readingData))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: MiniLoadThen.appLine(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        MiniLoadThen.appLine(msg: (String(kStr_targetTitle) + String(kStr_displayEqualViewName)) + "\(code)" + (String(kStr_dismissContent.suffix(4)) + "ror:") + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: MiniLoadThen.appLine(msg: "IM账号被挤下线。")
        MiniLoadThen.appLine(msg: (String(kStr_listData.prefix(5)) + "挤下线。"))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        ToSocketManager.shared.atPush(userLoginOut: true)
        //: LayerConversationListener.shared.isConnection = false
        LayerConversationListener.shared.isConnection = false
        // 销毁上传busy状态定时器
        //: (TalkingApplication.shared as! TalkingApplication).destroy()
        (LayerKingfisherCompatible.shared as! LayerKingfisherCompatible).blackTitle()

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: kStr_actionName.map{typeReturn(icon: $0)}, encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = StreetwiseAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if ViewManagerAddrTool.share.interfaceLang == CountClusterLiteral.ar.rawValue || ViewManagerAddrTool.share.interfaceLang == CountClusterLiteral.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            ViewManagerAddrTool.share.interfaceLang == CountClusterLiteral.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.dorsum(fontSize: 13)
            config.rightFont = UIFont.dorsum(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        ClickReactiveCompatible.follow()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        ClickReactiveCompatible.follow(title: (String(kStr_backTitle) + kStr_acceptData.replacingOccurrences(of: "share", with: "t")).localized, message: msg, leftBtnTitle: (String(kStr_neverData.suffix(6))).localized, rightBtnTitle: (String(kStr_mainData.prefix(4)) + "ogin").localized, leftBlock: {
            // 销毁直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            VideoPushListener.isGesture().outAssets()
            // 销毁语聊房
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            SubCheckedReactiveCompatible.replyShared().viewBy()
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            TableThen.equal { t in
                //: ProgressHUD.dismiss()
                TintProgressHUD.inkingPadDismiss()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: kLet_statusFailData, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            TableThen.dismiss { t in
                //: if t {
                if t {
                    //: LayerConversationListener.shared.func__LogingIn { _ in
                    LayerConversationListener.shared.deleteOff { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if VideoPushListener.isGesture().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(VideoPushListener.isGesture().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    ToSocketManager.shared.statusSocket()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
