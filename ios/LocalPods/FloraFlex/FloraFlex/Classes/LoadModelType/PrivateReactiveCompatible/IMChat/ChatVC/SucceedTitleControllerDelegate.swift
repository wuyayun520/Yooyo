
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let kStr_mainWithTitle:[UInt8] = [0x17,0x10,0x17,0xa,0x56,0x1d,0x11,0x1a,0x1b,0xc,0x44,0x57,0x5e,0x16,0x1f,0xd,0x5e,0x10,0x11,0xa,0x5e,0x1c,0x1b,0x1b,0x10,0x5e,0x17,0x13,0xe,0x12,0x1b,0x13,0x1b,0x10,0xa,0x1b,0x1a]

/*: "F5F5F5" :*/
fileprivate let kStr_imageData:[Character] = ["F","5","F","5","F","5"]

/*: "TalkingChatTextMsgCellID" :*/
fileprivate let kStr_equalValue:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","T","e","x"]
fileprivate let kStr_originalText:String = "status string equal stringtMsgC"

/*: "TalkingChatImageMsgCellID" :*/
fileprivate let kStr_buildMidData:String = "type selfTalk"
fileprivate let kStr_maxTitle:[Character] = ["a","t","I","m","a","g","e","M","s","g","C"]
fileprivate let kStr_textModelData:[Character] = ["e","l","l","I","D"]

/*: "TalkingChatVideoMsgCellID" :*/
fileprivate let kStr_storeName:String = "Talkinfalse the"
fileprivate let kStr_sizeTitle:[Character] = ["g","C","h"]
fileprivate let kStr_fileValue:String = "true scale var textatVid"
fileprivate let kStr_fillData:String = "CellIDlabel judge"

/*: "TalkingChatGiftMsgCellID" :*/
fileprivate let kStr_likeTitle:String = "Talkinself table label"
fileprivate let kStr_padName:String = "ftMsreturn text"

/*: "OverfillMessageCell" :*/
fileprivate let kStr_messageTitle:String = "closed to data section selfTalk"
fileprivate let kStr_countAngleText:String = "top add imagehatAskFo"
fileprivate let kStr_withValue:String = "light size pushrGiftM"

/*: "TalkingChatTipsMsgCellID" :*/
fileprivate let kStr_emptyValue:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","T","i","p","s","M","s","g","C","e","l","l"]
fileprivate let kStr_leadingTitle:[Character] = ["I","D"]

/*: "TalkingChatMatchMsgCellID" :*/
fileprivate let kStr_areaTitle:String = "gift all if imageTalki"
fileprivate let kStr_layerName:String = "any pic messagengChat"
fileprivate let kStr_makeName:String = "to make namesgCellID"

/*: "TalkingChatAudioMsgCellID" :*/
fileprivate let kStr_formatSharedValue:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","A","u","d","i","o","M","s","g","C","e","l"]
fileprivate let kStr_textData:String = "text if group startlID"

/*: "TalkingChatSystemNotifJumpMsgCellID" :*/
fileprivate let kStr_mediumData:[UInt8] = [0x44,0x49,0x6c,0x6c,0x65,0x43,0x67,0x73,0x4d,0x70,0x6d,0x75,0x4a,0x66,0x69,0x74,0x6f,0x4e,0x6d,0x65,0x74,0x73,0x79,0x53,0x74,0x61,0x68,0x43,0x67,0x6e,0x69,0x6b,0x6c,0x61,0x54]

/*: "TSystemMessageCell_ReuseId" :*/
fileprivate let kStr_commonValue:[Character] = ["T","S","y","s","t","e","m","M","e","s","s","a","g","e","C"]
fileprivate let kStr_textName:String = "equal content let shareell_R"
fileprivate let kStr_fromData:String = "euseIdto class view let collection"

/*: "#128CFF" :*/
fileprivate let kStr_pointData:String = "#128CFFmake section player me"

/*: "Feedback" :*/
fileprivate let kStr_playerText:String = "image let height sufficient extraFeed"

/*: "#8370FF" :*/
fileprivate let kStr_mediumName:String = "birthday return#8370FF"

/*: "Quick Greeting" :*/
fileprivate let kStr_modelText:[Character] = ["Q","u","i","c","k"," ","G","r","e"]
fileprivate let kStr_tableValue:[Character] = ["e","t","i","n","g"]

/*: "Beauty Photos" :*/
fileprivate let kStr_nameCellColorText:String = "will let centerBeauty"
fileprivate let kStr_frameModelData:String = " Photosstate file if"

/*: "url" :*/
fileprivate let kStr_removeShareSendText:String = "uradd"

/*: "unlockGift" :*/
fileprivate let kStr_timePushData:String = "unloicon"

/*: "%d" :*/
fileprivate let kStr_pathDataName:[Character] = ["%","d"]

/*: "isBurn" :*/
fileprivate let kStr_showData:[UInt8] = [0xe,0x18,0xe7,0x1a,0x17,0x13]

fileprivate func pathDuring(head num: UInt8) -> UInt8 {
    let value = Int(num) - 165
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "giftId" :*/
fileprivate let kStr_labData:[Character] = ["g","i","f","t","I","d"]

/*: "rYMsgType" :*/
fileprivate let kStr_playerName:String = "scene selected tip true requestrYMsg"
fileprivate let kStr_timeText:String = "Typereturn in view manager show"

/*: "GJ:CallCustom" :*/
fileprivate let kStr_componentName:[Character] = ["G","J",":","C","a","l"]
fileprivate let kStr_lengthData:String = "player tag result equallCustom"

/*: "changeDirection" :*/
fileprivate let kStr_busyValue:[Character] = ["c","h","a","n","g"]
fileprivate let kStr_selectName:String = "if as typeeDir"

/*: "extra" :*/
fileprivate let kStr_modelTouchData:String = "extviewa"

/*: "msgInfo" :*/
fileprivate let kStr_sectionText:[Character] = ["m","s","g","I","n","f","o"]

/*: "toUid" :*/
fileprivate let kStr_iconData:String = "self class frame intoUid"

/*: "wantGift" :*/
fileprivate let kStr_value:String = "end indexwantGift"

/*: "jumpKey" :*/
fileprivate let kStr_addText:String = "hidden"
fileprivate let kStr_appTitle:String = "block to to screenumpKey"

/*: "truePersonAuth" :*/
fileprivate let kStr_redRemoveText:String = "commentue"
fileprivate let kStr_colorValue:String = "Authinput key block range style"

/*: "logType" :*/
fileprivate let kStr_allTitle:String = "to size angle pushlogType"

/*: "msgType" :*/
fileprivate let kStr_handleValue:[UInt8] = [0x65,0x70,0x79,0x54,0x67,0x73,0x6d]

/*: "txt" :*/
fileprivate let kStr_normalTargetTitle:String = "trackxtrack"

/*: "type" :*/
fileprivate let kStr_attachData:String = "tcelle"

/*: "QuickMsg" :*/
fileprivate let kStr_versionData:String = "add"
fileprivate let kStr_borderValue:String = "to let giftuickMsg"

/*: "params" :*/
fileprivate let kStr_videoValue:[UInt8] = [0x73,0x6d,0x61,0x72,0x61,0x70]

/*: "content" :*/
fileprivate let kStr_statusData:[UInt8] = [0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "id" :*/
fileprivate let kStr_makeTitle:String = "iat"

/*: "txtId" :*/
fileprivate let kStr_interactionText:[Character] = ["t","x","t","I","d"]

/*: "message" :*/
fileprivate let kStr_maleValue:[UInt8] = [0xca,0xc2,0xd4,0xd4,0xc6,0xc0,0xc2]

private func layerNameKind(temp num: UInt8) -> UInt8 {
    return num ^ 167
}

/*: "TxtMsg" :*/
fileprivate let kStr_tabTitle:String = "more heightTxtMsg"

/*: "Voice hello download failed. Procedure" :*/
fileprivate let kStr_inputData:[UInt8] = [0xa5,0xbe,0xb8,0xb2,0xb4,0x6f,0xb7,0xb4,0xbb,0xbb,0xbe,0x6f,0xb3,0xbe,0xc6,0xbd,0xbb,0xbe,0xb0,0xb3,0x6f,0xb5,0xb0,0xb8,0xbb,0xb4,0xb3,0x7d,0x6f,0x9f,0xc1,0xbe,0xb2,0xb4,0xb3,0xc4,0xc1,0xb4]

fileprivate func letListWill(background num: UInt8) -> UInt8 {
    let value = Int(num) + 177
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "length" :*/
fileprivate let kStr_ofName:[Character] = ["l","e","n","g","t","h"]

/*: "audio" :*/
fileprivate let kStr_selectedLayerTitle:String = "AUDIO"

/*: "audioId" :*/
fileprivate let kStr_equalData:[Character] = ["a","u","d","i","o","I","d"]

/*: "audioLength" :*/
fileprivate let kStr_intimateValue:[UInt8] = [0x29,0x3d,0x2c,0x21,0x27,0x4,0x2d,0x26,0x2f,0x3c,0x20]

private func touchUser(make num: UInt8) -> UInt8 {
    return num ^ 72
}

/*: "audioSandbox" :*/
fileprivate let kStr_backgroundName:String = "groupudio"
fileprivate let kStr_equalTitle:String = "bouse"

/*: "senderId" :*/
fileprivate let kStr_intimateData:[UInt8] = [0xc6,0xb8,0xc1,0xb7,0xb8,0xc5,0x9c,0xb7]

fileprivate func bottomIndex(pop num: UInt8) -> UInt8 {
    let value = Int(num) - 83
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "AudioMsg" :*/
fileprivate let kStr_layerEdgeTitle:[Character] = ["A","u","d","i","o"]
fileprivate let kStr_labTitle:[Character] = ["M","s","g"]

/*: "Can't send blank message" :*/
fileprivate let kStr_appName:[Character] = ["C","a","n","\'","t"," ","s","e","n","d"," ","b"]
fileprivate let kStr_backgroundData:[Character] = ["l","a","n","k"," ","m","e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let kStr_whiteData:String = "quosource"

/*: "quoteMsgInfo" :*/
fileprivate let kStr_colorTitle:[Character] = ["q","u","o","t"]
fileprivate let kStr_data:[Character] = ["e","M","s","g","I","n","f","o"]

/*: "contentType" :*/
fileprivate let kStr_levelName:[UInt8] = [0x7a,0x76,0x77,0x6d,0x7c,0x77,0x6d,0x4d,0x60,0x69,0x7c]

/*: "ImgMsg" :*/
fileprivate let kStr_lengthValue:String = "player inside party view selectedImgMsg"

/*: "img" :*/
fileprivate let kStr_receiveContent:String = "countmg"

/*: "imageUri" :*/
fileprivate let kStr_upData:[UInt8] = [0x2d,0x29,0x25,0x23,0x21,0x11,0x36,0x2d]

/*: "customSend" :*/
fileprivate let kStr_netFatalData:[UInt8] = [0x7c,0x6a,0x6c,0x6b,0x70,0x72,0x4c,0x7a,0x71,0x7b]

private func barCancelInput(lab num: UInt8) -> UInt8 {
    return num ^ 31
}

/*: "pic" :*/
fileprivate let kStr_scaleName:String = "pipossibility"

/*: "VideoMsg" :*/
fileprivate let kStr_tableQuantityValue:[Character] = ["V","i","d","e","o","M","s","g"]

/*: "video" :*/
fileprivate let kStr_topData:String = "videshould"

/*: "coverImg" :*/
fileprivate let kStr_makeData:[UInt8] = [0xc8,0xc4,0xdd,0xce,0xd9,0xe2,0xc6,0xcc]

private func masterKey(to num: UInt8) -> UInt8 {
    return num ^ 171
}

/*: "videoUrl" :*/
fileprivate let kStr_sizeValue:[UInt8] = [0xd9,0xc6,0xcb,0xca,0xc0,0xfa,0xdd,0xc3]

private func tabFalse(info num: UInt8) -> UInt8 {
    return num ^ 175
}

/*: "duration" :*/
fileprivate let kStr_resultValue:[UInt8] = [0x6e,0x6f,0x69,0x74,0x61,0x72,0x75,0x64]

/*: "len" :*/
fileprivate let kStr_modelButtonValue:[UInt8] = [0xe,0x7,0xc]

/*: "Documents/User/voice/" :*/
fileprivate let kStr_contentListTitle:String = "Documwith equal"
fileprivate let kStr_makeText:[Character] = ["r","/","v","o","i","c","e","/"]

/*: "Documents/User/record/" :*/
fileprivate let kStr_collectionValue:String = "net text protocol blackDocu"
fileprivate let kStr_cellData:String = "/User/let source if user"
fileprivate let kStr_dataTitle:String = "rmanager"
fileprivate let kStr_stateFirstName:String = "cord/add status view"

/*: "msgIncome" :*/
fileprivate let kStr_nowData:[Character] = ["m","s","g"]
fileprivate let kStr_timeSizeData:[Character] = ["I","n","c","o","m","e"]

/*: "mfBean" :*/
fileprivate let kStr_tableKeyName:[UInt8] = [0x67,0x60,0x3c,0x5f,0x5b,0x68]

fileprivate func refreshControl(kit num: UInt8) -> UInt8 {
    let value = Int(num) - 250
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "messageType" :*/
fileprivate let kStr_cleanText:String = "MESSAG"

/*: "WantGift" :*/
fileprivate let kStr_modelStartTitle:[Character] = ["W","a","n","t","G"]
fileprivate let kStr_requestThinValue:[Character] = ["i","f","t"]

/*: _PrivateChatTipsTitle_ :*/
fileprivate let kStr_fatalValue:String = "make return_Priv"
fileprivate let kStr_styleTitle:String = "hatTipself self view half"
fileprivate let kStr_cellTitle:String = "_"

/*: "msgId" :*/
fileprivate let kStr_indexValue:String = "msgIdinput face"

/*: "totalMfCoin" :*/
fileprivate let kStr_fileStatusContent:String = "toticon"
fileprivate let kStr_modelName:String = "empty"

/*: "attachSysMsg" :*/
fileprivate let kStr_youText:String = "attachSimage self"
fileprivate let kStr_timeName:String = "hung view makeysMsg"

/*: "intimatePhoto" :*/
fileprivate let kStr_styleName:[Character] = ["i","n","t","i","m","a","t","e","P","h"]
fileprivate let kStr_numberGiftName:String = "OTO"

/*: _ :*/
fileprivate let kStr_sharedData:String = "_"

/*: "totalIntimate" :*/
fileprivate let kStr_shouldValue:String = "totalIcenter extra let info let"

/*: "uid" :*/
fileprivate let kStr_giftValue:[UInt8] = [0xcb,0xd7,0xda]

private func shouldComponent(scene num: UInt8) -> UInt8 {
    return num ^ 190
}

/*: "intimate" :*/
fileprivate let kStr_seatContent:[UInt8] = [0x83,0x84,0x9e,0x83,0x87,0x8b,0x9e,0x8f]

private func makeBottom(inside num: UInt8) -> UInt8 {
    return num ^ 234
}

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let kStr_colorContent:[UInt8] = [0x2e,0x6c,0x65,0x76,0x65,0x6c,0x20,0x79,0x63,0x61,0x6d,0x69,0x74,0x6e,0x69,0x20,0x66,0x6f,0x20,0x6b,0x63,0x61,0x6c,0x20,0x72,0x75,0x6f,0x79,0x20,0x6f,0x74,0x20,0x65,0x75,0x64,0x20,0x6e,0x6f,0x69,0x74,0x63,0x6e,0x75,0x66,0x20,0x65,0x68,0x74,0x20,0x65,0x73,0x75,0x20,0x74,0x27,0x6e,0x61,0x43]

/*: "giftNum" :*/
fileprivate let kStr_sexData:String = "giftNumequal for"

/*: "pkgItemsetId" :*/
fileprivate let kStr_titleData:String = "in letpkgIte"
fileprivate let kStr_whiteContent:String = "layer"

/*: "from" :*/
fileprivate let kStr_intimateInputCenterTitle:String = "listrom"

/*: "wantGiftId" :*/
fileprivate let kStr_tempName:[Character] = ["w","a","n","t","G","i","f","t","I"]
fileprivate let kStr_picName:[Character] = ["d"]

/*: "WantGiftMsg" :*/
fileprivate let kStr_voiceTitle:[Character] = ["W","a","n","t","G","i","f","t","M","s","g"]

/*: "gift" :*/
fileprivate let kStr_directionArrayHeightText:[Character] = ["g","i","f","t"]

/*: "View info" :*/
fileprivate let kStr_fatalTitle:[Character] = ["V","i","e","w"," ","i","n"]
fileprivate let kStr_valueManagerLabelData:[Character] = ["f","o"]

/*: "Notes" :*/
fileprivate let kStr_infoText:String = "Notesfirst index name view size"

/*: "Report" :*/
fileprivate let kStr_finishName:String = "Reportalways display"

/*: "View read receipts?" :*/
fileprivate let kStr_originalName:String = "sex model valueView r"
fileprivate let kStr_inviteValue:String = "eceipts?let to player let"

/*: "He" :*/
fileprivate let kStr_toolName:[Character] = ["H","e"]

/*: "She" :*/
fileprivate let kStr_titleValue:String = "tool nameShe"

/*: "%@ is using too low a version" :*/
fileprivate let kStr_succeedData:String = "%@ ismake let"
fileprivate let kStr_pushCellItemName:String = "g toany app"
fileprivate let kStr_requestValue:String = "self equal indexa ver"

/*: "Shoot video at least 5 seconds" :*/
fileprivate let kStr_imageName:String = "normal comeShoo"
fileprivate let kStr_messageData:String = "eo adata to return self push"
fileprivate let kStr_tableSendTitle:String = "t 5 segift model center page image"
fileprivate let kStr_shareGenderText:String = "conmain"

/*: "Upload failed" :*/
fileprivate let kStr_playTitle:String = "Upload faself origin day var section"
fileprivate let kStr_blockElectData:String = "ilcolord"

/*: "播放状态: :*/
fileprivate let kStr_pathValue:String = "播image态:"

/*: "c2c_" :*/
fileprivate let kStr_userData:String = "cookie type image partyc2c_"

/*: "msg" :*/
fileprivate let kStr_interactionName:String = "MSG"

/*: "tips" :*/
fileprivate let kStr_layerValue:[Character] = ["t","i","p","s"]

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let kStr_viewValue:[UInt8] = [0xb3,0xc9,0xcf,0x7a,0xbd,0xbb,0xc8,0x7a,0xcf,0xcd,0xbf,0x7a,0xce,0xc2,0xbf,0x7a,0xd0,0xc3,0xbe,0xbf,0xc9,0x7a,0xbd,0xc2,0xbb,0xce,0x7a,0xc0,0xbf,0xbb,0xce,0xcf,0xcc,0xbf,0x7a,0xce,0xc9,0x7a,0xc1,0xc3,0xd0,0xbf,0x7a,0x7f,0x9a,0x7a,0xbb,0x7a,0xbd,0xbb,0xc6,0xc6]

fileprivate func managerSize(model num: UInt8) -> UInt8 {
    let value = Int(num) + 166
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "cellData" :*/
fileprivate let kStr_errorTitle:String = "cellDataview progress build"

/*: "System notification" :*/
fileprivate let kStr_countValue:[Character] = ["S","y","s","t","e","m"," ","n","o","t"]
fileprivate let kStr_blockValue:[Character] = ["i","f","i","c","a","t","i","o","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SucceedTitleControllerDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum ClientFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class SucceedTitleControllerDelegate: LayerRecognizerDelegate {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var isHaveSession = false
    var isHaveSession = false
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = FailureReactiveCompatible()
    private var userWrap = FailureReactiveCompatible() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = ClientFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: FailureReactiveCompatible = FailureReactiveCompatible()) {
    init(chatID: String, isFrom: ClientFromEnum = .Normal, userWrap: FailureReactiveCompatible = FailureReactiveCompatible()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        SucceedTitleControllerDelegate.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        FillPathChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == TableMacroDefine.getXiaoMiID() {
        if targetId == TableMacroDefine.toVoice() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == TableMacroDefine.getCustomerServiceID() {
        } else if targetId == TableMacroDefine.isothermal() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kStr_mainWithTitle.map{$0^126}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if LocationThen.share.appConfigMode.disableShootScreen {
            if LocationThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = UpBackgroundThen(frame: CGRect(x: 0, y: 0, width: kLet_halfData, height: kLet_noSeatName))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        dataLoad()
        //: createUI()
        dataAdd()
        //: createUIConstraints()
        equalFileBlock()
        //: bindInteraction()
        stickToEyeContactTotaleraction()
        //: addTapGestureRecognizer()
        makeBy()
        //: func__requestChatInfo()
        timeApp()
        //: func__getFillDraftText()
        impregnateConstraint()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        QueryedVoiceManagerDelegate.shared.appSend()
        //: func__addVoiceNotification()
        fasteningNotification()
        //: addReadPeceiptNotification()
        toColor()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            influence()
            //: func__TipsNotification()
            funcNotificationTipsEqual()
            //: if LocationThen.share.loginUserMode.sex == Gender.male.rawValue {
            if LocationThen.share.loginUserMode.sex == SucceedComparable.male.rawValue {
                //: func__setReplyTipsView()
                disdain()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        indexTime(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        indexTime(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            side()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: TUIMessageController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = TUIMessageController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (String(kStr_imageData)))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: "TalkingChatTextMsgCellID")
        msgVC.tableView.register(ProtectionThen.self, forCellReuseIdentifier: (String(kStr_equalValue) + String(kStr_originalText.suffix(5)) + "ellID"))
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: "TalkingChatImageMsgCellID")
        msgVC.tableView.register(CompartmentReactiveCompatible.self, forCellReuseIdentifier: (String(kStr_buildMidData.suffix(4)) + "ingCh" + String(kStr_maxTitle) + String(kStr_textModelData)))
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: "TalkingChatVideoMsgCellID")
        msgVC.tableView.register(ToSubReactiveCompatible.self, forCellReuseIdentifier: (String(kStr_storeName.prefix(6)) + String(kStr_sizeTitle) + String(kStr_fileValue.suffix(5)) + "eoMsg" + String(kStr_fillData.prefix(6))))
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: "TalkingChatGiftMsgCellID")
        msgVC.tableView.register(MakeMsgCell.self, forCellReuseIdentifier: (String(kStr_likeTitle.prefix(6)) + "gChatGi" + String(kStr_padName.prefix(4)) + "gCellID"))
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: "TalkingChatAskForGiftMsgCell")
        msgVC.tableView.register(OverfillMessageCell.self, forCellReuseIdentifier: (String(kStr_messageTitle.suffix(4)) + "ingC" + String(kStr_countAngleText.suffix(8)) + String(kStr_withValue.suffix(6)) + "sgCell"))
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: "TalkingChatTipsMsgCellID")
        msgVC.tableView.register(EffectReactiveCompatible.self, forCellReuseIdentifier: (String(kStr_emptyValue) + String(kStr_leadingTitle)))
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: "TalkingChatMatchMsgCellID")
        msgVC.tableView.register(PathMsgCell.self, forCellReuseIdentifier: (String(kStr_areaTitle.suffix(5)) + String(kStr_layerName.suffix(6)) + "MatchM" + String(kStr_makeName.suffix(8))))
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: "TalkingChatAudioMsgCellID")
        msgVC.tableView.register(ChatView.self, forCellReuseIdentifier: (String(kStr_formatSharedValue) + String(kStr_textData.suffix(3))))
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: "TalkingChatSystemNotifJumpMsgCellID")
        msgVC.tableView.register(SprechgesangThen.self, forCellReuseIdentifier: String(bytes: kStr_mediumData.reversed(), encoding: .utf8)!)
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: "TSystemMessageCell_ReuseId")
        msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: (String(kStr_commonValue) + String(kStr_textName.suffix(5)) + String(kStr_fromData.prefix(6))))
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: AdministratorMatchDelegate = {
        //: let m = TalkingCallMenuManager()
        let m = AdministratorMatchDelegate()
        //: m.uid = targetId
        m.uid = targetId
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: ClientColorInputView = {
        //: var type: InputViewType = .PrivateChat
        var type: MakeViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = ClientColorInputView(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(kStr_pointData.prefix(7))))!, .font: UIFont.groupSize(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(kStr_playerText.suffix(4)) + "back").localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(imbed), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: QueryedReactiveCompatible = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = QueryedReactiveCompatible()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: GiftThen = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = GiftThen(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.groupSize(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(kStr_mediumName.suffix(7)))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(kStr_modelText) + String(kStr_tableValue)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.fromBuild().cgColor
        btn.layer.borderColor = UIColor.fromBuild().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(nor), for: .touchUpInside)
        //: btn.isHidden = !LocationThen.share.appUserConfigMode.showDirectGreet || LocationThen.share.loginUserMode.sex == Gender.male.rawValue || LocationThen.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !LocationThen.share.appUserConfigMode.showDirectGreet || LocationThen.share.loginUserMode.sex == SucceedComparable.male.rawValue || LocationThen.share.appStatus == SharedInsetTarget.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: SucceedReactiveCompatible? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = SucceedReactiveCompatible()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: WithBlockView = {
        //: let v = TalkingSelectTranslationView()
        let v = WithBlockView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.version(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: PointProtectionResultView = {
        //: let v = TalkingTranslationResultView()
        let v = PointProtectionResultView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.observer(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.groupSize(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(kStr_mediumName.suffix(7)))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(kStr_nameCellColorText.suffix(6)) + String(kStr_frameModelData.prefix(7))).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.fromBuild().cgColor
        btn.layer.borderColor = UIColor.fromBuild().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(photosEqual), for: .touchUpInside)
        //: btn.isHidden = LocationThen.share.loginUserMode.sex == Gender.male.rawValue || LocationThen.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = LocationThen.share.loginUserMode.sex == SucceedComparable.male.rawValue || LocationThen.share.appStatus == SharedInsetTarget.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: FlipAddView = {
        //: let v = TalkingLiveRoomGamesView()
        let v = FlipAddView()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: MagnitudeBannerView = {
        //: let view = TalkingPrivateBannerView.init()
        let view = MagnitudeBannerView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: PathScreenView = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = PathScreenView()
        //: v.isHidden = (LocationThen.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (LocationThen.share.appStatus == SharedInsetTarget.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: ClientTopView = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = ClientTopView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.outGift()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension SucceedTitleControllerDelegate: ExaggerateReactiveCompatible {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func alongBlock(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if LocationThen.share.loginUserMode.sex == Gender.male.rawValue {
                if LocationThen.share.loginUserMode.sex == SucceedComparable.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if LocationThen.share.loginUserMode.sex == Gender.male.rawValue {
                if LocationThen.share.loginUserMode.sex == SucceedComparable.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.beforeAdvertisingFinish(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension SucceedTitleControllerDelegate: TMessageControllerDelegate {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func nor() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        FillPathChatManager.beauty(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.detailDownArray(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.toDict(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func photosEqual() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        FillPathChatManager.everyLast(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = TitleMiniDataSource()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().failureFinish(urlStr: dict?[(kStr_removeShareSendText.replacingOccurrences(of: "add", with: "l"))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(kStr_timePushData.replacingOccurrences(of: "icon", with: "c") + "kGift")] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = MethodModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: kStr_showData.map{pathDuring(head: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(kStr_labData))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.blow(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
    func messageController(_: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TimeCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: TimeCellData.self), self.userWrap.uid.count > 0 {
            //: cellData.msgModel.userInfoModel = self.userWrap
            cellData.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(kStr_playerName.suffix(5)) + String(kStr_timeText.prefix(4)))].stringValue == (String(kStr_componentName) + String(kStr_lengthData.suffix(7))) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(String(kStr_busyValue) + String(kStr_selectName.suffix(4)) + "ection")].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData.msgModel.user.uid = jsonDic[(kStr_modelTouchData.replacingOccurrences(of: "view", with: "r"))][(String(kStr_sectionText))][(String(kStr_iconData.suffix(5)))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData.msgModel
                let msgModel = cellData.msgModel
                //: cellData = ShowCellData(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = ShowCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData.msgModel = msgModel
                cellData.msgModel = msgModel
            }
        }
        //: if cellData.isKind(of: CanPointCellData.self) == true || cellData.isKind(of: LayerCellData.self) == true || cellData.isKind(of: ShowCellData.self) == true || cellData.isKind(of: CountCellData.self) == true {
        if cellData.isKind(of: CanPointCellData.self) == true || cellData.isKind(of: LayerCellData.self) == true || cellData.isKind(of: ShowCellData.self) == true || cellData.isKind(of: CountCellData.self) == true {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
        //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
        FillPathChatManager.endEqual(cellData: cellData, msgView: self.msgView)

        // 女性记录最新回复时间, 刷新表格
        //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
        FillPathChatManager.createFlush(cellData.msgIncome, data.timestamp, msgView)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
    func messageController(_: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = appData(data: data)

        //: if data.isKind(of: ShowCellData.self) {
        if data.isKind(of: ShowCellData.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTextMsgCellID")
            let cell = ProtectionThen(style: .default, reuseIdentifier: (String(kStr_equalValue) + String(kStr_originalText.suffix(5)) + "ellID"))
            //: let cellData = data as! ShowCellData
            let cellData = data as! ShowCellData

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.to() {
                //: if cellData.msgModel.quoteMsgInfo.uid == LocationThen.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == LocationThen.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = LocationThen.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = LocationThen.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: LayerCellData.self) {
        if data.isKind(of: LayerCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: "TalkingChatImageMsgCellID")
            let cell = CompartmentReactiveCompatible(style: .default, reuseIdentifier: (String(kStr_buildMidData.suffix(4)) + "ingCh" + String(kStr_maxTitle) + String(kStr_textModelData)))
            //: let cellData = data as! LayerCellData
            let cellData = data as! LayerCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: CountCellData.self) {
        if data.isKind(of: CountCellData.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: "TalkingChatVideoMsgCellID")
            let cell = ToSubReactiveCompatible(style: .default, reuseIdentifier: (String(kStr_storeName.prefix(6)) + String(kStr_sizeTitle) + String(kStr_fileValue.suffix(5)) + "eoMsg" + String(kStr_fillData.prefix(6))))
            //: let cellData = data as! CountCellData
            let cellData = data as! CountCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: FillCellData.self) {
        if data.isKind(of: FillCellData.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingChatGiftMsgCellID")
            let cell = MakeMsgCell(style: .default, reuseIdentifier: (String(kStr_likeTitle.prefix(6)) + "gChatGi" + String(kStr_padName.prefix(4)) + "gCellID"))
            //: let cellData = data as! FillCellData
            let cellData = data as! FillCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: LoadCellData.self) {
        if data.isKind(of: LoadCellData.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingChatAskForGiftMsgCell")
            let cell = OverfillMessageCell(style: .default, reuseIdentifier: (String(kStr_messageTitle.suffix(4)) + "ingC" + String(kStr_countAngleText.suffix(8)) + String(kStr_withValue.suffix(6)) + "sgCell"))
            //: let cellData = data as! LoadCellData
            let cellData = data as! LoadCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = EngineHandyJSON()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.input(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(kStr_value.suffix(8))))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: CanPointCellData.self) {
        if data.isKind(of: CanPointCellData.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: "TalkingChatAudioMsgCellID")
            let cell = ChatView(style: .default, reuseIdentifier: (String(kStr_formatSharedValue) + String(kStr_textData.suffix(3))))
            //: let cellData = data as! CanPointCellData
            let cellData = data as! CanPointCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TimeCellData.self) {
        if data.isKind(of: TimeCellData.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: "TalkingChatMatchMsgCellID")
            let cell = PathMsgCell(style: .default, reuseIdentifier: (String(kStr_areaTitle.suffix(5)) + String(kStr_layerName.suffix(6)) + "MatchM" + String(kStr_makeName.suffix(8))))
            //: let cellData = data as! TimeCellData
            let cellData = data as! TimeCellData
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: SelectCellData.self) {
        if data.isKind(of: SelectCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTipsMsgCellID")
            let cell = EffectReactiveCompatible(style: .default, reuseIdentifier: (String(kStr_emptyValue) + String(kStr_leadingTitle)))
            //: let cellData = data as! SelectCellData
            let cellData = data as! SelectCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: DiagonalCellData.self) {
        if data.isKind(of: DiagonalCellData.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: "TalkingChatSystemNotifJumpMsgCellID")
            let cell = SprechgesangThen(style: .default, reuseIdentifier: String(bytes: kStr_mediumData.reversed(), encoding: .utf8)!)
            //: let cellData = data as! DiagonalCellData
            let cellData = data as! DiagonalCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != TableMacroDefine.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != TableMacroDefine.isothermal() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: EffectPushManager.share.func__pushToUserDetailVC(uid: String(uid))
            EffectPushManager.share.userAdd(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: CompartmentReactiveCompatible.self) { // 图片
            //: let imageCellData = cell.messageData as! LayerCellData
            let imageCellData = cell.messageData as! LayerCellData
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId)
                let vc = SeekViewController(imageCellData: imageCellData, toUid: self.targetId)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.eraseCurrent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_sizeData)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.coordinate(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.total(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                showBorder()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(kLet_deviceLikeSucceedData)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = TheoreticalAccountTransformable()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = ModelTransformable()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [TheoreticalAccountTransformable] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = BottomRecognizerDelegate(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                showBorder()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: ToSubReactiveCompatible.self) { // 视频
            //: let videoCellData = cell.messageData as! CountCellData
            let videoCellData = cell.messageData as! CountCellData
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = QueryQualityViewDelegate(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                showBorder()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId)
                let vc = ButtonPlayerDelegate(videoCellData: videoCellData, toUid: self.targetId)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.eraseCurrent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_sizeData)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.coordinate(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.total(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                showBorder()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: ChatView.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! ChatView
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! CanPointCellData, cellView: audioCell)
            playUp(cellData: audioCell.cellData as! CanPointCellData, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: ProtectionThen.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! ProtectionThen
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(kStr_modelTouchData.replacingOccurrences(of: "view", with: "r"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((kStr_addText.replacingOccurrences(of: "hidden", with: "j") + String(kStr_appTitle.suffix(6)))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(kStr_addText.replacingOccurrences(of: "hidden", with: "j") + String(kStr_appTitle.suffix(6)))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (kStr_redRemoveText.replacingOccurrences(of: "comment", with: "tr") + "Person" + String(kStr_colorValue.prefix(4))) {
                    //: if LocationThen.share.loginUserMode.isTPAuth == TPUserAuth.isSuccessed.rawValue {
                    if LocationThen.share.loginUserMode.isTPAuth == FillContiguousBytes.isSuccessed.rawValue {
                        //: return
                        return
                            //: } else if LocationThen.share.loginUserMode.isTPAuth == TPUserAuth.unknown.rawValue {
                    } else if LocationThen.share.loginUserMode.isTPAuth == FillContiguousBytes.unknown.rawValue {
                        //: EffectPushManager.share.func__pushUserVerifyController(toast: nil)
                        EffectPushManager.share.retirement(toast: nil)
                        //: } else if LocationThen.share.loginUserMode.isTPAuth == TPUserAuth.isOnGoing.rawValue {
                    } else if LocationThen.share.loginUserMode.isTPAuth == FillContiguousBytes.isOnGoing.rawValue {
                        //: let vc = TalkingFinalVerificationVC()
                        let vc = ClientPathViewController()
                        //: EffectPushManager.share.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        EffectPushManager.share.quantityimateTarget()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: EffectPushManager.share.func__pushUserVerifyController(toast: nil)
                        EffectPushManager.share.retirement(toast: nil)
                    }
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<SharedTransformable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: EffectPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    EffectPushManager.share.requestModel(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(kStr_playerName.suffix(5)) + String(kStr_timeText.prefix(4)))] as? String == (String(kStr_componentName) + String(kStr_lengthData.suffix(7))) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(kStr_sectionText))][(String(kStr_allTitle.suffix(7)))].intValue == 1 {
                    //: checkAndCallVoice()
                    resultVoice()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    progressSize()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: EffectReactiveCompatible.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! EffectReactiveCompatible
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(kStr_modelTouchData.replacingOccurrences(of: "view", with: "r"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((kStr_addText.replacingOccurrences(of: "hidden", with: "j") + String(kStr_appTitle.suffix(6)))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<SharedTransformable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: EffectPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    EffectPushManager.share.requestModel(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: PathMsgCell.self) {
            //: EffectPushManager.share.func__pushToUserDetailVC(uid: targetId)
            EffectPushManager.share.userAdd(uid: targetId)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: TalkingLoadCollectionThen?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: CompartmentReactiveCompatible.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! CompartmentReactiveCompatible
            //: cellData = imageCell.cellData as! LayerCellData
            cellData = imageCell.cellData as! LayerCellData
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: ToSubReactiveCompatible.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! ToSubReactiveCompatible
            //: cellData = videoCell.cellData as! CountCellData
            cellData = videoCell.cellData as! CountCellData
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: ChatView.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! ChatView
            //: cellData = audioCell.cellData as! CanPointCellData
            cellData = audioCell.cellData as! CanPointCellData
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: ProtectionThen.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! ProtectionThen
            //: cellData = textCell.cellData as! ShowCellData
            cellData = textCell.cellData as! ShowCellData
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: TUIMessageCellData? = TUISocialChatManager.handleTXMessageReSendModel(with: cellData!)
            let reCellData: TUIMessageCellData? = TUISocialChatManager.handleTXMessageReSendModel(with: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: CountCellData.self) {
                if reCellData!.isKind(of: CountCellData.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    possibility(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    deadline(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: TUIMessageCellData!) {
    func messageController(_: TUIMessageController!, onReplyMessage cellData: TUIMessageCellData!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = FillPathChatManager.wedgeId(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: TUIMessageCellData!) {
    func messageController(_: TUIMessageController!, onClickReplyMessage cellData: TUIMessageCellData!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        FillPathChatManager.executeField(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: TUIMessageCellData) -> TUIMessageCellData {
    private func appData(data: TUIMessageCellData) -> TUIMessageCellData {
        //: let isSelf = data.msgModel.user.uid == Int(LocationThen.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(LocationThen.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if LocationThen.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if LocationThen.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = LocationThen.share.loginUserMode.headPic!
                data.msgModel.user.headPic = LocationThen.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = LocationThen.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = LocationThen.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension SucceedTitleControllerDelegate {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func topName() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: kStr_handleValue.reversed(), encoding: .utf8)!: (kStr_normalTargetTitle.replacingOccurrences(of: "track", with: "t"))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(kStr_attachData.replacingOccurrences(of: "cell", with: "yp")): (kStr_versionData.replacingOccurrences(of: "add", with: "Q") + String(kStr_borderValue.suffix(7)))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(kStr_sectionText)): msgInfo, String(bytes: kStr_videoValue.reversed(), encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
        self.deadline(msgData: cellData, reSend: false)
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func toDict(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: kStr_statusData.reversed(), encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: kStr_statusData.reversed(), encoding: .utf8)!: content, (String(kStr_interactionText)): greetId, String(bytes: kStr_handleValue.reversed(), encoding: .utf8)!: (kStr_normalTargetTitle.replacingOccurrences(of: "track", with: "t"))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: kStr_maleValue.map{layerNameKind(temp: $0)}, encoding: .utf8)!: content, (String(kStr_interactionText)): greetId, (kStr_attachData.replacingOccurrences(of: "cell", with: "yp")): (String(kStr_tabTitle.suffix(6)))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(kStr_sectionText)): msgInfo, String(bytes: kStr_videoValue.reversed(), encoding: .utf8)!: paramsDic]
            //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
            let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.deadline(msgData: cellData, reSend: false)
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            EngineDataDelegate.shared.coequal(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.detailDownArray(showMsg: String(bytes: kStr_inputData.map{letListWill(background: $0)}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        FillPathChatManager.fromEachOffe(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(String(kStr_ofName))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: kStr_handleValue.reversed(), encoding: .utf8)!: (kStr_selectedLayerTitle.lowercased()),
                               //: "audioId": greetId,
                               (String(kStr_equalData)): greetId,
                               //: "audioLength": length,
                               String(bytes: kStr_intimateValue.map{touchUser(make: $0)}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (kStr_backgroundName.replacingOccurrences(of: "group", with: "a") + "Sand" + kStr_equalTitle.replacingOccurrences(of: "use", with: "x")): model.unZipFilePath,
                               //: "senderId": LocationThen.share.loginUserMode.userID]
                               String(bytes: kStr_intimateData.map{bottomIndex(pop: $0)}, encoding: .utf8)!: LocationThen.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(kStr_equalData)): greetId,
                                 //: "audioLength": length,
                                 String(bytes: kStr_intimateValue.map{touchUser(make: $0)}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (kStr_backgroundName.replacingOccurrences(of: "group", with: "a") + "Sand" + kStr_equalTitle.replacingOccurrences(of: "use", with: "x")): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (kStr_attachData.replacingOccurrences(of: "cell", with: "yp")): (String(kStr_layerEdgeTitle) + String(kStr_labTitle))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(kStr_sectionText)): msgInfo, String(bytes: kStr_videoValue.reversed(), encoding: .utf8)!: paramsDic]
                //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.deadline(msgData: cellData, reSend: false)
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func observer(msgStr: String, quoteModel: DataModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            detailDownArray(showMsg: (String(kStr_appName) + String(kStr_backgroundData)).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: kStr_statusData.reversed(), encoding: .utf8)!: temStr, String(bytes: kStr_handleValue.reversed(), encoding: .utf8)!: (kStr_normalTargetTitle.replacingOccurrences(of: "track", with: "t"))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: kStr_maleValue.map{layerNameKind(temp: $0)}, encoding: .utf8)!: temStr, (kStr_attachData.replacingOccurrences(of: "cell", with: "yp")): (String(kStr_tabTitle.suffix(6)))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(kStr_sectionText)): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(kStr_whiteData.replacingOccurrences(of: "source", with: "te") + "MsgId")] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(String(kStr_colorTitle) + String(kStr_data))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: kStr_videoValue.reversed(), encoding: .utf8)!] = paramsDic

        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
        cellData.msgModel.quoteMsgInfo = quoteModel ?? DataModel()

        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        deadline(msgData: cellData, reSend: false)
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func blow(img: UIImage, sendModel: MethodModel) {
        //: let imagePath = LayerCellData.getChatImagePath(image: img)
        let imagePath = LayerCellData.laterImage(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: kStr_levelName.map{$0^25}, encoding: .utf8)!: (String(kStr_lengthValue.suffix(6))), String(bytes: kStr_handleValue.reversed(), encoding: .utf8)!: (kStr_receiveContent.replacingOccurrences(of: "count", with: "i")), String(bytes: kStr_upData.map{$0^68}, encoding: .utf8)!: imagePath, String(bytes: kStr_netFatalData.map{barCancelInput(lab: $0)}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(kStr_scaleName.replacingOccurrences(of: "possibility", with: "c")): imagePath,
                                        //: "type": "ImgMsg",
                                        (kStr_attachData.replacingOccurrences(of: "cell", with: "yp")): (String(kStr_lengthValue.suffix(6))),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: kStr_showData.map{pathDuring(head: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(kStr_labData)): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(kStr_sectionText)): msgInfo, String(bytes: kStr_videoValue.reversed(), encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        deadline(msgData: cellData, reSend: false)
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: TUIMessageCellData?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func possibility(msgData: TUIMessageCellData?, sendModel: MethodModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: kStr_levelName.map{$0^25}, encoding: .utf8)!: (String(kStr_tableQuantityValue)), String(bytes: kStr_handleValue.reversed(), encoding: .utf8)!: (kStr_topData.replacingOccurrences(of: "should", with: "o")), String(bytes: kStr_netFatalData.map{barCancelInput(lab: $0)}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(kStr_attachData.replacingOccurrences(of: "cell", with: "yp")): (String(kStr_tableQuantityValue)),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: kStr_makeData.map{masterKey(to: $0)}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: kStr_sizeValue.map{tabFalse(info: $0)}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: kStr_resultValue.reversed(), encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: kStr_showData.map{pathDuring(head: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(kStr_labData)): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(kStr_sectionText)): msgInfo, String(bytes: kStr_videoValue.reversed(), encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: let uploadCellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dict, isSender: true, isTip: false)
            let uploadCellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dict, isSender: true, isTip: false)
            //: self.msgView.sendCustomMessage(uploadCellData)
            self.msgView.sendCustomMessage(uploadCellData)
            //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
            self.equalEnter(msgData: uploadCellData, sendModel: sendModel)
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.equalEnter(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        deadline(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func makeTime(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: kStr_handleValue.reversed(), encoding: .utf8)!: (kStr_selectedLayerTitle.lowercased()),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: kStr_intimateValue.map{touchUser(make: $0)}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (kStr_backgroundName.replacingOccurrences(of: "group", with: "a") + "Sand" + kStr_equalTitle.replacingOccurrences(of: "use", with: "x")): voicePath,
                       //: "senderId": LocationThen.share.loginUserMode.userID]
                       String(bytes: kStr_intimateData.map{bottomIndex(pop: $0)}, encoding: .utf8)!: LocationThen.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(kStr_backgroundName.replacingOccurrences(of: "group", with: "a") + "Sand" + kStr_equalTitle.replacingOccurrences(of: "use", with: "x")): voicePath,
                         //: "type": "AudioMsg",
                         (kStr_attachData.replacingOccurrences(of: "cell", with: "yp")): (String(kStr_layerEdgeTitle) + String(kStr_labTitle)),
                         //: "len": "\(voiceTime)"]
                         String(bytes: kStr_modelButtonValue.map{$0^98}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(kStr_sectionText)): msgInfo, String(bytes: kStr_videoValue.reversed(), encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        deadline(msgData: cellData, reSend: false)
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: TUIMessageCellData, reSend: Bool) {
    private func deadline(msgData: TUIMessageCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            detailDownArray(showMsg: kLet_accuracyContent)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(kStr_iconData.suffix(5)))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: CountCellData.self) == false && msgData.isKind(of: LoadCellData.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: CountCellData.self) == false, msgData.isKind(of: LoadCellData.self) == false, params[(kStr_attachData.replacingOccurrences(of: "cell", with: "yp"))] as? String != (kStr_versionData.replacingOccurrences(of: "add", with: "Q") + String(kStr_borderValue.suffix(7))) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.sendCustomMessage(msgData)
        }
        //: let recordTime = TUIMsgChatManager.getMessageInsertTime()
        let recordTime = TUIMsgChatManager.getMessageInsertTime()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((kStr_scaleName.replacingOccurrences(of: "possibility", with: "c"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(kStr_scaleName.replacingOccurrences(of: "possibility", with: "c"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(kStr_scaleName.replacingOccurrences(of: "possibility", with: "c"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                pathLine(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(kStr_attachData.replacingOccurrences(of: "cell", with: "yp"))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(kStr_layerEdgeTitle) + String(kStr_labTitle)) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((kStr_backgroundName.replacingOccurrences(of: "group", with: "a") + "Sand" + kStr_equalTitle.replacingOccurrences(of: "use", with: "x"))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(kStr_backgroundName.replacingOccurrences(of: "group", with: "a") + "Sand" + kStr_equalTitle.replacingOccurrences(of: "use", with: "x"))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (kStr_backgroundName.replacingOccurrences(of: "group", with: "a") + "Sand" + kStr_equalTitle.replacingOccurrences(of: "use", with: "x")))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(kStr_contentListTitle.prefix(5)) + "ents/Use" + String(kStr_makeText))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = kLet_timeViewContent + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(kStr_collectionValue.suffix(4)) + "ments" + String(kStr_cellData.prefix(6)) + kStr_dataTitle.replacingOccurrences(of: "manager", with: "e") + String(kStr_stateFirstName.prefix(5)))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = kLet_homeBottomName + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(kStr_equalData))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(kStr_selectedLayerTitle.lowercased())] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: UpsetCountRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        UpsetCountRequestTool.behindUp(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (kStr_versionData.replacingOccurrences(of: "add", with: "Q") + String(kStr_borderValue.suffix(7))) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.eraseCurrent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_eventTitle)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(kStr_interactionText))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    FillPathChatManager.fromEachOffe(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.deleteCustomMessage(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(kStr_sectionText))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(kStr_nowData) + String(kStr_timeSizeData))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(kStr_nowData) + String(kStr_timeSizeData))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if VideoPushListener.isGesture().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: kLet_countData, object: nil, userInfo: [String(bytes: kStr_tableKeyName.map{refreshControl(kit: $0)}, encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.protrudeRequest(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(kStr_cleanText.lowercased() + "eType")] as? Int
            if messageType == 25 || type == "QuickMsg" {
                //: if messageType == 25 {
                if messageType == 25 {
                    msgInfo["msgType"] = "WantGift"
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    kLet_dismissValue.digitizerInsideStatus(eventID: kLet_requestData)
                } else if type == "QuickMsg" {
                    msgInfo["msgType"] = "txt"
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(kStr_sectionText)): msgInfo]
                //: updateMsgData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                updateMsgData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                //: self.msgView.sendCustomMessage(updateMsgData)
                self.msgView.sendCustomMessage(updateMsgData)
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            let key =  "\(LocationThen.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            if LocationThen.share.loginUserMode.sex == SucceedComparable.male.rawValue && type != "QuickMsg" {
                //: self.removeReplyTipsView()
                self.aboveComment()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo[(String(kStr_indexValue.prefix(5)))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)

            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            FillPathChatManager.createFlush(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            if dataDict.keys.contains("totalMfCoin") {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(kStr_fileStatusContent.replacingOccurrences(of: "icon", with: "a") + "lMfCoi" + kStr_modelName.replacingOccurrences(of: "empty", with: "n"))] as! NSNumber
                //: LocationThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                LocationThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            if dataDict.keys.contains("attachSysMsg") {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(String(kStr_youText.prefix(7)) + String(kStr_timeName.suffix(5)))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.makeup(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((String(kStr_styleName) + kStr_numberGiftName.lowercased())) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(String(kStr_styleName) + kStr_numberGiftName.lowercased())] as! [String: Any]
                //: let model = AddModel(dic: intimatePhotoDict)
                let model = AddModel(dic: intimatePhotoDict)
                //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                updateMsgData.msgModel.msgInfo.intimatePhoto = model
                //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((kStr_topData.replacingOccurrences(of: "should", with: "o"))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(kStr_topData.replacingOccurrences(of: "should", with: "o"))] as! [String: Any]
                //: let model = ViewJsonModel(dic: intimateVideoDict)
                let model = ViewJsonModel(dic: intimateVideoDict)
                //: updateMsgData.msgModel.msgInfo.video = model
                updateMsgData.msgModel.msgInfo.video = model
                //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        asAppear()

        //: updateSession()
        skullSessionModify()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func skullSessionModify() {
        //: ExamineedReactiveCompatible.cache_updateSessionAndPostNotif(with: targetId)
        ExamineedReactiveCompatible.fromDetail(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func eraseCurrent(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard LocationThen.share.loginUserMode.status != 1 else {
            guard LocationThen.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    detailDownArray(showMsg: errorStr)
                }
                //: return
                return
            }

            //: EffectPushManager.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            EffectPushManager.share.physicsLab(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.pushClear()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = ItemView(frame: CGRect(x: 0, y: 0, width: kLet_halfData, height: kLet_noSeatName))
            //: view.show()
            view.momentWithoutPhone()
            //: } else if errorCode == 60003, !LocationThen.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !LocationThen.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            ClientBottomReactiveCompatible.shared.destabilisation()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                detailDownArray(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension SucceedTitleControllerDelegate {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func total(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(kStr_sectionText))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.protrudeRequest(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func numberimate() {
        //: if self.userWrap.intimate >= LocationThen.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= LocationThen.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.restorePush(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(LocationThen.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(LocationThen.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (kLet_managerValue.array(forKey: kLet_giftTitle)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.stopUp()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                kLet_managerValue.set(intimateCacheData, forKey: kLet_giftTitle)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func page(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(kStr_modelTouchData.replacingOccurrences(of: "view", with: "r"))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(kStr_sectionText))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(String(kStr_shouldValue.prefix(6)) + "ntimate")]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.commence(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func protrudeRequest(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((String(kStr_shouldValue.prefix(6)) + "ntimate")), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(String(kStr_shouldValue.prefix(6)) + "ntimate")] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.commence(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func commence(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = ExamineedReactiveCompatible.cache_getCachedUserInfo(targetId: self.targetId)
        let model = ExamineedReactiveCompatible.belowStart(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= LocationThen.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= LocationThen.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.restorePush(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.indexCountimate(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.screenCountimate(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.screenCountimate(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: ExamineedReactiveCompatible.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        ExamineedReactiveCompatible.addPosition(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: kStr_giftValue.map{shouldComponent(scene: $0)}, encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: kStr_seatContent.map{makeBottom(inside: $0)}, encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: kLet_firstMatchValue,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension SucceedTitleControllerDelegate: QualityThen {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func responseTime() {
        //: guard LocationThen.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard LocationThen.share.loginUserMode.sex == SucceedComparable.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.canvasUponLanguage(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func playerShow(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func withEnter(actionType: SharedVarArg) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= LocationThen.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= LocationThen.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    photos()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    showBy()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                photos()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if LocationThen.share.loginUserMode.sex == Gender.female.rawValue {
            if LocationThen.share.loginUserMode.sex == SucceedComparable.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if LocationThen.share.loginUserMode.loungePlus {
                if LocationThen.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= LocationThen.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= LocationThen.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.evaluate(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                showBy()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= LocationThen.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= LocationThen.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    sufficient()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    showBy()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                sufficient()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.withOmit(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func showBy() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.detailDownArray(showMsg: String(bytes: kStr_colorContent.reversed(), encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func alongModel(giftModel _: EngineHandyJSON, giftNum _: String, model _: BottomMemberModel) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func quantity(giftModel: EngineHandyJSON, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        input(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: LoadCellData?, fromStr: String?) {
    func input(giftModel: EngineHandyJSON, giftNum: String, cellData: LoadCellData?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            detailDownArray(showMsg: kLet_accuracyContent)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(kStr_iconData.suffix(5)))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(kStr_labData))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(kStr_sexData.prefix(7)))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(kStr_titleData.suffix(6)) + "msetI" + kStr_whiteContent.replacingOccurrences(of: "layer", with: "d"))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(kStr_intimateInputCenterTitle.replacingOccurrences(of: "list", with: "f"))] = fromStr
        }
        // 送礼接口
        //: UpsetCountRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        UpsetCountRequestTool.imageWithBuild(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.eraseCurrent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_clickValue)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.chronicle(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.coordinate(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.skullSessionModify()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(kStr_sectionText))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((String(kStr_shouldValue.prefix(6)) + "ntimate")), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(String(kStr_shouldValue.prefix(6)) + "ntimate")] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.commence(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.changeIntimateGiftMsg(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                kLet_dismissValue.digitizerInsideStatus(eventID: kLet_micValue)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func qualify(giftModel: EngineHandyJSON, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        kLet_dismissValue.digitizerInsideStatus(eventID: kLet_recordLicenseTitle)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            detailDownArray(showMsg: kLet_accuracyContent)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= LocationThen.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= LocationThen.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            showBy()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(String(kStr_tempName) + String(kStr_picName))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(kStr_attachData.replacingOccurrences(of: "cell", with: "yp"))] = (String(kStr_voiceTitle))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: kStr_handleValue.reversed(), encoding: .utf8)!: (String(kStr_modelStartTitle) + String(kStr_requestThinValue))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(kStr_sectionText)): msgInfo, String(bytes: kStr_videoValue.reversed(), encoding: .utf8)!: params]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        deadline(msgData: cellData, reSend: false)
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func statusSmall(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(kLet_clickGiftText - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func nameGesture(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func firstRecognizer(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.firstRecognizer(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                early(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func atAcross() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if QueryedVoiceManagerDelegate.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            QueryedVoiceManagerDelegate.shared.selectLikePlayer()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension SucceedTitleControllerDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func coordinate(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(kStr_sectionText))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: kStr_handleValue.reversed(), encoding: .utf8)!] = (String(kStr_directionArrayHeightText))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(kStr_sectionText))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.sendCustomMessage(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(kStr_nowData) + String(kStr_timeSizeData))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(kStr_nowData) + String(kStr_timeSizeData))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic[(String(kStr_indexValue.prefix(5)))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        cancel(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((kStr_fileStatusContent.replacingOccurrences(of: "icon", with: "a") + "lMfCoi" + kStr_modelName.replacingOccurrences(of: "empty", with: "n"))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(kStr_fileStatusContent.replacingOccurrences(of: "icon", with: "a") + "lMfCoi" + kStr_modelName.replacingOccurrences(of: "empty", with: "n"))] as! NSNumber
            //: LocationThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            LocationThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: LocationThen.share.loginUserMode.mf_coin)
            msgInputView.giftView.listener(needReload: false, mf_coin: LocationThen.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func influence() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.doingModel(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func cancel(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.primrosePath(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func pageArr(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.sleepingDraught(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func ambages() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension SucceedTitleControllerDelegate {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func resultVoice() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.modePosition { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.skullSessionModify()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func progressSize() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.engraft { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.skullSessionModify()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension SucceedTitleControllerDelegate {
    //: @objc private func feedbackBtnClick() {
    @objc private func imbed() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = LawyerClientRelationFieldDelegate()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        showBorder()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func loadSumimate() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = FillView(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.compartmentList(cellTitleList: [(String(kStr_fatalTitle) + String(kStr_valueManagerLabelData)).localized, (String(kStr_infoText.prefix(5))).localized, (String(kStr_finishName.prefix(6))).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .dataFormat()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: EffectPushManager.share.func__pushToUserDetailVC(uid: self.targetId)
                EffectPushManager.share.userAdd(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                outGift()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = EquivalentReactiveCompatible(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.showAppView(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func outGift() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = TiedNotesView()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.pushAway(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.beyondWindowActivity(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func all() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            early(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            showBorder()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func selectCounto(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = TableTransformable.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.alongTarget(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension SucceedTitleControllerDelegate {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func toColor() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(nameNotification(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: kLet_ageValue,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(equalList),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: kLet_viewData,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func nameNotification(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard LocationThen.share.loginUserMode.loungePlus == true else { return }
        guard LocationThen.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func equalList() {
        //: LocationThen.share.loginUserMode.loungePlus = true
        LocationThen.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func hiddenGesture() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? TUIMessageCellData else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? TUIMessageCellData else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if FillPathChatManager.vox(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.createStart(tipStr: (String(kStr_originalName.suffix(6)) + "ead r" + String(kStr_inviteValue.prefix(8))).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension SucceedTitleControllerDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func photos() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        PerspectiveThen.albumImage(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = SmartPickTool.delay(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.cellPhoto(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func getGoing() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        PerspectiveThen.leadershipMore(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.cellPhoto(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func cellPhoto(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: LocationThen.share.appStatus == AppSkinStatus.normal.rawValue else {
            LocationThen.share.appStatus == SharedInsetTarget.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = MethodModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.blow(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = BottomRecognizerDelegate(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = MethodModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.blow(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        showBorder()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension SucceedTitleControllerDelegate {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func sufficient() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard FillPathChatManager.someSearch(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == SucceedComparable.male.rawValue ? "He".localized : (String(kStr_titleValue.suffix(3))).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.detailDownArray(showMsg: (String(kStr_succeedData.prefix(5)) + " usin" + String(kStr_pushCellItemName.prefix(4)) + "o low " + String(kStr_requestValue.suffix(5)) + "sion").imageArguments(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.indexMake()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func indexMake() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = SmartPickTool.delay(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            SmartPickTool.strongPoint(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.towardUp(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.detailDownArray(showMsg: (String(kStr_imageName.suffix(4)) + "t vid" + String(kStr_messageData.prefix(4)) + "t leas" + String(kStr_tableSendTitle.prefix(6)) + kStr_shareGenderText.replacingOccurrences(of: "main", with: "ds")).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func towardUp(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: ValueEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = FillViewController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = MethodModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.possibility(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        TintProgressHUD.inkingPadDismiss()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        showBorder()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: TUIMessageCellData, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func equalEnter(msgData: TUIMessageCellData, sendModel: MethodModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        ViewSignatureProvider.shared.hidePath(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.detailDownArray(showMsg: (String(kStr_playTitle.prefix(9)) + kStr_blockElectData.replacingOccurrences(of: "color", with: "e")).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.possibility(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension SucceedTitleControllerDelegate {
    //: func func__addVoiceNotification() {
    func fasteningNotification() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(nameChemicalAgentActive),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(constraintBack),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(toNotification(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(outWindow(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: kLet_showValue,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(page(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: kLet_endFinishName,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func indexTime(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            QueryedVoiceManagerDelegate.shared.appSend()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            QueryedVoiceManagerDelegate.shared.imageDownDelegate()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func nameChemicalAgentActive() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        indexTime(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func constraintBack() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        indexTime(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func toNotification(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            indexTime(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            indexTime(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: CanPointCellData, cellView: TalkingChatAudioMsgCell) {
    func playUp(cellData: CanPointCellData, cellView: ChatView) {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard ToSocketManager.shared.isTalking == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.detailDownArray(showMsg: kLet_tableEndValue)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? TUIMessageCellData)!.isKind(of: CanPointCellData.self) == false {
            if (indexModel as? TUIMessageCellData)!.isKind(of: CanPointCellData.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! CanPointCellData
            let audioModel = indexModel as! CanPointCellData
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: cellData.voiceModel.db_voiceCacheWrap = ExamineedReactiveCompatible.cache_getAudioDB(message: message)
        cellData.voiceModel.db_voiceCacheWrap = ExamineedReactiveCompatible.month(message: message)
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        QueryedVoiceManagerDelegate.shared.selectLikePlayer()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if QueryedVoiceManagerDelegate.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            pathLine(message: (kStr_pathValue.replacingOccurrences(of: "image", with: "放状")) + "\(QueryedVoiceManagerDelegate.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if QueryedVoiceManagerDelegate.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                QueryedVoiceManagerDelegate.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        QueryedVoiceManagerDelegate.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        QueryedVoiceManagerDelegate.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: TUIMessageCellData = self.msgView.uiMsgs.object(at: index) as! TUIMessageCellData
                let subModel: TUIMessageCellData = self.msgView.uiMsgs.object(at: index) as! TUIMessageCellData
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: CanPointCellData.self) {
                if subModel.isKind(of: CanPointCellData.self) {
                    //: let audioModel = subModel as! CanPointCellData
                    let audioModel = subModel as! CanPointCellData
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: voiceModel.db_voiceCacheWrap = ExamineedReactiveCompatible.cache_getAudioDB(message: subMessage)
                    voiceModel.db_voiceCacheWrap = ExamineedReactiveCompatible.month(message: subMessage)
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: GoFarReactiveCompatible.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    GoFarReactiveCompatible.gestureModelHandle(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        QueryedVoiceManagerDelegate.shared.addArr(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension SucceedTitleControllerDelegate {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func timeApp() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.sharedGreetWrap()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.toDict(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? TUIMessageCellData
                let indexModel = model as? TUIMessageCellData
                //: if indexModel?.isKind(of: CanPointCellData.self) == true || indexModel?.isKind(of: LayerCellData.self) == true || indexModel?.isKind(of: ShowCellData.self) == true || indexModel?.isKind(of: CountCellData.self) == true {
                if indexModel?.isKind(of: CanPointCellData.self) == true || indexModel?.isKind(of: LayerCellData.self) == true || indexModel?.isKind(of: ShowCellData.self) == true || indexModel?.isKind(of: CountCellData.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.asAppear()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.middle()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.hiddenGesture()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func sharedGreetWrap() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.timeFinish(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.magnitudeeraction(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.numberimate()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == LocationThen.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == LocationThen.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == LocationThen.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == LocationThen.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.beyondWindowActivity(userWrap)
    }

    //: func func__getFillDraftText() {
    func impregnateConstraint() {
        //: let userID = "c2c_"+targetId
        let userID = (String(kStr_userData.suffix(4))) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.responseTime()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func side() {
        //: let userID = "c2c_"+targetId
        let userID = (String(kStr_userData.suffix(4))) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension SucceedTitleControllerDelegate {
    //: func func__TipsNotification() {
    func funcNotificationTipsEqual() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(changeShow(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: kLet_pingTitle,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func changeShow(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: SelectCellData  = userinfo["msg"] as! SelectCellData
        let celldata: SelectCellData = userinfo[(kStr_interactionName.lowercased())] as! SelectCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func createStart(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: kStr_statusData.reversed(), encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: kStr_handleValue.reversed(), encoding: .utf8)!: (String(kStr_layerValue))]
        //: var message: TUIMessageCellData?
        var message: TUIMessageCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessageModel(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = TUISocialChatManager.insertTXMessageModel(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
            message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func middle() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        FillPathChatManager.centerName { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.createStart(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func makeup(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.createStart(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func asAppear() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        FillPathChatManager.makeHandler(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.createStart(tipStr: String(bytes: kStr_viewValue.map{managerSize(model: $0)}, encoding: .utf8)!.imageArguments(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func outWindow(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: TUIMessageCellData = dict?["cellData"] as! TUIMessageCellData
        let cellData: TUIMessageCellData = dict?[(String(kStr_errorTitle.prefix(8)))] as! TUIMessageCellData
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension SucceedTitleControllerDelegate: EqualTipsProtocol {
    //: func func__setReplyTipsView() {
    func disdain() {
        //: let key =  "\(LocationThen.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(LocationThen.share.loginUserMode.userID)" + (String(kStr_fatalValue.suffix(5)) + "ateC" + String(kStr_styleTitle.prefix(6)) + "sTitle" + kStr_cellTitle.capitalized) + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && LocationThen.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && LocationThen.share.appStatus == SharedInsetTarget.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.nameEnd()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            alongBlock(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            aboveComment()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func ingredient(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        observer(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        aboveComment()
    }

    //: func removeReplyTipsView() {
    func aboveComment() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            alongBlock(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension SucceedTitleControllerDelegate {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func dataLoad() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: kLet_pathName, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: kLet_noSeatName - kLet_clickGiftText, width: kLet_halfData, height: kLet_noSeatName)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: kLet_halfData,
                                     //: height: ScreenHeight)
                                     height: kLet_noSeatName)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func early(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.giftFew() {
            //: if TalkingVoiceRoomManager.shared().isParty == true {
            if SubCheckedReactiveCompatible.replyShared().isParty == true {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: kLet_clickData, object: nil, userInfo: nil)
            }
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: ShadowRecognizerDelegate.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: kLet_noSeatName - kLet_clickGiftText,
                                     //: width: ScreenWidth,
                                     width: kLet_halfData,
                                     //: height: ScreenHeight)
                                     height: kLet_noSeatName)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
            //: if TalkingVoiceRoomManager.shared().isParty == false {
            if SubCheckedReactiveCompatible.replyShared().isParty == false {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: kLet_clickData, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension SucceedTitleControllerDelegate {
    /// 创建UI
    //: private func createUI() {
    private func dataAdd() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(kStr_countValue) + String(kStr_blockValue)).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func equalFileBlock() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? kLet_clickGiftText : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.loftiness(width: contentView.width,
                                  //: height: contentView.height,
                                  height: contentView.height,
                                  //: corners: [.topLeft, .topRight],
                                  corners: [.topLeft, .topRight],
                                  //: cornerRadii: .init(width: 8, height: 8))
                                  cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? kLet_senseFormatValue : kLet_netName)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + kLet_formatNetValue)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? kLet_senseFormatValue : kLet_netName)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + kLet_formatNetValue)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + kLet_formatNetValue)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func stickToEyeContactTotaleraction() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.all()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.loadSumimate()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.withEnter(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                kLet_dismissValue.digitizerInsideStatus(eventID: kLet_keyFormalName, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
