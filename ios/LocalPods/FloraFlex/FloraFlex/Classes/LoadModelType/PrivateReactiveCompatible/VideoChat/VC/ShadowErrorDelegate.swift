
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let kStr_nameTitle:[UInt8] = [0x6f,0x68,0x6f,0x72,0x2e,0x65,0x69,0x62,0x63,0x74,0x3c,0x2f,0x26,0x6e,0x67,0x75,0x26,0x68,0x69,0x72,0x26,0x64,0x63,0x63,0x68,0x26,0x6f,0x6b,0x76,0x6a,0x63,0x6b,0x63,0x68,0x72,0x63,0x62]

private func signalTip(view num: UInt8) -> UInt8 {
    return num ^ 6
}

/*: "uid" :*/
fileprivate let kStr_shareData:[UInt8] = [0xbc,0xb0,0xab]

fileprivate func equalGroup(gift num: UInt8) -> UInt8 {
    let value = Int(num) - 71
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "headPic" :*/
fileprivate let kStr_frontName:String = "image let view viewheadP"
fileprivate let kStr_dateData:String = "IC"

/*: "nickname" :*/
fileprivate let kStr_shareLogData:String = "nlabckname"

/*: "birthday" :*/
fileprivate let kStr_whiteData:[Character] = ["b","i","r","t","h"]
fileprivate let kStr_sizeData:String = "managery"

/*: "videoUrl" :*/
fileprivate let kStr_fromTitle:[Character] = ["v","i","d","e","o","U","r","l"]

/*: "cover" :*/
fileprivate let kStr_toRankName:String = "COVER"

/*: "location" :*/
fileprivate let kStr_photoTitle:[Character] = ["l","o","c","a","t","i","o","n"]

/*: "cmd" :*/
fileprivate let kStr_errorData:[UInt8] = [0x64,0x6d,0x63]

/*: "refuseCall" :*/
fileprivate let kStr_dataTitle:[Character] = ["r","e","f","u","s","e"]
fileprivate let kStr_modelContent:String = "var extra player key selfCall"

/*: "data" :*/
fileprivate let kStr_responseData:[UInt8] = [0x27,0x24,0x37,0x24]

fileprivate func toNameCorner(make num: UInt8) -> UInt8 {
    let value = Int(num) - 195
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Call interruption" :*/
fileprivate let kStr_messageSourceData:[Character] = ["C","a","l","l"," ","i","n","t","e","r","r","u","p","t"]
fileprivate let kStr_renderName:String = "texton"

/*: "stopCall" :*/
fileprivate let kStr_resultValue:[Character] = ["s"]
fileprivate let kStr_layerText:[Character] = ["t","o","p","C","a","l","l"]

/*: "logId" :*/
fileprivate let kStr_liveTouchData:[UInt8] = [0xcc,0xcf,0xc7,0xa9,0xc4]

fileprivate func dataWith(weight num: UInt8) -> UInt8 {
    let value = Int(num) - 96
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let kStr_kitData:String = "cancevideo"

/*: "No answer" :*/
fileprivate let kStr_actionContent:[Character] = ["N","o"," "]
fileprivate let kStr_okName:String = "cornerswer"

/*: "duration" :*/
fileprivate let kStr_fieldToName:[Character] = ["d","u","r","a","t"]
fileprivate let kStr_sharedTitle:String = "startn"

/*: "She has hung up" :*/
fileprivate let kStr_changeData:String = "style selfShe has"
fileprivate let kStr_crushText:String = "self equal color mode hung up"

/*: "cancelSex" :*/
fileprivate let kStr_voiceData:String = "castylece"

/*: "He has hung up" :*/
fileprivate let kStr_titleValue:[Character] = ["H","e"," ","h","a","s"," ","h","u","n","g"," ","u"]
fileprivate let kStr_userContent:String = "P"

/*: "endType" :*/
fileprivate let kStr_contactValueTitle:[Character] = ["e","n","d","T","y","p"]
fileprivate let kStr_photoName:[Character] = ["e"]

/*: "stopSex" :*/
fileprivate let kStr_data:String = "indicator clear begin scriptstopSex"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let kStr_randomValue:[UInt8] = [0xfc,0xdb,0xc6,0xc0,0xd3,0xd3,0xdc,0xd6,0xdc,0xd0,0xdb,0xc1,0x95,0xd2,0xda,0xd9,0xd1,0x95,0xd6,0xda,0xdc,0xdb,0xc6,0x99,0x95,0xd6,0xd4,0xd9,0xd9,0x95,0xdd,0xd4,0xc6,0x95,0xd0,0xdb,0xd1,0xd0,0xd1]

private func buttonTo(field num: UInt8) -> UInt8 {
    return num ^ 181
}

/*: "The other hung up the phone" :*/
fileprivate let kStr_equalName:String = "The otstatus write button bottom"
fileprivate let kStr_skipName:String = "humodel"
fileprivate let kStr_systemPathData:String = "extension video varhe phone"

/*: "type" :*/
fileprivate let kStr_fewData:String = "imageype"

/*: "jumpUrl" :*/
fileprivate let kStr_needColorValue:String = "user"
fileprivate let kStr_makeValue:[Character] = ["u","m","p","U","r","l"]

/*: "Cancel" :*/
fileprivate let kStr_requestLabelTitle:String = "name rawCancel"

/*: "Get gold coins" :*/
fileprivate let kStr_makeTitle:String = "Get goreturn play"
fileprivate let kStr_iconLabValue:[Character] = ["s"]

/*: "addBean" :*/
fileprivate let kStr_clickData:[Character] = ["a","d","d","B","e","a","n"]

/*: "refuseSex" :*/
fileprivate let kStr_progressData:[Character] = ["r","e","f","u","s","e","S","e","x"]

/*: "onAcceptCall" :*/
fileprivate let kStr_systemTitle:String = "view"
fileprivate let kStr_modelAppData:[Character] = ["A","c","c","e","p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let kStr_halfName:String = "succeedccep"
fileprivate let kStr_viewValue:String = "L"

/*: "Calls are cancelled" :*/
fileprivate let kStr_timeValue:String = "Calls let engine"
fileprivate let kStr_shareTitle:String = "cpathnc"
fileprivate let kStr_colorValue:[Character] = ["e","l","l","e","d"]

/*: "The call has ended" :*/
fileprivate let kStr_makeName:[Character] = ["T","h","e"," ","c","a","l","l"," ","h","a"]
fileprivate let kStr_effectStartText:String = "s endedself sex view smart"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ShadowErrorDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let kLet_failData = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class ShadowErrorDelegate: LayerRecognizerDelegate {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: TitleGiftChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: ChorusPlayListener? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: BlockView? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: VideoAddScreenView? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        ToSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ToSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ToSocketManager.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kStr_nameTitle.map{signalTip(view: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! EngineControllerDelegate
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.destabilization()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            equalAcross()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            placeboEffect(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currTalkingVC = nil
            ToSocketManager.shared.currTalkingVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.brisance()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        photoUser()
        //: setupSubViewsConstraint()
        active()
        //: initNotificationObserver()
        atShowSmall()
        //: req_callGetUserInfo()
        dataFileWith()
        //: timeoutRules()
        requestGift()
        //: setUpBGMPlayer()
        quantityIn()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.hidePreview()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if LocationThen.share.appConfigMode.disableShootScreen {
            if LocationThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = UpBackgroundThen(frame: CGRect(x: 0, y: 0, width: kLet_halfData, height: kLet_noSeatName))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isTalking = false
        ToSocketManager.shared.isTalking = false
        //: self.stopVCAllActivties()
        self.stopActivtiesAll()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: VideoVoiceThen = {
        //: let v = TalkingBGVideoView()
        let v = VideoVoiceThen()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.upAll(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: FillReactiveCompatible = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = FillReactiveCompatible(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: DoRecognizerDelegate = {
        //: let v = TalkingVideoWindowView()
        let v = DoRecognizerDelegate()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = kLet_halfData - 15 - originalScene(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if DeleteClientThen.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: kLet_postPartyValue + mediaBlock(h: 25) + 26 + 35, width: originalScene(w: 125), height: mediaBlock(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.humanAction()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: AdministratorCloudDelegate = {
        //: let m = TalkingVideoChatManager()
        let m = AdministratorCloudDelegate()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension ShadowErrorDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func dataFileWith() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        AdministratorCloudDelegate.shouldCompletion(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: kStr_shareData.map{equalGroup(gift: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(kStr_frontName.suffix(5)) + kStr_dateData.lowercased())].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(kStr_shareLogData.replacingOccurrences(of: "lab", with: "i"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(String(kStr_whiteData) + kStr_sizeData.replacingOccurrences(of: "manager", with: "da"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(kStr_fromTitle))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(kStr_toRankName.lowercased())].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(kStr_photoTitle))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.quickBeginInfo()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.telecastingActionBastardy()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func telecastingActionBastardy() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = PointCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = PointCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.recordCamera(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.headCamera(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension ShadowErrorDelegate {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func equalAcross() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        PerspectiveThen.disappearEvent { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.detailDownArray(showMsg: kLet_viewValue)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ToSocketManager.shared.lessNutsAndBolts(info: [String(bytes: kStr_errorData.reversed(), encoding: .utf8)!: (String(kStr_dataTitle) + String(kStr_modelContent.suffix(4))), String(bytes: kStr_responseData.map{toNameCorner(make: $0)}, encoding: .utf8)!: [String(bytes: kStr_shareData.map{equalGroup(gift: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.halogenAnimated()
                //: return
                return
            }

            //: self.startPreview()
            self.hidePreview()
            //: self.stopBGM()
            self.iconFile()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = ChorusPlayListener.writerSName(type: .Response)
            //: self.startBGM()
            self.destabilization()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func hidePreview() {
        //: self.videoManager.startPreview()
        self.videoManager.totalry()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func stopActivtiesAll() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.iconFile()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        AddDanmuManager.errorResource()
        //: TalkingSocketManager.shared.callerDelegate = nil
        ToSocketManager.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        ToSocketManager.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        ToSocketManager.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func halogenAnimated(animated: Bool = true) {
        //: self.stopBGM()
        self.iconFile()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func humanAction() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.openDestabilization()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.telecastingActionBastardy()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func locationBy() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.createForPublish()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.telecastingActionBastardy()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.butylate()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func butylate() {
        //: guard LocationThen.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard LocationThen.share.loginUserMode.sex == SucceedComparable.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = VideoAddScreenView()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.galaxy()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension ShadowErrorDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func atShowSmall() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(quantityercalationNotif(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(listStatus(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: kLet_errSuccessText,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(changeShape(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: kLet_noData,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        kLet_failData.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func quantityercalationNotif(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            halogenAnimated()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func changeShape(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard ToSocketManager.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        ToSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ToSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ToSocketManager.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.isochronal()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func listStatus(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.telecastingActionBastardy()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension ShadowErrorDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.detailDownArray(showMsg: (String(kStr_messageSourceData) + kStr_renderName.replacingOccurrences(of: "text", with: "i")))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                ToSocketManager.shared.lessNutsAndBolts(info: [String(bytes: kStr_errorData.reversed(), encoding: .utf8)!: (String(kStr_resultValue) + String(kStr_layerText)), String(bytes: kStr_responseData.map{toNameCorner(make: $0)}, encoding: .utf8)!: [String(bytes: kStr_liveTouchData.map{dataWith(weight: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                ToSocketManager.shared.lessNutsAndBolts(info: [String(bytes: kStr_errorData.reversed(), encoding: .utf8)!: (kStr_kitData.replacingOccurrences(of: "video", with: "l") + "Call"), String(bytes: kStr_responseData.map{toNameCorner(make: $0)}, encoding: .utf8)!: [String(bytes: kStr_liveTouchData.map{dataWith(weight: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ToSocketManager.shared.lessNutsAndBolts(info: [String(bytes: kStr_errorData.reversed(), encoding: .utf8)!: (String(kStr_dataTitle) + String(kStr_modelContent.suffix(4))), String(bytes: kStr_responseData.map{toNameCorner(make: $0)}, encoding: .utf8)!: [String(bytes: kStr_shareData.map{equalGroup(gift: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.withSection()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                ToSocketManager.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            halogenAnimated()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension ShadowErrorDelegate {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func quantityIn() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? LocationPropertyProtocol.Call : LocationPropertyProtocol.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = ChorusPlayListener.writerSName(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func destabilization() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.be()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func iconFile() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.gossipAttribute()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension ShadowErrorDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func requestGift() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? kLet_errContent : self.chatModel?.bring()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            receiveAfter()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.receiveAfter()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func receiveAfter() {
        //: guard !TalkingSocketManager.shared.isTalking else { return }
        guard !ToSocketManager.shared.isTalking else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.detailDownArray(showMsg: (String(kStr_actionContent) + kStr_okName.replacingOccurrences(of: "corner", with: "an")).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: kStr_errorData.reversed(), encoding: .utf8)!: (kStr_kitData.replacingOccurrences(of: "video", with: "l") + "Call"), String(bytes: kStr_responseData.map{toNameCorner(make: $0)}, encoding: .utf8)!: [String(bytes: kStr_liveTouchData.map{dataWith(weight: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: kStr_errorData.reversed(), encoding: .utf8)!: (String(kStr_dataTitle) + String(kStr_modelContent.suffix(4))), String(bytes: kStr_responseData.map{toNameCorner(make: $0)}, encoding: .utf8)!: [String(bytes: kStr_shareData.map{equalGroup(gift: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        ToSocketManager.shared.lessNutsAndBolts(info: socketDict)
        //: popCurrentViewController()
        halogenAnimated()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension ShadowErrorDelegate {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func modifyAndData(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: kStr_liveTouchData.map{dataWith(weight: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(kStr_fieldToName) + kStr_sharedTitle.replacingOccurrences(of: "start", with: "io"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.pullModifyError(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.fileFor()
            //: createTalkRoom()
            locationBy()
            //: self.stopBGM()
            self.iconFile()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func placeboEffect(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.openDestabilization()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.upAll(show: false)
        //: self.stopBGM()
        self.iconFile()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.fileFor()
        //: self.createTalkRoom()
        self.locationBy()
    }
}

// MARK: - ManagerCallerDelegate【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension ShadowErrorDelegate: ManagerCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func constituent(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(kStr_changeData.suffix(7)) + String(kStr_crushText.suffix(8))).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(kStr_voiceData.replacingOccurrences(of: "style", with: "n") + "lSex")].stringValue == SucceedComparable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(kStr_titleValue) + kStr_userContent.lowercased()).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.detailDownArray(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.withSection()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            ToSocketManager.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        halogenAnimated()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func dataTextItem(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        placeboEffect(logId: json[String(bytes: kStr_liveTouchData.map{dataWith(weight: $0)}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func stopButton(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(kStr_contactValueTitle) + String(kStr_photoName))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(kStr_changeData.suffix(7)) + String(kStr_crushText.suffix(8))).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(kStr_data.suffix(7)))].stringValue == SucceedComparable.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(kStr_titleValue) + kStr_userContent.lowercased()).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if LocationThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if LocationThen.share.appStatus == SharedInsetTarget.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: kStr_randomValue.map{buttonTo(field: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(kStr_equalName.prefix(6)) + "her " + kStr_skipName.replacingOccurrences(of: "model", with: "ng") + " up t" + String(kStr_systemPathData.suffix(8))).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.detailDownArray(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.withSection()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            ToSocketManager.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.fromWeltanschauungDismiss()
        //: popCurrentViewController()
        halogenAnimated()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func asShow(msg: String, data: [String: Any]) {
        //: guard LocationThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard LocationThen.share.appStatus == SharedInsetTarget.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(kStr_fewData.replacingOccurrences(of: "image", with: "t"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(kStr_needColorValue.replacingOccurrences(of: "user", with: "j") + String(kStr_makeValue))] as? String else { return }
            //: let httpsUrl = LocationThen.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = LocationThen.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = CanPlumeConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: EffectPushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            EffectPushManager.share.deleteTake(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            ClickReactiveCompatible.follow(title: nil,
                                    //: message: msg,
                                    message: msg,
                                    //: leftBtnTitle: "Cancel".localized,
                                    leftBtnTitle: (String(kStr_requestLabelTitle.suffix(6))).localized,
                                    //: rightBtnTitle: "Get gold coins".localized) {
                                    rightBtnTitle: (String(kStr_makeTitle.prefix(6)) + "ld coin" + String(kStr_iconLabValue)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                ClickReactiveCompatible.closeHideRelease()
                //: } rightBlock: {
            } rightBlock: {
                //: EffectPushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                EffectPushManager.share.physicsLab(clickEvent: kLet_maxData, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func graduateChangeDowery(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.nameArr(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func currentIn(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(kStr_clickData))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.living(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.modelTableTo(point: addBean)
        }
    }
}

// MARK: - RuggedizationPartyDelegate【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension ShadowErrorDelegate: RuggedizationPartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func crop(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(kStr_changeData.suffix(7)) + String(kStr_crushText.suffix(8))).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(kStr_progressData))].stringValue == SucceedComparable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(kStr_titleValue) + kStr_userContent.lowercased()).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.detailDownArray(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.withSection()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            ToSocketManager.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        halogenAnimated()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func greet(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        placeboEffect(logId: json[String(bytes: kStr_liveTouchData.map{dataWith(weight: $0)}, encoding: .utf8)!].intValue)
    }
}

// MARK: - CanErrorDelegate【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension ShadowErrorDelegate: CanErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func recieve(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(kStr_dataTitle) + String(kStr_modelContent.suffix(4))) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.detailDownArray(showMsg: (String(kStr_actionContent) + kStr_okName.replacingOccurrences(of: "corner", with: "an")).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.withSection()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                ToSocketManager.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            halogenAnimated()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (kStr_systemTitle.replacingOccurrences(of: "view", with: "on") + String(kStr_modelAppData)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard LocationThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard LocationThen.share.appStatus == SharedInsetTarget.normal.rawValue else { return }
            //: EffectPushManager.share.func__jumpToWebRecharge(sufficient: false)
            EffectPushManager.share.physicsLab(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (kStr_systemTitle.replacingOccurrences(of: "view", with: "on") + String(kStr_modelAppData)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.detailDownArray(showMsg: msg)
            //: popCurrentViewController()
            halogenAnimated()
        }
    }
}

// MARK: - EqualObjectProtocol【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension ShadowErrorDelegate: EqualObjectProtocol {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func totalerchangeTo() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        ToSocketManager.shared.lessNutsAndBolts(info: [String(bytes: kStr_errorData.reversed(), encoding: .utf8)!: (kStr_halfName.replacingOccurrences(of: "succeed", with: "a") + "tCal" + kStr_viewValue.lowercased()), String(bytes: kStr_responseData.map{toNameCorner(make: $0)}, encoding: .utf8)!: [String(bytes: kStr_shareData.map{equalGroup(gift: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func addBy() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.detailDownArray(showMsg: (String(kStr_timeValue.prefix(6)) + "are " + kStr_shareTitle.replacingOccurrences(of: "path", with: "a") + String(kStr_colorValue)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            ToSocketManager.shared.lessNutsAndBolts(info: [String(bytes: kStr_errorData.reversed(), encoding: .utf8)!: (kStr_kitData.replacingOccurrences(of: "video", with: "l") + "Call"), String(bytes: kStr_responseData.map{toNameCorner(make: $0)}, encoding: .utf8)!: [String(bytes: kStr_liveTouchData.map{dataWith(weight: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.detailDownArray(showMsg: (String(kStr_timeValue.prefix(6)) + "are " + kStr_shareTitle.replacingOccurrences(of: "path", with: "a") + String(kStr_colorValue)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            ToSocketManager.shared.lessNutsAndBolts(info: [String(bytes: kStr_errorData.reversed(), encoding: .utf8)!: (String(kStr_dataTitle) + String(kStr_modelContent.suffix(4))), String(bytes: kStr_responseData.map{toNameCorner(make: $0)}, encoding: .utf8)!: [String(bytes: kStr_shareData.map{equalGroup(gift: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.detailDownArray(showMsg: (String(kStr_makeName) + String(kStr_effectStartText.prefix(7))).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            ToSocketManager.shared.lessNutsAndBolts(info: [String(bytes: kStr_errorData.reversed(), encoding: .utf8)!: (String(kStr_resultValue) + String(kStr_layerText)), String(bytes: kStr_responseData.map{toNameCorner(make: $0)}, encoding: .utf8)!: [String(bytes: kStr_liveTouchData.map{dataWith(weight: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        halogenAnimated()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func valuePush(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.collectionTo(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func contactTimePicture(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.upAll(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.everyView(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.telecastingActionBastardy()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func atWindow() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        ToSocketManager.shared.currTalkingVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = BlockView.sizeIcon()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.versionDown()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.dataMode()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.giftFew()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        halogenAnimated()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func quantityeraction() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension ShadowErrorDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func photoUser() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.voiceColor(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func active() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
