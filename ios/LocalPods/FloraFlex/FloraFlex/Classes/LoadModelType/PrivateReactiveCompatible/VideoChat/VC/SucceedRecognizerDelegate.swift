
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let kStr_addText:[UInt8] = [0x54,0x59,0x54,0x5f,0x13,0x4e,0x5a,0x4f,0x50,0x5d,0x25,0x14,0xb,0x53,0x4c,0x5e,0xb,0x59,0x5a,0x5f,0xb,0x4d,0x50,0x50,0x59,0xb,0x54,0x58,0x5b,0x57,0x50,0x58,0x50,0x59,0x5f,0x50,0x4f]

fileprivate func andSo(max num: UInt8) -> UInt8 {
    let value = Int(num) + 21
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid" :*/
fileprivate let kStr_managerUserText:[UInt8] = [0x76,0x6a,0x67]

private func nameRow(background num: UInt8) -> UInt8 {
    return num ^ 3
}

/*: "headPic" :*/
fileprivate let kStr_blockTitle:String = "right action name mode ifheadPic"

/*: "nickname" :*/
fileprivate let kStr_ofValue:[Character] = ["n","i","c","k","n","a"]
fileprivate let kStr_lineValue:String = "manager"

/*: "birthday" :*/
fileprivate let kStr_cellNameText:String = "birthdaoriginal"

/*: "videoUrl" :*/
fileprivate let kStr_fullValue:[Character] = ["v","i","d","e","o","U","r","l"]

/*: "location" :*/
fileprivate let kStr_appValue:[Character] = ["l","o","c","a","t","i"]
fileprivate let kStr_playModeText:[Character] = ["o","n"]

/*: "cmd" :*/
fileprivate let kStr_timeLayerName:[UInt8] = [0x64,0x6d,0x63]

/*: "refuseCall" :*/
fileprivate let kStr_objectValue:String = "refuplayer"
fileprivate let kStr_popName:String = "original dropeCall"

/*: "data" :*/
fileprivate let kStr_sharedStatusData:[UInt8] = [0x61,0x74,0x61,0x64]

/*: "Call interruption" :*/
fileprivate let kStr_equalTitle:[Character] = ["C","a","l","l"," ","i","n"]
fileprivate let kStr_playerValue:String = "finisherr"

/*: "stopCall" :*/
fileprivate let kStr_talkName:String = "height in self selfstopC"
fileprivate let kStr_recordName:String = "atitletitle"

/*: "logId" :*/
fileprivate let kStr_listSizeData:[UInt8] = [0x21,0x24,0x1c,0xfe,0x19]

fileprivate func cancelApp(permission num: UInt8) -> UInt8 {
    let value = Int(num) + 75
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let kStr_viewName:[Character] = ["c","a","n","c","e","l","C","a","l","l"]

/*: "No answer" :*/
fileprivate let kStr_tarSizeName:[Character] = ["N"]
fileprivate let kStr_noBackgroundName:[Character] = ["o"," ","a","n","s","w","e","r"]

/*: "duration" :*/
fileprivate let kStr_photoText:[Character] = ["d","u","r","a","t"]
fileprivate let kStr_normalData:String = "hiddenon"

/*: "She has hung up" :*/
fileprivate let kStr_mediaFloorTextContent:String = "size if share returnShe has"
fileprivate let kStr_halfData:String = "me equal element hung up"

/*: "cancelSex" :*/
fileprivate let kStr_constraintText:[Character] = ["c","a","n","c","e","l","S","e","x"]

/*: "He has hung up" :*/
fileprivate let kStr_equalErrorData:String = "row last makeHe has"

/*: "endType" :*/
fileprivate let kStr_selectedData:String = "endTypemode color content"

/*: "stopSex" :*/
fileprivate let kStr_inputCurrentValue:String = "make true time mode tostopSex"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let kStr_smallValue:[UInt8] = [0xda,0xfd,0xe0,0xe6,0xf5,0xf5,0xfa,0xf0,0xfa,0xf6,0xfd,0xe7,0xb3,0xf4,0xfc,0xff,0xf7,0xb3,0xf0,0xfc,0xfa,0xfd,0xe0,0xbf,0xb3,0xf0,0xf2,0xff,0xff,0xb3,0xfb,0xf2,0xe0,0xb3,0xf6,0xfd,0xf7,0xf6,0xf7]

private func sendMake(cell num: UInt8) -> UInt8 {
    return num ^ 147
}

/*: "The other hung up the phone" :*/
fileprivate let kStr_name:[Character] = ["T","h","e"," ","o","t","h","e","r"," ","h","u","n","g"," ","u","p"," ","t","h","e"," ","p","h","o","n","e"]

/*: "type" :*/
fileprivate let kStr_rawData:String = "tapppe"

/*: "jumpUrl" :*/
fileprivate let kStr_modelValue:String = "jumpUany table price self"
fileprivate let kStr_inputData:String = "constraint"

/*: "Cancel" :*/
fileprivate let kStr_changeName:String = "Cancellet model self hidden"

/*: "Get gold coins" :*/
fileprivate let kStr_touchText:String = "Get gcontent let cell"
fileprivate let kStr_partyData:String = "ofromns"

/*: "addBean" :*/
fileprivate let kStr_moveValue:[Character] = ["a"]
fileprivate let kStr_enableTempTitle:[Character] = ["d","d","B","e","a","n"]

/*: "refuseSex" :*/
fileprivate let kStr_redContent:[Character] = ["r","e","f","u","s","e","S","e","x"]

/*: "onAcceptCall" :*/
fileprivate let kStr_imageName:[Character] = ["o","n","A","c","c"]
fileprivate let kStr_bringTitle:[Character] = ["e","p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let kStr_fromShareValue:[Character] = ["a","c","c","e","p","t","C","a"]
fileprivate let kStr_makeContent:String = "social"

/*: "Calls are cancelled" :*/
fileprivate let kStr_infoData:[Character] = ["C","a","l","l","s"," ","a","r","e"," ","c","a","n","c","e","l","l","e","d"]

/*: "The call has ended" :*/
fileprivate let kStr_cornerData:String = "The callmake cell"
fileprivate let kStr_againValue:String = " has price size"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SucceedRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let kLet_screenDomainValue = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class SucceedRecognizerDelegate: LayerRecognizerDelegate {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: TitleGiftChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: ChorusPlayListener? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: LocationMiniView? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        ToSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ToSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ToSocketManager.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kStr_addText.map{andSo(max: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! EngineControllerDelegate
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.beforeBegin()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            vocalisationAuth()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currTalkingVC = nil
            ToSocketManager.shared.currTalkingVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.calorieChart()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        dogEar()
        //: setupSubViewsConstraint()
        equalSetup()
        //: initNotificationObserver()
        pathWith()
        //: req_callGetUserInfo()
        imageInfo()
        //: timeoutRules()
        translation()
        //: resumeTalk()
        reStartSend()
        //: setUpBGMPlayer()
        setTagLinksman()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if LocationThen.share.appConfigMode.disableShootScreen {
            if LocationThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = UpBackgroundThen(frame: CGRect(x: 0, y: 0, width: kLet_halfData, height: kLet_noSeatName))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isTalking = false
        ToSocketManager.shared.isTalking = false
        //: self.stopVCAllActivties()
        self.lendStart()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: ShowView = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = ShowView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: FacingPagesCloudDelegate = {
        //: let m = TalkingVoiceChatManager()
        let m = FacingPagesCloudDelegate()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension SucceedRecognizerDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func imageInfo() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        AdministratorCloudDelegate.shouldCompletion(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: kStr_managerUserText.map{nameRow(background: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(kStr_blockTitle.suffix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(String(kStr_ofValue) + kStr_lineValue.replacingOccurrences(of: "manager", with: "me"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(kStr_cellNameText.replacingOccurrences(of: "original", with: "y"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(kStr_fullValue))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(kStr_appValue) + String(kStr_playModeText))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.shouldInfo()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension SucceedRecognizerDelegate {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func vocalisationAuth() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        PerspectiveThen.schlep(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.detailDownArray(showMsg: kLet_viewValue)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ToSocketManager.shared.lessNutsAndBolts(info: [String(bytes: kStr_timeLayerName.reversed(), encoding: .utf8)!: (kStr_objectValue.replacingOccurrences(of: "player", with: "s") + String(kStr_popName.suffix(5))), String(bytes: kStr_sharedStatusData.reversed(), encoding: .utf8)!: [String(bytes: kStr_managerUserText.map{nameRow(background: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.enterAnimated()
                //: return
                return
            }

            //: self.stopBGM()
            self.placeBgm()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = ChorusPlayListener.writerSName(type: .Response)
            //: self.startBGM()
            self.beforeBegin()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func lendStart() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.placeBgm()
        //: TalkingSocketManager.shared.callerDelegate = nil
        ToSocketManager.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        ToSocketManager.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        ToSocketManager.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func enterAnimated(animated: Bool = true) {
        //: self.stopBGM()
        self.placeBgm()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func equalTalk() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.flunk(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func reStartSend() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            equalTalk()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension SucceedRecognizerDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func pathWith() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(tabStatus(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(changedNote(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: kLet_noData,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        kLet_screenDomainValue.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func tabStatus(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            enterAnimated()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func changedNote(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard ToSocketManager.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        ToSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ToSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ToSocketManager.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.stream(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension SucceedRecognizerDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.detailDownArray(showMsg: (String(kStr_equalTitle) + kStr_playerValue.replacingOccurrences(of: "finish", with: "t") + "uption").localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                ToSocketManager.shared.lessNutsAndBolts(info: [String(bytes: kStr_timeLayerName.reversed(), encoding: .utf8)!: (String(kStr_talkName.suffix(5)) + kStr_recordName.replacingOccurrences(of: "title", with: "l")), String(bytes: kStr_sharedStatusData.reversed(), encoding: .utf8)!: [String(bytes: kStr_listSizeData.map{cancelApp(permission: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                ToSocketManager.shared.lessNutsAndBolts(info: [String(bytes: kStr_timeLayerName.reversed(), encoding: .utf8)!: (String(kStr_viewName)), String(bytes: kStr_sharedStatusData.reversed(), encoding: .utf8)!: [String(bytes: kStr_listSizeData.map{cancelApp(permission: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ToSocketManager.shared.lessNutsAndBolts(info: [String(bytes: kStr_timeLayerName.reversed(), encoding: .utf8)!: (kStr_objectValue.replacingOccurrences(of: "player", with: "s") + String(kStr_popName.suffix(5))), String(bytes: kStr_sharedStatusData.reversed(), encoding: .utf8)!: [String(bytes: kStr_managerUserText.map{nameRow(background: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.withSection()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                ToSocketManager.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            enterAnimated()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.withModule(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension SucceedRecognizerDelegate {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func setTagLinksman() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? LocationPropertyProtocol.Call : LocationPropertyProtocol.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = ChorusPlayListener.writerSName(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func beforeBegin() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.be()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func placeBgm() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.gossipAttribute()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension SucceedRecognizerDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func translation() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? kLet_errContent : self.chatModel?.bring()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            photoChange()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.photoChange()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func photoChange() {
        //: guard !TalkingSocketManager.shared.isTalking else { return }
        guard !ToSocketManager.shared.isTalking else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.detailDownArray(showMsg: (String(kStr_tarSizeName) + String(kStr_noBackgroundName)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: kStr_timeLayerName.reversed(), encoding: .utf8)!: (String(kStr_viewName)), String(bytes: kStr_sharedStatusData.reversed(), encoding: .utf8)!: [String(bytes: kStr_listSizeData.map{cancelApp(permission: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: kStr_timeLayerName.reversed(), encoding: .utf8)!: (kStr_objectValue.replacingOccurrences(of: "player", with: "s") + String(kStr_popName.suffix(5))), String(bytes: kStr_sharedStatusData.reversed(), encoding: .utf8)!: [String(bytes: kStr_managerUserText.map{nameRow(background: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        ToSocketManager.shared.lessNutsAndBolts(info: socketDict)
        //: popCurrentViewController()
        enterAnimated()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension SucceedRecognizerDelegate {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func clickData(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: kStr_listSizeData.map{cancelApp(permission: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(kStr_photoText) + kStr_normalData.replacingOccurrences(of: "hidden", with: "i"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.behindDisplayTime(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.phallicStage()
            //: createTalkRoom()
            equalTalk()
            //: self.stopBGM()
            self.placeBgm()
        }
    }
}

// MARK: - ManagerCallerDelegate【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension SucceedRecognizerDelegate: ManagerCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func constituent(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(kStr_mediaFloorTextContent.suffix(7)) + String(kStr_halfData.suffix(8))).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(kStr_constraintText))].stringValue == SucceedComparable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(kStr_equalErrorData.suffix(6)) + " hung up").localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.detailDownArray(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.withSection()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            ToSocketManager.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        enterAnimated()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func dataTextItem(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: kStr_listSizeData.map{cancelApp(permission: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.placeBgm()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.phallicStage()
        //: self.createTalkRoom()
        self.equalTalk()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func stopButton(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(kStr_selectedData.prefix(7)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(kStr_mediaFloorTextContent.suffix(7)) + String(kStr_halfData.suffix(8))).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(kStr_inputCurrentValue.suffix(7)))].stringValue == SucceedComparable.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(kStr_equalErrorData.suffix(6)) + " hung up").localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if LocationThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if LocationThen.share.appStatus == SharedInsetTarget.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: kStr_smallValue.map{sendMake(cell: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(kStr_name)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.detailDownArray(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.withSection()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            ToSocketManager.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.everyLastApp()
        //: self.popCurrentViewController()
        self.enterAnimated()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: kLet_detailContent, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func asShow(msg: String, data: [String: Any]) {
        //: guard LocationThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard LocationThen.share.appStatus == SharedInsetTarget.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(kStr_rawData.replacingOccurrences(of: "app", with: "y"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(kStr_modelValue.prefix(5)) + kStr_inputData.replacingOccurrences(of: "constraint", with: "rl"))] as? String else { return }
            //: let httpsUrl = LocationThen.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = LocationThen.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = CanPlumeConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: EffectPushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            EffectPushManager.share.deleteTake(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            ClickReactiveCompatible.follow(title: nil,
                                    //: message: msg,
                                    message: msg,
                                    //: leftBtnTitle: "Cancel".localized,
                                    leftBtnTitle: (String(kStr_changeName.prefix(6))).localized,
                                    //: rightBtnTitle: "Get gold coins".localized) {
                                    rightBtnTitle: (String(kStr_touchText.prefix(5)) + "old c" + kStr_partyData.replacingOccurrences(of: "from", with: "i")).localized)
            {
                //: TalkingAlertShow.hideAlert()
                ClickReactiveCompatible.closeHideRelease()
                //: } rightBlock: {
            } rightBlock: {
                //: EffectPushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                EffectPushManager.share.physicsLab(clickEvent: kLet_maxData, sufficient: false)
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func currentIn(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(kStr_moveValue) + String(kStr_enableTempTitle))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.popPoint(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.gallery(point: addBean)
        }
    }
}

// MARK: - RuggedizationPartyDelegate【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension SucceedRecognizerDelegate: RuggedizationPartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func crop(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(kStr_mediaFloorTextContent.suffix(7)) + String(kStr_halfData.suffix(8))).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(kStr_redContent))].stringValue == SucceedComparable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(kStr_equalErrorData.suffix(6)) + " hung up").localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.detailDownArray(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.withSection()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            ToSocketManager.shared.currTalkingVC = nil
        }
        //: popCurrentViewController()
        enterAnimated()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func greet(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: kStr_listSizeData.map{cancelApp(permission: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.placeBgm()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.phallicStage()
        //: self.createTalkRoom()
        self.equalTalk()
    }
}

// MARK: - CanErrorDelegate【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension SucceedRecognizerDelegate: CanErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func recieve(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (kStr_objectValue.replacingOccurrences(of: "player", with: "s") + String(kStr_popName.suffix(5))) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.detailDownArray(showMsg: (String(kStr_tarSizeName) + String(kStr_noBackgroundName)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.withSection()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                ToSocketManager.shared.currTalkingVC = nil
            }
            //: self.popCurrentViewController()
            self.enterAnimated()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(kStr_imageName) + String(kStr_bringTitle)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard LocationThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard LocationThen.share.appStatus == SharedInsetTarget.normal.rawValue else { return }
            //: EffectPushManager.share.func__jumpToWebRecharge(sufficient: false)
            EffectPushManager.share.physicsLab(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(kStr_imageName) + String(kStr_bringTitle)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.detailDownArray(showMsg: msg)
            //: self.popCurrentViewController()
            self.enterAnimated()
        }
    }
}

// MARK: - WithObjectProtocol【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension SucceedRecognizerDelegate: WithObjectProtocol {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func sectionBlockGift() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        ToSocketManager.shared.lessNutsAndBolts(info: [String(bytes: kStr_timeLayerName.reversed(), encoding: .utf8)!: (String(kStr_fromShareValue) + kStr_makeContent.replacingOccurrences(of: "social", with: "ll")), String(bytes: kStr_sharedStatusData.reversed(), encoding: .utf8)!: [String(bytes: kStr_managerUserText.map{nameRow(background: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func discontinueView() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.detailDownArray(showMsg: (String(kStr_infoData)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            ToSocketManager.shared.lessNutsAndBolts(info: [String(bytes: kStr_timeLayerName.reversed(), encoding: .utf8)!: (String(kStr_viewName)), String(bytes: kStr_sharedStatusData.reversed(), encoding: .utf8)!: [String(bytes: kStr_listSizeData.map{cancelApp(permission: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.detailDownArray(showMsg: (String(kStr_infoData)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            ToSocketManager.shared.lessNutsAndBolts(info: [String(bytes: kStr_timeLayerName.reversed(), encoding: .utf8)!: (kStr_objectValue.replacingOccurrences(of: "player", with: "s") + String(kStr_popName.suffix(5))), String(bytes: kStr_sharedStatusData.reversed(), encoding: .utf8)!: [String(bytes: kStr_managerUserText.map{nameRow(background: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.detailDownArray(showMsg: (String(kStr_cornerData.prefix(8)) + String(kStr_againValue.prefix(5)) + "ended").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            ToSocketManager.shared.lessNutsAndBolts(info: [String(bytes: kStr_timeLayerName.reversed(), encoding: .utf8)!: (String(kStr_talkName.suffix(5)) + kStr_recordName.replacingOccurrences(of: "title", with: "l")), String(bytes: kStr_sharedStatusData.reversed(), encoding: .utf8)!: [String(bytes: kStr_listSizeData.map{cancelApp(permission: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.everyLastApp()
        //: self.popCurrentViewController()
        self.enterAnimated()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func backData(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.giantStar(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func poke(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.notVoice(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func notShould() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        ToSocketManager.shared.currTalkingVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = LocationMiniView.upwardlyView()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.giftFew()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.computerFileName()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.makeRemove(talkingTime)
        //: popCurrentViewController()
        enterAnimated()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func nameColor(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.makeRemove(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func toALesserExtent(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.footlooseRepast(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension SucceedRecognizerDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func dogEar() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func equalSetup() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
