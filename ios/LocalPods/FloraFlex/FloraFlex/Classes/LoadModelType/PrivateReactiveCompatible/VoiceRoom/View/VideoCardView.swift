
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let kStr_mainAtValue:[UInt8] = [0x9,0xe,0x9,0x14,0xc8,0x3,0xf,0x4,0x5,0x12,0xda,0xc9,0xc0,0x8,0x1,0x13,0xc0,0xe,0xf,0x14,0xc0,0x2,0x5,0x5,0xe,0xc0,0x9,0xd,0x10,0xc,0x5,0xd,0x5,0xe,0x14,0x5,0x4]

fileprivate func tabApp(selected num: UInt8) -> UInt8 {
    let value = Int(num) + 96
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Followers" :*/
fileprivate let kStr_mediaTitle:String = "self listenerFollowe"
fileprivate let kStr_theReloadQuoteValue:String = "items"

/*: "Following" :*/
fileprivate let kStr_areaTitle:[Character] = ["F","o","l","l","o","w","i","n"]
fileprivate let kStr_byValue:[Character] = ["g"]

/*: "#E6E6E6" :*/
fileprivate let kStr_halfValue:String = "color on#E6E6E"
fileprivate let kStr_shareData:[Character] = ["6"]

/*: "btn_message_more" :*/
fileprivate let kStr_fewTitle:String = "model inside for info failurebtn_messa"
fileprivate let kStr_errorSucceedData:String = "text name inputge_more"

/*: "UID:  :*/
fileprivate let kStr_selectedData:[Character] = ["U","I","D",":"," "]

/*: "level_ :*/
fileprivate let kStr_appMakeToText:[Character] = ["l","e","v","e","l","_"]

/*: "Gift" :*/
fileprivate let kStr_seatData:[UInt8] = [0x27,0x9,0x6,0x14]

private func typeModel(from num: UInt8) -> UInt8 {
    return num ^ 96
}

/*: "Mike" :*/
fileprivate let kStr_makeCellName:[UInt8] = [0x4,0x20,0x22,0x1c]

fileprivate func insideBack(rank num: UInt8) -> UInt8 {
    let value = Int(num) + 73
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Follow" :*/
fileprivate let kStr_labTitle:[UInt8] = [0x77,0x6f,0x6c,0x6c,0x6f,0x46]

/*: "gift_voice_card" :*/
fileprivate let kStr_frameText:[Character] = ["g","i","f","t","_"]
fileprivate let kStr_rangeTaskValue:String = "vlocationc"
fileprivate let kStr_textTitle:[Character] = ["e","_","c","a","r","d"]

/*: "btn_party_voice_nor" :*/
fileprivate let kStr_tableTitle:String = "return make sendbtn_pa"
fileprivate let kStr_arrayBackgroundName:String = "none back returnoice_nor"

/*: "btn_party_voice_pre" :*/
fileprivate let kStr_makeText:[Character] = ["b","t","n","_","p","a","r","t","y","_","v","o"]
fileprivate let kStr_commonData:String = "ice_pretime left manager"

/*: "attentionUid" :*/
fileprivate let kStr_contentName:String = "astope"

/*: "source" :*/
fileprivate let kStr_blackValue:[Character] = ["s","o","u","r","c"]
fileprivate let kStr_toMoreValue:[Character] = ["e"]

/*: "Kick out" :*/
fileprivate let kStr_useValue:String = "add child as table putKick "
fileprivate let kStr_strongName:[Character] = ["o","u","t"]

/*: "Unmute" :*/
fileprivate let kStr_sizeModelName:[Character] = ["U","n","m","u","t","e"]

/*: "Mute" :*/
fileprivate let kStr_pathData:String = "for make succeed value equalMute"

/*: "Kick off" :*/
fileprivate let kStr_closeValue:[Character] = ["K","i","c","k"," ","o","f","f"]

/*: "Block" :*/
fileprivate let kStr_enableText:String = "self view class blockBlock"

/*: "Report" :*/
fileprivate let kStr_scaleValue:String = "Reportimage self pop"

/*: "#F6F6F6" :*/
fileprivate let kStr_aspectData:String = "#"
fileprivate let kStr_observerValue:[Character] = ["F","6","F","6","F","6"]

/*: "Confirm to kick out the room?" :*/
fileprivate let kStr_toName:String = "Confirsize add touch voice user"
fileprivate let kStr_managerValue:String = " kick mini return"
fileprivate let kStr_playValue:String = "he roommake tool share type"
fileprivate let kStr_contentSizeData:String = "remove"

/*: "Cancel" :*/
fileprivate let kStr_toText:String = "max"
fileprivate let kStr_labText:String = "aimageel"

/*: "Sure" :*/
fileprivate let kStr_situationData:[Character] = ["S","u","r","e"]

/*: "Are you sure you want to cancel the mute?" :*/
fileprivate let kStr_goldPassTitle:[UInt8] = [0x3f,0x65,0x74,0x75,0x6d,0x20,0x65,0x68,0x74,0x20,0x6c,0x65,0x63,0x6e,0x61,0x63,0x20,0x6f,0x74,0x20,0x74,0x6e,0x61,0x77,0x20,0x75,0x6f,0x79,0x20,0x65,0x72,0x75,0x73,0x20,0x75,0x6f,0x79,0x20,0x65,0x72,0x41]

/*: "Confirm" :*/
fileprivate let kStr_makeName:[Character] = ["C","o","n","f","i","r","m"]

/*: "The mute has been lifted~" :*/
fileprivate let kStr_equalScaleName:String = "The mustyle size format"
fileprivate let kStr_managerViewData:String = "first need trues bee"
fileprivate let kStr_accuracyData:String = "path size label half colorn li"
fileprivate let kStr_removeValue:String = "fted~right make"

/*: "Confirm to kick off the seat?" :*/
fileprivate let kStr_toData:String = "private at info menuConfir"
fileprivate let kStr_lastTitle:String = "KICK"
fileprivate let kStr_pairText:String = "he slab am"
fileprivate let kStr_dataName:String = "eat?model else model position"

/*: "OK" :*/
fileprivate let kStr_appearValue:[Character] = ["O","K"]

/*: "Shielding Success" :*/
fileprivate let kStr_beginText:[Character] = ["S","h","i","e","l","d","i","n","g"," ","S","u","c","c"]
fileprivate let kStr_hiddenData:String = "scenes"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  VideoCardView.swift
//  AbroadTalking
//

//: import UIKit
import UIKit

//: @objc protocol TalkingVoiceRoomCardViewDelegate: NSObjectProtocol {
@objc protocol TitleViewDelegate: NSObjectProtocol {
    // @
    //: func func__atUserClick(uid: String, nickname: String)
    func nickname(uid: String, nickname: String)

    //: func func__giftUserClick(uid: String)
    func keyOn(uid: String)

    //: func func__muteUserMickClick(uid: String, mikeStatus: Int, position: Int)
    func clickMagnitudeerval(uid: String, mikeStatus: Int, position: Int)

    //: func func__followUserSucess(uid: String)
    func go(uid: String)
}

//: class TalkingVoiceRoomCardView: UIView {
class VideoCardView: UIView {
    //: var popView: TalkingPopView?
    var popView: BackgroundThen?
    //: var data =  [String: Any]()
    var data = [String: Any]()
    //: var type: LiveUserCardType?
    var type: BottomLayerQuickLookable?
    //: var uid = ""
    var uid = ""
    //: var liveUserId = String(TalkingVoiceRoomManager.shared().partyModel.streamerInfo.uid)
    var liveUserId = String(SubCheckedReactiveCompatible.replyShared().partyModel.streamerInfo.uid) // 主播ID

    //: var userModel = TalkingUserInfoModel.init()
    var userModel = LoadMeasurable()

    //: open weak var delegate: TalkingVoiceRoomCardViewDelegate?
    open weak var delegate: TitleViewDelegate?

    //: init(frame: CGRect, uid: String) {
    init(frame: CGRect, uid: String) {
        //: super.init(frame: frame)
        super.init(frame: frame)

        //: if uid == LocationThen.share.loginUserMode.userID {
        if uid == LocationThen.share.loginUserMode.userID {
            //: self.type = .user_me
            self.type = .user_me
            //: } else {
        } else {
            //: self.type = .user_other
            self.type = .user_other
        }
        //: self.uid = uid
        self.uid = uid
        //: setupSubviews()
        upwardlyArea()
        //: setupSubViewsConstraint()
        written()
        //: reqUserCardInfo()
        multiplicity()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kStr_mainAtValue.map{tabApp(selected: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {}

    // 懒加载初始化

    //: lazy var contentView: UIView = {
    lazy var contentView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = UIColor.white
        view.backgroundColor = UIColor.white
        //: view.layer.cornerRadius = 8
        view.layer.cornerRadius = 8
        //: return view
        return view
        //: }()
    }()

    //: lazy var uidLb: UILabel = {
    lazy var uidLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 12)
        label.font = .groupSize(type: .Regular, fontSize: 12)
        //: label.textColor = UIColor.statusMin()
        label.textColor = UIColor.statusMin()
        //: return label
        return label
        //: }()
    }()

    //: lazy var topIcon: UIButton = {
    lazy var topIcon: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(iconBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(cuttingEdge), for: .touchUpInside)
        //: btn.layer.cornerRadius = 40
        btn.layer.cornerRadius = 40
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.layer.borderWidth = 2
        btn.layer.borderWidth = 2
        //: btn.layer.borderColor = UIColor.white.cgColor
        btn.layer.borderColor = UIColor.white.cgColor
        //: return btn
        return btn

        //: }()
    }()

    //: lazy var nameLb: UILabel = {
    lazy var nameLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Semibold, fontSize: 15)
        label.font = .groupSize(type: .Semibold, fontSize: 15)
        //: label.textColor = UIColor.appTitleColor()
        label.textColor = UIColor.dataFormat()
        //: return label
        return label
        //: }()
    }()

    //: lazy var levelIcon: UIImageView = {
    lazy var levelIcon: UIImageView = {
        //: let imag = UIImageView.init()
        let imag = UIImageView()
        //: imag.contentMode = .scaleAspectFill
        imag.contentMode = .scaleAspectFill
        //: return imag
        return imag
        //: }()
    }()

    //: lazy var signLb: UILabel = {
    lazy var signLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 13)
        label.font = .groupSize(type: .Regular, fontSize: 13)
        //: label.textColor = UIColor.statusMin()
        label.textColor = UIColor.statusMin()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.numberOfLines = 2
        label.numberOfLines = 2
        //: return label
        return label
        //: }()
    }()

    //: private lazy var followersLab: UILabel = {
    private lazy var followersLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .statusMin()
        lab.textColor = .statusMin()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Followers".localized
        lab.text = (String(kStr_mediaTitle.suffix(7)) + kStr_theReloadQuoteValue.replacingOccurrences(of: "item", with: "r")).localized
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 12)
        lab.font = UIFont.groupSize(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followersNum: UILabel = {
    private lazy var followersNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appTitleColor()
        lab.textColor = .dataFormat()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 15)
        lab.font = UIFont.groupSize(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingLab: UILabel = {
    private lazy var followingLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .statusMin()
        lab.textColor = .statusMin()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Following".localized
        lab.text = (String(kStr_areaTitle) + String(kStr_byValue)).localized
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 12)
        lab.font = UIFont.groupSize(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingNum: UILabel = {
    private lazy var followingNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appTitleColor()
        lab.textColor = .dataFormat()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 15)
        lab.font = UIFont.groupSize(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var lineView: UIView = {
    private lazy var lineView: UIView = {
        //: let view = UIView()
        let view = UIView()
        //: view.backgroundColor = UIColor.init(hex: "#E6E6E6")
        view.backgroundColor = UIColor(hex: (String(kStr_halfValue.suffix(6)) + String(kStr_shareData)))
        //: return view
        return view
        //: }()
    }()

    //: private lazy var stackView: UIStackView = {
    private lazy var stackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .fill
        v.alignment = .fill
        //: v.distribution = .fillEqually
        v.distribution = .fillEqually
        //: return v
        return v
        //: }()
    }()

    //: private lazy var moreBtn: UIButton = {
    private lazy var moreBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.setImage(UIImage.bundleBy(name: "btn_message_more"), for: .normal)
        btn.setImage(UIImage.bundleBy(name: (String(kStr_fewTitle.suffix(9)) + String(kStr_errorSucceedData.suffix(7)))), for: .normal)
        //: btn.addTarget(self, action: #selector(moreBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(upLog), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var muteView: TalkingLiveRoomMuteView = {
    private lazy var muteView: GiftMuteView = {
        //: let v = TalkingLiveRoomMuteView()
        let v = GiftMuteView()
        //: v.viewType  = .voiceRoom
        v.viewType = .voiceRoom
        //: return v
        return v
        //: }()
    }()
}

//: extension TalkingVoiceRoomCardView {
extension VideoCardView {
    //: func reqUserCardInfo() {
    func multiplicity() {
        //: TalkingLiveManager.req__userCardInfo(uid: self.uid, streamerUid: liveUserId, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
        VideoPushListener.tool(uid: self.uid, streamerUid: liveUserId, roomId: SubCheckedReactiveCompatible.replyShared().partyModel.roomId) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }

            //: if let userModel = JSONDeserializer<TalkingUserInfoModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
            if let userModel = JSONDeserializer<LoadMeasurable>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                //: self.userModel = userModel
                self.userModel = userModel
                //: self.setUserCardData()
                self.set()
            }
        }
    }

    //: func setUserCardData() {
    func set() {
        //: uidLb.text = "UID: \(self.userModel.uid)"
        uidLb.text = (String(kStr_selectedData)) + "\(self.userModel.uid)"
        //: topIcon.setUrlImage(urlStr: self.userModel.headPic)
        topIcon.displayFinish(urlStr: self.userModel.headPic)
        //: nameLb.text = self.userModel.nickname
        nameLb.text = self.userModel.nickname
        //: levelIcon.image = UIImage.bundleBy(name: "level_\(self.userModel.level)")
        levelIcon.image = UIImage.bundleBy(name: (String(kStr_appMakeToText)) + "\(self.userModel.level)")
        //: signLb.text = self.userModel.signature
        signLb.text = self.userModel.signature
        //: followersNum.text = self.userModel.attentionNum
        followersNum.text = self.userModel.attentionNum
        //: followingNum.text = self.userModel.fansNum
        followingNum.text = self.userModel.fansNum

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: setBottomView(sex: self.userModel.sex)
            landmark(sex: self.userModel.sex)
        }
    }

    //: func setBottomView(sex: String) {
    func landmark(sex _: String) {
        //: var dataSource = Array<String>()
        var dataSource = [String]()
        //: if self.userModel.isOnMike {
        if self.userModel.isOnMike {
            //: dataSource.append("Gift")
            dataSource.append(String(bytes: kStr_seatData.map{typeModel(from: $0)}, encoding: .utf8)!)
            //: if liveUserId == LocationThen.share.loginUserMode.userID && self.userModel.mikeStatus != 0  {
            if liveUserId == LocationThen.share.loginUserMode.userID, self.userModel.mikeStatus != 0 { // 当前用户是主播非自己闭麦  闭麦/开麦 按钮
                //: dataSource.append("Mike")
                dataSource.append(String(bytes: kStr_makeCellName.map{insideBack(rank: $0)}, encoding: .utf8)!)
            }
        }
        //: dataSource.append("@")
        dataSource.append("@")

        //: if self.userModel.isAttention == false {
        if self.userModel.isAttention == false {
            //: dataSource.append("Follow")
            dataSource.append(String(bytes: kStr_labTitle.reversed(), encoding: .utf8)!)
        }

        //: for i in 0 ..< dataSource.count {
        for i in 0 ..< dataSource.count {
            //: let btnStr = dataSource[i]
            let btnStr = dataSource[i]
            //: let btn = UIButton()
            let btn = UIButton()
            //: btn.accessibilityLabel = btnStr
            btn.accessibilityLabel = btnStr
            //: btn.setTitleColor(UIColor.fromBuild(), for: UIControl.State.normal)
            btn.setTitleColor(UIColor.fromBuild(), for: UIControl.State.normal)
            //: if btnStr == "Gift" {
            if btnStr == String(bytes: kStr_seatData.map{typeModel(from: $0)}, encoding: .utf8)! {
                //: btn.setImage(UIImage.bundleBy(name: "gift_voice_card"), for: .normal)
                btn.setImage(UIImage.bundleBy(name: (String(kStr_frameText) + kStr_rangeTaskValue.replacingOccurrences(of: "location", with: "oi") + String(kStr_textTitle))), for: .normal)
                //: }else if btnStr == "Mike"{
            } else if btnStr == String(bytes: kStr_makeCellName.map{insideBack(rank: $0)}, encoding: .utf8)! {
                //: btn.setImage(UIImage.bundleBy(name: "btn_party_voice_nor"), for: .normal)
                btn.setImage(UIImage.bundleBy(name: (String(kStr_tableTitle.suffix(6)) + "rty_v" + String(kStr_arrayBackgroundName.suffix(8)))), for: .normal)
                //: btn.setImage(UIImage.bundleBy(name: "btn_party_voice_pre"), for: .selected)
                btn.setImage(UIImage.bundleBy(name: (String(kStr_makeText) + String(kStr_commonData.prefix(7)))), for: .selected)
                //: if self.userModel.mikeStatus != 2 {
                if self.userModel.mikeStatus != 2 {
                    //: btn.isSelected = true
                    btn.isSelected = true
                }
                //: }else{
            } else {
                //: btn.titleLabel?.font = UIFont.dorsum(fontSize: 15)
                btn.titleLabel?.font = UIFont.dorsum(fontSize: 15)
                //: btn.setTitle(btnStr.localized, for: UIControl.State.normal)
                btn.setTitle(btnStr.localized, for: UIControl.State.normal)
            }
            //: btn.addTarget(self, action: #selector(itemTouch(sender:)), for: UIControl.Event.touchUpInside)
            btn.addTarget(self, action: #selector(sectionEqual(sender:)), for: UIControl.Event.touchUpInside)
            //: self.stackView.addArrangedSubview(btn)
            self.stackView.addArrangedSubview(btn)

            //: if i != 0 {
            if i != 0 {
                //: let lineView = UIView.init()
                let lineView = UIView()
                //: lineView.backgroundColor = UIColor.init(hex: "#E6E6E6")
                lineView.backgroundColor = UIColor(hex: (String(kStr_halfValue.suffix(6)) + String(kStr_shareData)))
                //: btn.addSubview(lineView)
                btn.addSubview(lineView)
                //: lineView.snp.makeConstraints { make in
                lineView.snp.makeConstraints { make in
                    //: make.top.equalTo(10)
                    make.top.equalTo(10)
                    //: make.leading.equalToSuperview()
                    make.leading.equalToSuperview()
                    //: make.width.equalTo(1)
                    make.width.equalTo(1)
                    //: make.height.equalTo(24)
                    make.height.equalTo(24)
                }
            }
        }
    }

    //: @objc func itemTouch(sender: UIButton) {
    @objc func sectionEqual(sender: UIButton) {
        //: let accessLabel = sender.accessibilityLabel ?? ""
        let accessLabel = sender.accessibilityLabel ?? ""
        //: switch accessLabel {
        switch accessLabel {
        //: case "Gift":
        case String(bytes: kStr_seatData.map{typeModel(from: $0)}, encoding: .utf8)!:
            //: self.delegate?.func__giftUserClick(uid: self.userModel.uid)
            self.delegate?.keyOn(uid: self.userModel.uid)
            //: self.dismiss()
            self.subWith()
        //: case "Mike":
        case String(bytes: kStr_makeCellName.map{insideBack(rank: $0)}, encoding: .utf8)!:
            //: self.delegate?.func__muteUserMickClick(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            self.delegate?.clickMagnitudeerval(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            //: self.dismiss()
            self.subWith()
        //: case "@":
        case "@":
            //: self.delegate?.func__atUserClick(uid: self.userModel.uid, nickname: self.userModel.nickname)
            self.delegate?.nickname(uid: self.userModel.uid, nickname: self.userModel.nickname)
            //: self.dismiss()
            self.subWith()
        //: case "Follow":
        case String(bytes: kStr_labTitle.reversed(), encoding: .utf8)!:
            //: freshfollowUser(sender: sender)
            header(sender: sender)
        //: default: break
        default: break
        }
    }

    //: func freshfollowUser(sender: UIButton) {
    func header(sender: UIButton) {
        //: req_followUser(uid: self.userModel.uid) { succeed in
        utilizerCompletionFollow(uid: self.userModel.uid) { succeed in
            //: if succeed {
            if succeed {
                //: self.followingNum.text = "\((Int(self.userModel.fansNum ) ?? 0) + 1)"
                self.followingNum.text = "\((Int(self.userModel.fansNum) ?? 0) + 1)"
                //: self.stackView.removeArrangedSubview(sender)
                self.stackView.removeArrangedSubview(sender)
                //: sender.isHidden = true
                sender.isHidden = true
                //: self.delegate?.func__followUserSucess(uid: self.userModel.uid)
                self.delegate?.go(uid: self.userModel.uid)
            }
        }
    }

    //: func req_followUser(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
    func utilizerCompletionFollow(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: var dict = [String: Any]()
        var dict = [String: Any]()
        //: dict["attentionUid"] = uid
        dict[(kStr_contentName.replacingOccurrences(of: "stop", with: "tt") + "ntionUid")] = uid
        //: dict["source"] = "2"
        dict[(String(kStr_blackValue) + String(kStr_toMoreValue))] = "2"
        //: UpsetCountRequestTool.req_atationTool(isAttention: true, params: dict) { succeed, result, errorModel in
        UpsetCountRequestTool.busyIn(isAttention: true, params: dict) { succeed, _, _ in
            //: completion?(succeed)
            completion?(succeed)
        }
    }

    //: @objc private func iconBtnClick() {
    @objc private func cuttingEdge() {
        //: EffectPushManager.share.func__pushToUserDetailVC(uid: self.userModel.uid)
        EffectPushManager.share.userAdd(uid: self.userModel.uid)
        //: self.dismiss()
        self.subWith()
    }
}

//: extension TalkingVoiceRoomCardView {
extension VideoCardView {
    //: @objc func moreBtnClick() {
    @objc func upLog() {
        //: var titleArray: [String] = []
        var titleArray: [String] = []
        /// 主播 & 用户在房间内
        //: if liveUserId == LocationThen.share.loginUserMode.userID && self.userModel.isInRoom {
        if liveUserId == LocationThen.share.loginUserMode.userID, self.userModel.isInRoom {
            //: titleArray.append("Kick out".localized)
            titleArray.append((String(kStr_useValue.suffix(5)) + String(kStr_strongName)).localized)
            //: if self.userModel.isPartyMute {
            if self.userModel.isPartyMute {
                //: titleArray.append("Unmute".localized)
                titleArray.append((String(kStr_sizeModelName)).localized)
                //: }else {
            } else {
                //: titleArray.append("Mute".localized)
                titleArray.append((String(kStr_pathData.suffix(4))).localized)
            }
            //: if self.userModel.isOnMike {
            if self.userModel.isOnMike {
                //: titleArray.append("Kick off".localized)
                titleArray.append((String(kStr_closeValue)).localized)
            }
            //: if !self.userModel.inMyBlackList {
            if !self.userModel.inMyBlackList {
                //: titleArray.append("Block".localized)
                titleArray.append((String(kStr_enableText.suffix(5))).localized)
            }
        }
        //: titleArray.append("Report".localized)
        titleArray.append((String(kStr_scaleValue.prefix(6))).localized)

        //: Config.MenuCellConfig.menuCellHeight = 26
        Config.AddConfig.menuCellHeight = 26
        //: let dropMenu = DropDownMenuView.pullDropDrownMenu(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        let dropMenu = ShareDataSource.signalArray(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        //: dropMenu.menuBgColor = UIColor.init(hex: "#F6F6F6")!
        dropMenu.menuBgColor = UIColor(hex: (kStr_aspectData.capitalized + String(kStr_observerValue)))!
        //: dropMenu.menuDelegate = self
        dropMenu.menuDelegate = self
        //: dropMenu.menuStyle = .MenuCustStyle
        dropMenu.menuStyle = .MenuCustStyle
    }

    //: func show() {
    func buildShow() {
        //: popView = TalkingPopView.init(frame: UIScreen.main.bounds)
        popView = BackgroundThen(frame: UIScreen.main.bounds)
        //: self.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        self.frame = CGRect(x: 0, y: 0, width: kLet_halfData, height: kLet_noSeatName)
        //: popView?.initWithView(view: self)
        popView?.toViewText(view: self)
        //: popView?.showInView(view: self.currentViewController()?.view ?? TableMacroDefine.getWindow())
        popView?.beyondAllAdjust(view: self.giftFew()?.view ?? TableMacroDefine.oeilDeBoeufView())
        //: popView?.isRemoveTapGes = true
        popView?.isRemoveTapGes = true
    }

    //: @objc func dismiss() {
    @objc func subWith() {
        //: popView?.dismissView()
        popView?.placePushAside()
        //: popView = nil
        popView = nil
    }
}

//: extension TalkingVoiceRoomCardView: DropDownMenuViewDelegate {
extension VideoCardView: AliasViewDelegate {
    //: func didClickSelectedRow(index: Int, title: String) {
    func ofTime(index _: Int, title: String) {
        //: switch title {
        switch title {
        //: case "Kick out".localized:  // 踢出房间
        case (String(kStr_useValue.suffix(5)) + String(kStr_strongName)).localized: // 踢出房间
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick out the room?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            ClickReactiveCompatible.follow(title: nil, message: (String(kStr_toName.prefix(6)) + "m to" + String(kStr_managerValue.prefix(6)) + "out t" + String(kStr_playValue.prefix(7)) + kStr_contentSizeData.replacingOccurrences(of: "remove", with: "?")).localized, leftBtnTitle: (kStr_toText.replacingOccurrences(of: "max", with: "C") + kStr_labText.replacingOccurrences(of: "image", with: "nc")).localized, rightBtnTitle: (String(kStr_situationData)).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                TintProgressHUD.weight()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomKickout(targetUid: self.userModel.uid, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
                SharedReqTool.doRecord(targetUid: self.userModel.uid, roomId: SubCheckedReactiveCompatible.replyShared().partyModel.roomId) { _, _, _ in
                    //: ProgressHUD.dismiss()
                    TintProgressHUD.inkingPadDismiss()
                    //: self.dismiss()
                    self.subWith()
                }
            }
        //: case "Unmute".localized: // 解除禁言
        case (String(kStr_sizeModelName)).localized: // 解除禁言
            ClickReactiveCompatible.follow(title: nil, message: "Are you sure you want to cancel the mute?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Confirm".localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                TintProgressHUD.weight()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomUnMute(targetUid: self.userModel.uid, roomID:TalkingVoiceRoomManager.shared().partyModel.roomId, completion: { succeed, result, errorModel in
                SharedReqTool.magnitudeercalate(targetUid: self.userModel.uid, roomID: SubCheckedReactiveCompatible.replyShared().partyModel.roomId, completion: { succeed, _, _ in
                    //: ProgressHUD.dismiss()
                    TintProgressHUD.inkingPadDismiss()
                    //: if succeed {
                    if succeed {
                        self.property(showMsg: "The mute has been lifted~".localized)
                    }
                    //: self.dismiss()
                    self.subWith()
                    //: })
                })
            }
        //: case "Mute".localized: // 设置禁言
        case (String(kStr_pathData.suffix(4))).localized: // 设置禁言
            //: muteView.showView()
            muteView.theSub()
            //: muteView.targetUid = self.userModel.uid
            muteView.targetUid = self.userModel.uid
            //: self.dismiss()
            self.subWith()

        //: case "Kick off".localized: /// 踢下麦
        case (String(kStr_closeValue)).localized: /// 踢下麦
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick off the seat?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            ClickReactiveCompatible.follow(title: nil, message: (String(kStr_toData.suffix(6)) + "m to " + kStr_lastTitle.lowercased() + " off t" + String(kStr_pairText.prefix(4)) + String(kStr_dataName.prefix(4))).localized, leftBtnTitle: (kStr_toText.replacingOccurrences(of: "max", with: "C") + kStr_labText.replacingOccurrences(of: "image", with: "nc")).localized, rightBtnTitle: (String(kStr_situationData)).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingVoiceRoomManager.shared().voiceRoom_reqAdminChangeMike(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                SubCheckedReactiveCompatible.replyShared().dealDismiss(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                //: self.dismiss()
                self.subWith()
            }

        //: case "Report".localized:
        case (String(kStr_scaleValue.prefix(6))).localized:
            //: let reportView = TalkingReportAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            let reportView = EquivalentReactiveCompatible(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            //: reportView.showReportViewIn(view: nil)
            reportView.showAppView(view: nil)
        //: case "Block".localized:
        case (String(kStr_enableText.suffix(5))).localized:
            //: TalkingAlertShow.alert(title: nil, message: kMessage_blocking, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized) {
            ClickReactiveCompatible.follow(title: nil, message: kLet_clickTitle, leftBtnTitle: (kStr_toText.replacingOccurrences(of: "max", with: "C") + kStr_labText.replacingOccurrences(of: "image", with: "nc")).localized, rightBtnTitle: "OK".localized) {
                //: TalkingAlertShow.hideAlert()
                ClickReactiveCompatible.closeHideRelease()
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingAlertShow.hideAlert()
                ClickReactiveCompatible.closeHideRelease()
                //: TalkingUserRequestManager.func__pullBlackWithUserId(uid: self.userModel.uid, isBlack: true) { succeed, result, errorModel in
                SomeoneThen.viewUser(uid: self.userModel.uid, isBlack: true) { succeed, _, _ in
                    //: if succeed == true {
                    if succeed == true {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Shielding Success".localized)
                        self.property(showMsg: (String(kStr_beginText) + kStr_hiddenData.replacingOccurrences(of: "scene", with: "es")).localized)
                        //: self.dismiss()
                        self.subWith()
                    }
                }
            }
        //: default: break
        default: break
        }
    }
}

//: extension TalkingVoiceRoomCardView {
extension VideoCardView {
    // 添加视图
    //: private func setupSubviews() {
    private func upwardlyArea() {
        //: self.addSubview(contentView)
        self.addSubview(contentView)
        //: self.addSubview(topIcon)
        self.addSubview(topIcon)
        //: contentView.addSubview(uidLb)
        contentView.addSubview(uidLb)
        //: contentView.addSubview(nameLb)
        contentView.addSubview(nameLb)
        //: contentView.addSubview(levelIcon)
        contentView.addSubview(levelIcon)
        //: contentView.addSubview(signLb)
        contentView.addSubview(signLb)
        //: contentView.addSubview(followersLab)
        contentView.addSubview(followersLab)
        //: contentView.addSubview(followersNum)
        contentView.addSubview(followersNum)
        //: contentView.addSubview(followingLab)
        contentView.addSubview(followingLab)
        //: contentView.addSubview(followingNum)
        contentView.addSubview(followingNum)

        //: contentView.layoutIfNeeded()
        contentView.layoutIfNeeded()

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: contentView.addSubview(lineView)
            contentView.addSubview(lineView)
            //: contentView.addSubview(stackView)
            contentView.addSubview(stackView)
            //: contentView.addSubview(moreBtn)
            contentView.addSubview(moreBtn)
        }
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func written() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-44)
            make.bottom.equalToSuperview().offset(-44)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: if self.type == .user_me {
            if self.type == .user_me {
                //: make.height.equalTo(192)
                make.height.equalTo(192)
                //: }else {
            } else {
                //: make.height.equalTo(233)
                make.height.equalTo(233)
            }
        }
        //: topIcon.snp.makeConstraints { make in
        topIcon.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(contentView).offset(-33)
            make.top.equalTo(contentView).offset(-33)
            //: make.width.height.equalTo(80)
            make.width.height.equalTo(80)
        }
        //: uidLb.snp.makeConstraints { make in
        uidLb.snp.makeConstraints { make in
            //: make.leading.top.equalTo(12)
            make.leading.top.equalTo(12)
            //: make.trailing.equalTo(topIcon.snp.leading).offset(-10)
            make.trailing.equalTo(topIcon.snp.leading).offset(-10)
        }
        //: nameLb.snp.makeConstraints { make in
        nameLb.snp.makeConstraints { make in
            //: make.top.equalTo(topIcon.snp.bottom).offset(12)
            make.top.equalTo(topIcon.snp.bottom).offset(12)
            //: make.centerX.equalToSuperview().offset(-12)
            make.centerX.equalToSuperview().offset(-12)
            //: make.width.lessThanOrEqualTo(ScreenWidth/2)
            make.width.lessThanOrEqualTo(kLet_halfData / 2)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: levelIcon.snp.makeConstraints { make in
        levelIcon.snp.makeConstraints { make in
            //: make.leading.equalTo(nameLb.snp.trailing).offset(5)
            make.leading.equalTo(nameLb.snp.trailing).offset(5)
            //: make.centerY.equalTo(nameLb)
            make.centerY.equalTo(nameLb)
            //: make.size.equalTo(CGSize(width: 32, height: 14))
            make.size.equalTo(CGSize(width: 32, height: 14))
        }
        //: signLb.snp.makeConstraints { make in
        signLb.snp.makeConstraints { make in
            //: make.top.equalTo(nameLb.snp.bottom).offset(12)
            make.top.equalTo(nameLb.snp.bottom).offset(12)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.height.equalTo(38)
            make.height.equalTo(38)
        }
        //: followersNum.snp.makeConstraints { make in
        followersNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(-ScreenWidth/4+10)
            make.centerX.equalTo(contentView).offset(-kLet_halfData / 4 + 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followingNum.snp.makeConstraints { make in
        followingNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(ScreenWidth/4-10)
            make.centerX.equalTo(contentView).offset(kLet_halfData / 4 - 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followersLab.snp.makeConstraints { make in
        followersLab.snp.makeConstraints { make in
            //: make.top.equalTo(followersNum.snp.bottom).offset(5)
            make.top.equalTo(followersNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followersNum)
            make.centerX.equalTo(followersNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }
        //: followingLab.snp.makeConstraints { make in
        followingLab.snp.makeConstraints { make in
            //: make.top.equalTo(followingNum.snp.bottom).offset(5)
            make.top.equalTo(followingNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followingNum)
            make.centerX.equalTo(followingNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: lineView.snp.makeConstraints { make in
            lineView.snp.makeConstraints { make in
                //: make.top.equalTo(followersLab.snp.bottom).offset(12)
                make.top.equalTo(followersLab.snp.bottom).offset(12)
                //: make.leading.equalTo(7)
                make.leading.equalTo(7)
                //: make.trailing.equalTo(-7)
                make.trailing.equalTo(-7)
                //: make.height.equalTo(1)
                make.height.equalTo(1)
            }
            //: stackView.snp.makeConstraints { make in
            stackView.snp.makeConstraints { make in
                //: make.top.equalTo(lineView)
                make.top.equalTo(lineView)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(44)
                make.height.equalTo(44)
            }
            //: moreBtn.snp.makeConstraints { make in
            moreBtn.snp.makeConstraints { make in
                //: make.trailing.equalToSuperview()
                make.trailing.equalToSuperview()
                //: make.top.equalTo(12)
                make.top.equalTo(12)
                //: make.size.equalTo(CGSize(width: 44, height: 20))
                make.size.equalTo(CGSize(width: 44, height: 20))
            }
        }
    }

    //: override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
    override func touchesBegan(_ touches: Set<UITouch>, with _: UIEvent?) {
        //: let touch = touches.first!
        let touch = touches.first!
        //: var point = touch.location(in: self.currentViewController()?.view)
        var point = touch.location(in: self.giftFew()?.view)
        //: point = contentView.layer.convert(point, from: self.currentViewController()?.view.layer)
        point = contentView.layer.convert(point, from: self.giftFew()?.view.layer)
        //: if !contentView.layer.contains(point) {
        if !contentView.layer.contains(point) {
            //: self.dismiss()
            self.subWith()
        }
    }
}
