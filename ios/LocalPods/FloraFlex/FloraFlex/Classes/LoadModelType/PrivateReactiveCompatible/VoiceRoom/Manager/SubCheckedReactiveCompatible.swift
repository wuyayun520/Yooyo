
//: Declare String Begin

/*: "PARTY_STATUS_CHANGE_NOTIFICATION" :*/
fileprivate let kStr_tableValue:[UInt8] = [0x31,0x20,0x33,0x35,0x38,0x3e,0x32,0x35,0x20,0x35,0x34,0x32,0x3e,0x22,0x29,0x20,0x2f,0x26,0x24,0x3e,0x2f,0x2e,0x35,0x28,0x27,0x28,0x22,0x20,0x35,0x28,0x2e,0x2f]

private func labText(share num: UInt8) -> UInt8 {
    return num ^ 97
}

/*: "p- :*/
fileprivate let kStr_inputName:String = "p-not equal"

/*: - :*/
fileprivate let kStr_onData:String = "play"

/*: "redirectUrl" :*/
fileprivate let kStr_centerContent:String = "REDIREC"
fileprivate let kStr_dataAddContent:[Character] = ["t","U","r","l"]

/*: "VOICEROOM: 进房模型解析失败。" :*/
fileprivate let kStr_receiveValue:[Character] = ["V","O","I","C","E","R","O","O","M",":"," ","\u{8fdb}"]
fileprivate let kStr_toData:String = "房模型\u{89e3}"

/*: "Party room exception" :*/
fileprivate let kStr_firstName:[Character] = ["P","a","r","t","y"," "]
fileprivate let kStr_norData:String = "partyoom"
fileprivate let kStr_managerData:[Character] = ["e","p","t","i","o","n"]

/*: _ :*/
fileprivate let kStr_nameData:[Character] = ["_"]

/*: "male" :*/
fileprivate let kStr_ageBottomTitle:[UInt8] = [0xb2,0xbe,0xb3,0xba]

private func modelTransition(count num: UInt8) -> UInt8 {
    return num ^ 223
}

/*: "female" :*/
fileprivate let kStr_nameInfoText:[Character] = ["f","e","m","a","l","e"]

/*: "VOICEROOM: 群聊进房失败。groupId:  :*/
fileprivate let kStr_fileName:String = "VOICdata"
fileprivate let kStr_sendEqualData:String = " name进"
fileprivate let kStr_giftContent:String = "groupId:read let push text"
fileprivate let kStr_nowText:[Character] = [" "]

/*: ." :*/
fileprivate let kStr_layerName:[Character] = ["."]

/*: "VOICEROOM: 切房模型解析失败。" :*/
fileprivate let kStr_withTitle:String = "VOIintervalE"
fileprivate let kStr_styleName:String = ": 切房"

/*: "VOICEROOM: 群聊切房失败。groupId:  :*/
fileprivate let kStr_labelValue:String = "voice"
fileprivate let kStr_atTitle:String = ": 群follow"
fileprivate let kStr_blockText:String = "。groucontent icon self"
fileprivate let kStr_socialText:String = "view"

/*: "errCode" :*/
fileprivate let kStr_pairValue:[UInt8] = [0xc,0x1b,0x1b,0x2a,0x6,0xd,0xc]

private func rowAvailable(item num: UInt8) -> UInt8 {
    return num ^ 105
}

/*: "custom_kickout_message" :*/
fileprivate let kStr_infoText:[Character] = ["c","u","s","t","o","m","_","k","i","c","k","o","u","t"]
fileprivate let kStr_originalTitle:String = "image lab name_m"
fileprivate let kStr_errorData:String = "emodelmodelage"

/*: "closeRoom" :*/
fileprivate let kStr_iconName:[Character] = ["c","l","o","s","e","R"]
fileprivate let kStr_sizeValue:String = "voicevoicem"

/*: "adminCloseRoom" :*/
fileprivate let kStr_lockValue:[Character] = ["a","d","m","i","n","C","l","o"]
fileprivate let kStr_nowTitle:String = "seRoomfalse view let extension"

/*: "The room is closed, please go to another room" :*/
fileprivate let kStr_indexName:[UInt8] = [0x48,0x74,0x79,0x3c,0x6e,0x73,0x73,0x71,0x3c,0x75,0x6f,0x3c,0x7f,0x70,0x73,0x6f,0x79,0x78,0x30,0x3c,0x6c,0x70,0x79,0x7d,0x6f,0x79,0x3c,0x7b,0x73,0x3c,0x68,0x73,0x3c,0x7d,0x72,0x73,0x68,0x74,0x79,0x6e,0x3c,0x6e,0x73,0x73,0x71]

private func hiddenListStatus(of num: UInt8) -> UInt8 {
    return num ^ 28
}

/*: "OK" :*/
fileprivate let kStr_theoryText:String = "image"

/*: "You were kicked out of the room" :*/
fileprivate let kStr_viewTitle:[UInt8] = [0x77,0x41,0x5b,0xe,0x59,0x4b,0x5c,0x4b,0xe,0x45,0x47,0x4d,0x45,0x4b,0x4a,0xe,0x41,0x5b,0x5a,0xe,0x41,0x48,0xe,0x5a,0x46,0x4b,0xe,0x5c,0x41,0x41,0x43]

private func cookieValue(label num: UInt8) -> UInt8 {
    return num ^ 46
}

/*: "VOICEROOM：进房失败，roomID:  :*/
fileprivate let kStr_statusValue:String = "VOICEROOremove"
fileprivate let kStr_textHiddenMiniTitle:[Character] = ["：","进","房","失","败","\u{ff0c}","r","o","o","m","I","D",":"," "]

/*: , error:  :*/
fileprivate let kStr_serviceValue:String = "head view let, err"

/*: , reason:  :*/
fileprivate let kStr_itemTotalName:[Character] = [","," ","r","e"]
fileprivate let kStr_playerValue:String = "center let end weak addason: "

/*: , extendedData:  :*/
fileprivate let kStr_intervalValue:String = ", extereport constraint image"
fileprivate let kStr_nativeText:[Character] = ["a","t","a",":"," "]

/*: "VOICEROOM: 当前用户推流失败，roomID:  :*/
fileprivate let kStr_dataManagerText:[Character] = ["V","O","I","C","E","R","O"]
fileprivate let kStr_errorTitle:String = "info"
fileprivate let kStr_valueData:[Character] = ["M",":"," ","当","\u{524d}","用","户","\u{63a8}","\u{6d41}","失","败","，","r","o","o","m","I","D",":"," "]

/*: , errorCode:  :*/
fileprivate let kStr_labelTitle:[Character] = [","," ","e","r","r","o","r","C","o","d","e",":"]
fileprivate let kStr_cornerValue:String = " "

/*: , streamID:  :*/
fileprivate let kStr_sizeName:String = ", stredata temp"
fileprivate let kStr_showTitle:String = "pop"

/*: "An error occurred, please try again" :*/
fileprivate let kStr_containerName:[UInt8] = [0xb1,0xde,0x90,0xd5,0xe2,0xe2,0xdf,0xe2,0x90,0xdf,0xd3,0xd3,0xe5,0xe2,0xe2,0xd5,0xd4,0x9c,0x90,0xe0,0xdc,0xd5,0xd1,0xe3,0xd5,0x90,0xe4,0xe2,0xe9,0x90,0xd1,0xd7,0xd1,0xd9,0xde]

fileprivate func colorFalse(style num: UInt8) -> UInt8 {
    let value = Int(num) - 112
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "VOICEROOM: 拉流失败，roomID:  :*/
fileprivate let kStr_arrayText:String = "VOinputCEROO"
fileprivate let kStr_modelTitle:String = "view type letM: "
fileprivate let kStr_weightData:String = "拉\u{6d41}失\u{8d25}，"

/*: "VOICEROOM: 信令解析失败，command:  :*/
fileprivate let kStr_nameShareTitle:[Character] = ["V","O","I","C","E","R","O","O","M",":"," ","信","令","解","析","\u{5931}","败","，","c","o","m"]
fileprivate let kStr_appValue:[Character] = ["m","a","n","d",":"," "]

/*: , roomID:  :*/
fileprivate let kStr_tapValue:String = ", roself sub to make message"
fileprivate let kStr_dateTitle:String = "omID: self return female view"

/*: "data" :*/
fileprivate let kStr_blackTitle:String = "dstatuststatus"

/*: "position" :*/
fileprivate let kStr_nowName:String = "POSI"

/*: "xxxx--  :*/
fileprivate let kStr_clearValue:String = "to"
fileprivate let kStr_managerEqualName:String = "xx-- voice for text model"

/*: "opType" :*/
fileprivate let kStr_minPartyData:String = "opTypeview data view any"

/*: "party:changeMike" :*/
fileprivate let kStr_tableName:String = "load viewparty:"
fileprivate let kStr_timeText:String = "transform lab service modeleMike"

/*: "type" :*/
fileprivate let kStr_dataValue:String = "tylanguagee"

/*: "toPosition" :*/
fileprivate let kStr_clickName:[Character] = ["t","o","P","o","s","i","t"]
fileprivate let kStr_resultSectionData:String = "ititle"

/*: "party:adminChangeMike" :*/
fileprivate let kStr_cellValue:String = "piconty"
fileprivate let kStr_succeedData:String = "nChangcamera view second var"

/*: "You were kicked off the seat" :*/
fileprivate let kStr_itemBeginData:String = "name view share return showYou we"
fileprivate let kStr_scaleName:[Character] = ["i","c","k","e","d"]
fileprivate let kStr_messageEditText:String = " off array type color back tool"

/*: "party:changeCoin" :*/
fileprivate let kStr_firstWillValue:String = "else varparty:"
fileprivate let kStr_requestTitle:String = "kind remove view equal partygeCoi"
fileprivate let kStr_imageInfoName:[Character] = ["n"]

/*: "party:closeStream" :*/
fileprivate let kStr_hiddenData:String = "user gift bottom comment letparty:cl"
fileprivate let kStr_cancelTitle:[Character] = ["o","s"]
fileprivate let kStr_buttonCellTitle:String = "eStreamstyle distance model to medium"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SubCheckedReactiveCompatible.swift
//  Adjust
//
//  Created by DouXiu on 2024/9/12.
//

//: import ZegoExpressEngine
import ZegoExpressEngine

/// 语聊房状态变更通知
//: public let PARTY_STATUS_CHANGE_NOTIFICATION = NSNotification.Name(rawValue: "PARTY_STATUS_CHANGE_NOTIFICATION")
public let kLet_screenTitle = NSNotification.Name(rawValue: String(bytes: kStr_tableValue.map{labText(share: $0)}, encoding: .utf8)!)
/// 麦位数
//: public let MIKE_SEAT_COUNT = 8
public let kLet_messageData = 8

/// 语聊房角色
//: enum VoiceRoomRole: Int {
enum AccumulationExpressionConvertible: Int {
    //: case host = 1
    case host = 1 // 房主
    //: case audience = 2
    case audience = 2 // 观众
    //: case manager = 4
    case manager = 4 // 管理员
}

//: class TalkingVoiceRoomManager: NSObject {
class SubCheckedReactiveCompatible: NSObject {
    /// 语聊房模型
    //: private(set) var partyModel = TalkingVoiceRoomModel()
    private(set) var partyModel = ToModel()
    /// 是否正在语聊房中（状态变更时发送通知）
    //: private(set) var isParty: Bool = false {
    private(set) var isParty: Bool = false {
        //: didSet {
        didSet {
            //: NotificationCenter.default.post(name: PARTY_STATUS_CHANGE_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: kLet_screenTitle, object: self)
        }
    }

    /// 是否小窗口模式
    //: private(set) var isSmallMode: Bool = false
    private(set) var isSmallMode: Bool = false
    /// singleton
    //: private static var _instance: TalkingVoiceRoomManager?
    private static var _instance: SubCheckedReactiveCompatible?
    // 语聊房控制器
    //: private var voiceRoomVC: TalkingVoiceRoomViewController?
    private var voiceRoomVC: BottomViewObserverDelegate?
    /// 麦位信息字典 [麦位 0~7：麦位模型]
    //: private var mikePositionDict = [Int: TalkingMikeListItemModel]()
    private var mikePositionDict = [Int: SharedModelMeasurable]()
    //: private let mplock = NSLock()
    private let mplock = NSLock()
    /// 正在进房请求中（防止多次调用）
    //: private var isEnterRoomReq = false
    private var isEnterRoomReq = false
    /// 麦克风操作请求中（防止多次调用）
    //: private var isMicReq = false
    private var isMicReq = false

    //: override init() {
    override init() {
        //: super.init()
        super.init()
        //: self.mikePositionDict = initMikePosition()
        self.mikePositionDict = contentFinish()
    }

    //: class func shared() -> TalkingVoiceRoomManager {
    class func replyShared() -> SubCheckedReactiveCompatible {
        //: guard _instance != nil else {
        guard _instance != nil else {
            //: _instance = TalkingVoiceRoomManager()
            _instance = SubCheckedReactiveCompatible()
            //: return _instance!
            return _instance!
        }
        //: return _instance!
        return _instance!
    }

    //: class func destroy() {
    class func admin() {
        //: if TalkingVoiceRoomManager._instance != nil {
        if SubCheckedReactiveCompatible._instance != nil {
            //: TalkingVoiceRoomManager._instance = nil
            SubCheckedReactiveCompatible._instance = nil
        }
    }

    // MARK: - Lazy Load

    // 语聊房小窗口悬浮窗
    //: private lazy var miniView: TalkingVoiceRoomMiniView = {
    private lazy var miniView: RotundaMiniView = {
        //: let mini = TalkingVoiceRoomMiniView.buildLiveMiniView()
        let mini = RotundaMiniView.displayWindowNeed()
        //: mini.isHidden = true
        mini.isHidden = true
        //: mini.tapGestureBlock = { [weak self] in
        mini.tapGestureBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.voiceRoom_goback()
            self.reaction()
        }
        //: return mini
        return mini
        //: }()
    }()
}

// MARK: - 操作麦位请求

//: extension TalkingVoiceRoomManager {
extension SubCheckedReactiveCompatible {
    /// 房主/观众 麦位操作请求
    /// - Parameters:
    ///   - type: 1 上麦，2 下麦，3 切麦，4 开麦，5 闭麦
    ///   - position: 麦位
    ///   - toPosition: 目标麦位（切麦传值）
    //: func voiceRoom_reqChangeMike(type: Int, position: Int, toPosition: Int? = nil) {
    func analyze(type: Int, position: Int, toPosition: Int? = nil) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_changeMike(roomId: partyModel.roomId,
        SharedReqTool.subscribe(roomId: partyModel.roomId,
                                          //: uid: Int(LocationThen.share.loginUid) ?? 0,
                                          uid: Int(LocationThen.share.loginUid) ?? 0,
                                          //: type: type,
                                          type: type,
                                          //: position: position,
                                          position: position,
                                          //: toPos: toPosition)
                                          toPos: toPosition)
        //: { succeed, result, errorModel in
        { succeed, result, errorModel in
            //: self.isMicReq = false
            self.isMicReq = false
            //: guard succeed else {
            guard succeed else { // 失败提示
                //: if errorModel?.errorCode == 20001 {
                if errorModel?.errorCode == 20001 { // 已在麦位，开始推流，刷新UI
                    //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    //: let streamID = self.voiceRoom_getStreamID()
                    let streamID = self.full()
                    //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                    ZegoExpressEngine.shared().startPublishingStream(streamID)
                    //: guard let dict = result as? [String: Any] else { return }
                    guard let dict = result as? [String: Any] else { return }
                    //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: dict) else { return }
                    guard let mikeModel = SharedModelMeasurable.deserialize(from: dict) else { return }
                    //: self.updateMikePosition(position: position, model: mikeModel)
                    self.eventMethod(position: position, model: mikeModel)
                }
                //: return
                return
            }

            // 成功后只做逻辑处理，在接收信令处统一刷新UI
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                //: let streamID = self.voiceRoom_getStreamID()
                let streamID = self.full()
                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                ZegoExpressEngine.shared().startPublishingStream(streamID)

            //: case 2:
            case 2: // 下麦成功
                //: ZegoExpressEngine.shared().stopPublishingStream()
                ZegoExpressEngine.shared().stopPublishingStream()

            //: case 3:
            case 3: // 切麦成功
                //: break
                break

            //: case 4:
            case 4: // 开麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }
        }
    }

    /// 房主对他人操作请求
    /// - Parameters:
    ///   - uid: 用户Id
    ///   - type: 1 锁麦，2 解锁麦，3 踢下麦，4 开麦，5 闭麦
    ///   - position: 麦位
    //: func voiceRoom_reqAdminChangeMike(uid: Int, type: Int, position: Int) {
    func dealDismiss(uid: Int, type: Int, position: Int) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_adminChangeMike(roomId: partyModel.roomId,
        SharedReqTool.nativityWindow(roomId: partyModel.roomId,
                                               //: uid: uid,
                                               uid: uid,
                                               //: type: type,
                                               type: type,
                                               //: position: position)
                                               position: position)
        //: { _, _, _ in
        { _, _, _ in
            //: self.isMicReq = false
            self.isMicReq = false
        }
    }

    /// 获取推流Id
    //: private func voiceRoom_getStreamID() -> String {
    private func full() -> String {
        //: return "p-\(LocationThen.share.loginUid)-\(partyModel.roomId)"
        return "p-\(LocationThen.share.loginUid)-\(partyModel.roomId)"
    }
}

// MARK: - 权限检测，开启/进入/切换 语聊房

//: extension TalkingVoiceRoomManager {
extension SubCheckedReactiveCompatible {
    /// 检测状态并进入语聊房
    /// - Parameters:
    ///   - roomId: 房间Id（nil：创建房间；有值：进入房间）
    //: func voiceRoom_checkAndTurnOn(roomId: String? = nil) {
    func viewId(roomId: String? = nil) {
        //: guard isParty == false else { return }
        guard isParty == false else { return }
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true

        // 1. 权限检测
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        PerspectiveThen.schlep(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.detailDownArray(showMsg: kLet_viewValue)
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: return
                return
            }

            // 2. 接口是否禁播检测
            //: TalkingVoiceRoomReqTool.req_voiceRoomCheck(roomId: roomId) { succeed, result, errorModel in
            SharedReqTool.requestTarget(roomId: roomId) { succeed, result, errorModel in
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: guard succeed else {
                guard succeed else {
                    //: if errorModel?.errorCode == 302 {
                    if errorModel?.errorCode == 302 { // 失败，跳转H5
                        //: if let result = result as? [String: Any] {
                        if let result = result as? [String: Any] {
                            //: EffectPushManager.share.func__pushToWebVC(urlStr: result["redirectUrl"] as? String)
                            EffectPushManager.share.deleteTake(urlStr: result[(kStr_centerContent.lowercased() + String(kStr_dataAddContent))] as? String)
                        }
                        //: } else if errorModel?.errorMsg.count ?? 0 > 0 {
                    } else if errorModel?.errorMsg.count ?? 0 > 0 {
                        //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg)
                        self.detailDownArray(showMsg: errorModel!.errorMsg)
                    }
                    //: return
                    return
                }

                // 3. 是否正在音视频通话中
                //: guard TalkingSocketManager.shared.isTalking == false else {
                guard ToSocketManager.shared.isTalking == false else {
                    //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
                    self.detailDownArray(showMsg: kLet_tableEndValue)
                    //: return
                    return
                }

                // 4. 校验成功，解析模型
                //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
                guard let model = ToModel.deserialize(from: result as? Dictionary) else {
                    //: MiniLoadThen.appLine(msg: "VOICEROOM: 进房模型解析失败。")
                    MiniLoadThen.appLine(msg: (String(kStr_receiveValue) + kStr_toData + "析失败。"))
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.detailDownArray(showMsg: (String(kStr_firstName) + kStr_norData.replacingOccurrences(of: "party", with: "r") + " exc" + String(kStr_managerData)).localized)
                    //: return
                    return
                }
                //: self.partyModel = model
                self.partyModel = model

                // 5. 创建语聊房UI，同时调用sdk创建房间
                //: self.voiceRoomVC = TalkingVoiceRoomViewController()
                self.voiceRoomVC = BottomViewObserverDelegate()
                //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
                self.showBorder()?.pushViewController(self.voiceRoomVC!, animated: true)
                //: self.miniView.refreshMiniView()
                self.miniView.perspective()
                //: self.miniView.isHidden = true
                self.miniView.isHidden = true
                //: self.voiceRoom_create()
                self.designateCreate()
                //: UIApplication.shared.isIdleTimerDisabled = true
                UIApplication.shared.isIdleTimerDisabled = true // 防止息屏
                //: self.isParty = true
                self.isParty = true

                // 6. 埋点
                //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(LocationThen.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
                kLet_dismissValue.digitizerInsideStatus(eventID: "\(kLet_buttonBottomTitle)_\(LocationThen.share.loginUserMode.sex == SucceedComparable.male.rawValue ? String(bytes: kStr_ageBottomTitle.map{modelTransition(count: $0)}, encoding: .utf8)! : (String(kStr_nameInfoText)))")

                // 7. 初始化弹幕群聊
                //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                FlipDanmuManager.lay(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                    //: if succeed == false {
                    if succeed == false {
                        //: MiniLoadThen.appLine(msg: "VOICEROOM: 群聊进房失败。groupId: \(self.partyModel.chatGroupId).")
                        MiniLoadThen.appLine(msg: (kStr_fileName.replacingOccurrences(of: "data", with: "ER") + "OOM:" + kStr_sendEqualData.replacingOccurrences(of: "name", with: "群聊") + "房失败。" + String(kStr_giftContent.prefix(8)) + String(kStr_nowText)) + "\(self.partyModel.chatGroupId).")
                        //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                        self.detailDownArray(showMsg: (String(kStr_firstName) + kStr_norData.replacingOccurrences(of: "party", with: "r") + " exc" + String(kStr_managerData)).localized)
                        //: self.voiceRoom_releaseAllResource()
                        self.viewBy()
                    }
                }
            }
        }
    }

    /// 切换语聊房
    /// - Parameters:
    ///   - roomId: 当前进入房间Id
    ///   - beforeRoomId: 上一房间Id
    //: func voiceRoom_switch(roomId: String, beforeRoomId: String) {
    func withdraw(roomId: String, beforeRoomId: String) {
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true
        // 1. 调用切房接口
        //: TalkingVoiceRoomReqTool.req_voiceRoomSwitch(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
        SharedReqTool.viewStatus(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
            //: self.isEnterRoomReq = false
            self.isEnterRoomReq = false
            //: guard succeed else { return }
            guard succeed else { return }
            // 2. 释放上个房间资源 (使用partyModel旧值)
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.letOutOpenDeform()

            // 3. 主播切房，需要关闭底部tabbar动效 (使用partyModel旧值)
            //: if String(self.partyModel.streamerInfo.uid) == LocationThen.share.loginUid {
            if String(self.partyModel.streamerInfo.uid) == LocationThen.share.loginUid {
                //: self.isParty = false
                self.isParty = false
            }

            // 4. 校验成功，解析模型（给partyModel重新赋值）
            //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
            guard let model = ToModel.deserialize(from: result as? Dictionary) else {
                //: MiniLoadThen.appLine(msg: "VOICEROOM: 切房模型解析失败。")
                MiniLoadThen.appLine(msg: (kStr_withTitle.replacingOccurrences(of: "interval", with: "C") + "ROOM" + kStr_styleName.capitalized + "模型解析失败。"))
                //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                self.detailDownArray(showMsg: (String(kStr_firstName) + kStr_norData.replacingOccurrences(of: "party", with: "r") + " exc" + String(kStr_managerData)).localized)
                //: return
                return
            }
            //: self.partyModel = model
            self.partyModel = model

            // 5. 创建语聊房UI，同时调用sdk切房
            //: self.voiceRoomVC = TalkingVoiceRoomViewController()
            self.voiceRoomVC = BottomViewObserverDelegate()
            //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
            self.showBorder()?.pushViewController(self.voiceRoomVC!, animated: true)
            //: self.miniView.refreshMiniView()
            self.miniView.perspective()
            //: self.miniView.isHidden = true
            self.miniView.isHidden = true
            //: self.voiceRoom_switch(beforeRoomId: beforeRoomId)
            self.pastKind(beforeRoomId: beforeRoomId)
            //: self.isParty = true
            self.isParty = true
            //: self.isSmallMode = false
            self.isSmallMode = false

            // 6. 埋点
            //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(LocationThen.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
            kLet_dismissValue.digitizerInsideStatus(eventID: "\(kLet_buttonBottomTitle)_\(LocationThen.share.loginUserMode.sex == SucceedComparable.male.rawValue ? String(bytes: kStr_ageBottomTitle.map{modelTransition(count: $0)}, encoding: .utf8)! : (String(kStr_nameInfoText)))")

            // 7. 初始化弹幕群聊
            //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
            FlipDanmuManager.lay(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                //: if succeed == false {
                if succeed == false {
                    //: MiniLoadThen.appLine(msg: "VOICEROOM: 群聊切房失败。groupId: \(self.partyModel.chatGroupId).")
                    MiniLoadThen.appLine(msg: (kStr_labelValue.uppercased() + "ROOM" + kStr_atTitle.replacingOccurrences(of: "follow", with: "聊") + "切\u{623f}失败" + String(kStr_blockText.prefix(5)) + "pId:" + kStr_socialText.replacingOccurrences(of: "view", with: " ")) + "\(self.partyModel.chatGroupId).")
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.detailDownArray(showMsg: (String(kStr_firstName) + kStr_norData.replacingOccurrences(of: "party", with: "r") + " exc" + String(kStr_managerData)).localized)
                    //: self.voiceRoom_releaseAllResource()
                    self.viewBy()
                }
            }
        }
    }
}

// MARK: - Public Event

//: extension TalkingVoiceRoomManager {
extension SubCheckedReactiveCompatible {
    /// 最小化语聊房
    //: func voiceRoom_mini() {
    func capabilityName() {
        //: isSmallMode = true
        isSmallMode = true
        //: miniView.isHidden = false
        miniView.isHidden = false
        //: voiceRoomVC?.popCurrentViewController()
        voiceRoomVC?.phaseOfTheMoonObject()
    }

    /// 返回语聊房
    //: func voiceRoom_goback() {
    func reaction() {
        //: isSmallMode = false
        isSmallMode = false
        //: miniView.isHidden = true
        miniView.isHidden = true
        //: guard voiceRoomVC != nil else { return }
        guard voiceRoomVC != nil else { return }
        //: guard let currentVC = currentViewController() else { return }
        guard let currentVC = giftFew() else { return }
        // 如果顶部视图是私信页，先返回列表，避免多个私信页消息同步问题
        //: if currentVC.isKind(of: TalkingPrivateChatController.self) {
        if currentVC.isKind(of: SucceedTitleControllerDelegate.self) {
            //: if var vcArr = currentVC.navigationController?.viewControllers {
            if var vcArr = currentVC.navigationController?.viewControllers {
                //: vcArr.removeLast()
                vcArr.removeLast()
                //: vcArr.append(voiceRoomVC!)
                vcArr.append(voiceRoomVC!)
                //: currentVC.navigationController?.setViewControllers(vcArr, animated: true)
                currentVC.navigationController?.setViewControllers(vcArr, animated: true)
            }
            //: } else {
        } else {
            //: currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
            currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
        }
    }

    /// 销毁语聊房所有资源
    //: func voiceRoom_releaseAllResource() {
    func viewBy() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.isParty = false
            self.isParty = false // 有通知监听，需置为false
            //: UIApplication.shared.isIdleTimerDisabled = false
            UIApplication.shared.isIdleTimerDisabled = false // 恢复息屏
            //: self.miniView.removeFromSuperview()
            self.miniView.removeFromSuperview() // 移除小窗口
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.letOutOpenDeform() // 将语聊房视图从栈中移除并释放资源
            //: if self.partyModel.position >= 0 {
            if self.partyModel.position >= 0 { // 退房下麦
                //: self.voiceRoom_reqChangeMike(type: 2, position: self.partyModel.position)
                self.analyze(type: 2, position: self.partyModel.position)
            }
            //: ZegoExpressEngine.shared().logoutRoom()
            ZegoExpressEngine.shared().logoutRoom() // 退出房间
            //: ZegoExpressEngine.destroy(nil)
            ZegoExpressEngine.destroy(nil) // 销毁引擎
            //: TalkingVoiceRoomManager.destroy()
            SubCheckedReactiveCompatible.admin() // 销毁当前单例
        }
    }
}

// MARK: - Private Event

//: extension TalkingVoiceRoomManager {
extension SubCheckedReactiveCompatible {
    /// 创建语聊房
    //: private func voiceRoom_create() {
    private func designateCreate() {
        // 创建引擎
        //: let profile = ZegoEngineProfile()
        let profile = ZegoEngineProfile()
        //: profile.appID = TableMacroDefine.getVoiceRoomAppId()
        profile.appID = TableMacroDefine.isolate()
        //: profile.scenario = .standardChatroom
        profile.scenario = .standardChatroom
        //: ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        // 进阶配置，进入房间
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: let user = ZegoUser(userID: LocationThen.share.loginUid)
        let user = ZegoUser(userID: LocationThen.share.loginUid)
        //: ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
        ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
    }

    /// 切换语聊房
    /// - Parameter beforeRoomId: 上一房间Id
    //: private func voiceRoom_switch(beforeRoomId: String) {
    private func pastKind(beforeRoomId: String) {
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
        ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
    }
}

// MARK: - ZegoEventHandler

//: extension TalkingVoiceRoomManager: ZegoEventHandler {
extension SubCheckedReactiveCompatible: ZegoEventHandler {
    // MARK: - 进房/切房 监听回调

    /// 进房/切房 回调
    //: func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
    func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: if errorCode == 0, reason == .logined {
        if errorCode == 0, reason == .logined { // 进房成功
            // 启动声浪等级监控
            //: ZegoExpressEngine.shared().startSoundLevelMonitor()
            ZegoExpressEngine.shared().startSoundLevelMonitor()
            // 开启音量稳定控制
            //: ZegoExpressEngine.shared().enableAEC(true)
            ZegoExpressEngine.shared().enableAEC(true)
            // 获取嘉宾列表接口
            //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
            SharedReqTool.writeOff(roomId: partyModel.roomId) { list in
                // 保存麦位列表信息
                //: for (key, value) in list.enumerated() {
                for (key, value) in list.enumerated() {
                    //: self.modifyMikePosition(key: key, model: value)
                    self.area(key: key, model: value)
                }
                // 刷新麦位列表UI
                //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                self.voiceRoomVC?.mikePositionView.reloadUponViewPush()
                // 房主首次创建房间，主动上麦
                //: if self.partyModel.userRole == VoiceRoomRole.host.rawValue, self.partyModel.isCreate == true {
                if self.partyModel.userRole == AccumulationExpressionConvertible.host.rawValue, self.partyModel.isCreate == true {
                    //: self.voiceRoom_reqChangeMike(type: 1, position: 0)
                    self.analyze(type: 1, position: 0)
                }
            }

            //: } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
        } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
            //: if errorCode == 0, reason == .reconnected {
            if errorCode == 0, reason == .reconnected { // 重连成功
                //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
                SharedReqTool.writeOff(roomId: partyModel.roomId) { list in
                    // 更新麦位列表信息
                    //: for (key, value) in list.enumerated() {
                    for (key, value) in list.enumerated() {
                        //: self.modifyMikePosition(key: key, model: value)
                        self.area(key: key, model: value)
                        //: if String(value.uid) == LocationThen.share.loginUid {
                        if String(value.uid) == LocationThen.share.loginUid {
                            //: if value.mikeStatus == 1 {
                            if value.mikeStatus == 1 { // 房主闭麦
                                //: ZegoExpressEngine.shared().stopPublishingStream()
                                ZegoExpressEngine.shared().stopPublishingStream()
                                //: } else {
                            } else {
                                //: let streamID = self.voiceRoom_getStreamID()
                                let streamID = self.full()
                                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                                ZegoExpressEngine.shared().startPublishingStream(streamID)
                                //: let isMute = value.mikeStatus == 0 ? true : false
                                let isMute = value.mikeStatus == 0 ? true : false
                                //: ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                                ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                            }
                        }
                    }
                    // 刷新麦位列表UI
                    //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                    self.voiceRoomVC?.mikePositionView.reloadUponViewPush()
                }
                //: } else if errorCode != 0 {
            } else if errorCode != 0 {
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                kLet_dismissValue.showTheme(eventID: kLet_timeData, parameterStr: [String(bytes: kStr_pairValue.map{rowAvailable(item: $0)}, encoding: .utf8)!: String(errorCode)])
            }

            //: } else {
        } else { // 进房失败
            //: if errorCode == 1002055 || errorCode == 1002050 {
            if errorCode == 1_002_055 || errorCode == 1_002_050 { // 用户退出 || 被踢出房间
                //: let json = JSON(extendedData)
                let json = JSON(extendedData)
                //: let value = json["custom_kickout_message"].stringValue
                let value = json[(String(kStr_infoText) + String(kStr_originalTitle.suffix(2)) + kStr_errorData.replacingOccurrences(of: "model", with: "s"))].stringValue
                //: if value == "closeRoom" || value == "adminCloseRoom" { // 关闭房间
                if value == (String(kStr_iconName) + kStr_sizeValue.replacingOccurrences(of: "voice", with: "o")) || value == (String(kStr_lockValue) + String(kStr_nowTitle.prefix(6))) { // 关闭房间
                    //: voiceRoom_releaseAllResource()
                    viewBy()
                    //: let config = ShowAlertConfig()
                    let config = StreetwiseAlertConfig()
                    //: config.alignment = .center
                    config.alignment = .center
                    //: TalkingAlertShow.customAlert(message: "The room is closed, please go to another room".localized, rightBtnTitle: "OK".localized, rightBlock: {
                    ClickReactiveCompatible.anyAppearConfig(message: String(bytes: kStr_indexName.map{hiddenListStatus(of: $0)}, encoding: .utf8)!.localized, rightBtnTitle: "OK".localized, rightBlock: {
                        // 跳转到首页tab-party
                        //: NotificationCenter.default.post(name: SWITCH_TABBAR_HOME_PARTY_NOTIFICATION, object: nil, userInfo: nil)
                        NotificationCenter.default.post(name: kLet_packageTitle, object: nil, userInfo: nil)
                        //: }, config: config)
                    }, config: config)

                    //: } else {
                } else { // 被踢出房间
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked out of the room".localized)
                    detailDownArray(showMsg: String(bytes: kStr_viewTitle.map{cookieValue(label: $0)}, encoding: .utf8)!.localized)
                }

                //: } else {
            } else {
                //: MiniLoadThen.appLine(msg: "VOICEROOM：进房失败，roomID: \(roomID), error: \(errorCode), reason: \(reason), extendedData: \(extendedData).")
                MiniLoadThen.appLine(msg: (kStr_statusValue.replacingOccurrences(of: "remove", with: "M") + String(kStr_textHiddenMiniTitle)) + "\(roomID)" + (String(kStr_serviceValue.suffix(5)) + "or: ") + "\(errorCode)" + (String(kStr_itemTotalName) + String(kStr_playerValue.suffix(6))) + "\(reason)" + (String(kStr_intervalValue.prefix(6)) + "ndedD" + String(kStr_nativeText)) + "\(extendedData).")
                //: func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                detailDownArray(showMsg: (String(kStr_firstName) + kStr_norData.replacingOccurrences(of: "party", with: "r") + " exc" + String(kStr_managerData)).localized)
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                kLet_dismissValue.showTheme(eventID: kLet_timeData, parameterStr: [String(bytes: kStr_pairValue.map{rowAvailable(item: $0)}, encoding: .utf8)!: String(errorCode)])
            }
            //: voiceRoom_releaseAllResource()
            viewBy()
        }
    }

    // MARK: - 推流回调

    /// 当前用户推流状态回调
    //: func onPublisherStateUpdate(_ state: ZegoPublisherState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPublisherStateUpdate(_: ZegoPublisherState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 || errorCode == 1003020 || errorCode == 1002055 else {
        guard errorCode == 0 || errorCode == 1_003_020 || errorCode == 1_002_055 else { // 推流失败，用户下麦
            //: MiniLoadThen.appLine(msg: "VOICEROOM: 当前用户推流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            MiniLoadThen.appLine(msg: (String(kStr_dataManagerText) + kStr_errorTitle.replacingOccurrences(of: "info", with: "O") + String(kStr_valueData)) + "\(partyModel.roomId)" + (String(kStr_labelTitle) + kStr_cornerValue.capitalized) + "\(errorCode)" + (String(kStr_sizeName.prefix(6)) + "amID:" + kStr_showTitle.replacingOccurrences(of: "pop", with: " ")) + "\(streamID).")
            //: func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
            detailDownArray(showMsg: String(bytes: kStr_containerName.map{colorFalse(style: $0)}, encoding: .utf8)!.localized)
            //: let dict = getAllMikePositions()
            let dict = magnitudePositions()
            //: for (position, model) in dict where String(model.uid) == LocationThen.share.loginUid {
            for (position, model) in dict where String(model.uid) == LocationThen.share.loginUid {
                //: voiceRoom_reqChangeMike(type: 2, position: position)
                analyze(type: 2, position: position)
                //: resetMikePosition(position: position, model: model)
                pushByModel(position: position, model: model)
            }
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                viewBy()
            }
            //: return
            return
        }
    }

    // MARK: - 拉流回调

    /// 当前用户拉流状态回调
    //: func onPlayerStateUpdate(_ state: ZegoPlayerState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPlayerStateUpdate(_: ZegoPlayerState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 else {
        guard errorCode == 0 else { // 拉流失败
            //: MiniLoadThen.appLine(msg: "VOICEROOM: 拉流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            MiniLoadThen.appLine(msg: (kStr_arrayText.replacingOccurrences(of: "input", with: "I") + String(kStr_modelTitle.suffix(3)) + kStr_weightData + "roomID: ") + "\(partyModel.roomId)" + (String(kStr_labelTitle) + kStr_cornerValue.capitalized) + "\(errorCode)" + (String(kStr_sizeName.prefix(6)) + "amID:" + kStr_showTitle.replacingOccurrences(of: "pop", with: " ")) + "\(streamID).")
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                viewBy()
            }

            //: return
            return
        }
    }

    // MARK: - 流更新回调

    /// 流更新回调
    //: func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData: [AnyHashable: Any]?, roomID: String) {
    func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData _: [AnyHashable: Any]?, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: switch updateType {
        switch updateType {
        //: case .add:
        case .add:
            //: streamList.forEach { zego in
            streamList.forEach { zego in
                // 开始拉流
                //: ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
            }

        //: case .delete:
        case .delete:
            //: streamList.forEach { zego in
            streamList.forEach { zego in
                // 停止拉流
                //: ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
            }

        //: @unknown default:
        @unknown default:
            //: fatalError()
            fatalError()
        }
    }

    // MARK: - 声浪等级监听回调

    /// 本地声浪等级
    //: func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
    func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
        //: guard Int(truncating: soundLevel) > 0 else { return }
        guard Int(truncating: soundLevel) > 0 else { return }
        //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: partyModel.position, level: soundLevel)
        voiceRoomVC?.mikePositionView.outsideWhen(position: partyModel.position, level: soundLevel)
    }

    /// 远端声浪等级
    //: func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
    func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
        //: let dict = getAllMikePositions()
        let dict = magnitudePositions()
        //: for (streamID, level) in soundLevels {
        for (streamID, level) in soundLevels {
            //: let components = streamID.split(separator: "-").map(String.init)
            let components = streamID.split(separator: "-").map(String.init)
            //: guard components.count > 2 else { continue }
            guard components.count > 2 else { continue }
            //: if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
            if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
                //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: position, level: level)
                voiceRoomVC?.mikePositionView.outsideWhen(position: position, level: level)
            }
        }
    }

    // MARK: - 信令

    /// 上麦、下麦、锁麦、解锁麦、切麦操作会收到后端下发信令
    //: func onIMRecvCustomCommand(_ command: String, from fromUser: ZegoUser, roomID: String) {
    func onIMRecvCustomCommand(_ command: String, from _: ZegoUser, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: guard let decodedStr = command.removingPercentEncoding else {
        guard let decodedStr = command.removingPercentEncoding else {
            //: MiniLoadThen.appLine(msg: "VOICEROOM: 信令解析失败，command: \(command), roomID: \(roomID).")
            MiniLoadThen.appLine(msg: (String(kStr_nameShareTitle) + String(kStr_appValue)) + "\(command)" + (String(kStr_tapValue.prefix(4)) + String(kStr_dateTitle.prefix(6))) + "\(roomID).")
            //: return
            return
        }
        // 更新麦位信息，刷新列表UI
        //: let json = JSON(parseJSON: decodedStr)
        let json = JSON(parseJSON: decodedStr)
        //: let position = json["data"]["position"].intValue
        let position = json[(kStr_blackTitle.replacingOccurrences(of: "status", with: "a"))][(kStr_nowName.lowercased() + "tion")].intValue
        //: guard position < MIKE_SEAT_COUNT else { return }
        guard position < kLet_messageData else { return }
        //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: json["data"].dictionaryObject) else { return }
        guard let mikeModel = SharedModelMeasurable.deserialize(from: json[(kStr_blackTitle.replacingOccurrences(of: "status", with: "a"))].dictionaryObject) else { return }
        //: printLog(message: "xxxx-- \(json.rawValue)")
        pathLine(message: (kStr_clearValue.replacingOccurrences(of: "to", with: "xx") + String(kStr_managerEqualName.prefix(5))) + "\(json.rawValue)")
        //: if json["opType"].stringValue == "party:changeMike" { // 房主/观众 麦位操作信令
        if json[(String(kStr_minPartyData.prefix(6)))].stringValue == (String(kStr_tableName.suffix(6)) + "chang" + String(kStr_timeText.suffix(5))) { // 房主/观众 麦位操作信令
            //: let type = json["data"]["type"].intValue
            let type = json[(kStr_blackTitle.replacingOccurrences(of: "status", with: "a"))][(kStr_dataValue.replacingOccurrences(of: "language", with: "p"))].intValue
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: position, model: mikeModel)
                eventMethod(position: position, model: mikeModel)

            //: case 2:
            case 2: // 下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = imageGet(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    pushByModel(position: position, model: oldModel)
                }

            //: case 3:
            case 3: // 切麦
                //: resetMikePosition(position: position, model: mikeModel)
                pushByModel(position: position, model: mikeModel)
                //: let toPosition = json["data"]["toPosition"].intValue
                let toPosition = json[(kStr_blackTitle.replacingOccurrences(of: "status", with: "a"))][(String(kStr_clickName) + kStr_resultSectionData.replacingOccurrences(of: "title", with: "on"))].intValue
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: toPosition, model: mikeModel)
                eventMethod(position: toPosition, model: mikeModel)

            //: case 4, 5:
            case 4, 5: // 开麦、闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                eventMethod(position: position, model: mikeModel)

            //: default:
            default:
                //: break
                break
            }
            // 刷新礼物面板头像
            //: if type == 1 || type == 2 || type == 3 {
            if type == 1 || type == 2 || type == 3 {
                //: voiceRoomVC?.bottomView.refreshGiftIcon()
                voiceRoomVC?.bottomView.playerIcon()
            }

            //: } else if json["opType"].stringValue == "party:adminChangeMike" { // 房主对他人操作信令
        } else if json[(String(kStr_minPartyData.prefix(6)))].stringValue == (kStr_cellValue.replacingOccurrences(of: "icon", with: "ar") + ":admi" + String(kStr_succeedData.prefix(6)) + "eMike") { // 房主对他人操作信令
            //: switch json["data"]["type"].intValue {
            switch json[(kStr_blackTitle.replacingOccurrences(of: "status", with: "a"))][(kStr_dataValue.replacingOccurrences(of: "language", with: "p"))].intValue {
            //: case 1, 2:
            case 1, 2: // 锁麦、解锁麦
                //: updateMikePosition(position: position, model: mikeModel)
                eventMethod(position: position, model: mikeModel)

            //: case 3:
            case 3: // 踢下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = imageGet(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    pushByModel(position: position, model: oldModel)
                    // 刷新礼物面板头像
                    //: voiceRoomVC?.bottomView.refreshGiftIcon()
                    voiceRoomVC?.bottomView.playerIcon()
                    //: guard String(oldModel.uid) == LocationThen.share.loginUid else { return }
                    guard String(oldModel.uid) == LocationThen.share.loginUid else { return }
                    //: ZegoExpressEngine.shared().stopPublishingStream()
                    ZegoExpressEngine.shared().stopPublishingStream()
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked off the seat".localized)
                    detailDownArray(showMsg: (String(kStr_itemBeginData.suffix(6)) + "re k" + String(kStr_scaleName) + String(kStr_messageEditText.prefix(5)) + "the seat").localized)
                }

            //: case 4:
            case 4: // 开麦
                //: updateMikePosition(position: position, model: mikeModel)
                eventMethod(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == LocationThen.share.loginUid else { return }
                guard String(mikeModel.uid) == LocationThen.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                eventMethod(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == LocationThen.share.loginUid else { return }
                guard String(mikeModel.uid) == LocationThen.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }

            //: } else if json["opType"].stringValue == "party:changeCoin" { // 更新用户麦位金币
        } else if json[(String(kStr_minPartyData.prefix(6)))].stringValue == (String(kStr_firstWillValue.suffix(6)) + "chan" + String(kStr_requestTitle.suffix(5)) + String(kStr_imageInfoName)) { // 更新用户麦位金币
            //: TalkingVoiceRoomManager.shared().getAllMikePositions().forEach { position, model in
            SubCheckedReactiveCompatible.replyShared().magnitudePositions().forEach { position, model in
                //: if model.uid == mikeModel.uid {
                if model.uid == mikeModel.uid {
                    //: model.coin = mikeModel.coin
                    model.coin = mikeModel.coin
                    //: updateMikePosition(position: position, model: model)
                    eventMethod(position: position, model: model)
                    //: return
                    return
                }
            }

            //: } else if json["opType"].stringValue == "party:closeStream" { // 推流失败
        } else if json[(String(kStr_minPartyData.prefix(6)))].stringValue == (String(kStr_hiddenData.suffix(8)) + String(kStr_cancelTitle) + String(kStr_buttonCellTitle.prefix(7))) { // 推流失败
            //: ZegoExpressEngine.shared().stopPublishingStream()
            ZegoExpressEngine.shared().stopPublishingStream()
        }
    }
}

// MARK: - 信令处理

//: extension TalkingVoiceRoomManager {
extension SubCheckedReactiveCompatible {
    /// 更新麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func updateMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func eventMethod(position: Int, model: SharedModelMeasurable) {
        //: modifyMikePosition(key: position, model: model)
        area(key: position, model: model)
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.placeFile(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        quantityerval(uid: model.uid)
    }

    /// 重置麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func resetMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func pushByModel(position: Int, model: SharedModelMeasurable) {
        //: modifyMikePosition(key: position, model: TalkingMikeListItemModel())
        area(key: position, model: SharedModelMeasurable())
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.placeFile(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        quantityerval(uid: model.uid)
    }

    /// 更新本人麦位、底部麦克风状态
    /// - Parameter uid: 用户Id
    //: private func updateBottomMicStatusIfCurrentUser(uid: Int) {
    private func quantityerval(uid: Int) {
        //: guard String(uid) == LocationThen.share.loginUid else { return }
        guard String(uid) == LocationThen.share.loginUid else { return }
        //: let dict = getAllMikePositions()
        let dict = magnitudePositions()
        //: if let position = dict.first(where: { $0.value.uid == uid })?.key {
        if let position = dict.first(where: { $0.value.uid == uid })?.key {
            //: partyModel.position = position
            partyModel.position = position
            //: } else {
        } else {
            //: partyModel.position = -1
            partyModel.position = -1
        }
        //: voiceRoomVC?.bottomView.refreshBottomMicStatus()
        voiceRoomVC?.bottomView.bilgeStatus()
    }
}

// MARK: - 麦位字典安全操作

//: extension TalkingVoiceRoomManager {
extension SubCheckedReactiveCompatible {
    /// 获取初始化模型
    //: private func initMikePosition() -> [Int: TalkingMikeListItemModel] {
    private func contentFinish() -> [Int: SharedModelMeasurable] {
        //: for index in 0 ..< MIKE_SEAT_COUNT {
        for index in 0 ..< kLet_messageData {
            //: modifyMikePosition(key: index, model: TalkingMikeListItemModel())
            area(key: index, model: SharedModelMeasurable())
        }
        //: return mikePositionDict
        return mikePositionDict
    }

    /// 添加/修改 麦位
    //: func modifyMikePosition(key: Int, model: TalkingMikeListItemModel) {
    func area(key: Int, model: SharedModelMeasurable) {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: mikePositionDict[key] = model
        mikePositionDict[key] = model
    }

    /// 获取麦位
    //: func getMikePosition(key: Int) -> TalkingMikeListItemModel? {
    func imageGet(key: Int) -> SharedModelMeasurable? {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict[key]
        return mikePositionDict[key]
    }

    /// 获取所有麦位
    //: func getAllMikePositions() -> [Int: TalkingMikeListItemModel] {
    func magnitudePositions() -> [Int: SharedModelMeasurable] {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict
        return mikePositionDict
    }
}
