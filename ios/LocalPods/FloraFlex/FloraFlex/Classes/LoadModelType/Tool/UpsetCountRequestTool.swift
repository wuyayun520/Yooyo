
//: Declare String Begin

/*: "mf/business/giftList" :*/
fileprivate let kStr_editViewValue:[Character] = ["m","f"]
fileprivate let kStr_groupData:String = "/busicontent gift"
fileprivate let kStr_tableListGroupValue:String = "giftListmake in request let"

/*: "type" :*/
fileprivate let kStr_strokeMakeValue:[UInt8] = [0x67,0x6a,0x63,0x76]

private func upCellAt(path num: UInt8) -> UInt8 {
    return num ^ 19
}

/*: "mf/chatRoom/giftList" :*/
fileprivate let kStr_iconText:String = "mf/chmodel transition mode report model"
fileprivate let kStr_countryValue:String = "bubble make type make minm/gi"
fileprivate let kStr_arrayValue:String = "make disappearftList"

/*: "live/giftList" :*/
fileprivate let kStr_itemValue:String = "live/giftwith view"
fileprivate let kStr_tipValue:String = "equal last text equal elseList"

/*: "party/giftList" :*/
fileprivate let kStr_partyData:String = "PARTY"
fileprivate let kStr_rawCloseText:String = "succeed error bottom end at/gift"

/*: "IM/getMessageList" :*/
fileprivate let kStr_textContentViewValue:String = "hidden at address dismiss centerIM/ge"
fileprivate let kStr_quitName:[Character] = ["g","e","L","i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let kStr_modelTapBackgroundText:String = "share to makemf/b"
fileprivate let kStr_afterColorTitle:String = "ss/sif equal true party"
fileprivate let kStr_imageRoundRightValue:[Character] = ["e","n","d","M","s","g"]

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let kStr_cancelEnableValue:String = "mf/height let path"
fileprivate let kStr_buildTitle:String = "modelhat"
fileprivate let kStr_buttonName:String = "hidden add view input view/sen"

/*: "mf/business/sendGift" :*/
fileprivate let kStr_userValue:[Character] = ["m","f","/","b","u","s"]
fileprivate let kStr_backLimitName:String = "file"
fileprivate let kStr_sizeShareText:String = "NESS"
fileprivate let kStr_viewName:String = "content make color corner selfdGift"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let kStr_expressDayValue:String = "count normal barmf/ch"
fileprivate let kStr_wrapName:String = "endGiftfatal model view play"

/*: "live/sendGift" :*/
fileprivate let kStr_selectedToolSharedValue:[Character] = ["l","i","v","e","/"]
fileprivate let kStr_makeData:[Character] = ["s","e","n"]
fileprivate let kStr_cellAboutLanguageName:[Character] = ["d","G","i","f","t"]

/*: "party/sendGift" :*/
fileprivate let kStr_appearName:[Character] = ["p","a","r","t","y","/","s","e","n","d","G","i","f"]
fileprivate let kStr_femaleValue:String = "to"

/*: "mf/user/getChatInfo" :*/
fileprivate let kStr_rangeData:[Character] = ["m","f","/","u","s","e","r","/","g"]
fileprivate let kStr_normalValue:String = "etChatdetail super to table target"

/*: "IM/getMessageUserInfoList" :*/
fileprivate let kStr_allData:[Character] = ["I","M","/","g","e","t","M","e","s","s","a","g","e","U","s","e"]
fileprivate let kStr_timeName:String = "rIleading let return cover image"
fileprivate let kStr_infoPopVarContent:[Character] = ["n","f","o","L","i","s","t"]

/*: "mf/crush/whoLikeMeList" :*/
fileprivate let kStr_userText:String = "mf/cell top view video share"
fileprivate let kStr_endName:[Character] = ["c","r","u","s","h","/","w","h","o","L","i","k","e","M","e","L","i","s","t"]

/*: "mf/crush/ignore" :*/
fileprivate let kStr_afterValue:String = "mf/clevel in index"
fileprivate let kStr_showViewValue:String = "GNORE"

/*: "mf/crush/like" :*/
fileprivate let kStr_managerSectionValue:String = "color extension from modemf/cr"

/*: "mf/user/getRelation" :*/
fileprivate let kStr_holdTitle:[Character] = ["m","f","/","u","s","e","r","/","g","e","t","R"]
fileprivate let kStr_labTitle:[Character] = ["e","l","a","t","i","o","n"]

/*: "user/attention" :*/
fileprivate let kStr_skinName:[UInt8] = [0xb9,0xbf,0xa9,0xbe,0xe3,0xad,0xb8,0xb8,0xa9,0xa2,0xb8,0xa5,0xa3,0xa2]

/*: "user/removeAttention" :*/
fileprivate let kStr_rowData:String = "mainer"
fileprivate let kStr_underText:String = "size succeed source title viewAttention"

/*: "Can't earn points if you follow each other~" :*/
fileprivate let kStr_logData:[UInt8] = [0xa2,0x80,0x8f,0xc6,0x95,0xc1,0x84,0x80,0x93,0x8f,0xc1,0x91,0x8e,0x88,0x8f,0x95,0x92,0xc1,0x88,0x87,0xc1,0x98,0x8e,0x94,0xc1,0x87,0x8e,0x8d,0x8d,0x8e,0x96,0xc1,0x84,0x80,0x82,0x89,0xc1,0x8e,0x95,0x89,0x84,0x93,0x9f]

private func biologyLaboratory(again num: UInt8) -> UInt8 {
    return num ^ 225
}

/*: "follow Success" :*/
fileprivate let kStr_takeData:String = "fseatlow"
fileprivate let kStr_keyData:[Character] = [" ","S","u","c","c","e","s","s"]

/*: "unfollow Success" :*/
fileprivate let kStr_menuValue:String = "textfo"
fileprivate let kStr_colorText:String = "uccflexibles"
fileprivate let kStr_titleName:[Character] = ["s"]

/*: "translate/msg" :*/
fileprivate let kStr_angleFrameEnableValue:String = "trafromslat"
fileprivate let kStr_postValue:String = "e/msgas against value height"

/*: "msgId" :*/
fileprivate let kStr_replyContent:[UInt8] = [0x8,0x16,0x2,0x2c,0x1]

private func managerEqual(number num: UInt8) -> UInt8 {
    return num ^ 101
}

/*: "targetLng" :*/
fileprivate let kStr_enableName:[UInt8] = [0x67,0x6e,0x4c,0x74,0x65,0x67,0x72,0x61,0x74]

/*: "mf/greet/getAutoGreetContent" :*/
fileprivate let kStr_whiteName:String = "location modelmf/g"
fileprivate let kStr_textData:String = "/getAimage view"
fileprivate let kStr_collectionContent:[Character] = ["r","e","e","t","C","o","n","t","e","n","t"]

/*: "mf/greet/getIntimatePhoto" :*/
fileprivate let kStr_stopTitle:[Character] = ["m","f","/","g","r","e","e","t","/"]
fileprivate let kStr_ofBlockTitle:String = "getIlabel status"
fileprivate let kStr_bottomName:String = "tePhotoerror head"

/*: "toUid" :*/
fileprivate let kStr_femaleName:[UInt8] = [0x2c,0x37,0xd,0x31,0x3c]

private func listenSize(information num: UInt8) -> UInt8 {
    return num ^ 88
}

/*: "mf/chatRoom/info" :*/
fileprivate let kStr_shouldTitle:[Character] = ["m","f","/","c","h","a"]
fileprivate let kStr_lineName:String = "tRouser self gesture content size"
fileprivate let kStr_fastSumData:[Character] = ["o","m","/","i","n","f","o"]

/*: "mf/chatRoom/list" :*/
fileprivate let kStr_momentName:String = "status layer view with varmf/chat"
fileprivate let kStr_labName:String = "Roomadd end"

/*: "roomId" :*/
fileprivate let kStr_effectName:[UInt8] = [0xf7,0xea,0xea,0xe8,0xcc,0xe1]

private func componentPlay(system num: UInt8) -> UInt8 {
    return num ^ 133
}

/*: "needExtraInfo" :*/
fileprivate let kStr_beSeatFalseValue:[UInt8] = [0x9d,0x96,0x96,0x97,0xb6,0x8b,0x87,0x81,0x92,0xba,0x9d,0x95,0x9c]

private func appMenu(extended num: UInt8) -> UInt8 {
    return num ^ 243
}

/*: "mf/chatRoom/check" :*/
fileprivate let kStr_viewTitle:String = "skin let need time labelmf/chatR"
fileprivate let kStr_modelValue:String = "oom/super top"

/*: "mf/chatRoom/joinOrQuit" :*/
fileprivate let kStr_anonymousData:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","j","o","i","n","O"]
fileprivate let kStr_failureName:String = "rQuitmodel if"

/*: "mf/chatRoom/members" :*/
fileprivate let kStr_frameTitle:String = "self safety name letmf/ch"
fileprivate let kStr_atData:String = "MBERS"

/*: "translate/text" :*/
fileprivate let kStr_dataImageModelValue:String = "transmanager"
fileprivate let kStr_appearData:String = "title make cell show viewte/text"

/*: "text" :*/
fileprivate let kStr_videoValue:[UInt8] = [0xbe,0xaf,0xc2,0xbe]

fileprivate func countTo(style num: UInt8) -> UInt8 {
    let value = Int(num) - 74
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/user/queryStatus" :*/
fileprivate let kStr_panTitle:[Character] = ["m","f","/","u","s","e","r","/","q","u","e","r","y","S","t","a","t","u","s"]

/*: "toUids" :*/
fileprivate let kStr_yearText:[UInt8] = [0x73,0x64,0x69,0x55,0x6f,0x74]

/*: "call/getCallTab" :*/
fileprivate let kStr_talkData:String = "height load equalcall/"
fileprivate let kStr_pointLabName:String = "data content tollTab"

/*: "mf/user/remark" :*/
fileprivate let kStr_reportValue:[Character] = ["m","f","/","u","s","e","r","/"]
fileprivate let kStr_nowData:String = "retoolark"

/*: "content" :*/
fileprivate let kStr_imageTitleText:[UInt8] = [0xd2,0xde,0xdf,0xc5,0xd4,0xdf,0xc5]

private func itemKind(to num: UInt8) -> UInt8 {
    return num ^ 177
}

/*: "top" :*/
fileprivate let kStr_colorData:[UInt8] = [0x26,0x3d,0x22]

private func giftUser(hidden num: UInt8) -> UInt8 {
    return num ^ 82
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  UpsetCountRequestTool.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/7.
//

//: import UIKit
import UIKit

//: public class UpsetCountRequestTool: NSObject {
public class UpsetCountRequestTool: NSObject {
    //: public typealias RequestEndBlock = (_ t: Bool) -> Void
    public typealias RequestEndBlock = (_ t: Bool) -> Void
    //: typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    //: public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [FailureReactiveCompatible]) -> Void
    public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [FailureReactiveCompatible]) -> Void

    /// 私信礼物面板
    /// - Parameters:
    ///   - type: 0=默认， 1=私密照片，2=私密视频,3=节目单
    ///   - completion: 回调
    //: class func req_ChatGiftList(type: String, completion: @escaping FinishBlock) {
    class func systemObject(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: reqModel.requestPath = "mf/business/giftList"
        reqModel.requestPath = (String(kStr_editViewValue) + String(kStr_groupData.prefix(5)) + "ness/" + String(kStr_tableListGroupValue.prefix(8)))
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: kStr_strokeMakeValue.map{upCellAt(path: $0)}, encoding: .utf8)!: type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_ChatRoomGiftList(type: String, completion: @escaping FinishBlock) {
    class func withPresent(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/giftList"
        reqModel.requestPath = (String(kStr_iconText.prefix(5)) + "atRoo" + String(kStr_countryValue.suffix(4)) + String(kStr_arrayValue.suffix(6)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_LiveGiftList(type: String, completion: @escaping FinishBlock) {
    class func bite(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: reqModel.requestPath = "live/giftList"
        reqModel.requestPath = (String(kStr_itemValue.prefix(9)) + String(kStr_tipValue.suffix(4)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 语聊房礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_partyGiftList(type: String, completion: @escaping FinishBlock) {
    class func liberalParty(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SizeRequestModel()
        //: reqModel.requestPath = "party/giftList"
        reqModel.requestPath = (kStr_partyData.lowercased() + String(kStr_rawCloseText.suffix(5)) + "List")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求系统消息
    /// - Parameter completion: 回调
    //: @objc class func req_sysMessageList(completion: @escaping FinishBlocks) {
    @objc class func cellApp(completion: @escaping FinishBlocks) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: reqModel.requestPath = "IM/getMessageList"
        reqModel.requestPath = (String(kStr_textContentViewValue.suffix(5)) + "tMessa" + String(kStr_quitName))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: completion(true, result as! NSDictionary)
                completion(true, result as! NSDictionary)
            }
        }
    }

    /// 私聊发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendPrivete(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func behindUp(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendMsg"
        reqModel.requestPath = (String(kStr_modelTapBackgroundText.suffix(4)) + "usine" + String(kStr_afterColorTitle.prefix(4)) + String(kStr_imageRoundRightValue))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 公共聊天室发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendRoomMsg(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func sendWill(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SizeRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendMsg"
        reqModel.requestPath = (String(kStr_cancelEnableValue.prefix(3)) + kStr_buildTitle.replacingOccurrences(of: "model", with: "c") + "Room" + String(kStr_buttonName.suffix(4)) + "dMsg")
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftPrivete( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func imageWithBuild(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendGift"
        reqModel.requestPath = (String(kStr_userValue) + kStr_backLimitName.replacingOccurrences(of: "file", with: "i") + kStr_sizeShareText.lowercased() + "/sen" + String(kStr_viewName.suffix(5)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftChatRoom( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func paramLoadWill(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendGift"
        reqModel.requestPath = (String(kStr_expressDayValue.suffix(5)) + "atRoom/s" + String(kStr_wrapName.prefix(7)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftLive( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func dateEnter(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "live/sendGift"
        reqModel.requestPath = (String(kStr_selectedToolSharedValue) + String(kStr_makeData) + String(kStr_cellAboutLanguageName))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 语聊房送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_sendPartyGift(param: [String: Any], completion: @escaping FinishBlock) {
    class func projectCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "party/sendGift"
        reqModel.requestPath = (String(kStr_appearName) + kStr_femaleValue.replacingOccurrences(of: "to", with: "t"))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求聊天用户信息
    /// - Parameters:
    //: class func req_refreshUserChatInfo( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func atCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: reqModel.requestPath = "mf/user/getChatInfo"
        reqModel.requestPath = (String(kStr_rangeData) + String(kStr_normalValue.prefix(6)) + "Info")
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求用户消息列表
    /// - Parameters:
    //: @objc class public func req_refreshMsgUserListInfo( param: Dictionary<String, Any>, completion: @escaping RequestMsgUserListBlock) {
    @objc public class func infoAdd(param: [String: Any], completion: @escaping RequestMsgUserListBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: reqModel.requestPath = "IM/getMessageUserInfoList"
        reqModel.requestPath = (String(kStr_allData) + String(kStr_timeName.prefix(2)) + String(kStr_infoPopVarContent))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, _ in
            //: guard succeed else {
            guard succeed else {
                //: completion(succeed, [])
                completion(succeed, [])
                //: return
                return
            }

            //: if let datas = Array<FailureReactiveCompatible>.deserialize(from: result as? Array) {
            if let datas = Array<FailureReactiveCompatible>.deserialize(from: result as? Array) {
                //: completion(succeed, datas as! [FailureReactiveCompatible])
                completion(succeed, datas as! [FailureReactiveCompatible])
            }
        }
    }

    /// 请求用户消息列表
    //: @objc class public func req_giftEffectDataUrl(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
    @objc public class func tantamount(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
        //: GJ.startGiftZipRequest(giftUrl: giftUrl, path: path) { succeed, result, errorModel in
        kLet_firstName.ambagesDataCompletion(giftUrl: giftUrl, path: path) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 谁心动我 - 列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func cellAdd(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: reqModel.requestPath = "mf/crush/whoLikeMeList"
        reqModel.requestPath = (String(kStr_userText.prefix(3)) + String(kStr_endName))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - nope拒绝操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeIgnore(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func startCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: reqModel.requestPath = "mf/crush/ignore"
        reqModel.requestPath = (String(kStr_afterValue.prefix(4)) + "rush/i" + kStr_showViewValue.lowercased())
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - like操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMelike(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func clip(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: reqModel.requestPath = "mf/crush/like"
        reqModel.requestPath = (String(kStr_managerSectionValue.suffix(5)) + "ush/like")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_atationList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func clearAfterCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: reqModel.requestPath = "mf/user/getRelation"
        reqModel.requestPath = (String(kStr_holdTitle) + String(kStr_labTitle))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注操作
    /// - Parameters:
    /// - isAttention: 关注/取消
    ///   - completion: 回调
    //: class func req_atationTool(isAttention: Bool, params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func busyIn(isAttention: Bool, params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: let path = isAttention==true ? "user/attention" :"user/removeAttention"
        let path = isAttention == true ? String(bytes: kStr_skinName.map{$0^204}, encoding: .utf8)! : (kStr_rowData.replacingOccurrences(of: "main", with: "us") + "/remove" + String(kStr_underText.suffix(9)))
        //: reqModel.requestPath = path
        reqModel.requestPath = path
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            // 统一处理toast
            //: if succeed {
            if succeed {
                //: if isAttention {
                if isAttention {
                    //: if LocationThen.share.loginUserMode.sex == Gender.female.rawValue {
                    if LocationThen.share.loginUserMode.sex == SucceedComparable.female.rawValue { // 女性
                        //: object.func__showStatusBarSuccessMsg(showMsg: "Can't earn points if you follow each other~".localized)
                        kLet_keyData.property(showMsg: String(bytes: kStr_logData.map{biologyLaboratory(again: $0)}, encoding: .utf8)!.localized)
                        //: } else {
                    } else {
                        //: object.func__showStatusBarSuccessMsg(showMsg: "follow Success".localized)
                        kLet_keyData.property(showMsg: (kStr_takeData.replacingOccurrences(of: "seat", with: "ol") + String(kStr_keyData)).localized)
                    }
                    //: } else {
                } else {
                    //: object.func__showStatusBarSuccessMsg(showMsg: "unfollow Success".localized)
                    kLet_keyData.property(showMsg: (kStr_menuValue.replacingOccurrences(of: "text", with: "un") + "llow S" + kStr_colorText.replacingOccurrences(of: "flexible", with: "e") + String(kStr_titleName)).localized)
                }

                //: } else {
            } else {
                //: object.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                kLet_keyData.detailDownArray(showMsg: errorModel?.errorMsg ?? "")
            }
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - msgId: 消息ID
    ///   - type=0表示私聊消息，type=1表示聊天室消息
    ///   - completion: 回调
    //: @objc class public func req_translateMsg(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
    @objc public class func colorWithCompletion(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: reqModel.requestPath = "translate/msg"
        reqModel.requestPath = (kStr_angleFrameEnableValue.replacingOccurrences(of: "from", with: "n") + String(kStr_postValue.prefix(5)))
        //: reqModel.params = ["msgId": msgId, "targetLng": TableMacroDefine.getSystemLangCode(), "type": "\(type)"]
        reqModel.params = [String(bytes: kStr_replyContent.map{managerEqual(number: $0)}, encoding: .utf8)!: msgId, String(bytes: kStr_enableName.reversed(), encoding: .utf8)!: TableMacroDefine.repossessBeginBushido(), String(bytes: kStr_strokeMakeValue.map{upCellAt(path: $0)}, encoding: .utf8)!: "\(type)"]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
            completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
        }
    }

    /// 获取一键打招呼内容
    ///   - completion: 回调
    //: class func req_getAutoGreetContent(completion: @escaping FinishBlock) {
    class func mainTargetCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: reqModel.requestPath = "mf/greet/getAutoGreetContent"
        reqModel.requestPath = (String(kStr_whiteName.suffix(4)) + "reet" + String(kStr_textData.prefix(5)) + "utoG" + String(kStr_collectionContent))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取随机私密照片
    ///   - completion: 回调
    //: class func req_getIntimatePhoto(toUid: String, completion: @escaping FinishBlock) {
    class func sumimate(toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: reqModel.requestPath = "mf/greet/getIntimatePhoto"
        reqModel.requestPath = (String(kStr_stopTitle) + String(kStr_ofBlockTitle.prefix(4)) + "ntima" + String(kStr_bottomName.prefix(7)))
        //: reqModel.params = ["toUid": toUid]
        reqModel.params = [String(bytes: kStr_femaleName.map{listenSize(information: $0)}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取聊天广场在线人数和家族信息
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomInfo(completion: @escaping FinishBlock) {
    class func tin(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/info"
        reqModel.requestPath = (String(kStr_shouldTitle) + String(kStr_lineName.prefix(3)) + String(kStr_fastSumData))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天广场列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomList(completion: @escaping FinishBlock) {
    class func reportCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/list"
        reqModel.requestPath = (String(kStr_momentName.suffix(7)) + String(kStr_labName.prefix(4)) + "/list")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测聊天室
    /// - Parameters:
    ///   - roomId: 聊天室Id
    ///   - completion: 回调
    //: class func req_checkChatRoom(roomId: String, completion: @escaping FinishBlock) {
    class func setAboutCompletion(roomId: String, completion: @escaping FinishBlock) {
        //: let params = ["roomId": roomId, "needExtraInfo": "1"]
        let params = [String(bytes: kStr_effectName.map{componentPlay(system: $0)}, encoding: .utf8)!: roomId, String(bytes: kStr_beSeatFalseValue.map{appMenu(extended: $0)}, encoding: .utf8)!: "1"]
        //: let reqModel = TalkingRequestModel()
        let reqModel = SizeRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/check"
        reqModel.requestPath = (String(kStr_viewTitle.suffix(8)) + String(kStr_modelValue.prefix(4)) + "check")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 进入或退出聊天室
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_chatRoomJoinOrQuit(params: [String: Any], completion: @escaping FinishBlock) {
    class func scene(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SizeRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/joinOrQuit"
        reqModel.requestPath = (String(kStr_anonymousData) + String(kStr_failureName.prefix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室在线名单
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomMembers(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func membersCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/members"
        reqModel.requestPath = (String(kStr_frameTitle.suffix(5)) + "atRoom/me" + kStr_atData.lowercased())
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - text: 文本
    ///   - completion: 回调
    //: class func req_translateText(text: String, completion: @escaping FinishBlock) {
    class func repeatFinishCompletion(text: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SizeRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "translate/text"
        reqModel.requestPath = (kStr_dataImageModelValue.replacingOccurrences(of: "manager", with: "la") + String(kStr_appearData.suffix(7)))
        //: reqModel.params = ["text": text]
        reqModel.params = [String(bytes: kStr_videoValue.map{countTo(style: $0)}, encoding: .utf8)!: text]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 批量查询消息列表用户在线状态
    /// - Parameters:
    ///   - toUids: 用户ids
    ///   - completion: 回调
    //: @objc class public func req_queryStatus(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
    @objc public class func available(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SizeRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/user/queryStatus"
        reqModel.requestPath = (String(kStr_panTitle))
        //: reqModel.params = ["toUids": toUids, "type": 1]
        reqModel.params = [String(bytes: kStr_yearText.reversed(), encoding: .utf8)!: toUids, String(bytes: kStr_strokeMakeValue.map{upCellAt(path: $0)}, encoding: .utf8)!: 1]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, _ in
            //: completion(succeed, result as? [[String: Any]] ?? [])
            completion(succeed, result as? [[String: Any]] ?? [])
        }
    }

    /// 拨打通话记录
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getCallTab(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func currencyEqual(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: reqModel.requestPath = "call/getCallTab"
        reqModel.requestPath = (String(kStr_talkData.suffix(5)) + "getCa" + String(kStr_pointLabName.suffix(5)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 新增/修改备注
    /// - Parameters:
    ///   - content: 备注内容
    ///   - isTop: 是否置顶，1：是，2：否
    ///   - toUid: 对方uid
    ///   - completion: 回调
    //: class func req_remark(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
    class func completionForFrame(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SizeRequestModel()
        //: reqModel.requestPath = "mf/user/remark"
        reqModel.requestPath = (String(kStr_reportValue) + kStr_nowData.replacingOccurrences(of: "tool", with: "m"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = ["content": content, "top": isTop, "toUid": toUid]
        reqModel.params = [String(bytes: kStr_imageTitleText.map{itemKind(to: $0)}, encoding: .utf8)!: content, String(bytes: kStr_colorData.map{giftUser(hidden: $0)}, encoding: .utf8)!: isTop, String(bytes: kStr_femaleName.map{listenSize(information: $0)}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
