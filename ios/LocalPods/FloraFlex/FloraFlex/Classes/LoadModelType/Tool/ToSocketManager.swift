
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let kStr_washValue:String = "SshadowCKE"
fileprivate let kStr_aName:String = "R_KEYclass extension"

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let kStr_topTouchDataValue:[UInt8] = [0x4e,0x4f,0x49,0x54,0x41,0x43,0x49,0x46,0x49,0x54,0x4f,0x4e,0x5f,0x4c,0x4c,0x41,0x43,0x54,0x53,0x45,0x55,0x51,0x45,0x52,0x5f,0x54,0x41,0x48,0x43,0x4f,0x45,0x44,0x49,0x56,0x5f,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let kStr_commentViewName:[UInt8] = [0x97,0x80,0x86,0x8c,0x93,0x80,0x9a,0x83,0x89,0x8a,0x84,0x91,0x96,0x86,0x97,0x80,0x80,0x8b,0x9a,0x8b,0x8a,0x91,0x8c,0x83,0x8c,0x86,0x84,0x91,0x8c,0x8a,0x8b]

private func playerForce(center num: UInt8) -> UInt8 {
    return num ^ 197
}

/*: "isCalling" :*/
fileprivate let kStr_postImageValue:[Character] = ["i","s","C","a","l","l","i","n"]
fileprivate let kStr_showData:[Character] = ["g"]

/*: "isTalking" :*/
fileprivate let kStr_equalData:String = "add view falseisTal"

/*: "SOCKET 连接成功." :*/
fileprivate let kStr_sharedData:String = "hiddenO"
fileprivate let kStr_intimateTableData:String = "CKET 连image to"

/*: "SOCKET 连接失败, error: :*/
fileprivate let kStr_exclusiveTitle:String = "ScancelCKET"
fileprivate let kStr_contentInnerViewData:String = " \u{8fde}接失败, e"
fileprivate let kStr_toData:[Character] = ["r","r","o","r",":"]

/*: ." :*/
fileprivate let kStr_backgroundName:[Character] = ["."]

/*: "SOCKET 失败 :*/
fileprivate let kStr_viewRefreshValue:String = "laboratoryOCK"

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let kStr_labValue:[Character] = ["\u{6b21}",","," ","切","换","过","S","o","c","k","e","t","域","名","重","新","\u{5c1d}","试","，","地","址","："]

/*: "SOCKET 已关闭, code: :*/
fileprivate let kStr_textName:[Character] = ["S","O","C","K","E","T"," ","已"]
fileprivate let kStr_screenValue:[Character] = ["关"]
fileprivate let kStr_quoteValue:String = "in let闭, code:"

/*: , reason: :*/
fileprivate let kStr_whiteTitleContent:String = "count"
fileprivate let kStr_indexName:[Character] = [" ","r","e","a","s","o","n",":"]

/*: "ws:// :*/
fileprivate let kStr_voiceValue:[Character] = ["w","s",":","/","/"]

/*: /?sid= :*/
fileprivate let kStr_normalBackName:String = "/?sid=data center"

/*: &uid= :*/
fileprivate let kStr_equalValue:String = "&uid=data false return icon string"

/*: &platform=iphone&version= :*/
fileprivate let kStr_textValue:String = "var self view report to&pla"
fileprivate let kStr_containerLastData:String = "m=iphohi data app from"
fileprivate let kStr_liveTitle:[Character] = ["n","e","&","v","e","r","s","i","o","n","="]

/*: &packageId= :*/
fileprivate let kStr_titleValue:String = "&packcase name"

/*: "1v1server. :*/
fileprivate let kStr_partyName:String = "to index error text extension1v1ser"

/*: .com:7766" :*/
fileprivate let kStr_hiddenYearValue:String = "user year.com:"

/*: "43.129.185.194:7767" :*/
fileprivate let kStr_playerData:String = "4more.129"
fileprivate let kStr_shareData:String = ".185"
fileprivate let kStr_iconTextValue:String = "error67"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let kStr_transformName:[UInt8] = [0xc2,0xbe,0xb2,0xba,0xb4,0xc3,0x8f,0x56,0x2c,0x0,0x56,0x2a,0xb,0xe1,0xd4,0xd0,0xd2,0xd7,0xd0,0xd1,0xd8,0xdb,0xd8,0xe3,0xe8,0xb2,0xd7,0xd0,0xdd,0xd6,0xd4,0xd3,0x56,0xa,0x0,0x54,0xff,0x1b,0x5e,0x2b,0xfb,0x55,0x5,0x1c,0x56,0x2c,0x0,0x54,0xf4,0x22,0x58,0x6,0x1c,0x9d]

fileprivate func mainBean(to num: UInt8) -> UInt8 {
    let value = Int(num) - 111
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cmd" :*/
fileprivate let kStr_successValue:String = "cmanagerd"

/*: "ping" :*/
fileprivate let kStr_leadingSinceName:[Character] = ["p","i","n","g"]

/*: "callHeartbeat" :*/
fileprivate let kStr_actionData:[Character] = ["c","a","l","l","H","e","a","r","t","b","e","a"]
fileprivate let kStr_sizeText:[Character] = ["t"]

/*: "recievedDate" :*/
fileprivate let kStr_userTitle:String = "countecie"
fileprivate let kStr_makeText:[Character] = ["e"]

/*: "liveHeartBeat" :*/
fileprivate let kStr_containerData:String = "extra view yearliveHe"
fileprivate let kStr_regularData:String = "clickt"

/*: "🔗 关闭 socket" :*/
fileprivate let kStr_equalText:String = "🔗 关\u{95ed}"
fileprivate let kStr_menuValue:String = "ET"

/*: "The user logs out" :*/
fileprivate let kStr_pleaseTitle:[Character] = ["T"]
fileprivate let kStr_viewName:String = "he usethis user last"
fileprivate let kStr_pathValue:String = "render view targetgs out"

/*: "SOCKET  :*/
fileprivate let kStr_makeFromText:String = "SOCKET var for"

/*: 重连. 重试次数： :*/
fileprivate let kStr_ofText:[Character] = ["重","连","."," ","重","试"]
fileprivate let kStr_withStyleValue:String = "\u{6b21}数："

/*: , 地址： :*/
fileprivate let kStr_lineIndexReturnValue:[Character] = [","," ","地","\u{5740}","："]

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let kStr_frameContent:[Character] = ["S","O","C","K","E","T"," ","连","接","正","常"]
fileprivate let kStr_inputName:String = " "
fileprivate let kStr_toTitle:[Character] = ["不","需","要","\u{91cd}","新","连","接","."]

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let kStr_labelData:[Character] = ["S","O","C","K","E","T"," ","正","在","\u{8fde}","接"," ","不","需","要","\u{91cd}","\u{65b0}","\u{8fde}","接"]
fileprivate let kStr_videoTitle:[Character] = ["."]

/*: "&resume=1" :*/
fileprivate let kStr_showAlwaysText:String = "var let content time&resum"
fileprivate let kStr_byTitle:String = "user direction succeed anye=1"

/*: "&lang= :*/
fileprivate let kStr_colorStreamValue:String = "control aspect equal&lang="

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let kStr_userData:String = "cellCKET"
fileprivate let kStr_beforeValue:[Character] = [","," ","地","址","\u{ff1a}"]

/*: "onNetDisabled" :*/
fileprivate let kStr_byData:String = "will equal title fitonNetD"
fileprivate let kStr_moreTitle:String = "voicesabled"

/*: "errno" :*/
fileprivate let kStr_timeText:String = "errliveo"

/*: "data" :*/
fileprivate let kStr_viewContent:[Character] = ["d","a","t","a"]

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let kStr_dataVisibleText:String = "SOCKgift"
fileprivate let kStr_nameEqualData:[Character] = ["活","c","m","d",":"," ","p","i","n","g","."]

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let kStr_makeTableIfValue:[UInt8] = [0x50,0x4c,0x40,0x48,0x46,0x57,0x23,0x77,0x71,0x77,0x60,0xe7,0xbc,0x9e,0xe5,0xb7,0xb8,0x60,0x6e,0x67,0x39,0x23,0x60,0x62,0x6f,0x6f,0x4b,0x66,0x62,0x71,0x77,0x61,0x66,0x62,0x77,0x2d]

private func rawData(manager num: UInt8) -> UInt8 {
    return num ^ 3
}

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let kStr_senseHalfValue:[UInt8] = [0x1d,0x1,0xd,0x5,0xb,0x1a,0x6e,0x22,0x27,0x38,0x2b,0xaa,0xf1,0xd3,0xa8,0xfa,0xf5,0x2d,0x23,0x2a,0x74,0x6e,0x22,0x27,0x38,0x2b,0x6,0x2b,0x2f,0x3c,0x3a,0xc,0x2b,0x2f,0x3a,0x60]

/*: "onP" :*/
fileprivate let kStr_buttonNameIfData:String = "onPstring raw"

/*: "msg" :*/
fileprivate let kStr_digitalBlockValue:String = "mvalue"

/*: "🔗 处理消息：cmdName: :*/
fileprivate let kStr_showText:[Character] = ["🔗"," ","\u{5904}","理","\u{6d88}","息","：","c","m","d","N","a","m"]
fileprivate let kStr_bottomData:[Character] = ["e",":"]

/*: , errNo: :*/
fileprivate let kStr_ofName:String = "target return, errN"
fileprivate let kStr_fatalValue:String = "let var leadingo:"

/*: , msg: :*/
fileprivate let kStr_cornerData:[Character] = [","," ","m","s","g",":"]

/*: , data: :*/
fileprivate let kStr_detailValue:String = ", "
fileprivate let kStr_numberName:String = "data:language image case item size"

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let kStr_cellData:String = "SOCKET \u{63a5}"
fileprivate let kStr_videoContent:[Character] = ["收","异","常","消","\u{606f}","：","c","m","d","N","a","m","e",":"]

/*: "onReceivedGift" :*/
fileprivate let kStr_imageTextValue:[Character] = ["o","n","R","e","c","e","i"]
fileprivate let kStr_mValue:String = "selected"
fileprivate let kStr_tempValue:[Character] = ["e","d","G","i","f","t"]

/*: "onVideoMatch" :*/
fileprivate let kStr_dataValue:[Character] = ["o","n","V","i","d"]
fileprivate let kStr_toolData:String = "eoMatchmedia height type container center"

/*: "onRequestCall" :*/
fileprivate let kStr_textData:[Character] = ["o","n","R","e","q","u","e","s","t","C","a","l"]
fileprivate let kStr_scaleName:String = "L"

/*: "onCancelCall" :*/
fileprivate let kStr_userText:[Character] = ["o","n","C","a","n"]
fileprivate let kStr_successName:String = "celCallcontent equal item"

/*: "onRefuseCall" :*/
fileprivate let kStr_readData:[Character] = ["o","n","R","e","f"]
fileprivate let kStr_colorErrorValue:String = "add if letuseCall"

/*: "onAcceptCall" :*/
fileprivate let kStr_nameBorderValue:[Character] = ["o","n","A","c"]
fileprivate let kStr_chooseText:[Character] = ["c","e","p","t","C","a","l","l"]

/*: "onStopCall" :*/
fileprivate let kStr_addText:String = "onStopif cell block if"

/*: "freeCallTimes" :*/
fileprivate let kStr_messageSizeData:String = "click to source imagefreeCall"
fileprivate let kStr_styleName:[Character] = ["T","i","m","e","s"]

/*: "mfCoin" :*/
fileprivate let kStr_mName:String = "mfCoindepth case manager equal"

/*: "onMissCoin" :*/
fileprivate let kStr_monthProfileName:String = "function makeonMis"

/*: "acceptCall" :*/
fileprivate let kStr_contentText:[Character] = ["a","c","c","e","p","t","C","a","l"]
fileprivate let kStr_countValue:[Character] = ["l"]

/*: "stopCall" :*/
fileprivate let kStr_topicValue:[Character] = ["s","t","o","p","C","a","l"]
fileprivate let kStr_backgroundValue:String = "L"

/*: "requestCall" :*/
fileprivate let kStr_sessionName:[Character] = ["r","e","q","u"]
fileprivate let kStr_pushMakeName:String = "self location label contentestCall"

/*: "[Invite you to a video call...]" :*/
fileprivate let kStr_labelPlayerValue:[UInt8] = [0xe1,0xf3,0xd4,0xcc,0xd3,0xce,0xdf,0x9a,0xc3,0xd5,0xcf,0x9a,0xce,0xd5,0x9a,0xdb,0x9a,0xcc,0xd3,0xde,0xdf,0xd5,0x9a,0xd9,0xdb,0xd6,0xd6,0x94,0x94,0x94,0xe7]

/*: "type" :*/
fileprivate let kStr_sceneText:[Character] = ["t","y","p","e"]

/*: "[Invite you to a voice call...]" :*/
fileprivate let kStr_titleSucceedValue:[UInt8] = [0xe6,0xf4,0xd3,0xcb,0xd4,0xc9,0xd8,0x9d,0xc4,0xd2,0xc8,0x9d,0xc9,0xd2,0x9d,0xdc,0x9d,0xcb,0xd2,0xd4,0xde,0xd8,0x9d,0xde,0xdc,0xd1,0xd1,0x93,0x93,0x93,0xe0]

/*: "uid" :*/
fileprivate let kStr_infoContent:String = "UID"

/*: "nickname" :*/
fileprivate let kStr_timeContent:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "headPic" :*/
fileprivate let kStr_textEventData:[Character] = ["h","e","a","d","P","i"]
fileprivate let kStr_duringText:String = "video"

/*: "onCallResume" :*/
fileprivate let kStr_listViewValue:String = "task rank private image targetonCall"
fileprivate let kStr_rawData:[Character] = ["R","e","s","u","m","e"]

/*: "cancelCall" :*/
fileprivate let kStr_nameData:[Character] = ["c","a","n","c","e"]
fileprivate let kStr_viewFramePushText:[Character] = ["l","C","a","l","l"]

/*: "refuseCall" :*/
fileprivate let kStr_backName:[Character] = ["r","e","f","u","s","e","C","a","l","l"]

/*: "onCallMotivation" :*/
fileprivate let kStr_imageHalfLabValue:String = "onCallmain size color up"
fileprivate let kStr_emptyName:String = "atpinn"

/*: "onLiveWarning" :*/
fileprivate let kStr_startValue:[Character] = ["o","n","L","i","v","e","W","a","r","n","i"]
fileprivate let kStr_downData:[Character] = ["n","g"]

/*: "reason" :*/
fileprivate let kStr_customValue:String = "recropon"

/*: "time" :*/
fileprivate let kStr_photoData:String = "timview"

/*: "onStopLive" :*/
fileprivate let kStr_enableName:String = "model return push let contentonSto"
fileprivate let kStr_indexTitle:[Character] = ["p","L","i","v","e"]

/*: "onSendProgramGift" :*/
fileprivate let kStr_rowName:String = "name for var a heightonSen"
fileprivate let kStr_clickGreetData:[Character] = ["d","P","r","o","g","r","a","m","G","i","f","t"]

/*: "onGameFs" :*/
fileprivate let kStr_topTitle:String = "make object otheronGa"

/*: "broadStatus" :*/
fileprivate let kStr_toFrameData:String = "brouser"

/*: "onCallChatMsg" :*/
fileprivate let kStr_imageData:String = "mode time model frontonCallC"
fileprivate let kStr_layerName:String = "hatMsgcount complete gift line var"

/*: "onNotificationPopupWin" :*/
fileprivate let kStr_iconData:[Character] = ["o","n","N","o","t","i","f","i","c","a","t","i","o","n"]
fileprivate let kStr_shareTitle:String = "PopupWinformat pic height return sex"

/*: "onUserBlock" :*/
fileprivate let kStr_clipData:String = "onUserBloby equal i read"
fileprivate let kStr_sizeName:String = "cshared"

/*: "freeCallWin" :*/
fileprivate let kStr_giftValue:[Character] = ["f","r","e","e","C","a","l","l","W"]
fileprivate let kStr_sectionInfoData:String = "iview"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ToSocketManager.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let kLet_clickText = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let kLet_screenValue = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let kLet_bottomProfileData = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let kLet_deviceName = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let kLet_marginTitle = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let kLet_topData = (kStr_washValue.replacingOccurrences(of: "shadow", with: "O") + "T_ADD" + String(kStr_aName.prefix(5)))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let kLet_changeValue = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let kLet_successContent = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let kLet_recordText = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let kLet_qualityData = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let kLet_attributeScreenTitle = NSNotification.Name(rawValue: String(bytes: kStr_topTouchDataValue.reversed(), encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let kLet_informationBottomScreenName = NSNotification.Name(rawValue: String(bytes: kStr_commentViewName.map{playerForce(center: $0)}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol CanErrorDelegate: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func recieve(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol BackgroundMatchDelegate: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func tableData(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func socketSpringTask(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol ManagerCallerDelegate: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func dataTextItem(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func stopButton(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func constituent(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func asShow(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol RuggedizationPartyDelegate: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func crop(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func greet(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func currentIn(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class ToSocketManager: NSObject {
    //: enum SocketRetryType: String {
    enum LocationType: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currTalkingVC: AnyObject?
    var currTalkingVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: DeleteClientThen.shared.direction == .rightToLeft ? 0 : kLet_halfData,
        //: y: StatusBarHeight+190
        y: kLet_postPartyValue + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: DeleteClientThen.shared.direction == .rightToLeft ? 0 : kLet_halfData,
        //: y: StatusBarHeight+190
        y: kLet_postPartyValue + 190
    )
    //: var partyMiniPosition = CGPoint(
    var partyMiniPosition = CGPoint( // 语聊房小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: DeleteClientThen.shared.direction == .rightToLeft ? 0 : kLet_halfData,
        //: y: StatusBarHeight+500
        y: kLet_postPartyValue + 500
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: CanErrorDelegate? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: BackgroundMatchDelegate? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: ManagerCallerDelegate? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: RuggedizationPartyDelegate? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    //: @objc dynamic var isTalking = false
    @objc dynamic var isTalking = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = ToSocketManager()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(domain(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(queryToWebsocket),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(kStr_postImageValue) + String(kStr_showData)))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (LayerKingfisherCompatible.shared as! LayerKingfisherCompatible).imageAdd()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isTalking")
            .observeWeakly(Bool.self, (String(kStr_equalData.suffix(5)) + "king"))
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (LayerKingfisherCompatible.shared as! LayerKingfisherCompatible).imageAdd()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension ToSocketManager: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: MiniLoadThen.appLine(msg: "SOCKET 连接成功.")
        MiniLoadThen.appLine(msg: (kStr_sharedData.replacingOccurrences(of: "hidden", with: "S") + String(kStr_intimateTableData.prefix(6)) + "接成功."))
        //: sendPing()
        anyLine()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: kLet_noData,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: kLet_topData)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        afterKeyView()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: MiniLoadThen.appLine(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        MiniLoadThen.appLine(msg: (kStr_exclusiveTitle.replacingOccurrences(of: "cancel", with: "O") + kStr_contentInnerViewData + String(kStr_toData)) + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = LocationThen.share.reachability?.connection, status != .unavailable else {
        guard let status = LocationThen.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == kLet_deviceName {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = squareMoment(isRetry: true)
            //: MiniLoadThen.appLine(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            MiniLoadThen.appLine(msg: (kStr_viewRefreshValue.replacingOccurrences(of: "laboratory", with: "S") + "ET 失败") + "\(kLet_deviceName)" + (String(kStr_labValue)) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.duringText(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.upMessage(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: MiniLoadThen.appLine(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        MiniLoadThen.appLine(msg: (String(kStr_textName) + String(kStr_screenValue) + String(kStr_quoteValue.suffix(8))) + "\(code)" + (kStr_whiteTitleContent.replacingOccurrences(of: "count", with: ",") + String(kStr_indexName)) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.atPush(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == kLet_qualityData {
            //: return
            return
        }
        //: if let status = LocationThen.share.reachability?.connection, status != .unavailable {
        if let status = LocationThen.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.duringText(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func squareMoment(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = LocationThen.share.appConfigMode.ws
        self.linkAddrArr = LocationThen.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = willAdd()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: kLet_topData)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = LocationThen.share.loginUid
        let uid = LocationThen.share.loginUid
        //: let sid = LocationThen.share.loginSessionId
        let sid = LocationThen.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(kStr_normalBackName.prefix(6))) + "\(sid)" + (String(kStr_equalValue.prefix(5))) + "\(uid)" + (String(kStr_textValue.suffix(4)) + "tfor" + String(kStr_containerLastData.prefix(6)) + String(kStr_liveTitle)) + "\(kLet_inviteTitle)" + (String(kStr_titleValue.prefix(5)) + "ageId=") + "\(kLet_guideDoingTitle)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func willAdd() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return kLet_barVoiceValue ? [(String(kStr_partyName.suffix(6)) + "ver.") + "\(kLet_priceName)" + (String(kStr_hiddenYearValue.suffix(5)) + "7766")] : [(kStr_playerData.replacingOccurrences(of: "more", with: "3") + kStr_shareData.capitalized + ".194:" + kStr_iconTextValue.replacingOccurrences(of: "error", with: "77"))]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension ToSocketManager {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func domain(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: MiniLoadThen.appLine(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            MiniLoadThen.appLine(msg: String(bytes: kStr_transformName.map{mainBean(to: $0)}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.atPush(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.duringText(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func queryToWebsocket() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.duringText(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.blockList()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension ToSocketManager {
    /// socket保活心跳包
    //: private func sendPing() {
    private func anyLine() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: kLet_clickText, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.lessNutsAndBolts(info: [(kStr_successValue.replacingOccurrences(of: "manager", with: "m")): (String(kStr_leadingSinceName))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func toRow() {
        //: endTalkingPing()
        disappear()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: kLet_screenValue, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isTalking || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currTalkingVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currTalkingVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isTalking || (self.giftFew()?.isKind(of: ShadowErrorDelegate.self) == false && self.giftFew()?.isKind(of: SucceedRecognizerDelegate.self) == false) && (self.currTalkingVC?.isKind(of: ShadowErrorDelegate.self) == false && self.currTalkingVC?.isKind(of: SucceedRecognizerDelegate.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.lessNutsAndBolts(info: [(kStr_successValue.replacingOccurrences(of: "manager", with: "m")): (String(kStr_actionData) + String(kStr_sizeText))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func disappear() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func blockList() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(kStr_userTitle.replacingOccurrences(of: "count", with: "r") + "vedDat" + String(kStr_makeText))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < kLet_buttonData {
                //: LocationThen.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                LocationThen.share.queryed(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension ToSocketManager {
    /// 开启直播心跳包
    //: func startLivePing() {
    func revolutionaryCalendarMonthRequest() {
        //: endLivePing()
        appearIn()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: kLet_bottomProfileData, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.lessNutsAndBolts(info: [(kStr_successValue.replacingOccurrences(of: "manager", with: "m")): (String(kStr_containerData.suffix(6)) + "artB" + kStr_regularData.replacingOccurrences(of: "click", with: "ea"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func appearIn() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension ToSocketManager {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func afterKeyView() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = PhonationContainerView()
        //: let keyWindow = TableMacroDefine.getWindow()
        let keyWindow = TableMacroDefine.oeilDeBoeufView()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isTalking
        self.containerView?.isHidden = !self.isTalking
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func miniSize() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中/语聊房
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard VideoPushListener.isGesture().isLive == false else { return }
        //: guard TalkingVoiceRoomManager.shared().isParty == false else { return }
        guard SubCheckedReactiveCompatible.replyShared().isParty == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension ToSocketManager {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func atPush(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        pathLine(message: (kStr_equalText + " sock" + kStr_menuValue.lowercased()))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: kLet_qualityData, reason: (String(kStr_pleaseTitle) + String(kStr_viewName.prefix(6)) + "r lo" + String(kStr_pathValue.suffix(6))))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        disappear()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            miniSize()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func statusSocket() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = ToSocketManager.shared.squareMoment(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.duringText(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func duringText(timeInterval: TimeInterval, type: LocationType) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: MiniLoadThen.appLine(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            MiniLoadThen.appLine(msg: (String(kStr_makeFromText.prefix(7))) + "\(type.rawValue)" + (String(kStr_ofText) + kStr_withStyleValue) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.progressTime(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func progressTime(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: MiniLoadThen.appLine(msg: "SOCKET 连接正常 不需要重新连接.")
                MiniLoadThen.appLine(msg: (String(kStr_frameContent) + kStr_inputName.capitalized + String(kStr_toTitle)))
                //: } else {
            } else {
                //: MiniLoadThen.appLine(msg: "SOCKET 正在连接 不需要重新连接.")
                MiniLoadThen.appLine(msg: (String(kStr_labelData) + String(kStr_videoTitle)))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isTalking {
        if self.isTalking {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(kStr_showAlwaysText.suffix(6)) + String(kStr_byTitle.suffix(3))))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(kStr_colorStreamValue.suffix(6))) + "\(ViewManagerAddrTool.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        atPush(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = kLet_marginTitle
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: MiniLoadThen.appLine(msg: "SOCKET 开始连接, 地址：\(newStr).")
        MiniLoadThen.appLine(msg: (kStr_userData.replacingOccurrences(of: "cell", with: "SO") + " 开始连接" + String(kStr_beforeValue)) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func lessNutsAndBolts(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if LocationThen.share.reachability?.connection == nil || LocationThen.share.reachability?.connection == .unavailable {
        if LocationThen.share.reachability?.connection == nil || LocationThen.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(kStr_successValue.replacingOccurrences(of: "manager", with: "m")): (String(kStr_byData.suffix(6)) + kStr_moreTitle.replacingOccurrences(of: "voice", with: "i")),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (kStr_timeText.replacingOccurrences(of: "live", with: "n")): "\(kLet_recordText)",
                                       //: "data": [:]]
                                       (String(kStr_viewContent)): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            upMessage(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(kStr_successValue.replacingOccurrences(of: "manager", with: "m"))] as? String == (String(kStr_leadingSinceName)) {
                //: MiniLoadThen.appLine(msg: "SOCKET 连接保活cmd: ping.")
                MiniLoadThen.appLine(msg: (kStr_dataVisibleText.replacingOccurrences(of: "gift", with: "E") + "T 连接\u{4fdd}" + String(kStr_nameEqualData)))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(kStr_successValue.replacingOccurrences(of: "manager", with: "m"))] as? String == (String(kStr_actionData) + String(kStr_sizeText)) {
                //: MiniLoadThen.appLine(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                MiniLoadThen.appLine(msg: String(bytes: kStr_makeTableIfValue.map{rawData(manager: $0)}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(kStr_successValue.replacingOccurrences(of: "manager", with: "m"))] as? String == (String(kStr_containerData.suffix(6)) + "artB" + kStr_regularData.replacingOccurrences(of: "click", with: "ea")) {
                //: MiniLoadThen.appLine(msg: "SOCKET live保活cmd: liveHeartBeat.")
                MiniLoadThen.appLine(msg: String(bytes: kStr_senseHalfValue.map{$0^78}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(kStr_successValue.replacingOccurrences(of: "manager", with: "m"))] as? String == (String(kStr_leadingSinceName)) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(kStr_successValue.replacingOccurrences(of: "manager", with: "m")): (String(kStr_buttonNameIfData.prefix(3))),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (kStr_timeText.replacingOccurrences(of: "live", with: "n")): "\(kLet_changeValue)",
                                           //: "data": [:]]
                                           (String(kStr_viewContent)): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                upMessage(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.duringText(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func upMessage(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(kStr_successValue.replacingOccurrences(of: "manager", with: "m"))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(kStr_timeText.replacingOccurrences(of: "live", with: "n"))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(kStr_digitalBlockValue.replacingOccurrences(of: "value", with: "sg"))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(String(kStr_viewContent))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        pathLine(message: (String(kStr_showText) + String(kStr_bottomData)) + "\(cmdName)" + (String(kStr_ofName.suffix(6)) + String(kStr_fatalValue.suffix(2))) + "\(errorNo)" + (String(kStr_cornerData)) + "\(msg)" + (kStr_detailValue.capitalized + String(kStr_numberName.prefix(5))) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        watchReLodgeInHandle(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: MiniLoadThen.appLine(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            MiniLoadThen.appLine(msg: (kStr_cellData + String(kStr_videoContent)) + "\(cmdName)" + (String(kStr_ofName.suffix(6)) + String(kStr_fatalValue.suffix(2))) + "\(errorNo)" + (String(kStr_cornerData)) + "\(msg)" + (kStr_detailValue.capitalized + String(kStr_numberName.prefix(5))) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: LocationThen.share.loginUserMode.freeCallTimes = 0
                LocationThen.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !LocationThen.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !LocationThen.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                ClientBottomReactiveCompatible.shared.destabilisation()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.recieve(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(kStr_imageTextValue) + kStr_mValue.replacingOccurrences(of: "selected", with: "v") + String(kStr_tempValue)) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: kLet_statusOfName, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(kStr_dataValue) + String(kStr_toolData.prefix(7))) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.tableData(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(kStr_textData) + kStr_scaleName.lowercased()) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.socketSpringTask(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(kStr_userText) + String(kStr_successName.prefix(7))) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(kStr_readData) + String(kStr_colorErrorValue.suffix(7))) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(kStr_nameBorderValue) + String(kStr_chooseText)) { // 接收方发送acceptCall回执
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            toRow()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.greet(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(kStr_addText.prefix(6)) + "Call") { // 接收方发送stopCall回执
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            disappear()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(kStr_messageSizeData.suffix(8)) + String(kStr_styleName))] as? Int {
                //: LocationThen.share.loginUserMode.freeCallTimes = freeCallTimes
                LocationThen.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: LocationThen.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            LocationThen.share.loginUserMode.mf_coin = json[(String(kStr_viewContent))][(String(kStr_mName.prefix(6)))].stringValue
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kLet_detailContent, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (LayerKingfisherCompatible.shared as! LayerKingfisherCompatible).deadlineReport()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(kStr_monthProfileName.suffix(5)) + "sCoin") { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.asShow(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (String(kStr_contentText) + String(kStr_countValue)) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            toRow()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.dataTextItem(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(kStr_topicValue) + kStr_backgroundValue.lowercased()) { // 接收方/拨打方 挂断了通话
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            disappear()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.stopButton(data: data)
            // 更新余额
            //: LocationThen.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            LocationThen.share.loginUserMode.mf_coin = json[(String(kStr_viewContent))][(String(kStr_mName.prefix(6)))].stringValue

            //: isShowFreeStopCallPopUpView(data: data)
            duringCapacity(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kLet_detailContent, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (LayerKingfisherCompatible.shared as! LayerKingfisherCompatible).deadlineReport()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (String(kStr_sessionName) + String(kStr_pushMakeName.suffix(7))) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: kLet_attributeScreenTitle, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: LocationThen.share.recieve1v1TalkRequest(info: data)
                LocationThen.share.queryed(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(kStr_userTitle.replacingOccurrences(of: "count", with: "r") + "vedDat" + String(kStr_makeText))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: kStr_labelPlayerValue.map{$0^186}, encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(String(kStr_viewContent))][(String(kStr_sceneText))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: kStr_titleSucceedValue.map{$0^189}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                ItemLineBaseDelegate.regiment(uid: String(json[(String(kStr_viewContent))][(kStr_infoContent.lowercased())].intValue),
                                           //: title: data["nickname"] as? String,
                                           title: data[(String(kStr_timeContent))] as? String,
                                           //: body: body,
                                           body: body,
                                           //: imageUrl: data["headPic"] as? String)
                                           imageUrl: data[(String(kStr_textEventData) + kStr_duringText.replacingOccurrences(of: "video", with: "c"))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                ItemLineBaseDelegate.shared.toNeed()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(kStr_listViewValue.suffix(6)) + String(kStr_rawData)) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            toRow()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(kStr_listViewValue.suffix(6)) + String(kStr_rawData))] = "1"
            //: LocationThen.share.recieve1v1TalkResumeRequest(info: resumeData)
            LocationThen.share.memoryFill(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (LayerKingfisherCompatible.shared as! LayerKingfisherCompatible).imageAdd()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (String(kStr_nameData) + String(kStr_viewFramePushText)) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            disappear()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.constituent(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                ItemLineBaseDelegate.mTheoryIdentifier(identifier: String(json[(String(kStr_viewContent))][(kStr_infoContent.lowercased())].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                ItemLineBaseDelegate.shared.stopActionDismiss()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (String(kStr_backName)) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            disappear()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.crop(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(kStr_imageHalfLabValue.prefix(6)) + "Motiv" + kStr_emptyName.replacingOccurrences(of: "pin", with: "io")) { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if LocationThen.share.loginUserMode.sex == Gender.female.rawValue {
            if LocationThen.share.loginUserMode.sex == SucceedComparable.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.currentIn?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(kStr_startValue) + String(kStr_downData)) { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            VideoPushListener.sharedPlayer(content: msg, reason: json[(String(kStr_viewContent))][(kStr_customValue.replacingOccurrences(of: "crop", with: "as"))].stringValue, time: json[(String(kStr_viewContent))][(kStr_photoData.replacingOccurrences(of: "view", with: "e"))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(kStr_enableName.suffix(5)) + String(kStr_indexTitle)) { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            VideoPushListener.isGesture().outAssets()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(kStr_rowName.suffix(5)) + String(kStr_clickGreetData)) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.property(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(kStr_topTitle.suffix(4)) + "meFs") { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(kStr_toFrameData.replacingOccurrences(of: "user", with: "ad") + "Status")] as? Int ?? -1
            //: if LocationThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if LocationThen.share.appStatus == SharedInsetTarget.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: kLet_informationBottomScreenName, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: kLet_statusOfName, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: kLet_informationBottomScreenName, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(kStr_imageData.suffix(7)) + String(kStr_layerName.prefix(6))) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            AddDanmuManager.photo().imageMsg(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(kStr_iconData) + String(kStr_shareTitle.prefix(8))) { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            ClientBottomReactiveCompatible.shared.blockAcrossDic(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(kStr_clipData.prefix(9)) + kStr_sizeName.replacingOccurrences(of: "shared", with: "k")) { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            VideoPushListener.isGesture().outAssets()
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            SubCheckedReactiveCompatible.replyShared().viewBy()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: kLet_statusFailData, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension ToSocketManager {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func watchReLodgeInHandle(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (String(kStr_sessionName) + String(kStr_pushMakeName.suffix(7))) || cmdName == (String(kStr_textData) + kStr_scaleName.lowercased()) {
            //: if TalkingLiveManager.shared().isLive == true {
            if VideoPushListener.isGesture().isLive == true {
                //: NEED_RESTORE_LIVE = true
                kLet_agentValue = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                VideoPushListener.drawUp(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                VideoPushListener.isGesture().outAssets()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard kLet_agentValue == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (String(kStr_nameData) + String(kStr_viewFramePushText)) || cmdName == (String(kStr_userText) + String(kStr_successName.prefix(7))) ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (String(kStr_backName)) || cmdName == (String(kStr_readData) + String(kStr_colorErrorValue.suffix(7))) ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (String(kStr_topicValue) + kStr_backgroundValue.lowercased()) || cmdName == (String(kStr_addText.prefix(6)) + "Call")
        {
            //: NEED_RESTORE_LIVE = false
            kLet_agentValue = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = MakeAlertView(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                VideoPushListener.isGesture().nurseLogHandler()
            }
            //: alert.show()
            alert.clickFor()
        }
    }

    /// 免费1分钟通话结束弹窗
    //: private func isShowFreeStopCallPopUpView(data: [String: Any]) {
    private func duringCapacity(data: [String: Any]) {
        //: if let freeCallTimes = data["freeCallTimes"] as? Int {
        if let freeCallTimes = data[(String(kStr_messageSizeData.suffix(8)) + String(kStr_styleName))] as? Int {
            //: LocationThen.share.loginUserMode.freeCallTimes = freeCallTimes
            LocationThen.share.loginUserMode.freeCallTimes = freeCallTimes
        }
        //: let freeCallWin = data["freeCallWin"] as? Bool
        let freeCallWin = data[(String(kStr_giftValue) + kStr_sectionInfoData.replacingOccurrences(of: "view", with: "n"))] as? Bool
        //: if freeCallWin == true && LocationThen.share.loginUserMode.sex == Gender.male.rawValue {
        if freeCallWin == true, LocationThen.share.loginUserMode.sex == SucceedComparable.male.rawValue {
            //: let view = TalkingFreeStopCallPopUpView.init()
            let view = LineView()
            //: view.show()
            view.outsideMatch()
            //: view.setShowData(data: data)
            view.touch(data: data)
        }
    }
}
