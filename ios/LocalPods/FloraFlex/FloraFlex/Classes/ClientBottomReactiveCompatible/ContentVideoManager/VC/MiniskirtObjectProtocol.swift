
//: Declare String Begin

/*: "cmd" :*/
fileprivate let kStr_modelName:[UInt8] = [0x85,0x8b,0x82]

private func sourceOff(view num: UInt8) -> UInt8 {
    return num ^ 230
}

/*: "cancelCall" :*/
fileprivate let kStr_name:String = "titleantitlee"

/*: "data" :*/
fileprivate let kStr_lineData:[UInt8] = [0x9a,0x9f,0x8a,0x9f]

private func colorView(head num: UInt8) -> UInt8 {
    return num ^ 254
}

/*: "logId" :*/
fileprivate let kStr_indexName:String = "logIdweight key sun equal app"

/*: "#322870" :*/
fileprivate let kStr_bubbleValue:String = "#322870"

/*: "Tap to start video chatting" :*/
fileprivate let kStr_imageContent:String = "Tap gift succeed var mode"
fileprivate let kStr_gestureName:String = "message lab insert downart "
fileprivate let kStr_timeData:String = "hattimageg"

/*: "icon_close_nor" :*/
fileprivate let kStr_userName:String = "icon_cvar background type on"
fileprivate let kStr_fromValue:String = "detail"
fileprivate let kStr_fatalName:String = "regular info image name elseose_nor"

/*: "icon_views_number" :*/
fileprivate let kStr_pathDomainName:String = "icoop"
fileprivate let kStr_groupViewTitle:[Character] = ["_","v","i","e","w","s","_","n","u","m","b","e","r"]

/*: "%@ chatting" :*/
fileprivate let kStr_equalText:String = "share right appear%@ cha"

/*: "icon_lounge" :*/
fileprivate let kStr_labButtonName:String = "pop"
fileprivate let kStr_giftValue:String = "con_lotop layer lab view shared"

/*: "icon_rank_coin" :*/
fileprivate let kStr_sectionValue:String = "label display posticon_"
fileprivate let kStr_succeedData:[Character] = ["o","i","n"]

/*: "vip_btn_top" :*/
fileprivate let kStr_viewScaleContent:[Character] = ["v","i","p","_","b","t","n","_","t","o","p"]

/*: "btn_nearby_settings_nor" :*/
fileprivate let kStr_imageDownName:String = "text sharebtn_"
fileprivate let kStr_valueText:String = "by_scolor succeed view true float"
fileprivate let kStr_closeWhiteText:[Character] = ["o","r"]

/*: "%@ Coins/min" :*/
fileprivate let kStr_modeData:[Character] = ["%","@"," ","C","o","i","n","s","/","m","i","n"]

/*: "get json error" :*/
fileprivate let kStr_labFieldValue:String = "get if section type label day"
fileprivate let kStr_appOffHiddenTitle:String = "user value view import error"

/*: "matchId" :*/
fileprivate let kStr_modelText:String = "MA"
fileprivate let kStr_backValue:String = "tchIditem selected"

/*: "errno" :*/
fileprivate let kStr_makeValue:[UInt8] = [0xb4,0xa3,0xa3,0xbf,0xbe]

private func successTemp(to num: UInt8) -> UInt8 {
    return num ^ 209
}

/*: "VIPFee" :*/
fileprivate let kStr_languageName:String = "VIPFeepath true follow show"

/*: "Matching now" :*/
fileprivate let kStr_ofName:String = "Matchiindex self play send"

/*: "userTouch" :*/
fileprivate let kStr_tempInfoTitle:String = "userTouself true title super"
fileprivate let kStr_groupTitle:[Character] = ["c","h"]

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let kStr_originAgencyValue:[Character] = ["m","f","/","f","r","e","e","C","a","l","l","/"]
fileprivate let kStr_shareData:String = "getVif conversation guard"

/*: "fromType" :*/
fileprivate let kStr_screenValue:[UInt8] = [0xad,0xb9,0xb6,0xb4,0x9b,0xc0,0xb7,0xac]

fileprivate func redManager(shared num: UInt8) -> UInt8 {
    let value = Int(num) - 71
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "You've been barred from receiving calls" :*/
fileprivate let kStr_contentValue:[UInt8] = [0x1,0x17,0x1d,0xcf,0x1e,0xd,0xc8,0xa,0xd,0xd,0x16,0xc8,0xa,0x9,0x1a,0x1a,0xd,0xc,0xc8,0xe,0x1a,0x17,0x15,0xc8,0x1a,0xd,0xb,0xd,0x11,0x1e,0x11,0x16,0xf,0xc8,0xb,0x9,0x14,0x14,0x1b]

fileprivate func infoAdd(label num: UInt8) -> UInt8 {
    let value = Int(num) + 88
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "source" :*/
fileprivate let kStr_showTitle:[UInt8] = [0xa5,0xb9,0xa3,0xa4,0xb5,0xb3]

private func northNorthwest(property num: UInt8) -> UInt8 {
    return num ^ 214
}

/*: "type" :*/
fileprivate let kStr_toData:[UInt8] = [0xa6,0xab,0xa2,0xb7]

/*: "uid" :*/
fileprivate let kStr_countValue:[UInt8] = [0x64,0x69,0x75]

/*: "fromFreeCall" :*/
fileprivate let kStr_statusName:String = "view letfromFreeC"
fileprivate let kStr_makeShowContent:String = "triumphll"

/*: "requestCall" :*/
fileprivate let kStr_appTitle:String = "addequ"
fileprivate let kStr_screenTitle:[Character] = ["e","s","t","C","a","l","l"]

/*: "onVideoMatch" :*/
fileprivate let kStr_henName:String = "onVideto next to"
fileprivate let kStr_atText:[Character] = ["o"]
fileprivate let kStr_shouldData:[Character] = ["M","a","t","c","h"]

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let kStr_resultBackName:[UInt8] = [0x99,0x7f,0xb3,0xb2,0xad,0xab,0x9f,0xb2,0xa7,0xa1,0x5e,0xb4,0xa7,0xa2,0xa3,0xad,0x9b,0x5e,0x90,0xa3,0xa1,0xa3,0xa7,0xb4,0xa3,0x5e,0xb4,0xa7,0xa2,0xa3,0xad,0x5e,0xa1,0x9f,0xaa,0xaa,0x5e,0xa7,0xac,0xa1,0xad,0xab,0xa7,0xac,0xa5]

fileprivate func scaleValue(style num: UInt8) -> UInt8 {
    let value = Int(num) + 194
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "refuseCall" :*/
fileprivate let kStr_screenName:String = "REFUS"
fileprivate let kStr_taskValue:String = "eCallother return normal"

/*: "onCancelCall" :*/
fileprivate let kStr_pathText:[Character] = ["o","n","C","a","n","c","e","l","C","a","l","l"]

/*: "retryAfter" :*/
fileprivate let kStr_userValue:[Character] = ["r"]
fileprivate let kStr_textValue:String = "elabely"

/*: "onRequestCall" :*/
fileprivate let kStr_socialEqualName:String = "equal dismiss data from indexonR"
fileprivate let kStr_styleAddData:String = "eqabsest"

/*: "failType" :*/
fileprivate let kStr_methodName:String = "fmake"
fileprivate let kStr_viewValue:[Character] = ["i","l","T","y","p","e"]

/*: "callWaitingTime" :*/
fileprivate let kStr_minValue:String = "presentl"
fileprivate let kStr_makeContent:String = "lWaitoriginal message time equal path"

/*: "toUid" :*/
fileprivate let kStr_colorLabelValue:String = "view presenttoUid"

/*: "Free 1 min" :*/
fileprivate let kStr_showValue:[Character] = ["F","r","e","e"," ","1"," ","m","i","n"]

/*: "%@/min" :*/
fileprivate let kStr_failName:[Character] = ["%","@","/","m","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MiniskirtObjectProtocol.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class MiniskirtObjectProtocol: LayerRecognizerDelegate {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.themePrice()
            }
        }
        //: self.resetCamera()
        self.dataDelayedAction()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        equalRate()
        //: setupSubViewsConstraint()
        upConstraint()
        //: bindInteraction()
        horseradishRoot()
        //: videoManager.startPreview(isMatch: true)
        videoManager.totalry(isMatch: true)
        //: setPlayer(isRandon: false)
        setToRandon(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            photoVideo()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            ToSocketManager.shared.lessNutsAndBolts(info: [String(bytes: kStr_modelName.map{sourceOff(view: $0)}, encoding: .utf8)!: (kStr_name.replacingOccurrences(of: "title", with: "c") + "lCall"), String(bytes: kStr_lineData.map{colorView(head: $0)}, encoding: .utf8)!: [(String(kStr_indexName.prefix(5))): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        alongLoadTimer()
        //: if TalkingSocketManager.shared.isTalking == false {
        if ToSocketManager.shared.isTalking == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            ToSocketManager.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            ToSocketManager.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            ToSocketManager.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            ToSocketManager.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: AdministratorCloudDelegate = {
        //: let m = TalkingVideoChatManager()
        let m = AdministratorCloudDelegate()
        //: let info = TalkingVideoChatModel()
        let info = TitleGiftChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .dataFormat()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(photoVideo), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: kLet_halfData + 1, height: kLet_noSeatName + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (kStr_bubbleValue.capitalized))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(kStr_imageContent.prefix(4)) + "to st" + String(kStr_gestureName.suffix(4)) + "video c" + kStr_timeData.replacingOccurrences(of: "image", with: "in")).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.bundleBy(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.bundleBy(name: (String(kStr_userName.prefix(6)) + kStr_fromValue.replacingOccurrences(of: "detail", with: "l") + String(kStr_fatalName.suffix(7)))), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(rededicate), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: OverdoTalkingButton = {
        //: let btn = TalkingButton()
        let btn = OverdoTalkingButton()
        //: btn.setImage(UIImage.bundleBy(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.bundleBy(name: (kStr_pathDomainName.replacingOccurrences(of: "op", with: "n") + String(kStr_groupViewTitle))), for: .normal)
        //: btn.setImage(UIImage.bundleBy(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.bundleBy(name: (kStr_pathDomainName.replacingOccurrences(of: "op", with: "n") + String(kStr_groupViewTitle))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.groupSize(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(kStr_equalText.suffix(6)) + "tting").imageArguments(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.bundleBy(name: "icon_lounge")
        img.image = UIImage.bundleBy(name: (kStr_labButtonName.replacingOccurrences(of: "pop", with: "i") + String(kStr_giftValue.prefix(6)) + "unge"))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.bundleBy(name: "icon_rank_coin")
        img.image = UIImage.bundleBy(name: (String(kStr_sectionValue.suffix(5)) + "rank_c" + String(kStr_succeedData)))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.groupSize(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.bundleBy(name: "vip_btn_top")
        imgV.image = UIImage.bundleBy(name: (String(kStr_viewScaleContent)))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: OverdoTalkingButton = {
        //: let btn = TalkingButton()
        let btn = OverdoTalkingButton()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = originalScene(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(accept), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.bundleBy(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.bundleBy(name: (String(kStr_imageDownName.suffix(4)) + "near" + String(kStr_valueText.prefix(4)) + "ettings_n" + String(kStr_closeWhiteText))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.bundleBy(name: "icon_rank_coin")
        imgV.image = UIImage.bundleBy(name: (String(kStr_sectionValue.suffix(5)) + "rank_c" + String(kStr_succeedData)))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .makeColor()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(kStr_modeData)).imageArguments(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.groupSize(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: MakeAlertView = //: return MakeAlertView(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension MiniskirtObjectProtocol {
    //: private func setPlayer(isRandon: Bool) {
    private func setToRandon(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = LoadGrappleEffectTool.default.sunnahType(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            pathLine(message: (String(kStr_labFieldValue.prefix(4)) + "json" + String(kStr_appOffHiddenTitle.suffix(6))))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func mature(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(kStr_modelText.lowercased() + String(kStr_backValue.prefix(5))): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: kStr_modelName.map{sourceOff(view: $0)}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: kStr_makeValue.map{successTemp(to: $0)}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        ContentVideoManager.bold(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.context()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(kStr_modelText.lowercased() + String(kStr_backValue.prefix(5)))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func untilMinimizeYear() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        ContentVideoManager.videoToCompletion { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(kStr_languageName.prefix(6)))].intValue
            //: self.refreshVideoCallPrice()
            self.themePrice()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension MiniskirtObjectProtocol {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func accept() {
        //: EffectPushManager.share.func__pushToSubscribeAlert()
        EffectPushManager.share.toShould()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func rededicate() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        velleityBtn()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        kLet_dismissValue.digitizerInsideStatus(eventID: kLet_barFaceText)

        //: if LocationThen.share.appConfigMode.displayMatchV4PopUp, LocationThen.share.loginUserMode.freeCallTimes > 0,
        if LocationThen.share.appConfigMode.displayMatchV4PopUp, LocationThen.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.clickFor()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        giftFew()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func photoVideo() {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard ToSocketManager.shared.isTalking == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.detailDownArray(showMsg: kLet_tableEndValue)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        liveBegin()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(kStr_ofName.prefix(6)) + "ng now").localized
        //: setPlayer(isRandon: true)
        setToRandon(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if LocationThen.share.loginUserMode.randomVideoType == 2 {
        if LocationThen.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            kLet_dismissValue.digitizerInsideStatus(eventID: kLet_netUserValue)
            //: req_getVideoInfo()
            showKey()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            kLet_dismissValue.digitizerInsideStatus(eventID: kLet_clickSystemText)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            ToSocketManager.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            ToSocketManager.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            ToSocketManager.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            ToSocketManager.shared.errorDelegate = self
            //: restart_randomVideo()
            deep()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func deep(cmd: String = (String(kStr_tempInfoTitle.prefix(7)) + String(kStr_groupTitle)), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        alongLoadTimer()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        mature(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func context() {
        // 停止定时器
        //: destroyWaitingTimer()
        alongLoadTimer()
        //: showCloseBtn()
        velleityBtn()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(kStr_imageContent.prefix(4)) + "to st" + String(kStr_gestureName.suffix(4)) + "video c" + kStr_timeData.replacingOccurrences(of: "image", with: "in")).localized
        //: setPlayer(isRandon: false)
        setToRandon(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isTalking == false {
        if ToSocketManager.shared.isTalking == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            ToSocketManager.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            ToSocketManager.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            ToSocketManager.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            ToSocketManager.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        themePrice()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func alongLoadTimer() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func showKey() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SizeRequestModel()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(kStr_originAgencyValue) + String(kStr_shareData.prefix(4)) + "ideoInfo")
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: kStr_screenValue.map{redManager(shared: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_firstName.afterFailureCompletion(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.context()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: LocationThen.share.loginUserMode.randomVideoType = 1
                    LocationThen.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.photoVideo()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.detailDownArray(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = ArtifactModel.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = TalkingSearchedPlayListener()
                //: player.setMute(bEnable: true)
                player.eraseEnable(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.cardView(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                LocationPathReactiveCompatible.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                PerspectiveThen.disappearEvent { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.tableExit()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func tableExit() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        kLet_dismissValue.digitizerInsideStatus(eventID: kLet_onTitle)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard LocationPathReactiveCompatible.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.detailDownArray(showMsg: String(bytes: kStr_contentValue.map{infoAdd(label: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            LocationPathReactiveCompatible.shared.timeByCompletion(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.context()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = ColourationViewDelegate()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.giftFew()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.context()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func liveBegin() {
        //: if LocationThen.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if LocationThen.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(LocationThen.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(LocationThen.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func velleityBtn() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func dataDelayedAction() {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard ToSocketManager.shared.isTalking == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.totalry()
        }
    }
}

// MARK: - BackgroundMatchDelegate

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension MiniskirtObjectProtocol: BackgroundMatchDelegate {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func tableData(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = BottomResultModel.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(kStr_modelText.lowercased() + String(kStr_backValue.prefix(5))): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: kStr_showTitle.map{northNorthwest(property: $0)}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: kStr_toData.map{$0^210}, encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: kStr_countValue.reversed(), encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = EndueProtectionBasicType.randow
        //: if LocationThen.share.loginUserMode.freeCallTimes > 0 && LocationThen.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if LocationThen.share.loginUserMode.freeCallTimes > 0 && LocationThen.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (String(kStr_statusName.suffix(9)) + kStr_makeShowContent.replacingOccurrences(of: "triumph", with: "a")))
            //: TalkingSocketManager.shared.isFreeCall = true
            ToSocketManager.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: kStr_modelName.map{sourceOff(view: $0)}, encoding: .utf8)!: (kStr_appTitle.replacingOccurrences(of: "add", with: "r") + String(kStr_screenTitle)), String(bytes: kStr_lineData.map{colorView(head: $0)}, encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        ToSocketManager.shared.lessNutsAndBolts(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            ToSocketManager.shared.lessNutsAndBolts(info: [String(bytes: kStr_modelName.map{sourceOff(view: $0)}, encoding: .utf8)!: (kStr_name.replacingOccurrences(of: "title", with: "c") + "lCall"), String(bytes: kStr_lineData.map{colorView(head: $0)}, encoding: .utf8)!: [(String(kStr_indexName.prefix(5))): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.deep(cmd: (String(kStr_henName.prefix(6)) + String(kStr_atText) + String(kStr_shouldData)))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func socketSpringTask(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(kStr_indexName.prefix(5)))].intValue
    }
}

// MARK: - ManagerCallerDelegate【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension MiniskirtObjectProtocol: ManagerCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func constituent(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func stopButton(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func asShow(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func dataTextItem(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            ItemLineBaseDelegate.regiment(body: String(bytes: kStr_resultBackName.map{scaleValue(style: $0)}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = BottomResultModel.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            responseEqual(toUid: String(uid))
        }
        //: stop_randomVideo()
        context()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.duringEqual()
        //: LocationThen.share.start1v1TalkCall(info: data, situation: .Calling)
        LocationThen.share.startCover(info: data, situation: .Calling)
    }
}

// MARK: - RuggedizationPartyDelegate【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension MiniskirtObjectProtocol: RuggedizationPartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func crop(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        deep(cmd: (kStr_screenName.lowercased() + String(kStr_taskValue.prefix(5))))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func greet(data _: [String: Any]) {}
}

// MARK: - CanErrorDelegate

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension MiniskirtObjectProtocol: CanErrorDelegate {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func recieve(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(kStr_pathText)) { // 取消通话异常
            //: stop_randomVideo()
            context()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(kStr_henName.prefix(6)) + String(kStr_atText) + String(kStr_shouldData)), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            context()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(kStr_modelText.lowercased() + String(kStr_backValue.prefix(5)))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.detailDownArray(showMsg: msg)
            //: uploadFailTime(type: 2)
            muffType(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(kStr_henName.prefix(6)) + String(kStr_atText) + String(kStr_shouldData)), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(kStr_userValue) + kStr_textValue.replacingOccurrences(of: "label", with: "tr") + "After")].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(kStr_modelText.lowercased() + String(kStr_backValue.prefix(5)))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.deep(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(kStr_socialEqualName.suffix(3)) + kStr_styleAddData.replacingOccurrences(of: "abs", with: "u") + "Call"), errorNo == CollectionNameConvertible.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            context()
            //: guard LocationThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard LocationThen.share.appStatus == SharedInsetTarget.normal.rawValue else { return }
            //: EffectPushManager.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            EffectPushManager.share.physicsLab(clickEvent: kLet_marginText, sufficient: false)
            //: uploadFailTime(type: 1)
            muffType(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        deep(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension MiniskirtObjectProtocol {
    //: func uploadFailTime(type: Int) {
    func muffType(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(kStr_methodName.replacingOccurrences(of: "make", with: "a") + String(kStr_viewValue))] = type
        //: parm["callWaitingTime"] = time
        parm[(kStr_minValue.replacingOccurrences(of: "present", with: "ca") + String(kStr_makeContent.prefix(5)) + "ingTime")] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        kLet_dismissValue.showTheme(eventID: kLet_deviceTitle, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func responseEqual(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(kStr_minValue.replacingOccurrences(of: "present", with: "ca") + String(kStr_makeContent.prefix(5)) + "ingTime")] = time
        //: parm["toUid"] = toUid
        parm[(String(kStr_colorLabelValue.suffix(5)))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        kLet_dismissValue.showTheme(eventID: kLet_marginValue, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension MiniskirtObjectProtocol {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func themePrice() {
        //: guard LocationThen.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard LocationThen.share.appStatus == SharedInsetTarget.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !LocationThen.share.loginUserMode.loungePlus
        vipIcon.isHidden = !LocationThen.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = EndueProtectionBasicType.randow
        //: if LocationThen.share.loginUserMode.freeCallTimes > 0 && LocationThen.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if LocationThen.share.loginUserMode.freeCallTimes > 0, LocationThen.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if LocationThen.share.appUserConfigMode.callBtnStyle == 1 {
            if LocationThen.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(kStr_showValue)).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = LocationThen.share.loginUserMode.loungePlus ? vipFee:fee
            let price = LocationThen.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(kStr_failName)).imageArguments(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.dorsum(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(kStr_failName)).imageArguments(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.dorsum(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = LocationThen.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = LocationThen.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = LocationThen.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = LocationThen.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !LocationThen.share.loginUserMode.loungePlus
            priceStackView.isHidden = !LocationThen.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func equalRate() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func upConstraint() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(originalScene(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(originalScene(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: originalScene(w: 240), height: originalScene(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: originalScene(w: 32), height: originalScene(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(originalScene(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(kLet_postPartyValue + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(originalScene(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func horseradishRoot() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(themePrice),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: kLet_sessionValue,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(untilMinimizeYear),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: kLet_endValue,
                                               //: object: nil)
                                               object: nil)
    }
}
