
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let kStr_nextMakeName:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Load..." :*/
fileprivate let kStr_errorInfoName:[Character] = ["L","o","a","d",".",".","."]

/*: "Skip" :*/
fileprivate let kStr_playName:[Character] = ["S","k","i","p"]

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let kStr_mValue:[UInt8] = [0xa,0x36,0x36,0x32,0x16,0x2d,0x2d,0x2e,0x6c,0xc,0x23,0x36,0x2b,0x34,0x27,0x16,0x2d,0x8,0x31,0x6a,0x65,0x30,0x27,0x21,0x2a,0x23,0x30,0x25,0x27,0x65,0x6b]

private func showData(app num: UInt8) -> UInt8 {
    return num ^ 66
}

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let kStr_mainNoText:[UInt8] = [0xfb,0xe5,0xe2,0xe8,0xe3,0xfb,0xa2,0xe3,0xe2,0xdc,0xed,0xeb,0xe9,0xdf,0xe4,0xe3,0xfb,0xaa,0xaa,0xe3,0xe2,0xdc,0xed,0xeb,0xe9,0xdf,0xe4,0xe3,0xfb,0xa4,0xa5,0xb7]

private func effectBirthCancel(lab num: UInt8) -> UInt8 {
    return num ^ 140
}

/*: "jsEvent(onPageShow):  :*/
fileprivate let kStr_viewValue:String = "jsEvent(oindex gift color model view"
fileprivate let kStr_normalTitle:String = "cell add intimatenPageSh"
fileprivate let kStr_name:[Character] = ["o","w",")",":"," "]

/*: --- :*/
fileprivate let kStr_imageLessTitle:[Character] = ["-","-","-"]

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let kStr_hiddenTitle:[UInt8] = [0xec,0xf2,0xf5,0xff,0xf4,0xec,0xb5,0xf4,0xf5,0xcb,0xfa,0xfc,0xfe,0xd3,0xf2,0xff,0xfe,0xbd,0xbd,0xf4,0xf5,0xcb,0xfa,0xfc,0xfe,0xd3,0xf2,0xff,0xfe,0xb3,0xb2,0xa0]

/*: "jsEvent(onPageHide):  :*/
fileprivate let kStr_appText:String = "cell normaljsEven"
fileprivate let kStr_sexText:[Character] = ["g","e","H","i","d","e",")",":"," "]

/*: "title" :*/
fileprivate let kStr_effectTitle:[Character] = ["t","i","t","l","e"]

/*: "canGoBack" :*/
fileprivate let kStr_userData:String = "canGouser image bottom self"

/*: "PHPSESSID= :*/
fileprivate let kStr_timeText:String = "showHshowS"
fileprivate let kStr_playTitle:[Character] = ["D","="]

/*: ";uid= :*/
fileprivate let kStr_listData:String = "jump half raw for;uid="

/*: "Cookie" :*/
fileprivate let kStr_listenerValue:String = "item"
fileprivate let kStr_buttonTitle:[Character] = ["o","o","k","i","e"]

/*: "nav_back_black_nor" :*/
fileprivate let kStr_pointData:String = "nav_bapoint tab in video if"
fileprivate let kStr_colorName:String = "kit request float left equalk_nor"

/*: "ico_intimate_list_sm" :*/
fileprivate let kStr_valuePageData:String = "should for app background leftico_in"
fileprivate let kStr_bottomPathContent:String = "translate as user to userst_sm"

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let kStr_succeedName:[UInt8] = [0xe3,0xe8,0xe4,0xf2,0xea,0xe2,0xe9,0xf3,0xa9,0xe0,0xe2,0xf3,0xc2,0xeb,0xe2,0xea,0xe2,0xe9,0xf3,0xf4,0xc5,0xfe,0xd3,0xe6,0xe0,0xc9,0xe6,0xea,0xe2,0xaf,0xa0,0xe5,0xe8,0xe3,0xfe,0xa0,0xae,0xdc,0xb7,0xda,0xa9,0xf4,0xf3,0xfe,0xeb,0xe2,0xa9,0xe5,0xe6,0xe4,0xec,0xe0,0xf5,0xe8,0xf2,0xe9,0xe3,0xba,0xa0,0xf5,0xe0,0xe5,0xe6,0xaf,0xb7,0xab,0xb7,0xab,0xb7,0xab,0xb7,0xae,0xa0]

private func textModel(block num: UInt8) -> UInt8 {
    return num ^ 135
}

/*: "Alert" :*/
fileprivate let kStr_renderValue:String = "effect location label colorAlert"

/*: "OK" :*/
fileprivate let kStr_makeVoiceValue:[Character] = ["O","K"]

/*: "Cancle" :*/
fileprivate let kStr_listValue:String = "Canclelive let full else return"

/*: "Done" :*/
fileprivate let kStr_cellName:[Character] = ["D","o","n","e"]

/*: "js call method name =  :*/
fileprivate let kStr_viewBottomTitle:[Character] = ["j","s"," ","c","a","l","l"," ","m","e","t","h","o","d"," "]
fileprivate let kStr_topModelName:[Character] = ["n","a","m","e"," ","="," "]

/*: , message =  :*/
fileprivate let kStr_sizeValue:String = ", mesself type instance color"
fileprivate let kStr_viewErrorValue:String = "as letsage = "

/*: "http://" :*/
fileprivate let kStr_centerName:String = "false else title type blockhttp://"

/*: "https://" :*/
fileprivate let kStr_locationName:[Character] = ["h"]
fileprivate let kStr_selectedName:String = "instance elsettps://"

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let kStr_searchName:[UInt8] = [0xa,0x3b,0x29,0x20,0x7d,0x20,0x5d,0x30,0x5b,0x29,0x27,0x3d,0x27,0x28,0x74,0x69,0x6c,0x70,0x73,0x2e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x20,0x6e,0x72,0x75,0x74,0x65,0x72,0x20,0x7b,0x20,0x29,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x28,0x6e,0x6f,0x69,0x74,0x63,0x6e,0x75,0x66,0x28,0x70,0x61,0x6d,0x2e,0x29,0x27,0x20,0x3b,0x27,0x28,0x74,0x69,0x6c,0x70,0x73,0x2e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x2e,0x74,0x6e,0x65,0x6d,0x75,0x63,0x6f,0x64,0x20,0x3d,0x20,0x73,0x65,0x6d,0x61,0x4e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x20,0x72,0x61,0x76]

/*: "uid= :*/
fileprivate let kStr_changeData:String = "main make disk view alluid="

/*: ;domain= :*/
fileprivate let kStr_makeTitle:String = "model for center block group;domain="

/*: ;path=/" :*/
fileprivate let kStr_addText:[Character] = [";","p","a","t","h","=","/"]

/*: "document.cookie=' :*/
fileprivate let kStr_mTextValue:String = "DOCUM"
fileprivate let kStr_showValue:[Character] = ["i","e","=","\'"]

/*: ';\n" :*/
fileprivate let kStr_dealTitle:[Character] = ["\'",";","\n"]

/*: ;domin= :*/
fileprivate let kStr_groupValue:[Character] = [";","d","o","m","i","n","="]

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let kStr_emptyIndexName:[UInt8] = [0xcc,0xc3,0x85,0x8d,0xc6,0xca,0xca,0xce,0xcc,0xc0,0xeb,0xc4,0xc8,0xc0,0xd6,0x8b,0xcc,0xcb,0xc1,0xc0,0xdd,0xea,0xc3,0x8d,0x82,0xf5,0xed,0xf5,0xf6,0xe0,0xf6,0xf6,0xec,0xe1,0x82,0x8c,0x85,0x98,0x98,0x85,0x88,0x94,0x8c,0x85,0xde,0x85,0xc1,0xca,0xc6,0xd0,0xc8,0xc0,0xcb,0xd1,0x8b,0xc6,0xca,0xca,0xce,0xcc,0xc0,0x98,0x82]

private func topRequest(add num: UInt8) -> UInt8 {
    return num ^ 165
}

/*: '; };\n" :*/
fileprivate let kStr_atNumberData:String = "\'; };"
fileprivate let kStr_removeNameTitle:String = "\n"

/*: '" :*/
fileprivate let kStr_countName:String = "\'"

/*: "h5Domain =  :*/
fileprivate let kStr_fillFemaleName:[Character] = ["h","5","D","o","m","a","i","n"," ","="," "]

/*: "getAllCookies =  :*/
fileprivate let kStr_toValue:[Character] = ["g","e","t","A","l","l","C","o","o","k"]
fileprivate let kStr_hiddenName:String = "ies = label currency asset"

/*: "PHPSESSID" :*/
fileprivate let kStr_withData:String = "PselectionPS"
fileprivate let kStr_pushText:String = "essid"

/*: "uid" :*/
fileprivate let kStr_textModelData:String = "UID"

/*: "appCookies =  :*/
fileprivate let kStr_nameData:[Character] = ["a","p","p","C","o","o","k","i"]
fileprivate let kStr_showData:[Character] = ["e","s"," ","="," "]

/*: "UID" :*/
fileprivate let kStr_halfText:String = "UIcontainer"

/*: "navigator.userAgent" :*/
fileprivate let kStr_mainPlayerName:[Character] = ["n","a","v","i","g","a","t","o","r",".","u","s","e","r","A","g","e","n","t"]

/*: "xunwu_iphone" :*/
fileprivate let kStr_arrayTextName:String = "addunwu"
fileprivate let kStr_keyHiddenTitle:String = "mic"
fileprivate let kStr_attentionData:[Character] = ["i","p","h","o","n","e"]

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let kStr_regularText:String = "xunwu_if open time name"
fileprivate let kStr_statusData:String = "guard effect playe xun"
fileprivate let kStr_barVisualFileName:String = "action typersion/"

/*:  xunwu_package/ :*/
fileprivate let kStr_serverName:String = "content target level xunwu"
fileprivate let kStr_countData:[Character] = ["k","a","g","e","/"]

/*:  xunwu_bundleId/ :*/
fileprivate let kStr_listName:[Character] = [" ","x","u","n","w","u","_","b","u","n","d","l","e","I","d","/"]

/*:   webkit/1" :*/
fileprivate let kStr_appName:[Character] = [" "," ","w","e","b"]
fileprivate let kStr_ofText:[Character] = ["k","i","t","/","1"]

/*: "  :*/
fileprivate let kStr_giftValue:String = " "

/*: " xunwu_device_id/ :*/
fileprivate let kStr_leadingName:[Character] = [" ","x","u","n","w","u","_","d","e","v","i"]
fileprivate let kStr_toText:String = "ce_id/at to"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ClientReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class ClientReactiveCompatible: WithViewController {
    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: CanPlumeConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: VariantLayerNameProtocol?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: VariantLayerNameProtocol?, webConfig: CanPlumeConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kStr_nextMakeName.reversed(), encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        viewImageKey()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        humanActivity()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(kStr_errorInfoName))
        //: addNotification()
        cube()
        //: func__checkConfig()
        appearStatusTime()
        //: func__setUpWebView()
        sharedLocal()
        //: func__registerObserver()
        visualiser()
        //: func__beginStartRequest()
        gaugeMessage()
        //: func_checkAppleIAP()
        edibleFruit()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: kLet_positionValue, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all
        webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            nameDown(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = false
        w.allowsBackForwardNavigationGestures = false
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.fromBuild(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.fromBuild(), .font: UIFont.groupSize(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(kStr_playName)).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension ClientReactiveCompatible {
    /// 添加通知
    //: private func addNotification() {
    private func cube() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(modelFrom),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: kLet_screenKeyHeightData,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(viewImageKey),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func modelFrom() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: kStr_mValue.map{showData(app: $0)}, encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func viewImageKey() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: kStr_mainNoText.map{effectBirthCancel(lab: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            pathLine(message: (String(kStr_viewValue.prefix(9)) + String(kStr_normalTitle.suffix(7)) + String(kStr_name)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func humanActivity() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: kStr_hiddenTitle.map{$0^155}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            pathLine(message: (String(kStr_appText.suffix(6)) + "t(onPa" + String(kStr_sexText)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension ClientReactiveCompatible {
    //: func func__registerObserver() {
    func visualiser() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (String(kStr_effectTitle)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(kStr_userData.prefix(5)) + "Back"))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(nextContent), name: kLet_detailContent, object: nil)
    }

    //: func func__setUpWebView() {
    func sharedLocal() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        ditheredColour()
        //: func__addBridgeMethod()
        basic()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.bottomValue()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            TintProgressHUD.fit(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(kLet_postPartyValue)
            }
        }
    }

    //: func func__beginStartRequest() {
    func gaugeMessage() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.playerRequest()
            }
            //: } else {
        } else {
            //: func__startRequest()
            playerRequest()
        }
    }

    //: func func__startRequest() {
    func playerRequest() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = LocationThen.share.loginUid
        let uid = LocationThen.share.loginUid
        //: let ssid = LocationThen.share.loginSessionId
        let ssid = LocationThen.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (kStr_timeText.replacingOccurrences(of: "show", with: "P") + "ESSI" + String(kStr_playTitle)) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(kStr_listData.suffix(5))) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (kStr_listenerValue.replacingOccurrences(of: "item", with: "C") + String(kStr_buttonTitle)))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        spectralColour()
        //: func__initRightButton()
        possessionButton()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func edibleFruit() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] type, reportMoney in
            ViewLayerManager.shared.completionHandle = { [weak self] type, reportMoney in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if type == .veritySucceed {
                if type == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney)
                    self.edibleFruitTo(price: reportMoney)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            ViewLayerManager.shared.selectDown()
        }
    }

    //: func func__initLeftButton() {
    func larboardButton() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.bundleBy(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.bundleBy(name: (String(kStr_pointData.prefix(6)) + "ck_blac" + String(kStr_colorName.suffix(5)))), style: .plain, target: self, action: #selector(titleCount(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func possessionButton() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(VariantLayerNameProtocol.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.bundleBy(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.bundleBy(name: (String(kStr_valuePageData.suffix(6)) + "timate_li" + String(kStr_bottomPathContent.suffix(5)))), style: .plain, target: self, action: #selector(sovietSocialistRepublicVideo))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func titleCount(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        effectGroupTechniqueMethod()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.giftFew() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: ClientReactiveCompatible.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! ClientReactiveCompatible).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! ClientReactiveCompatible).viewImageKey()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func appearStatusTime() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func ambient(sucess: Bool) {
        //: ProgressHUD.dismiss()
        TintProgressHUD.inkingPadDismiss()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func spectralColour() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: kStr_succeedName.map{textModel(block: $0)}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func tourlUrl(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: EffectPushManager.share.func__pushToWebVC(urlStr: url)
            EffectPushManager.share.deleteTake(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func americanState(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func nextContent() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: kLet_detailContent, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func sovietSocialistRepublicVideo() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = EraseViewDelegate(frame: CGRect(x: 0, y: 0, width: kLet_halfData, height: kLet_noSeatName))
        //: view.show()
        view.dataGift()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension ClientReactiveCompatible: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            TintProgressHUD.fit(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            ambient(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        larboardButton()
        //: func__clearJSBgColor()
        spectralColour()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            ambient(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            ambient(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(kStr_renderValue.suffix(5))), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(kStr_listValue.prefix(6))), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(kStr_cellName)).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension ClientReactiveCompatible: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        pathLine(message: (String(kStr_viewBottomTitle) + String(kStr_topModelName)) + "\(message.name)" + (String(kStr_sizeValue.prefix(5)) + String(kStr_viewErrorValue.suffix(7))) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.handleMessage(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func basic() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in SelectedComparable.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func effectGroupTechniqueMethod() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in SelectedComparable.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension ClientReactiveCompatible {
    //: func func__addUserScript() {
    func checkStubModel() {
        //: var htmlDomain: String = LocationThen.share.appConfigMode.urlH5Domain
        var htmlDomain: String = LocationThen.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(kStr_centerName.suffix(7)))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(kStr_centerName.suffix(7))), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((String(kStr_locationName) + String(kStr_selectedName.suffix(7)))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(kStr_locationName) + String(kStr_selectedName.suffix(7))), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = LocationThen.share.loginUserMode.userID
        let tempUid: String = LocationThen.share.loginUserMode.userID
        //: let tempSessionID: String = LocationThen.share.loginSessionId
        let tempSessionID: String = LocationThen.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: kStr_searchName.reversed(), encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(kStr_changeData.suffix(4))) + "\(tempUid)" + (String(kStr_makeTitle.suffix(8))) + "\(htmlDomain)" + (String(kStr_addText))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((kStr_mTextValue.lowercased() + "ent.cook" + String(kStr_showValue)) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (kStr_timeText.replacingOccurrences(of: "show", with: "P") + "ESSI" + String(kStr_playTitle)) + "\(tempSessionID)" + (String(kStr_groupValue)) + "\(htmlDomain)" + (String(kStr_addText))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((kStr_mTextValue.lowercased() + "ent.cook" + String(kStr_showValue)) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: kStr_emptyIndexName.map{topRequest(add: $0)}, encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(kStr_changeData.suffix(4))) + "\(tempUid)" + (String(kStr_makeTitle.suffix(8))) + "\(htmlDomain)" + (String(kStr_addText))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((kStr_mTextValue.lowercased() + "ent.cook" + String(kStr_showValue)) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(kStr_changeData.suffix(4))) + "\(tempSessionID)" + (String(kStr_makeTitle.suffix(8))) + "\(htmlDomain)" + (String(kStr_addText))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((kStr_mTextValue.lowercased() + "ent.cook" + String(kStr_showValue)) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func nameDown(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = LocationThen.share.appConfigMode.urlH5Domain
            let htmlDomain: String = LocationThen.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            pathLine(message: (String(kStr_fillFemaleName)) + "\(htmlDomain)")
            //: let userID: String = LocationThen.share.loginUserMode.userID
            let userID: String = LocationThen.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                pathLine(message: (String(kStr_toValue) + String(kStr_hiddenName.prefix(6))) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (kStr_withData.replacingOccurrences(of: "selection", with: "H") + kStr_pushText.uppercased()) {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (kStr_textModelData.lowercased()), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.gaugeMessage()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.brainpanExecutiveSession(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func brainpanExecutiveSession(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = LocationThen.share.appConfigMode.urlH5Domain
                let htmlDomain: String = LocationThen.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                pathLine(message: (String(kStr_fillFemaleName)) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: ViewManagerAddrTool.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                pathLine(message: (String(kStr_nameData) + String(kStr_showData)) + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (kStr_halfText.replacingOccurrences(of: "container", with: "D")), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (kStr_textModelData.lowercased())
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (kStr_withData.replacingOccurrences(of: "selection", with: "H") + kStr_pushText.uppercased()), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (kStr_withData.replacingOccurrences(of: "selection", with: "H") + kStr_pushText.uppercased())
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.gaugeMessage()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func ditheredColour() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((String(kStr_mainPlayerName))) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            pathLine(message: (String(kStr_mainPlayerName)))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((kStr_arrayTextName.replacingOccurrences(of: "add", with: "x") + kStr_keyHiddenTitle.replacingOccurrences(of: "mic", with: "_") + String(kStr_attentionData))) {
                let str = "xunwu_iphone xunwu_version/\(kLet_inviteTitle) xunwu_package/\(kLet_guideDoingTitle) xunwu_bundleId/\(kLet_listTitle)  webkit/1"
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.hideBlock { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(kStr_leadingName) + String(kStr_toText.prefix(6))) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }
}
