
//: Declare String Begin

/*: "bundle" :*/
fileprivate let kStr_sourceViewName:String = "bundatae"

/*: "nav_back_black_nor" :*/
fileprivate let kStr_homeData:String = "equal for name rawnav_b"
fileprivate let kStr_colorTitle:String = "black_norpop name edit tool if"

/*: "icon_translation_go" :*/
fileprivate let kStr_acceptName:String = "range let change image viewicon_"
fileprivate let kStr_fullBeforeContent:[Character] = ["t","i","o","n","_","g","o"]

/*: "iv_me_vip" :*/
fileprivate let kStr_failureSizeContent:[Character] = ["i","v","_","m","e","_","v","i","p"]

/*: "btn_dongtai_pinglun_nor" :*/
fileprivate let kStr_theValue:String = "btn_dobackground success"
fileprivate let kStr_sharedShareValue:[Character] = ["p","i","n","g","l","u","n","_","n","o","r"]

/*: "btn_me_new_edit" :*/
fileprivate let kStr_blockName:[Character] = ["b","t","n","_","m","e","_"]
fileprivate let kStr_makeData:String = "N"
fileprivate let kStr_appText:[Character] = ["e","w","_","e","d","i","t"]

/*: "btn_me_edit" :*/
fileprivate let kStr_loadName:String = "return user modelbtn_m"
fileprivate let kStr_galleryName:String = "e_editgift view transform to self"

/*: "icon_photo_giftbg" :*/
fileprivate let kStr_normalText:String = "right view gesture var equalicon_p"
fileprivate let kStr_finishContent:String = "_giftbgcell raw array"

/*: "icon_gift_more_arrow" :*/
fileprivate let kStr_viewTitle:[Character] = ["i","c","o","n","_","g","i"]
fileprivate let kStr_cellName:String = "ft_morplease bar"

/*: "bg_talk_other" :*/
fileprivate let kStr_voiceValue:[Character] = ["b","g","_","t","a"]
fileprivate let kStr_managerName:[Character] = ["l","k","_","o","t","h","e","r"]

/*: "bg_talk_me" :*/
fileprivate let kStr_progressErrorName:String = "main release break in selfbg_ta"
fileprivate let kStr_toValue:String = "bottom"

/*: "icon_talk_left_voive_1" :*/
fileprivate let kStr_reasonData:String = "subon"
fileprivate let kStr_buttonData:String = "leequal"
fileprivate let kStr_indexName:[Character] = ["_","1"]

/*: "icon_talk_left_voive_2" :*/
fileprivate let kStr_objectName:[Character] = ["i","c","o","n","_","t","a","l","k","_","l","e","f","t","_","v","o","i","v","e"]
fileprivate let kStr_equalValue:[Character] = ["_","2"]

/*: "icon_talk_left_voive_3" :*/
fileprivate let kStr_statusOfData:[Character] = ["i","c","o","n","_","t","a","l","k","_","l"]
fileprivate let kStr_withName:[Character] = ["e","f","t","_","v","o","i","v","e","_","3"]

/*: "icon_talk_right_voive_1" :*/
fileprivate let kStr_ageSourceText:[Character] = ["i","c","o","n","_","t","a","l","k","_","r","i","g","h"]
fileprivate let kStr_imageName:String = "t_vvar deadline present"
fileprivate let kStr_sectionData:[Character] = ["o","i","v","e","_","1"]

/*: "icon_talk_right_voive_2" :*/
fileprivate let kStr_familyData:[Character] = ["i","c","o","n","_","t","a","l","k","_","r","i","g","h","t","_","v","o"]
fileprivate let kStr_thumbTitle:String = "self status playerive_2"

/*: "icon_talk_right_voive_3" :*/
fileprivate let kStr_afterFrameData:[Character] = ["i","c","o","n","_","t","a","l","k","_","r","i","g","h","t","_"]
fileprivate let kStr_atImageName:[Character] = ["v","o","i","v","e","_","3"]

/*: "icon_gift_num_bg" :*/
fileprivate let kStr_rowRequestTitle:[Character] = ["i","c","o","n","_","g","i","f","t","_","n","u","m","_","b","g"]

/*: "group_someoneatme" :*/
fileprivate let kStr_pinContainerData:String = "groufemale"
fileprivate let kStr_infoErrorValue:String = "ncontenttme"

/*: "btn_back_white" :*/
fileprivate let kStr_borderName:String = "pic sendbtn_b"
fileprivate let kStr_totalBottomValue:String = "whiuser"

/*: "icon_VectorStroke" :*/
fileprivate let kStr_pathValue:[Character] = ["i","c","o","n","_"]
fileprivate let kStr_whiteName:String = "let directionVectorS"
fileprivate let kStr_sizeWithData:String = "tdataoke"

/*: "btnJQbEAB_etihw_kcab_shadow" :*/
fileprivate let kStr_equalTitle:String = "height model errorbtnJQb"
fileprivate let kStr_textName:String = "cell menuihw_"
fileprivate let kStr_eventUpTitle:String = "sizea"
fileprivate let kStr_moreMakeValue:[Character] = ["b","_","s","h","a","d","o","w"]

/*: "bg_talk_closed_tc_intimate" :*/
fileprivate let kStr_colorLabelData:String = "corner name from let shouldbg_t"
fileprivate let kStr_selectedName:String = "omoded"
fileprivate let kStr_coverName:String = "TIMATE"

/*: "icon_talk_reply_voive" :*/
fileprivate let kStr_makeGiftContent:String = "iengine"
fileprivate let kStr_liveValue:[Character] = ["o","n","_","t","a","l","k","_","r","e","p","l","y","_","v","o","i","v","e"]

/*: "ico_intimate_list_sm" :*/
fileprivate let kStr_sharedData:String = "source youico_in"
fileprivate let kStr_fatalName:String = "in image add equale_list_sm"

/*: "female_bg_random" :*/
fileprivate let kStr_valueName:String = "fsourceale"
fileprivate let kStr_userData:String = "ranneighborom"

/*: "party_mini_bg_userDetail" :*/
fileprivate let kStr_indexShareValue:[Character] = ["p","a","r","t","y","_","m","i","n","i","_","b","g","_","u"]
fileprivate let kStr_visualName:[Character] = ["s","e","r","D","e","t","a","i","l"]

/*: "CIGaussianBlur" :*/
fileprivate let kStr_closeTitle:[Character] = ["C","I","G","a","u","s","s","i","a","n","B","l","u","r"]

/*: "icon_mid_picture_default" :*/
fileprivate let kStr_fillTitle:String = "icon_min graduate sound"
fileprivate let kStr_makeValue:String = "ture_deadd extension send image"
fileprivate let kStr_timeName:String = "listult"

/*: "icon_big_picture_default" :*/
fileprivate let kStr_messageText:[Character] = ["i","c","o","n","_","b","i","g","_","p","i","c","t","u","r","e","_","d","e","f","a","u","l","t"]

/*: "icon_big_picture_square" :*/
fileprivate let kStr_mainValue:String = "icon_bcolor let margin app hidden"
fileprivate let kStr_noText:String = "ig_picblock image else file"
fileprivate let kStr_centerName:String = "equal observer count_square"

/*: "gift_icon_default" :*/
fileprivate let kStr_explainValue:[Character] = ["g","i","f","t","_","i","c"]
fileprivate let kStr_makeTimeName:[Character] = ["o","n","_","d","e","f","a","u","l","t"]

/*: "icon_female_icon_default" :*/
fileprivate let kStr_requestData:[Character] = ["i","c","o","n","_","f","e","m","a","l","e","_","i","c","o"]
fileprivate let kStr_shareRestoreContent:[Character] = ["n","_","d","e","f","a","u","l","t"]

/*: "icon_male_icon_default" :*/
fileprivate let kStr_lengthName:[Character] = ["i","c","o","n","_","m","a","l","e","_","i","c","o","n","_","d","e","f","a","u","l","t"]

/*: "icon_circle_pic_default" :*/
fileprivate let kStr_topUpTitle:[Character] = ["i","c","o","n","_","c","i","r"]
fileprivate let kStr_listName:String = "cle_pic_custom make user else string"
fileprivate let kStr_indexData:[Character] = ["d","e","f","a","u","l","t"]

/*: "ata:image/jpeg;base64, :*/
fileprivate let kStr_labEmptyData:[Character] = ["a","t"]
fileprivate let kStr_viewName:String = "self face show makea:imag"
fileprivate let kStr_componentValue:String = "equal pathg;bas"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  UIImageExtension.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/25.
//

//: import Foundation
import Foundation
//: import SwiftUI
import SwiftUI
//: import UIKit
import UIKit

//: enum PodspecBundle {
enum GearingPodspecBundle {
    //: static let bundle: Bundle = {
    static let bundle: Bundle = {
        //: if let bundleURL = Bundle.main.url(forResource: PodspecName, withExtension: "bundle") {
        if let bundleURL = Bundle.main.url(forResource: kLet_fileContent, withExtension: (kStr_sourceViewName.replacingOccurrences(of: "data", with: "dl"))) {
            //: return Bundle(url: bundleURL) ?? Bundle.main
            return Bundle(url: bundleURL) ?? Bundle.main
        }
        //: return Bundle.main
        return Bundle.main
        //: }()
    }()

    /// 需要镜像的图片资源
    //: static let flipsImages: Set<String> = {
    static let flipsImages: Set<String> = //: return ["nav_back_black_nor",
        [(String(kStr_homeData.suffix(5)) + "ack_" + String(kStr_colorTitle.prefix(9))),
         //: "icon_translation_go",
         (String(kStr_acceptName.suffix(5)) + "transla" + String(kStr_fullBeforeContent)),
         //: "iv_me_vip",
         (String(kStr_failureSizeContent)),
         //: "btn_dongtai_pinglun_nor",
         (String(kStr_theValue.prefix(6)) + "ngtai_" + String(kStr_sharedShareValue)),
         //: "btn_me_new_edit",
         (String(kStr_blockName) + kStr_makeData.lowercased() + String(kStr_appText)),
         //: "btn_me_edit",
         (String(kStr_loadName.suffix(5)) + String(kStr_galleryName.prefix(6))),
         //: "icon_photo_giftbg",
         (String(kStr_normalText.suffix(6)) + "hoto" + String(kStr_finishContent.prefix(7))),
         //: "icon_gift_more_arrow",
         (String(kStr_viewTitle) + String(kStr_cellName.prefix(6)) + "e_arrow"),
         //: "bg_talk_other",
         (String(kStr_voiceValue) + String(kStr_managerName)),
         //: "bg_talk_me",
         (String(kStr_progressErrorName.suffix(5)) + "lk_m" + kStr_toValue.replacingOccurrences(of: "bottom", with: "e")),
         //: "icon_talk_left_voive_1",
         (kStr_reasonData.replacingOccurrences(of: "sub", with: "ic") + "_talk_" + kStr_buttonData.replacingOccurrences(of: "equal", with: "ft") + "_voive" + String(kStr_indexName)),
         //: "icon_talk_left_voive_2",
         (String(kStr_objectName) + String(kStr_equalValue)),
         //: "icon_talk_left_voive_3",
         (String(kStr_statusOfData) + String(kStr_withName)),
         //: "icon_talk_right_voive_1",
         (String(kStr_ageSourceText) + String(kStr_imageName.prefix(3)) + String(kStr_sectionData)),
         //: "icon_talk_right_voive_2",
         (String(kStr_familyData) + String(kStr_thumbTitle.suffix(5))),
         //: "icon_talk_right_voive_3",
         (String(kStr_afterFrameData) + String(kStr_atImageName)),
         //: "icon_gift_num_bg",
         (String(kStr_rowRequestTitle)),
         //: "group_someoneatme",
         (kStr_pinContainerData.replacingOccurrences(of: "female", with: "p") + "_someo" + kStr_infoErrorValue.replacingOccurrences(of: "content", with: "ea")),
         //: "btn_back_white",
         (String(kStr_borderName.suffix(5)) + "ack_" + kStr_totalBottomValue.replacingOccurrences(of: "user", with: "te")),
         //: "icon_VectorStroke",
         (String(kStr_pathValue) + String(kStr_whiteName.suffix(7)) + kStr_sizeWithData.replacingOccurrences(of: "data", with: "r")),
         //: "btnJQbEAB_etihw_kcab_shadow",
         (String(kStr_equalTitle.suffix(6)) + "EAB_et" + String(kStr_textName.suffix(4)) + kStr_eventUpTitle.replacingOccurrences(of: "size", with: "kc") + String(kStr_moreMakeValue)),
         //: "bg_talk_closed_tc_intimate",
         (String(kStr_colorLabelData.suffix(4)) + "alk_cl" + kStr_selectedName.replacingOccurrences(of: "mode", with: "se") + "_tc_in" + kStr_coverName.lowercased()),
         //: "icon_talk_reply_voive",
         (kStr_makeGiftContent.replacingOccurrences(of: "engine", with: "c") + String(kStr_liveValue)),
         //: "ico_intimate_list_sm",
         (String(kStr_sharedData.suffix(6)) + "timat" + String(kStr_fatalName.suffix(9))),
         //: "female_bg_random",
         (kStr_valueName.replacingOccurrences(of: "source", with: "em") + "_bg_" + kStr_userData.replacingOccurrences(of: "neighbor", with: "d")),
         //: "party_mini_bg_userDetail"]
         (String(kStr_indexShareValue) + String(kStr_visualName))]
    //: }()
}

//: public extension UIImage {
public extension UIImage {
    //: static func imageFillColor(color: UIColor) -> UIImage {
    static func locationAcross(color: UIColor) -> UIImage {
        //: let size = CGSize(width: 1, height: 1)
        let size = CGSize(width: 1, height: 1)
        //: let renderer = UIGraphicsImageRenderer(size: size)
        let renderer = UIGraphicsImageRenderer(size: size)
        //: let image = renderer.image { context in
        let image = renderer.image { context in
            //: context.cgContext.setFillColor(color.cgColor)
            context.cgContext.setFillColor(color.cgColor)
            //: context.fill(CGRect(x: 0, y: 0, width: 1, height: 1))
            context.fill(CGRect(x: 0, y: 0, width: 1, height: 1))
        }
        //: return image
        return image
    }

    //: static func imageFillColor(color: UIColor, size: CGSize) -> UIImage {
    static func untilSkinColour(color: UIColor, size: CGSize) -> UIImage {
        //: let renderer = UIGraphicsImageRenderer(size: size)
        let renderer = UIGraphicsImageRenderer(size: size)
        //: let img = renderer.image { context in
        let img = renderer.image { context in
            //: color.set()
            color.set()
            //: context.fill(CGRect(x: 0, y: 0, width: size.width, height: size.height))
            context.fill(CGRect(x: 0, y: 0, width: size.width, height: size.height))
        }
        //: return img
        return img
    }

    //: static func imageFillColor(color: UIColor, size: CGSize, radius: CGFloat) -> UIImage {
    static func imageEqual(color: UIColor, size: CGSize, radius: CGFloat) -> UIImage {
        //: let renderer = UIGraphicsImageRenderer(size: size)
        let renderer = UIGraphicsImageRenderer(size: size)
        //: let img = renderer.image { context in
        let img = renderer.image { context in
            //: let path = UIBezierPath(roundedRect: CGRect(x: 0, y: 0, width: size.width, height: size.height), cornerRadius: radius)
            let path = UIBezierPath(roundedRect: CGRect(x: 0, y: 0, width: size.width, height: size.height), cornerRadius: radius)
            //: context.cgContext.setFillColor(color.cgColor)
            context.cgContext.setFillColor(color.cgColor)
            //: context.cgContext.addPath(path.cgPath)
            context.cgContext.addPath(path.cgPath)
            //: context.cgContext.fillPath()
            context.cgContext.fillPath()
        }
        //: return img
        return img
    }

    //: static func imageGradientColor(colors: [CGColor], size: CGSize) -> UIImage {
    static func upTo(colors: [CGColor], size: CGSize) -> UIImage {
        //: let renderer = UIGraphicsImageRenderer(size: size)
        let renderer = UIGraphicsImageRenderer(size: size)
        //: let img = renderer.image { context in
        let img = renderer.image { context in
            //: let gradientLayer = CAGradientLayer()
            let gradientLayer = CAGradientLayer()
            //: gradientLayer.colors = colors
            gradientLayer.colors = colors
            //: gradientLayer.startPoint = CGPoint(x: 0, y: 0.5)
            gradientLayer.startPoint = CGPoint(x: 0, y: 0.5)
            //: gradientLayer.endPoint = CGPoint(x: 1.0, y: 0.5)
            gradientLayer.endPoint = CGPoint(x: 1.0, y: 0.5)
            //: gradientLayer.frame = CGRect(x: 0, y: 0, width: ceil(size.width), height: ceil(size.height))
            gradientLayer.frame = CGRect(x: 0, y: 0, width: ceil(size.width), height: ceil(size.height))
            //: gradientLayer.render(in: context.cgContext)
            gradientLayer.render(in: context.cgContext)
        }
        //: return img
        return img
    }

    //: static func imageGradientTopToBottonColor(colors: [CGColor], size: CGSize) -> UIImage {
    static func gradient(colors: [CGColor], size: CGSize) -> UIImage {
        //: let renderer = UIGraphicsImageRenderer(size: size)
        let renderer = UIGraphicsImageRenderer(size: size)
        //: let img = renderer.image { context in
        let img = renderer.image { context in
            //: let gradientLayer = CAGradientLayer()
            let gradientLayer = CAGradientLayer()
            //: gradientLayer.colors = colors
            gradientLayer.colors = colors
            //: gradientLayer.frame = CGRect(x: 0, y: 0, width: ceil(size.width), height: ceil(size.height))
            gradientLayer.frame = CGRect(x: 0, y: 0, width: ceil(size.width), height: ceil(size.height))
            //: gradientLayer.render(in: context.cgContext)
            gradientLayer.render(in: context.cgContext)
        }
        //: return img
        return img
    }

    /// 实现高斯模糊效果
    /// - Parameters:
    ///   - theImage: 传入图片
    ///   - blurValue: 模糊程度
    /// - Returns: 输出图片
    //: static func blur(theImage: UIImage, blurValue: CGFloat = 15.0) -> UIImage {
    static func sizeVisualizationFinish(theImage: UIImage, blurValue: CGFloat = 15.0) -> UIImage {
        //: let context = CIContext(options: nil)
        let context = CIContext(options: nil)
        //: let inputImage = CIImage(cgImage: theImage.cgImage!)
        let inputImage = CIImage(cgImage: theImage.cgImage!)
        //: let filter = CIFilter(name: "CIGaussianBlur")
        let filter = CIFilter(name: (String(kStr_closeTitle)))
        //: filter?.setValue(inputImage, forKey: kCIInputImageKey)
        filter?.setValue(inputImage, forKey: kCIInputImageKey)
        //: filter?.setValue(blurValue, forKey: kCIInputRadiusKey)
        filter?.setValue(blurValue, forKey: kCIInputRadiusKey)
        //: let result = filter?.value(forKey: kCIOutputImageKey) as! CIImage
        let result = filter?.value(forKey: kCIOutputImageKey) as! CIImage
        //: let cgImage = context.createCGImage(result, from: inputImage.extent)
        let cgImage = context.createCGImage(result, from: inputImage.extent)
        //: let returnImage = UIImage(cgImage: cgImage!)
        let returnImage = UIImage(cgImage: cgImage!)
        //: return returnImage
        return returnImage
    }

    //: @objc static func BundleImageNamed(name: String) -> UIImage {
    @objc static func bundleBy(name: String) -> UIImage {
        //: guard name.count > 0 else {
        guard name.count > 0 else {
            //: return UIImage()
            return UIImage()
        }
        //: if let image = SVGAEffectTool.default.getImage(name: name) {
        if let image = LoadGrappleEffectTool.default.mLive(name: name) {
            //: return processImage(image: image, name: name)
            return imageArea(image: image, name: name)
        }
        //: return UIImage()
        return UIImage()
    }

    /// 镜像图片
    //: private static func processImage(image: UIImage, name: String) -> UIImage {
    private static func imageArea(image: UIImage, name: String) -> UIImage {
        //: guard LanguageManager.shared.direction == .rightToLeft else {
        guard DeleteClientThen.shared.direction == .rightToLeft else {
            //: return image
            return image
        }
        //: if PodspecBundle.flipsImages.contains(name) {
        if GearingPodspecBundle.flipsImages.contains(name) {
            //: return image.imageFlippedForRightToLeftLayoutDirection()
            return image.imageFlippedForRightToLeftLayoutDirection()
        }
        //: return image
        return image
    }

    //: @objc static func placeImgSquare() -> UIImage {
    @objc static func elect() -> UIImage {
        //: return BundleImageNamed(name: "icon_mid_picture_default")
        return bundleBy(name: (String(kStr_fillTitle.prefix(6)) + "id_pic" + String(kStr_makeValue.prefix(7)) + kStr_timeName.replacingOccurrences(of: "list", with: "fa")))
    }

    //: static func placeImgBanner() -> UIImage {
    static func imageBy() -> UIImage {
        //: return BundleImageNamed(name: "icon_big_picture_default")
        return bundleBy(name: (String(kStr_messageText)))
    }

    //: @objc static func placeSquareBigImgBanner() -> UIImage {
    @objc static func infoPlayer() -> UIImage {
        //: return BundleImageNamed(name: "icon_big_picture_square")
        return bundleBy(name: (String(kStr_mainValue.prefix(6)) + String(kStr_noText.prefix(6)) + "ture" + String(kStr_centerName.suffix(7))))
    }

    //: @objc static func giftPlaceHolderImage() -> UIImage {
    @objc static func byproduct() -> UIImage {
        //: return BundleImageNamed(name: "gift_icon_default")
        return bundleBy(name: (String(kStr_explainValue) + String(kStr_makeTimeName)))
    }

    //: static func placeHolderImage(sex: String) -> UIImage {
    static func deliver(sex: String) -> UIImage {
        //: if sex == Gender.female.rawValue {
        if sex == SucceedComparable.female.rawValue {
            //: return BundleImageNamed(name: "icon_female_icon_default")
            return bundleBy(name: (String(kStr_requestData) + String(kStr_shareRestoreContent)))
        }
        //: return BundleImageNamed(name: "icon_male_icon_default")
        return bundleBy(name: (String(kStr_lengthName)))
    }

    //: static func placeImgCircle() -> UIImage {
    static func placeCircle() -> UIImage {
        //: return BundleImageNamed(name: "icon_circle_pic_default")
        return bundleBy(name: (String(kStr_topUpTitle) + String(kStr_listName.prefix(8)) + String(kStr_indexData)))
    }

    //: @objc static func resizedInImageWithName(name: String) -> UIImage {
    @objc static func duringStreetwise(name: String) -> UIImage {
        //: let ret = UIImage.bundleBy(name: name)
        let ret = UIImage.bundleBy(name: name)
        //: let edge = UIEdgeInsets(top: 30, left: 10, bottom: 10, right: 15)
        let edge = UIEdgeInsets(top: 30, left: 10, bottom: 10, right: 15)
        //: return ret.resizableImage(withCapInsets: edge, resizingMode: .stretch)
        return ret.resizableImage(withCapInsets: edge, resizingMode: .stretch)
    }

    //: func resize(with newSize: CGSize, highQuality: Bool = false) -> UIImage? {
    func applicationTo(with newSize: CGSize, highQuality: Bool = false) -> UIImage? {
        //: let _newSize = highQuality ? CGSize(width: newSize.width * 2, height: newSize.height * 2) : newSize
        let _newSize = highQuality ? CGSize(width: newSize.width * 2, height: newSize.height * 2) : newSize
        //: var scaleFactor: CGFloat = 0.0
        var scaleFactor: CGFloat = 0.0
        //: if !size.equalTo(newSize) {
        if !size.equalTo(newSize) {
            //: let widthFactor: CGFloat = _newSize.width / size.width
            let widthFactor: CGFloat = _newSize.width / size.width
            //: let heightFactor: CGFloat = _newSize.height / size.height
            let heightFactor: CGFloat = _newSize.height / size.height
            //: if widthFactor < heightFactor {
            if widthFactor < heightFactor {
                //: scaleFactor = heightFactor
                scaleFactor = heightFactor // scale to fit height
                //: } else {
            } else {
                //: scaleFactor = widthFactor
                scaleFactor = widthFactor // scale to fit width
            }
        }

        //: scaleFactor = min(scaleFactor, 1.0)
        scaleFactor = min(scaleFactor, 1.0)
        //: let targetWidth = ceil(size.width * scaleFactor)
        let targetWidth = ceil(size.width * scaleFactor)
        //: let targetHeight = ceil(size.height * scaleFactor)
        let targetHeight = ceil(size.height * scaleFactor)

        //: let renderer = UIGraphicsImageRenderer(size: CGSize(width: targetWidth, height: targetHeight))
        let renderer = UIGraphicsImageRenderer(size: CGSize(width: targetWidth, height: targetHeight))
        //: let newImage = renderer.image { _ in
        let newImage = renderer.image { _ in
            //: draw(in: CGRect(x: 0, y: 0, width: targetWidth, height: targetHeight))
            draw(in: CGRect(x: 0, y: 0, width: targetWidth, height: targetHeight))
        }
        //: return newImage
        return newImage
    }

    //: @objc static func base64EncodeImageRestore(imageData: String) -> UIImage {
    @objc static func imagePush(imageData: String) -> UIImage {
        //: let urlString = "ata:image/jpeg;base64,\(imageData)"
        let urlString = (String(kStr_labEmptyData) + String(kStr_viewName.suffix(6)) + "e/jpe" + String(kStr_componentValue.suffix(5)) + "e64,") + "\(imageData)"
        //: let url = URL(string: urlString)
        let url = URL(string: urlString)
        //: if let data = try? Data(contentsOf: url!) {
        if let data = try? Data(contentsOf: url!) {
            //: let image = UIImage(data: data)!
            let image = UIImage(data: data)!
            //: return image
            return image
            //: } else {
        } else {
            //: return UIImage()
            return UIImage()
        }
    }
}

//: class ImageWithoutRender: UIImage {
class SharedWithoutThen: UIImage {
    //: override func withRenderingMode(_ renderingMode: UIImage.RenderingMode) -> UIImage {
    override func withRenderingMode(_: UIImage.RenderingMode) -> UIImage {
        //: return self
        return self
    }
}
