
//: Declare String Begin

/*: / :*/
fileprivate let kStr_redName:[Character] = ["/"]

/*:  ( :*/
fileprivate let kStr_sharedViewContent:[Character] = [" ","("]

/*: ; build: :*/
fileprivate let kStr_endData:String = "view let; bu"

/*: ; iOS  :*/
fileprivate let kStr_tingValue:String = "private as make response let; iOS "

/*: ;  :*/
fileprivate let kStr_giftCellContent:String = ";cookie"

/*: )" :*/
fileprivate let kStr_titleName:String = ")"

/*: "audio" :*/
fileprivate let kStr_topTitle:String = "audtoo"

/*: + :*/
fileprivate let kStr_priceTitle:[Character] = ["+"]

/*: .wav" :*/
fileprivate let kStr_constraintName:[Character] = [".","w","a","v"]

/*: "audio/wav" :*/
fileprivate let kStr_keyPadName:String = "aviewdio"

/*: .jpg" :*/
fileprivate let kStr_colorName:[Character] = [".","j","p","g"]

/*: "image/jpeg" :*/
fileprivate let kStr_makeReasonData:String = "send object asimage/jp"
fileprivate let kStr_rawTitle:[Character] = ["e","g"]

/*: "://app." :*/
fileprivate let kStr_fieldText:[Character] = [":","/","/","a","p","p","."]

/*: "data" :*/
fileprivate let kStr_toData:String = "daprice"

/*: ":null" :*/
fileprivate let kStr_titleValue:String = ":nullcontent after view event view"

/*: ":{}" :*/
fileprivate let kStr_onTitle:String = ":{}"

/*: "Request failed, Try again later" :*/
fileprivate let kStr_cameraName:[UInt8] = [0x46,0x71,0x65,0x61,0x71,0x67,0x60,0x34,0x72,0x75,0x7d,0x78,0x71,0x70,0x38,0x34,0x40,0x66,0x6d,0x34,0x75,0x73,0x75,0x7d,0x7a,0x34,0x78,0x75,0x60,0x71,0x66]

private func fileFill(name num: UInt8) -> UInt8 {
    return num ^ 20
}

/*: "json error" :*/
fileprivate let kStr_dateData:[Character] = ["j","s"]
fileprivate let kStr_backgroundText:[Character] = ["o","n"," ","e","r","r","o","r"]

/*: "platform=iphone&version= :*/
fileprivate let kStr_valueModeData:[Character] = ["p","l","a","t","f","o","r","m","=","i","p","h","o","n","e","&","v","e","r","s","i"]
fileprivate let kStr_sizeName:String = "min tailon="

/*: &packageId= :*/
fileprivate let kStr_myName:String = "&packimage status return"
fileprivate let kStr_closeValue:[Character] = ["a","g","e","I","d","="]

/*: &bundleId= :*/
fileprivate let kStr_underName:[Character] = ["&","b","u","n","d","l","e"]
fileprivate let kStr_managerValue:String = "analyze from resource falseId="

/*: &lang= :*/
fileprivate let kStr_cardData:String = "&lang=view app change cell data"

/*: "?!@#$^&%*+,:;='" :*/
fileprivate let kStr_infoTitle:[Character] = ["?","!","@","#","$","^","&","%","*"]
fileprivate let kStr_tapValue:String = "+,:;=\'"

/*: "`<>()[]{}/\\| " :*/
fileprivate let kStr_topGreetTitle:String = "`<>()["
fileprivate let kStr_reasonErrorValue:String = "]{}/\\| "

/*: "/route/slowindex" :*/
fileprivate let kStr_titleCellName:[Character] = ["/","r","o","u","t","e","/","s","l","o","w","i","n","d","e","x"]

/*: "/route/index" :*/
fileprivate let kStr_managerName:String = "select in/route"

/*: "?path= :*/
fileprivate let kStr_videoAddText:String = "?path=gift self return line text"

/*: "&GJNonceStr= :*/
fileprivate let kStr_shareWhyTitle:String = "list layer data lab&GJNonc"
fileprivate let kStr_commentLineValue:String = "eStr=index as action app corner"

/*: "& :*/
fileprivate let kStr_selectedData:String = "&"

/*: = :*/
fileprivate let kStr_whiteName:String = "="

/*: "&params= :*/
fileprivate let kStr_valueContent:String = "&paramslet sex"
fileprivate let kStr_untilNoData:String = "="

/*: "/ :*/
fileprivate let kStr_shadowData:String = "border"

/*: "? :*/
fileprivate let kStr_fileToData:[Character] = ["?"]

/*: "token" :*/
fileprivate let kStr_rowData:[Character] = ["t","o","k","e","n"]

/*: "请求成功 :*/
fileprivate let kStr_enterText:String = "请求成功"

/*: "AAAAAAAA" :*/
fileprivate let kStr_postEqualLineText:String = "soundsoundsoundsoundsoundsoundsound"
fileprivate let kStr_iconVideoContent:[Character] = ["A"]

/*: "AA" :*/
fileprivate let kStr_firstText:[Character] = ["A","A"]

/*: "BB" :*/
fileprivate let kStr_stopText:String = "time"

/*: "下载完成 -  :*/
fileprivate let kStr_handleToValue:String = "下载完成 - "

/*: "/Documents/" :*/
fileprivate let kStr_textContent:[Character] = ["/","D","o","c","u","m","e"]
fileprivate let kStr_talkData:[Character] = ["n","t","s","/"]

/*: ".mp4" :*/
fileprivate let kStr_aspectName:String = ".mp4style main time open net"

/*: "移动地址: :*/
fileprivate let kStr_bottomValue:String = "移动\u{5730}址:"

/*: "后台任务下载回来" :*/
fileprivate let kStr_nameTitleData:[Character] = ["后"]
fileprivate let kStr_clearText:String = "\u{53f0}任务\u{4e0b}载回来"

/*: "下载成功 : :*/
fileprivate let kStr_makeTitle:String = "下载\u{6210}功 "
fileprivate let kStr_userData:[Character] = [":"]

/*: "Net Error" :*/
fileprivate let kStr_firstValue:[Character] = ["N"]
fileprivate let kStr_sendData:String = "with value title backgroundet Error"

/*: "下载失败 : :*/
fileprivate let kStr_succeedData:[Character] = ["下","载","失","败"," "]
fileprivate let kStr_rawText:String = "info"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ToolSeekObjectProtocol.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let kLet_bottomTitle = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let kLet_likeValue = "\(kLet_pageContent)/\(kLet_inviteText) (\(kLet_listTitle)" + (String(kStr_endData.suffix(4)) + "ild:") + "\(kLet_noContent)" + (String(kStr_tingValue.suffix(6))) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: CanErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let kLet_firstName = ToolSeekObjectProtocol()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class ToolSeekObjectProtocol: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func afterFailureCompletion(model: SizeRequestModel, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.offModel() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.conceal(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.easyLay(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func conceal(model: SizeRequestModel, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.starLevel(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.titleSend(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    outcome(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, CanErrorResponse(errorCode: PointEquatable.NetError.rawValue, errorMsg: kLet_accuracyContent))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        detailDownArray(showMsg: kLet_accuracyContent)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (kStr_topTitle.replacingOccurrences(of: "to", with: "i")) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.aboveQuantity())+\(kLet_guideDoingTitle)" + (String(kStr_constraintName)), mimeType: (kStr_keyPadName.replacingOccurrences(of: "view", with: "u") + "/wav"))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(kStr_colorName)), mimeType: (String(kStr_makeReasonData.suffix(8)) + String(kStr_rawTitle)))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    outcome(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, CanErrorResponse(errorCode: PointEquatable.NetError.rawValue, errorMsg: kLet_accuracyContent))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        detailDownArray(showMsg: kLet_accuracyContent)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func easyLay(model: SizeRequestModel, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.starLevel(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.titleSend(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: kLet_liveValue ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = kLet_bottomTitle }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    outcome(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, CanErrorResponse(errorCode: PointEquatable.NetError.rawValue, errorMsg: kLet_accuracyContent))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        detailDownArray(showMsg: kLet_accuracyContent)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = kLet_bottomTitle }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    outcome(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, CanErrorResponse(errorCode: PointEquatable.NetError.rawValue, errorMsg: kLet_accuracyContent))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        detailDownArray(showMsg: kLet_accuracyContent)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func outcome(model: SizeRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            ViewManagerAddrTool.share.titlePush(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if kLet_liveValue, model.requestServer.contains((String(kStr_fieldText))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.startKey(key: ViewManagerAddrTool.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (kStr_toData.replacingOccurrences(of: "price", with: "ta")) + "\"" + (String(kStr_titleValue.prefix(5))), with: "" + "\"" + (kStr_toData.replacingOccurrences(of: "price", with: "ta")) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !kLet_liveValue {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(kStr_titleValue.prefix(5))), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<MiniBaseResponse>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == PointEquatable.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, CanErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    detailDownArray(showMsg: responseModel.msg ?? String(bytes: kStr_cameraName.map{fileFill(name: $0)}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case PointEquatable.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: kLet_statusFailData, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, CanErrorResponse(errorCode: PointEquatable.NetError.rawValue, errorMsg: (String(kStr_dateData) + String(kStr_backgroundText)).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func starLevel(model: SizeRequestModel) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (String(kStr_valueModeData) + String(kStr_sizeName.suffix(3))) + "\(kLet_inviteTitle)" + (String(kStr_myName.prefix(5)) + String(kStr_closeValue)) + "\(kLet_guideDoingTitle)" + (String(kStr_underName) + String(kStr_managerValue.suffix(3))) + "\(kLet_listTitle)" + (String(kStr_cardData.prefix(6))) + "\(ViewManagerAddrTool.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if kLet_liveValue, serverUrl.contains((String(kStr_fieldText))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if ViewManagerAddrTool.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(kStr_titleCellName)))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(kStr_managerName.suffix(6)) + "/index")) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(kStr_videoAddText.prefix(6))) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(kStr_shareWhyTitle.suffix(7)) + String(kStr_commentLineValue.prefix(5))) + "\(ViewManagerAddrTool.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.averageCost(key: ViewManagerAddrTool.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(kStr_valueContent.prefix(7)) + kStr_untilNoData.capitalized) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            pathLine(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func titleSend(model: SizeRequestModel) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(kLet_likeValue)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (String(kStr_rowData)), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func handleBackground(model: SizeRequestModel) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.starLevel(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                pathLine(message: (kStr_enterText.capitalized) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (kStr_postEqualLineText.replacingOccurrences(of: "sound", with: "A") + String(kStr_iconVideoContent))) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        pathLine(message: (kStr_handleToValue.capitalized) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(kStr_textContent) + String(kStr_talkData)) + NSDate.aboveQuantity() + (String(kStr_aspectName.prefix(4)))
        //: printLog(message: "移动地址:\(documnets)")
        pathLine(message: (kStr_bottomValue) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            pathLine(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        pathLine(message: (String(kStr_nameTitleData) + kStr_clearText))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = ItemLineBaseDelegate.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func ambagesDataCompletion(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: kLet_voiceData.location6decrypt(), with: kLet_priceName)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                pathLine(message: (kStr_makeTitle + String(kStr_userData)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, CanErrorResponse(errorCode: PointEquatable.Normal.rawValue, errorMsg: (String(kStr_firstValue) + String(kStr_sendData.suffix(8))).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, CanErrorResponse(errorCode: PointEquatable.NetError.rawValue, errorMsg: (String(kStr_firstValue) + String(kStr_sendData.suffix(8))).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                pathLine(message: (String(kStr_succeedData) + kStr_rawText.replacingOccurrences(of: "info", with: ":")) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func endUser(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: kLet_voiceData.location6decrypt(), with: kLet_priceName)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                pathLine(message: (kStr_makeTitle + String(kStr_userData)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, CanErrorResponse(errorCode: PointEquatable.Normal.rawValue, errorMsg: (String(kStr_firstValue) + String(kStr_sendData.suffix(8))).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, CanErrorResponse(errorCode: PointEquatable.NetError.rawValue, errorMsg: (String(kStr_firstValue) + String(kStr_sendData.suffix(8))).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                pathLine(message: (String(kStr_succeedData) + kStr_rawText.replacingOccurrences(of: "info", with: ":")) + "\(errMsg)")
            }
        }
    }
}
