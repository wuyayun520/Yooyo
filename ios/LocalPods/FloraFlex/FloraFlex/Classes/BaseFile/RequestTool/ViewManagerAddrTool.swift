
//: Declare String Begin

/*: "http://" :*/
fileprivate let kStr_removeData:[Character] = ["h","t","t","p",":"]
fileprivate let kStr_frameContent:String = "//"

/*: "https://" :*/
fileprivate let kStr_modelText:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "http:// :*/
fileprivate let kStr_userText:String = "else data userhttp:/"
fileprivate let kStr_verticalData:String = "frame"

/*: "serverSelctIndex =  :*/
fileprivate let kStr_dismissTitle:[Character] = ["s","e","r","v","e","r","S","e","l","c","t","I","n","d"]
fileprivate let kStr_arrayValue:String = "ex = let nut true"

/*: "serverUrlStr =  :*/
fileprivate let kStr_modelName:[Character] = ["s","e","r","v","e","r","U","r","l","S","t","r"," ","="," "]

/*: "app. :*/
fileprivate let kStr_downText:String = "view true normalapp."

/*: .com" :*/
fileprivate let kStr_sizeData:String = "mini cell label block in.com"

/*: "IM/getMyBlackList" :*/
fileprivate let kStr_pathData:[Character] = ["I","M","/","g","e","t"]
fileprivate let kStr_actionValue:[Character] = ["M","y","B","l","a","c","k","L","i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let kStr_name:String = "mf/buspush show"
fileprivate let kStr_routeContainerText:String = "endMsgmake example i"

/*: "mf/business/sendGift" :*/
fileprivate let kStr_giftMakeData:[Character] = ["m","f","/","b","u","s","i","n","e","s","s","/","s","e","n","d","G","i","f","t"]

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let kStr_viewTitle:[Character] = ["m","f","/","c","h","a","t","R","o","o"]
fileprivate let kStr_magnitudeData:String = "shared message if viewm/sendMsg"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let kStr_noData:String = "cell letmf/cha"
fileprivate let kStr_modelContent:String = "height color let name titlesendGift"

/*: "recharge/applePayNotify" :*/
fileprivate let kStr_itemChangeTitle:String = "rechmanager"
fileprivate let kStr_listData:String = "ppof"
fileprivate let kStr_modelValue:String = "modefy"

/*: "user/appleLogin" :*/
fileprivate let kStr_nameImageData:String = "user/aas title task count"
fileprivate let kStr_tagDataRightTitle:String = "to"

/*: "en" :*/
fileprivate let kStr_headValue:[Character] = ["e","n"]

/*: "ar" :*/
fileprivate let kStr_errorName:String = "namer"

/*: "es" :*/
fileprivate let kStr_totalName:String = "view"

/*: "pt" :*/
fileprivate let kStr_userData:[Character] = ["p","t"]

/*: "GJ21FB" :*/
fileprivate let kStr_limitValueVarText:[Character] = ["G","J","2","1","F","B"]

/*: "完成了检测,请求状态正常" :*/
fileprivate let kStr_messageValue:[Character] = ["\u{5b8c}","成","了","检","\u{6d4b}",",","\u{8bf7}","求"]
fileprivate let kStr_clickFileName:String = "状\u{6001}正常"

/*: "接口请求失败" :*/
fileprivate let kStr_makeValue:[Character] = ["接","口","请","求","失","败"]

/*: "完成了检测,需要更换server url" :*/
fileprivate let kStr_hiddenData:[Character] = ["\u{5b8c}","\u{6210}","了","\u{68c0}","测",",","需","要","更","换","s","e","r","v","e"]
fileprivate let kStr_groupTitle:String = "up male lastr url"

/*: "HTTPProxy" :*/
fileprivate let kStr_titleLabData:String = "HTTback"

/*: "__SCOPED__" :*/
fileprivate let kStr_showResumeName:[Character] = ["_","_","S","C","O","P","E","D","_","_"]

/*: "tap" :*/
fileprivate let kStr_progressData:[Character] = ["t","a","p"]

/*: "tun" :*/
fileprivate let kStr_momentName:String = "tsource"

/*: "ipsec" :*/
fileprivate let kStr_listText:String = "IPSEC"

/*: "ppp" :*/
fileprivate let kStr_currentTitle:[Character] = ["p","p","p"]

/*: "func__updateTalkingCookies =  :*/
fileprivate let kStr_textData:String = "func__head let let"
fileprivate let kStr_ofToolName:String = "Talkinraw color label guard"
fileprivate let kStr_dataValue:String = "resume kit add casegCooki"

/*: "https:// :*/
fileprivate let kStr_assetValue:String = "HTTP"

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let kStr_shareValue:[UInt8] = [0x6c,0x7f,0x64,0x69,0x55,0x55,0x6e,0x6f,0x66,0x6f,0x7e,0x6f,0x59,0x6b,0x67,0x6f,0x49,0x65,0x65,0x61,0x63,0x6f,0x79,0x5d,0x63,0x7e,0x62,0x5f,0x78,0x66,0x2a,0x37,0x2a]

/*: "UID" :*/
fileprivate let kStr_activeName:[Character] = ["U","I","D"]

/*: "PHPSESSID" :*/
fileprivate let kStr_wrapValue:[Character] = ["P","H","P","S","E"]
fileprivate let kStr_selectionValue:[Character] = ["S","S","I","D"]

/*: "func__saveAllCookies =  :*/
fileprivate let kStr_textContent:String = "func__label by input"
fileprivate let kStr_indexName:[Character] = ["s","a","v","e","A","l","l","C","o","o","k","i","e","s"," ","="," "]

/*: "func__saveAllCookies  error" :*/
fileprivate let kStr_blockValue:String = "return jump truefunc__"
fileprivate let kStr_cornerNameContent:String = "self page gift self titlelCoo"
fileprivate let kStr_toData:String = "view"
fileprivate let kStr_groupVideoText:String = " errortitle if view to index"

/*: "🍪 删除 :*/
fileprivate let kStr_locationValue:String = "🍪 删\u{9664}"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ViewManagerAddrTool.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum CountClusterLiteral: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class ViewManagerAddrTool: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = ViewManagerAddrTool()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        shelve()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(kStr_removeData) + kStr_frameContent.capitalized)) || newValue.hasPrefix((String(kStr_modelText))) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if kLet_barVoiceValue && !kLet_liveValue {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(kStr_userText.suffix(6)) + kStr_verticalData.replacingOccurrences(of: "frame", with: "/")) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(kStr_userText.suffix(6)) + kStr_verticalData.replacingOccurrences(of: "frame", with: "/")) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    titlePush(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func shelve() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = kLet_managerValue.integer(forKey: kLet_micText)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        pathLine(message: (String(kStr_dismissTitle) + String(kStr_arrayValue.prefix(5))) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = plonkDown()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(kStr_removeData) + kStr_frameContent.capitalized)
        //: if Environment_Formal && !Encryption_Request {
        if kLet_barVoiceValue && !kLet_liveValue {
            //: serverPrefix = "http://"
            serverPrefix = (String(kStr_removeData) + kStr_frameContent.capitalized)
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        pathLine(message: (String(kStr_modelName)) + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func plonkDown() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func resistance(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array =  ["app.\(kLet_priceName).com", "app.\(kLet_priceName).com"]
//        [(String(kStr_downText.suffix(4))) + "\(kLet_priceName)" + (String(kStr_sizeData.suffix(4))), (String(kStr_downText.suffix(4))) + "\(kLet_priceName)" + (String(kStr_sizeData.suffix(4)))]
    //: }()

    lazy var slowPathArr: Array = {
        return ["IM/getMyBlackList",
                "mf/business/sendMsg",
                "mf/business/sendGift",
                "mf/chatRoom/sendMsg",
                "mf/chatRoom/sendGift",
                "recharge/applePayNotify",
                "user/appleLogin"]
    }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = TableMacroDefine.getSystemLangCode()
        let lang = TableMacroDefine.repossessBeginBushido()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .viewMake(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (String(kStr_limitValueVarText)) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension ViewManagerAddrTool {
    //: func checkServerUrl() {
    func queryEnable() {
        //: if LocationThen.share.networkStatus != .Unavailable {
        if LocationThen.share.networkStatus != .Unavailable {
            //: func_requestHost()
            willRequestHost()
            //: } else {
        } else {
            //: func_starCodeTime()
            redGiantStar()
        }
    }

    //: func func_starCodeTime() {
    func redGiantStar() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.willRequestHost()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func withLoad() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func willRequestHost() {
        //: if LocationThen.share.networkStatus == .Unavailable {
        if LocationThen.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        withLoad()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        SubThen.headCurrent { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                pathLine(message: (String(kStr_messageValue) + kStr_clickFileName))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                pathLine(message: (String(kStr_makeValue)))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.redGiantStar()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    pathLine(message: (String(kStr_hiddenData) + String(kStr_groupTitle.suffix(5))))
                    //: self.func__changeHostUrl()
                    self.host()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func host() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        withLoad()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        kLet_managerValue.set(serverSelctIndex, forKey: kLet_micText)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(kStr_removeData) + kStr_frameContent.capitalized)
        //: if Environment_Formal && !Encryption_Request {
        if kLet_barVoiceValue, !kLet_liveValue {
            //: serverPrefix = "http://"
            serverPrefix = (String(kStr_removeData) + kStr_frameContent.capitalized)
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension ViewManagerAddrTool {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func proxy() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(kStr_titleLabData.replacingOccurrences(of: "back", with: "PP") + "roxy")] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func lineVpn() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(kStr_showResumeName))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((String(kStr_progressData))) || keyStr.contains((kStr_momentName.replacingOccurrences(of: "source", with: "un"))) || keyStr.contains((kStr_listText.lowercased())) || keyStr.contains((String(kStr_currentTitle))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension ViewManagerAddrTool {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func titlePush(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        pathLine(message: (String(kStr_textData.prefix(6)) + "update" + String(kStr_ofToolName.prefix(6)) + String(kStr_dataValue.suffix(6)) + "es = ") + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = bombUpEqual()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = LocationThen.share.appConfigMode.urlH5Domain
        var urlDomain: String = LocationThen.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(kStr_removeData) + kStr_frameContent.capitalized), with: (String(kStr_modelText)))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((String(kStr_modelText))) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (kStr_assetValue.lowercased() + "s://") + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            anImage(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        generate()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func anImage(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    pathLine(message: String(bytes: kStr_shareValue.map{$0^10}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func bombUpEqual() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(kStr_removeData) + kStr_frameContent.capitalized)
        //: if Environment_Formal && !Encryption_Request {
        if kLet_barVoiceValue, !kLet_liveValue {
            //: serverPrefix = "https://"
            serverPrefix = (String(kStr_modelText))
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(kStr_downText.suffix(4))) + "\(kLet_priceName)" + (String(kStr_sizeData.suffix(4))))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func oreo(note _: Notification) {
        //: if LocationThen.share.loginUid.isEmptyString {
        if LocationThen.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            applicationDown()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            untilEvent()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func applicationDown() {
        //: var htmlDomain: String = LocationThen.share.appConfigMode.urlH5Domain
        var htmlDomain: String = LocationThen.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(kStr_activeName)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(kStr_activeName))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(kStr_wrapValue) + String(kStr_selectionValue)) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(String(kStr_wrapValue) + String(kStr_selectionValue))] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(kStr_activeName)) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(String(kStr_activeName))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(kStr_wrapValue) + String(kStr_selectionValue)) {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(String(kStr_wrapValue) + String(kStr_selectionValue))] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((String(kStr_activeName))), h5Cookie.keys.contains((String(kStr_activeName))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(String(kStr_activeName))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(String(kStr_activeName))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((String(kStr_wrapValue) + String(kStr_selectionValue))), h5Cookie.keys.contains((String(kStr_wrapValue) + String(kStr_selectionValue))) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(String(kStr_wrapValue) + String(kStr_selectionValue))]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(String(kStr_wrapValue) + String(kStr_selectionValue))]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func untilEvent() {
        //: let htmlDomain: String = LocationThen.share.appConfigMode.urlH5Domain
        let htmlDomain: String = LocationThen.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == LocationThen.share.loginUserMode.userID {
            if cookie.name == (String(kStr_activeName)), cookie.value == LocationThen.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (String(kStr_wrapValue) + String(kStr_selectionValue)) {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(kStr_activeName)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(kStr_activeName))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(kStr_wrapValue) + String(kStr_selectionValue)) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(String(kStr_wrapValue) + String(kStr_selectionValue))] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((String(kStr_activeName))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(kStr_activeName))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((String(kStr_wrapValue) + String(kStr_selectionValue))) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(kStr_wrapValue) + String(kStr_selectionValue))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func generate() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            pathLine(message: (String(kStr_textContent.prefix(6)) + String(kStr_indexName)) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            kLet_managerValue.set(cookieData, forKey: kLet_succeedText)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            pathLine(message: (String(kStr_blockValue.suffix(6)) + "saveAl" + String(kStr_cornerNameContent.suffix(4)) + "kies" + kStr_toData.replacingOccurrences(of: "view", with: " ") + String(kStr_groupVideoText.prefix(6))))
        }
    }

    //: func func__setAppCookies() {
    func spokenCommunication() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = kLet_managerValue.object(forKey: kLet_succeedText)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func boxEqual() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        kLet_managerValue.removeObject(forKey: kLet_succeedText)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            pathLine(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
