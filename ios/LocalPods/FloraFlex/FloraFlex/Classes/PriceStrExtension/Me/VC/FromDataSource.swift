
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let kStr_toViewName:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Open then %@ will recommend users to you. A new chat will be made after a successful recommendation." :*/
fileprivate let kStr_liveData:[UInt8] = [0x39,0x6,0x13,0x18,0x56,0x2,0x1e,0x13,0x18,0x56,0x53,0x36,0x56,0x1,0x1f,0x1a,0x1a,0x56,0x4,0x13,0x15,0x19,0x1b,0x1b,0x13,0x18,0x12,0x56,0x3,0x5,0x13,0x4,0x5,0x56,0x2,0x19,0x56,0xf,0x19,0x3,0x58,0x56,0x37,0x56,0x18,0x13,0x1,0x56,0x15,0x1e,0x17,0x2,0x56,0x1,0x1f,0x1a,0x1a,0x56,0x14,0x13,0x56,0x1b,0x17,0x12,0x13,0x56,0x17,0x10,0x2,0x13,0x4,0x56,0x17,0x56,0x5,0x3,0x15,0x15,0x13,0x5,0x5,0x10,0x3,0x1a,0x56,0x4,0x13,0x15,0x19,0x1b,0x1b,0x13,0x18,0x12,0x17,0x2,0x1f,0x19,0x18,0x58]

private func clickVideoTitle(data num: UInt8) -> UInt8 {
    return num ^ 118
}

/*: "Open then Applyname will recommend users to you. Use your identity to greet each other." :*/
fileprivate let kStr_cameraName:[UInt8] = [0xf4,0xcb,0xde,0xd5,0x9b,0xcf,0xd3,0xde,0xd5,0x9b,0xfa,0xcb,0xcb,0xd7,0xc2,0xd5,0xda,0xd6,0xde,0x9b,0xcc,0xd2,0xd7,0xd7,0x9b,0xc9,0xde,0xd8,0xd4,0xd6,0xd6,0xde,0xd5,0xdf,0x9b,0xce,0xc8,0xde,0xc9,0xc8,0x9b,0xcf,0xd4,0x9b,0xc2,0xd4,0xce,0x95,0x9b,0xee,0xc8,0xde,0x9b,0xc2,0xd4,0xce,0xc9,0x9b,0xd2,0xdf,0xde,0xd5,0xcf,0xd2,0xcf,0xc2,0x9b,0xcf,0xd4,0x9b,0xdc,0xc9,0xde,0xde,0xcf,0x9b,0xde,0xda,0xd8,0xd3,0x9b,0xd4,0xcf,0xd3,0xde,0xc9,0x95]

/*: "Your avatar and nickname will not be displayed in the ranking." :*/
fileprivate let kStr_firstValue:[UInt8] = [0x6a,0x80,0x86,0x83,0x31,0x72,0x87,0x72,0x85,0x72,0x83,0x31,0x72,0x7f,0x75,0x31,0x7f,0x7a,0x74,0x7c,0x7f,0x72,0x7e,0x76,0x31,0x88,0x7a,0x7d,0x7d,0x31,0x7f,0x80,0x85,0x31,0x73,0x76,0x31,0x75,0x7a,0x84,0x81,0x7d,0x72,0x8a,0x76,0x75,0x31,0x7a,0x7f,0x31,0x85,0x79,0x76,0x31,0x83,0x72,0x7f,0x7c,0x7a,0x7f,0x78,0x3f]

fileprivate func houseKit(at num: UInt8) -> UInt8 {
    let value = Int(num) - 17
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Open then %@ will recommend users to you. Use your identity to greet each other." :*/
fileprivate let kStr_addValue:[UInt8] = [0xd5,0xea,0xff,0xf4,0xba,0xee,0xf2,0xff,0xf4,0xba,0xbf,0xda,0xba,0xed,0xf3,0xf6,0xf6,0xba,0xe8,0xff,0xf9,0xf5,0xf7,0xf7,0xff,0xf4,0xfe,0xba,0xef,0xe9,0xff,0xe8,0xe9,0xba,0xee,0xf5,0xba,0xe3,0xf5,0xef,0xb4,0xba,0xcf,0xe9,0xff,0xba,0xe3,0xf5,0xef,0xe8,0xba,0xf3,0xfe,0xff,0xf4,0xee,0xf3,0xee,0xe3,0xba,0xee,0xf5,0xba,0xfd,0xe8,0xff,0xff,0xee,0xba,0xff,0xfb,0xf9,0xf2,0xba,0xf5,0xee,0xf2,0xff,0xe8,0xb4]

/*: "NameViewCell" :*/
fileprivate let kStr_femaleTitle:String = "photo fill letTalk"
fileprivate let kStr_withTitle:String = "ttincancel"

/*: "#FF2348" :*/
fileprivate let kStr_finishToName:[Character] = ["#","F","F","2","3","4","8"]

/*: "ViewCell" :*/
fileprivate let kStr_viewText:String = "Talkinevent view model image"
fileprivate let kStr_recordData:String = "data all video progress datangNotifCe"
fileprivate let kStr_dataTitle:String = "buttonbutton"

/*: "To set up a password, you need to verify the account, choose your verification method" :*/
fileprivate let kStr_giftValue:[UInt8] = [0x64,0x6f,0x68,0x74,0x65,0x6d,0x20,0x6e,0x6f,0x69,0x74,0x61,0x63,0x69,0x66,0x69,0x72,0x65,0x76,0x20,0x72,0x75,0x6f,0x79,0x20,0x65,0x73,0x6f,0x6f,0x68,0x63,0x20,0x2c,0x74,0x6e,0x75,0x6f,0x63,0x63,0x61,0x20,0x65,0x68,0x74,0x20,0x79,0x66,0x69,0x72,0x65,0x76,0x20,0x6f,0x74,0x20,0x64,0x65,0x65,0x6e,0x20,0x75,0x6f,0x79,0x20,0x2c,0x64,0x72,0x6f,0x77,0x73,0x73,0x61,0x70,0x20,0x61,0x20,0x70,0x75,0x20,0x74,0x65,0x73,0x20,0x6f,0x54]

/*: "Verified by Phone" :*/
fileprivate let kStr_sizeValue:String = "Vericover title"
fileprivate let kStr_interactionData:String = "self extension inside selfy Phone"

/*: "Verified by Email" :*/
fileprivate let kStr_pairText:[Character] = ["V","e","r","i","f","i","e","d"," ","b","y"]
fileprivate let kStr_data:[Character] = [" ","E","m","a","i","l"]

/*: "You're going to delete your profile,messages, photos and matched friends PERMANENTLY and your account will be UNRECOVERABLE.Continue to delete your account? " :*/
fileprivate let kStr_imageTitle:[UInt8] = [0x37,0x1,0x1b,0x49,0x1c,0xb,0x4e,0x9,0x1,0x7,0x0,0x9,0x4e,0x1a,0x1,0x4e,0xa,0xb,0x2,0xb,0x1a,0xb,0x4e,0x17,0x1,0x1b,0x1c,0x4e,0x1e,0x1c,0x1,0x8,0x7,0x2,0xb,0x42,0x3,0xb,0x1d,0x1d,0xf,0x9,0xb,0x1d,0x42,0x4e,0x1e,0x6,0x1,0x1a,0x1,0x1d,0x4e,0xf,0x0,0xa,0x4e,0x3,0xf,0x1a,0xd,0x6,0xb,0xa,0x4e,0x8,0x1c,0x7,0xb,0x0,0xa,0x1d,0x4e,0x3e,0x2b,0x3c,0x23,0x2f,0x20,0x2b,0x20,0x3a,0x22,0x37,0x4e,0xf,0x0,0xa,0x4e,0x17,0x1,0x1b,0x1c,0x4e,0xf,0xd,0xd,0x1,0x1b,0x0,0x1a,0x4e,0x19,0x7,0x2,0x2,0x4e,0xc,0xb,0x4e,0x3b,0x20,0x3c,0x2b,0x2d,0x21,0x38,0x2b,0x3c,0x2f,0x2c,0x22,0x2b,0x40,0x2d,0x1,0x0,0x1a,0x7,0x0,0x1b,0xb,0x4e,0x1a,0x1,0x4e,0xa,0xb,0x2,0xb,0x1a,0xb,0x4e,0x17,0x1,0x1b,0x1c,0x4e,0xf,0xd,0xd,0x1,0x1b,0x0,0x1a,0x51,0x4e]

private func quoteView(path num: UInt8) -> UInt8 {
    return num ^ 110
}

/*: "UNRECOVERABLE" :*/
fileprivate let kStr_buttonLessValue:String = "UNREmain"
fileprivate let kStr_toPlayerTitle:[Character] = ["O"]
fileprivate let kStr_backText:[Character] = ["V","E","R","A","B","L","E"]

/*: "Delete Account" :*/
fileprivate let kStr_selectValue:String = "view to time true caseDelete Ac"
fileprivate let kStr_blockName:String = "COUNT"

/*: "Cancel" :*/
fileprivate let kStr_colorData:String = "Cancelcorner model self push key"

/*: "Delete" :*/
fileprivate let kStr_rowData:String = "content"
fileprivate let kStr_endData:[Character] = ["e","l","e","t","e"]

/*: " ( :*/
fileprivate let kStr_userText:String = " ("

/*: s)" :*/
fileprivate let kStr_eventModelName:String = "tear cells)"

/*: "Logout succeeded!" :*/
fileprivate let kStr_equalNumberTimeData:String = "shared"
fileprivate let kStr_timeTitle:[Character] = ["o","g","o","u","t"," ","s","u","c","c","e","e","d","e","d","!"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FromDataSource.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/4/26.
//

//: import UIKit
import UIKit

//: enum SettingsUniversal: String {
enum LocationSequence: String {
    //: case Contact_information          = "Contact information"
    case Contact_information = "Contact information"
    //: case Notifications                = "Notifications"
    case Notifications
    //: case Social_Preferences           = "Social Preferences"
    case Social_Preferences = "Social Preferences"
}

//: enum SettingsUniversalType: String {
enum ShadowKeyRepresentable: String {
    //: case Phone_Number          = "Phone Number"
    case Phone_Number = "Phone Number"
    //: case Email_Address         = "Email Address"
    case Email_Address = "Email Address"
    //: case Turnon_Notif          = "Turn on Notification"
    case Turnon_Notif = "Turn on Notification"
    //: case Same_gendered         = "Same-gendered messaging"
    case Same_gendered = "Same-gendered messaging"
    //: case Cupid                 = "Cupid messaging"
    case Cupid = "Cupid messaging"
    //: case Ranking               = "Ranking anonymous"
    case Ranking = "Ranking anonymous"
    //: case Automatic             = "Automatic message"
    case Automatic = "Automatic message"
    //: case SetPassword           = "Set New Password"
    case SetPassword = "Set New Password"
    //: case DeleteA               = "Delete Account"
    case DeleteA = "Delete Account"
}

//: class TalkingSettingsUniversalVC: TalkingBaseViewController {
class FromDataSource: LayerRecognizerDelegate {
    //: fileprivate lazy var bag = DisposeBag()
    fileprivate lazy var bag = DisposeBag()
    //: var settingModel = TalkingSettingModel()
    var settingModel = LoadSettingModel()

    //: var  type = SettingsUniversal.Contact_information
    var type = LocationSequence.Contact_information

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kStr_toViewName.reversed(), encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: self.MainTable.reloadData()
        self.MainTable.reloadData()
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.bottomValue()
        //: designView()
        sendView()
        //: getSettingData()
        practicalApplication()
    }

    // MARK: - Lazy load

    //: private lazy var DataSource: [SettingsUniversalType] = {
    private lazy var DataSource: [ShadowKeyRepresentable] = {
        //: var array = [SettingsUniversalType]()
        var array = [ShadowKeyRepresentable]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-TabBarViewHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: kLet_halfData, height: kLet_noSeatName - kLet_screenContent), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: private lazy var DetailSource: [String] = {
    private lazy var DetailSource: [String] = {
        //: var array = ["", "Open then %@ will recommend users to you. A new chat will be made after a successful recommendation.".localizedArguments(AppName), "Open then Applyname will recommend users to you. Use your identity to greet each other.".localized, "Your avatar and nickname will not be displayed in the ranking.".localized]
        var array = ["", String(bytes: kStr_liveData.map{clickVideoTitle(data: $0)}, encoding: .utf8)!.imageArguments(kLet_pageContent), String(bytes: kStr_cameraName.map{$0^187}, encoding: .utf8)!.localized, String(bytes: kStr_firstValue.map{houseKit(at: $0)}, encoding: .utf8)!.localized]
        //: if LocationThen.share.loginUserMode.sex == "2" {
        if LocationThen.share.loginUserMode.sex == "2" {
            //: array[1] = "Open then %@ will recommend users to you. Use your identity to greet each other.".localizedArguments(AppName)
            array[1] = String(bytes: kStr_addValue.map{$0^154}, encoding: .utf8)!.imageArguments(kLet_pageContent)
        }
        //: return array
        return array
        //: }()
    }()
}

// MARK: - Load data

//: extension TalkingSettingsUniversalVC {
extension FromDataSource {
    //: func getSettingData() {
    func practicalApplication() {
        //: let dict = Dictionary<String, Any>()
        let dict = [String: Any]()
        //: TalkingMeRequestTool.req_GetSetting(params: dict) { succeed, result, errorModel in
        LoadEqualRequestTool.objectIcon(params: dict) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: self.settingModel = JSONDeserializer<TalkingSettingModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil)!
                self.settingModel = JSONDeserializer<LoadSettingModel>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil)!
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func setUnicersalView(type: SettingsUniversal) {
    func transitionKind(type: LocationSequence) {
        //: self.type = type
        self.type = type
        //: self.title = type.rawValue.localized
        self.title = type.rawValue.localized
        //: switch type {
        switch type {
        //: case .Contact_information:
        case .Contact_information:
            //: DataSource.append(.Phone_Number)
            DataSource.append(.Phone_Number)
            //: DataSource.append(.Email_Address)
            DataSource.append(.Email_Address)
            //: if LocationThen.share.loginUserMode.mobile?.count ?? 0 > 0 || LocationThen.share.loginUserMode.email.count > 0 {
            if LocationThen.share.loginUserMode.mobile?.count ?? 0 > 0 || LocationThen.share.loginUserMode.email.count > 0 {
                //: DataSource.append(.SetPassword)
                DataSource.append(.SetPassword)
            }
            //: DataSource.append(.DeleteA)
            DataSource.append(.DeleteA)
        //: break
        //: case .Notifications:
        case .Notifications:
            //: DataSource.append(.Turnon_Notif)
            DataSource.append(.Turnon_Notif)
        //: break
        //: case .Social_Preferences:
        case .Social_Preferences:
            //: DataSource.append(.Same_gendered)
            DataSource.append(.Same_gendered)
            //: DataSource.append(.Cupid)
            DataSource.append(.Cupid)
            //: DataSource.append(.Ranking)
            DataSource.append(.Ranking)
            //: DataSource.append(.Automatic)
            DataSource.append(.Automatic)
            //: break
        }
        //: self.MainTable.reloadData()
        self.MainTable.reloadData()
    }

    /// 获取修改【手机号】密码验证码
    //: private func req_sendModifyPwdSms() {
    private func halogen() {
        //: ProgressHUD.show()
        TintProgressHUD.weight()
        //: TalkingLoginRequestTool.req_sendModifyPwdSms { succeed, result, errorModel in
        TableThen.completionShow { succeed, _, _ in
            //: ProgressHUD.dismiss()
            TintProgressHUD.inkingPadDismiss()
            //: guard succeed else { return }
            guard succeed else { return }
            //: EffectPushManager.share.func__pushToSetNewPasswordVC(type: .PhoneLogin, content: LocationThen.share.loginUserMode.mobile ?? "", isNewPwd: false)
            EffectPushManager.share.sieve(type: .PhoneLogin, content: LocationThen.share.loginUserMode.mobile ?? "", isNewPwd: false)
        }
    }

    /// 获取修改【邮箱】密码验证码
    //: private func req_sendModifyPwdEmail() {
    private func mainModify() {
        //: ProgressHUD.show()
        TintProgressHUD.weight()
        //: TalkingLoginRequestTool.req_sendModifyPwdEmail { succeed, result, errorModel in
        TableThen.textCompletion { succeed, _, _ in
            //: ProgressHUD.dismiss()
            TintProgressHUD.inkingPadDismiss()
            //: guard succeed else { return }
            guard succeed else { return }
            //: EffectPushManager.share.func__pushToSetNewPasswordVC(type: .EmailLogin, content: LocationThen.share.loginUserMode.email, isNewPwd: false)
            EffectPushManager.share.sieve(type: .EmailLogin, content: LocationThen.share.loginUserMode.email, isNewPwd: false)
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingSettingsUniversalVC: UITableViewDelegate, UITableViewDataSource {
extension FromDataSource: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return DataSource.count
        return DataSource.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: switch type {
        switch type {
        //: case .Contact_information, .Notifications:
        case .Contact_information, .Notifications:
            //: return actualHeight(h: 50)
            return mediaBlock(h: 50)
        //: case .Social_Preferences:
        case .Social_Preferences:
            //: if DataSource[indexPath.row] == .Same_gendered {
            if DataSource[indexPath.row] == .Same_gendered {
                //: return actualHeight(h: 50)
                return mediaBlock(h: 50)
                //: }else if DataSource[indexPath.row] == .Cupid {
            } else if DataSource[indexPath.row] == .Cupid {
                //: if LocationThen.share.loginUserMode.sex == "2" {
                if LocationThen.share.loginUserMode.sex == "2" {
                    //: return actualHeight(h: 89)
                    return mediaBlock(h: 89)
                    //: } else {
                } else {
                    //: return actualHeight(h: 109)
                    return mediaBlock(h: 109)
                }
                //: }  else if (DataSource[indexPath.row] == .Ranking ||  DataSource[indexPath.row] == .Automatic) {
            } else if DataSource[indexPath.row] == .Ranking || DataSource[indexPath.row] == .Automatic {
                //: return actualHeight(h: 92)
                return mediaBlock(h: 92)
            }
            //: return actualHeight(h: 50)
            return mediaBlock(h: 50)
        }
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: switch self.type {
        switch self.type {
        //: case .Contact_information:
        case .Contact_information:
            //: let identifier = "TalkingSettingCell"
            let identifier = (String(kStr_femaleTitle.suffix(4)) + "ingSe" + kStr_withTitle.replacingOccurrences(of: "cancel", with: "g") + "Cell")
            //: let cell: TalkingSettingCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingSettingCell
            let cell: NameViewCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! NameViewCell

            //: cell.setCellMessage(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count-1 )
            cell.onObject(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count - 1)

            //: if DataSource[indexPath.row] == SettingsUniversalType.Phone_Number {
            if DataSource[indexPath.row] == ShadowKeyRepresentable.Phone_Number {
                //: if LocationThen.share.loginUserMode.mobile?.count ?? 0 > 0 {
                if LocationThen.share.loginUserMode.mobile?.count ?? 0 > 0 {
                    //: cell.setCellDetails(detail: LocationThen.share.loginUserMode.mobile ?? "")
                    cell.statusDetail(detail: LocationThen.share.loginUserMode.mobile ?? "")
                    //: } else {
                } else {
                    //: cell.editImag.isHidden = false
                    cell.editImag.isHidden = false
                }
                //: cell.titleBLB.textColor = .appTitleColor()
                cell.titleBLB.textColor = .dataFormat()
                //: } else if DataSource[indexPath.row] == SettingsUniversalType.Email_Address {
            } else if DataSource[indexPath.row] == ShadowKeyRepresentable.Email_Address {
                //: if LocationThen.share.loginUserMode.email.count > 0 {
                if LocationThen.share.loginUserMode.email.count > 0 {
                    //: cell.setCellDetails(detail: LocationThen.share.loginUserMode.email )
                    cell.statusDetail(detail: LocationThen.share.loginUserMode.email)
                    //: } else {
                } else {
                    //: cell.editImag.isHidden = false
                    cell.editImag.isHidden = false
                }
                //: } else if DataSource[indexPath.row] == SettingsUniversalType.SetPassword {
            } else if DataSource[indexPath.row] == ShadowKeyRepresentable.SetPassword {
                //: cell.editImag.isHidden = false
                cell.editImag.isHidden = false
                //: } else if DataSource[indexPath.row] == SettingsUniversalType.DeleteA {
            } else if DataSource[indexPath.row] == ShadowKeyRepresentable.DeleteA {
                //: cell.editImag.isHidden = true
                cell.editImag.isHidden = true
                //: cell.titleBLB.textColor = UIColor.init(hex: "#FF2348")
                cell.titleBLB.textColor = UIColor(hex: (String(kStr_finishToName)))
            }

            //: return cell
            return cell

        //: case .Notifications, .Social_Preferences:
        case .Notifications, .Social_Preferences:
            //: let identifier = "TalkingSettingNotifCell"
            let identifier = (String(kStr_viewText.prefix(6)) + "gSetti" + String(kStr_recordData.suffix(9)) + kStr_dataTitle.replacingOccurrences(of: "button", with: "l"))
            //: let cell: TalkingSettingNotifCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingSettingNotifCell
            let cell: ViewCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! ViewCell

            //: cell.setCellMessage(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count-1 )
            cell.subLast(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count - 1)
            //: if self.type == .Social_Preferences {
            if self.type == .Social_Preferences {
                //: cell.setCellDetails(detail: DetailSource[indexPath.row])
                cell.post(detail: DetailSource[indexPath.row])
            }
            //: if DataSource[indexPath.row] == .Turnon_Notif {
            if DataSource[indexPath.row] == .Turnon_Notif {
                //: UNUserNotificationCenter.current().getNotificationSettings { settings in
                UNUserNotificationCenter.current().getNotificationSettings { settings in
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: let result = settings.authorizationStatus == .authorized
                        let result = settings.authorizationStatus == .authorized
                        //: cell.setCellNotif(isShow: result ? 1 : -1)
                        cell.colorShow(isShow: result ? 1 : -1)
                    }
                }
                //: } else if DataSource[indexPath.row] == .Cupid {
            } else if DataSource[indexPath.row] == .Cupid {
                //: cell.setCellNotif(isShow: self.settingModel.isCupid ?? 1)
                cell.colorShow(isShow: self.settingModel.isCupid ?? 1)
                //: } else if DataSource[indexPath.row] == .Ranking {
            } else if DataSource[indexPath.row] == .Ranking {
                //: cell.setCellNotif(isShow: self.settingModel.isLeaderboardAnonymous ?? -1)
                cell.colorShow(isShow: self.settingModel.isLeaderboardAnonymous ?? -1)
                //: } else if DataSource[indexPath.row] == .Same_gendered {
            } else if DataSource[indexPath.row] == .Same_gendered {
                //: cell.setCellNotif(isShow: self.settingModel.isSameGendered ?? -1)
                cell.colorShow(isShow: self.settingModel.isSameGendered ?? -1)
                //: } else if DataSource[indexPath.row] == .Automatic {
            } else if DataSource[indexPath.row] == .Automatic {
                //: cell.setCellNotif(isShow: self.settingModel.autoGreetAuth ?? -1)
                cell.colorShow(isShow: self.settingModel.autoGreetAuth ?? -1)
            }
            //: return cell
            return cell
        }
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: if self.type == .Contact_information {
        if self.type == .Contact_information {
            //: if DataSource[indexPath.row].rawValue == SettingsUniversalType.Phone_Number.rawValue {
            if DataSource[indexPath.row].rawValue == ShadowKeyRepresentable.Phone_Number.rawValue {
                //: let mobile = LocationThen.share.loginUserMode.mobile ?? ""
                let mobile = LocationThen.share.loginUserMode.mobile ?? ""
                //: if mobile.isEmptyString {
                if mobile.isEmptyString {
                    //: let vc = TalkingBindPhoneVC()
                    let vc = ShowViewPhoneVc()
                    //: navigationController?.pushViewController(vc, animated: true)
                    navigationController?.pushViewController(vc, animated: true)
                }
                //: } else if DataSource[indexPath.row].rawValue == SettingsUniversalType.Email_Address.rawValue {
            } else if DataSource[indexPath.row].rawValue == ShadowKeyRepresentable.Email_Address.rawValue {
                //: if LocationThen.share.loginUserMode.email.count <= 0 {
                if LocationThen.share.loginUserMode.email.count <= 0 {
                    //: let vc = TalkingLoginBindEmailVC()
                    let vc = LayerEmailVc()
                    //: vc.isBack = true
                    vc.isBack = true
                    //: navigationController?.pushViewController(vc, animated: true)
                    navigationController?.pushViewController(vc, animated: true)
                }
                //: } else if DataSource[indexPath.row].rawValue == SettingsUniversalType.DeleteA.rawValue {
            } else if DataSource[indexPath.row].rawValue == ShadowKeyRepresentable.DeleteA.rawValue {
                //: writeOff()
                imageSearched()
                //: } else if DataSource[indexPath.row].rawValue == SettingsUniversalType.SetPassword.rawValue {
            } else if DataSource[indexPath.row].rawValue == ShadowKeyRepresentable.SetPassword.rawValue {
                //: let mobile = LocationThen.share.loginUserMode.mobile ?? ""
                let mobile = LocationThen.share.loginUserMode.mobile ?? ""
                //: let email = LocationThen.share.loginUserMode.email
                let email = LocationThen.share.loginUserMode.email
                //: if !mobile.isEmpty, !email.isEmpty {
                if !mobile.isEmpty, !email.isEmpty { // 手机号和邮箱都存在
                    //: let vc = TalkingMunuPopView(frame: self.view.frame)
                    let vc = FillView(frame: self.view.frame)
                    //: vc.title = "To set up a password, you need to verify the account, choose your verification method".localized
                    vc.title = String(bytes: kStr_giftValue.reversed(), encoding: .utf8)!.localized
                    //: vc.titleFont = UIFont.messageDown(fontSize: 12)
                    vc.titleFont = UIFont.messageDown(fontSize: 12)
                    //: vc.titleColor = UIColor.statusMin()
                    vc.titleColor = UIColor.statusMin()
                    //: vc.cellTitleFont = UIFont.dorsum(fontSize: 16)
                    vc.cellTitleFont = UIFont.dorsum(fontSize: 16)
                    //: vc.hasCancel = false
                    vc.hasCancel = false
                    //: vc.initwithList(cellTitleList: ["Verified by Phone".localized, "Verified by Email".localized])
                    vc.compartmentList(cellTitleList: [(String(kStr_sizeValue.prefix(4)) + "fied b" + String(kStr_interactionData.suffix(7))).localized, (String(kStr_pairText) + String(kStr_data)).localized])
                    //: vc.munuBlock = { [weak self] index, str in
                    vc.munuBlock = { [weak self] index, _ in
                        //: guard let self = self else { return }
                        guard let self = self else { return }
                        //: if index == 0 {
                        if index == 0 {
                            //: self.req_sendModifyPwdSms()
                            self.halogen()
                            //: } else if index == 1 {
                        } else if index == 1 {
                            //: self.req_sendModifyPwdEmail()
                            self.mainModify()
                        }
                    }

                    //: } else if !mobile.isEmpty {
                } else if !mobile.isEmpty {
                    //: req_sendModifyPwdSms()
                    halogen()

                    //: } else if !email.isEmpty {
                } else if !email.isEmpty {
                    //: req_sendModifyPwdEmail()
                    mainModify()
                }
            }
        }
    }
}

//: extension TalkingSettingsUniversalVC {
extension FromDataSource {
    /// 注销
    //: private func writeOff() {
    private func imageSearched() {
        //: guard TalkingPermissionTool.isLiveOrPartyActive() == false else { return }
        guard PerspectiveThen.blockMonth() == false else { return }
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard ToSocketManager.shared.isTalking == false else { // 音视频通话中
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.detailDownArray(showMsg: kLet_tableEndValue)
            //: return
            return
        }

        //: var seconds = 15
        var seconds = 15
        //: let timer: DispatchSourceTimer = DispatchSource.makeTimerSource(flags: [], queue: DispatchQueue.main)
        let timer: DispatchSourceTimer = DispatchSource.makeTimerSource(flags: [], queue: DispatchQueue.main)
        //: timer.schedule(deadline: .now(), repeating: 1.0)
        timer.schedule(deadline: .now(), repeating: 1.0)

        //: let str = "You're going to delete your profile,messages, photos and matched friends PERMANENTLY and your account will be UNRECOVERABLE.Continue to delete your account? ".localized
        let str = String(bytes: kStr_imageTitle.map{quoteView(path: $0)}, encoding: .utf8)!.localized

        //: let range =  str.exMatchStrRange("UNRECOVERABLE")
        let range = str.deleteText((kStr_buttonLessValue.replacingOccurrences(of: "main", with: "C") + String(kStr_toPlayerTitle) + String(kStr_backText)))

        //: let string = NSMutableAttributedString.init(string: str )
        let string = NSMutableAttributedString(string: str)
        //: if range.count > 0 {
        if range.count > 0 {
            //: string.addAttribute(.foregroundColor, value: UIColor.msgTipsColor(), range: range.first!)
            string.addAttribute(.foregroundColor, value: UIColor.welt(), range: range.first!)
        }
        //: TalkingAlertShow.attribAlert(title: "Delete Account".localized, attributedMessage: string, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized) {
        ClickReactiveCompatible.changeKey(title: (String(kStr_selectValue.suffix(9)) + kStr_blockName.lowercased()).localized, attributedMessage: string, leftBtnTitle: (String(kStr_colorData.prefix(6))).localized, rightBtnTitle: (kStr_rowData.replacingOccurrences(of: "content", with: "D") + String(kStr_endData)).localized) {
            //: TalkingAlertShow.hideAlert()
            ClickReactiveCompatible.closeHideRelease()
            //: timer.cancel()
            timer.cancel()
            //: } rightBlock: {
        } rightBlock: {
            //: TalkingAlertShow.hideAlert()
            ClickReactiveCompatible.closeHideRelease()

            //: self.LockAccount()
            self.fromAccount()
        }

        //: timer.setEventHandler {
        timer.setEventHandler {
            //: seconds -= 1
            seconds -= 1
            //: if seconds <= 0 {
            if seconds <= 0 {
                //: timer.cancel()
                timer.cancel()
                //: TalkingAlertShow.changeRightBtn(title: "Delete".localized, isTouch: true)
                ClickReactiveCompatible.increaseDismissCollideWith(title: (kStr_rowData.replacingOccurrences(of: "content", with: "D") + String(kStr_endData)).localized, isTouch: true)
                //: } else {
            } else {
                //: TalkingAlertShow.changeRightBtn(title: "Delete".localized + " (\(seconds)s)", isTouch: false)
                ClickReactiveCompatible.increaseDismissCollideWith(title: (kStr_rowData.replacingOccurrences(of: "content", with: "D") + String(kStr_endData)).localized + " (\(seconds)s)", isTouch: false)
            }
        }
        //: timer.resume()
        timer.resume()
    }

    //: private func LockAccount() {
    private func fromAccount() {
        //: ProgressHUD.show()
        TintProgressHUD.weight()
        //: TalkingMeRequestTool.req_LockAccount(params: [:]) { succeed, result, errorModel in
        LoadEqualRequestTool.pointDoing(params: [:]) { succeed, _, _ in
            //: ProgressHUD.dismiss()
            TintProgressHUD.inkingPadDismiss()
            //: if succeed {
            if succeed {
                //: self.logoutTool()
                self.priceLogoutEqual()
                //: self.func__showStatusBarSuccessMsg(showMsg: "Logout succeeded!".localized)
                self.property(showMsg: (kStr_equalNumberTimeData.replacingOccurrences(of: "shared", with: "L") + String(kStr_timeTitle)).localized)
            }
        }
    }

    //: private func logoutTool() {
    private func priceLogoutEqual() {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard ToSocketManager.shared.isTalking == false else { // 音视频通话中
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.detailDownArray(showMsg: kLet_tableEndValue)
            //: return
            return
        }

        //: ProgressHUD.show()
        TintProgressHUD.weight()
        //: TalkingLoginRequestTool.req_loginOut { t in
        TableThen.equal { t in
            //: ProgressHUD.dismiss()
            TintProgressHUD.inkingPadDismiss()
            //: if t {
            if t {
                //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: kLet_statusFailData, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - UI

//: extension TalkingSettingsUniversalVC {
extension FromDataSource {
    //: private func designView() {
    private func sendView() {
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingSettingCell.self, forCellReuseIdentifier: "TalkingSettingCell")
        MainTable.register(NameViewCell.self, forCellReuseIdentifier: (String(kStr_femaleTitle.suffix(4)) + "ingSe" + kStr_withTitle.replacingOccurrences(of: "cancel", with: "g") + "Cell"))
        //: MainTable.register(TalkingSettingNotifCell.self, forCellReuseIdentifier: "TalkingSettingNotifCell")
        MainTable.register(ViewCell.self, forCellReuseIdentifier: (String(kStr_viewText.prefix(6)) + "gSetti" + String(kStr_recordData.suffix(9)) + kStr_dataTitle.replacingOccurrences(of: "button", with: "l")))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.top.equalTo(20)
            make.top.equalTo(20)
            //: make.leading.trailing.bottom.equalTo(self.view)
            make.leading.trailing.bottom.equalTo(self.view)
        }
        //: MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
        MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
    }
}
